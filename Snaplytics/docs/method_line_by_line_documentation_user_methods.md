# Method-Level Line-by-Line Documentation

## Scope

- Targets: Django backend (`backend/`), Electron app (`electron-app/`), HeigenKiosk React Native app (`HeigenKiosk/`)
- Generated automatically from source files with `.py` and `.js` extensions (excluding migrations, node_modules, Android/iOS build trees, and non-code assets).

## Coverage Summary

- Total files with methods: 45
- Total methods documented: 373

## File: `HeigenKiosk/package-api.js`

- Methods documented: 4

### 1. `loadCategories`

- Signature: `async function loadCategories()`
- Source range: lines 17-36

#### Line-by-line

- Line 17:
  ```text
async function loadCategories() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 18:
  ```text
    try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 19:
  ```text
        categories = await getCategories();
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 20:
  ```text
        renderCategories();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 21:
  ```text
    } catch (err) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 22:
  ```text
        console.error('Failed to load categories:', err);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 23:
  ```text
        const grid = document.getElementById('categoriesGrid');
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 24:
  ```text
        if (grid) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 25:
  ```text
            grid.innerHTML = `
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 26:
  ```text
                <div style="grid-column:1/-1;text-align:center;padding:3rem;color:#dc2626;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 27:
  ```text
                    <p style="font-size:1.125rem;font-weight:600;">Could not load categories</p>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 28:
  ```text
                    <p style="margin-top:0.5rem;color:#717182;">${err.message}</p>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 29:
  ```text
                    <button onclick="loadCategories()" style="margin-top:1rem;padding:0.75rem 1.5rem;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 30:
  ```text
                        background:#d97706;color:#fff;border:none;border-radius:0.75rem;cursor:pointer;font-size:1rem;">
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 31:
  ```text
                        Retry
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 32:
  ```text
                    </button>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 33:
  ```text
                </div>`;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 34:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 35:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 36:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 2. `showToast`

- Signature: `function showToast(message, param)`
- Source range: lines 62-82

#### Line-by-line

- Line 62:
  ```text
function showToast(message, type = 'success') {
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 63:
  ```text
    const existing = document.getElementById('adminToast');
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 64:
  ```text
    if (existing) existing.remove();
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 65:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 66:
  ```text
    const toast = document.createElement('div');
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 67:
  ```text
    toast.id = 'adminToast';
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 68:
  ```text
    const bg = type === 'success' ? '#ecfdf5' : 'rgba(217,119,6,0.1)';
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 69:
  ```text
    const border = type === 'success' ? '#6ee7b7' : 'rgba(217,119,6,0.4)';
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 70:
  ```text
    const color = type === 'success' ? '#065f46' : '#92400e';
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 71:
  ```text
    toast.style.cssText = `
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 72:
  ```text
        position:fixed; bottom:2rem; right:2rem; z-index:9999;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 73:
  ```text
        background:${bg}; border:1.5px solid ${border}; color:${color};
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 74:
  ```text
        padding:1rem 1.5rem; border-radius:1rem;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 75:
  ```text
        box-shadow:0 10px 25px rgba(0,0,0,0.1);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 76:
  ```text
        font-size:0.9rem; font-weight:600; max-width:360px;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 77:
  ```text
        animation: slideIn 0.3s ease;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 78:
  ```text
    `;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 79:
  ```text
    toast.textContent = message;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 80:
  ```text
    document.body.appendChild(toast);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 81:
  ```text
    setTimeout(() => toast.remove(), 5000);
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 82:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 3. `renderCategories`

- Signature: `function renderCategories()`
- Source range: lines 114-149

#### Line-by-line

- Line 114:
  ```text
function renderCategories() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 115:
  ```text
    const grid = document.getElementById('categoriesGrid');
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 116:
  ```text
    if (!grid) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 117:
  ```text
    grid.innerHTML = '';
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 118:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 119:
  ```text
    categories.forEach((category) => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 120:
  ```text
        const card = document.createElement('div');
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 121:
  ```text
        card.className = 'flex flex-col rounded-2xl overflow-hidden bg-white shadow-sm hover:shadow-md transition-shadow';
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 122:
  ```text
        card.innerHTML = `
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 123:
  ```text
            <div onclick="navigateToPackagesList('${encodeURIComponent(category.name)}')" class="cursor-pointer">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 124:
  ```text
                <img src="${category.image || ''}" alt="${category.name}"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 125:
  ```text
                    class="w-full h-[156px] object-cover rounded-t-2xl"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 126:
  ```text
                    onerror="this.src='https://api.builder.io/api/v1/image/assets/TEMP/c85bfb6836c45dfd4826c29c28b7e2b3c390cf02?width=648'">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 127:
  ```text
            </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 128:
  ```text
            <div class="flex items-center p-3 bg-white">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 129:
  ```text
                <div class="flex-1 text-center">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 130:
  ```text
                    <p class="text-[#4F6E79] font-segoe text-base font-bold">${category.name}</p>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 131:
  ```text
                </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 132:
  ```text
                <button onclick="openEditModal('${category.id}')" class="flex-shrink-0 p-2 hover:bg-gray-100 rounded">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 133:
  ```text
                    <svg width="20" height="20" viewBox="0 0 20 20" fill="none">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 134:
  ```text
                        <path fill-rule="evenodd" clip-rule="evenodd" d="M9.43299 5.14706H4.16667C3.2462 5.14706 2.5 5.93714 2.5 6.91176V15.7353C2.5 16.7099 3.2462 17.5 4.16667 17.5H13.3334C14.2539 17.5 15.0001 16.7099 15.0001 15.7353V9.27681L11.7386 12.7301C11.4478 13.0381 11.0773 13.248 10.674 13.3334L8.43975 13.8065C6.98199 14.1152 5.69673 12.7544 5.98828 11.2109L6.43514 8.84519C6.5158 8.41815 6.71404 8.02595 7.00487 7.71801L9.43299 5.14706Z" fill="#4F6E79"/>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 135:
  ```text
                        <path fill-rule="evenodd" clip-rule="evenodd" d="M16.5387 3.59866C16.4542 3.38277 16.3303 3.18662 16.1742 3.02142C16.0182 2.85612 15.8329 2.72499 15.629 2.63552C15.4251 2.54605 15.2066 2.5 14.9859 2.5C14.7652 2.5 14.5466 2.54605 14.3427 2.63552C14.1388 2.72499 13.9536 2.85612 13.7975 3.02142L13.3426 3.50308L15.7193 6.01955L16.1742 5.53789C16.3303 5.3727 16.4542 5.17655 16.5387 4.96066C16.6232 4.74476 16.6667 4.51336 16.6667 4.27966C16.6667 4.04596 16.6232 3.81455 16.5387 3.59866ZM14.5408 7.26739L12.1641 4.75092L8.18338 8.96584C8.12522 9.02743 8.08557 9.10587 8.06944 9.19128L7.62258 11.557C7.56427 11.8657 7.82132 12.1378 8.11287 12.0761L10.3471 11.603C10.4278 11.5859 10.5019 11.5439 10.5601 11.4823L14.5408 7.26739Z" fill="#4F6E79"/>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 136:
  ```text
                    </svg>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 137:
  ```text
                </button>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 138:
  ```text
            </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 139:
  ```text
        `;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 140:
  ```text
        grid.appendChild(card);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 141:
  ```text
    });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 142:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 143:
  ```text
    // Create category button
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 144:
  ```text
    const createCard = document.createElement('button');
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 145:
  ```text
    createCard.onclick = openCreateModal;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 146:
  ```text
    createCard.className = 'flex flex-col items-center justify-center rounded-2xl border-4 border-dashed border-[#BDDAE0] h-[224px] hover:border-[#9DBAC0] transition-colors';
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 147:
  ```text
    createCard.innerHTML = '<p class="text-[#BDDAE0] font-segoe text-base font-bold">CREATE CATEGORY</p>';
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 148:
  ```text
    grid.appendChild(createCard);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 149:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 4. `openCreateModal`

- Signature: `function openCreateModal()`
- Source range: lines 159-161

#### Line-by-line

- Line 159:
  ```text
function openCreateModal() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 160:
  ```text
    console.warn('openCreateModal called — CRUD not yet wired');
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 161:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

## File: `HeigenKiosk/shared_package_data.js`

- Methods documented: 23

### 1. `apiFetch`

- Signature: `async function apiFetch(path, param)`
- Source range: lines 9-28

#### Line-by-line

- Line 9:
  ```text
async function apiFetch(path, options = {}) {
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 10:
  ```text
    const url = `${API_BASE}${path}`;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 11:
  ```text
    const res = await fetch(url, {
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 12:
  ```text
        headers: {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 13:
  ```text
            "Content-Type": "application/json",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 14:
  ```text
            ...(options.headers || {}),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 15:
  ```text
        },
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 16:
  ```text
        ...options,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 17:
  ```text
    });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 18:
  ```text
    if (!res.ok) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 19:
  ```text
        let msg = `HTTP ${res.status}`;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 20:
  ```text
        try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 21:
  ```text
            const e = await res.json();
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 22:
  ```text
            msg = e.detail || JSON.stringify(e);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 23:
  ```text
        } catch (_) {}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 24:
  ```text
        throw new Error(msg);
  ```
  Explanation: Raises an error/exception to signal failure to the caller.
- Line 25:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 26:
  ```text
    if (res.status === 204) return null;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 27:
  ```text
    return res.json();
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 28:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 2. `getPackages`

- Signature: `async function getPackages()`
- Source range: lines 32-34

#### Line-by-line

- Line 32:
  ```text
export async function getPackages() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 33:
  ```text
    return apiFetch("/packages/");
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 34:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 3. `getPackagesByCategory`

- Signature: `async function getPackagesByCategory(categoryName)`
- Source range: lines 36-41

#### Line-by-line

- Line 36:
  ```text
export async function getPackagesByCategory(categoryName) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 37:
  ```text
    const all = await getPackages();
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 38:
  ```text
    if (!categoryName) return all;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 39:
  ```text
    const lower = categoryName.toLowerCase();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 40:
  ```text
    return all.filter((p) => p.category?.toLowerCase() === lower);
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 41:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 4. `createPackage`

- Signature: `async function createPackage(data)`
- Source range: lines 43-48

#### Line-by-line

- Line 43:
  ```text
export async function createPackage(data) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 44:
  ```text
    return apiFetch("/packages/", {
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 45:
  ```text
        method: "POST",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 46:
  ```text
        body: JSON.stringify(data),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 47:
  ```text
    });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 48:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 5. `updatePackage`

- Signature: `async function updatePackage(id, data)`
- Source range: lines 50-55

#### Line-by-line

- Line 50:
  ```text
export async function updatePackage(id, data) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 51:
  ```text
    return apiFetch(`/packages/${id}/`, {
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 52:
  ```text
        method: "PUT",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 53:
  ```text
        body: JSON.stringify(data),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 54:
  ```text
    });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 55:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 6. `deletePackage`

- Signature: `async function deletePackage(id)`
- Source range: lines 57-59

#### Line-by-line

- Line 57:
  ```text
export async function deletePackage(id) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 58:
  ```text
    return apiFetch(`/packages/${id}/`, { method: "DELETE" });
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 59:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 7. `getAddons`

- Signature: `async function getAddons()`
- Source range: lines 63-65

#### Line-by-line

- Line 63:
  ```text
export async function getAddons() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 64:
  ```text
    return apiFetch("/addons/");
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 65:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 8. `getAddonsByCategory`

- Signature: `async function getAddonsByCategory(categoryName)`
- Source range: lines 67-78

#### Line-by-line

- Line 67:
  ```text
export async function getAddonsByCategory(categoryName) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 68:
  ```text
    const all = await getAddons();
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 69:
  ```text
    if (!categoryName) return all;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 70:
  ```text
    const lower = categoryName.toLowerCase();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 71:
  ```text
    return all.filter((a) => {
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 72:
  ```text
        if (!a.applies_to) return true;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 73:
  ```text
        const applies = Array.isArray(a.applies_to)
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 74:
  ```text
            ? a.applies_to.join(",")
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 75:
  ```text
            : String(a.applies_to);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 76:
  ```text
        return applies.toLowerCase().includes(lower) || applies === "*";
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 77:
  ```text
    });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 78:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 9. `createAddon`

- Signature: `async function createAddon(data)`
- Source range: lines 80-82

#### Line-by-line

- Line 80:
  ```text
export async function createAddon(data) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 81:
  ```text
    return apiFetch("/addons/", { method: "POST", body: JSON.stringify(data) });
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 82:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 10. `updateAddon`

- Signature: `async function updateAddon(id, data)`
- Source range: lines 84-89

#### Line-by-line

- Line 84:
  ```text
export async function updateAddon(id, data) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 85:
  ```text
    return apiFetch(`/addons/${id}/`, {
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 86:
  ```text
        method: "PUT",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 87:
  ```text
        body: JSON.stringify(data),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 88:
  ```text
    });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 89:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 11. `deleteAddon`

- Signature: `async function deleteAddon(id)`
- Source range: lines 91-93

#### Line-by-line

- Line 91:
  ```text
export async function deleteAddon(id) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 92:
  ```text
    return apiFetch(`/addons/${id}/`, { method: "DELETE" });
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 93:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 12. `getCategories`

- Signature: `async function getCategories()`
- Source range: lines 97-110

#### Line-by-line

- Line 97:
  ```text
export async function getCategories() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 98:
  ```text
    const packages = await getPackages();
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 99:
  ```text
    const seen = new Map();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 100:
  ```text
    packages.forEach((p) => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 101:
  ```text
        if (p.category && !seen.has(p.category)) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 102:
  ```text
            seen.set(p.category, {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 103:
  ```text
                id: p.category,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 104:
  ```text
                name: p.category,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 105:
  ```text
                image: null,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 106:
  ```text
            });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 107:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 108:
  ```text
    });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 109:
  ```text
    return Array.from(seen.values());
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 110:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 13. `getCustomers`

- Signature: `async function getCustomers()`
- Source range: lines 114-116

#### Line-by-line

- Line 114:
  ```text
export async function getCustomers() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 115:
  ```text
    return apiFetch("/customers/all/");
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 116:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 14. `getCustomer`

- Signature: `async function getCustomer(id)`
- Source range: lines 118-120

#### Line-by-line

- Line 118:
  ```text
export async function getCustomer(id) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 119:
  ```text
    return apiFetch(`/customers/${id}/`);
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 120:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 15. `createCustomer`

- Signature: `async function createCustomer(data)`
- Source range: lines 122-127

#### Line-by-line

- Line 122:
  ```text
export async function createCustomer(data) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 123:
  ```text
    return apiFetch("/customers/", {
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 124:
  ```text
        method: "POST",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 125:
  ```text
        body: JSON.stringify(data),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 126:
  ```text
    });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 127:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 16. `updateCustomer`

- Signature: `async function updateCustomer(id, data)`
- Source range: lines 129-134

#### Line-by-line

- Line 129:
  ```text
export async function updateCustomer(id, data) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 130:
  ```text
    return apiFetch(`/customers/${id}/`, {
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 131:
  ```text
        method: "PUT",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 132:
  ```text
        body: JSON.stringify(data),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 133:
  ```text
    });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 134:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 17. `deleteCustomers`

- Signature: `async function deleteCustomers(ids)`
- Source range: lines 136-141

#### Line-by-line

- Line 136:
  ```text
export async function deleteCustomers(ids) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 137:
  ```text
    return apiFetch("/customers/bulk-delete/", {
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 138:
  ```text
        method: "POST",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 139:
  ```text
        body: JSON.stringify({ ids }),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 140:
  ```text
    });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 141:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 18. `getBookings`

- Signature: `async function getBookings(param)`
- Source range: lines 145-148

#### Line-by-line

- Line 145:
  ```text
export async function getBookings(statusFilter = null) {
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 146:
  ```text
    const q = statusFilter ? `?status=${encodeURIComponent(statusFilter)}` : "";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 147:
  ```text
    return apiFetch(`/bookings/${q}`);
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 148:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 19. `getCustomerBookings`

- Signature: `async function getCustomerBookings(customerId)`
- Source range: lines 150-152

#### Line-by-line

- Line 150:
  ```text
export async function getCustomerBookings(customerId) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 151:
  ```text
    return apiFetch(`/customers/${customerId}/bookings/`);
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 152:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 20. `createBooking`

- Signature: `async function createBooking(customerId, data)`
- Source range: lines 154-159

#### Line-by-line

- Line 154:
  ```text
export async function createBooking(customerId, data) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 155:
  ```text
    return apiFetch(`/customers/${customerId}/bookings/`, {
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 156:
  ```text
        method: "POST",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 157:
  ```text
        body: JSON.stringify(data),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 158:
  ```text
    });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 159:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 21. `updateBookingStatus`

- Signature: `async function updateBookingStatus(bookingId, sessionStatus)`
- Source range: lines 161-166

#### Line-by-line

- Line 161:
  ```text
export async function updateBookingStatus(bookingId, sessionStatus) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 162:
  ```text
    return apiFetch(`/bookings/${bookingId}/status/`, {
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 163:
  ```text
        method: "PATCH",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 164:
  ```text
        body: JSON.stringify({ session_status: sessionStatus }),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 165:
  ```text
    });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 166:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 22. `getRecommendations`

- Signature: `async function getRecommendations(customerId, param, param)`
- Source range: lines 170-174

#### Line-by-line

- Line 170:
  ```text
export async function getRecommendations(customerId, targetDate = null, k = 3) {
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 171:
  ```text
    const params = new URLSearchParams({ k: String(k) });
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 172:
  ```text
    if (targetDate) params.append("date", targetDate);
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 173:
  ```text
    return apiFetch(`/recommendations/${customerId}/?${params.toString()}`);
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 174:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 23. `refreshRecommender`

- Signature: `async function refreshRecommender()`
- Source range: lines 183-199

#### Line-by-line

- Line 183:
  ```text
export async function refreshRecommender() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 184:
  ```text
    // Hit the recommendations endpoint to force recompute.
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 185:
  ```text
    // If you add a dedicated Django management command or view like
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 186:
  ```text
    // POST /api/recommendations/refresh/, swap this to that.
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 187:
  ```text
    try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 188:
  ```text
        const customers = await getCustomers();
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 189:
  ```text
        if (customers.length > 0) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 190:
  ```text
            await getRecommendations(customers[0].id);
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 191:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 192:
  ```text
        return {
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 193:
  ```text
            success: true,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 194:
  ```text
            message: "Recommender refreshed from latest booking data.",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 195:
  ```text
        };
  ```
  Explanation: Closes the current block/scope.
- Line 196:
  ```text
    } catch (err) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 197:
  ```text
        return { success: false, message: err.message };
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 198:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 199:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

## File: `HeigenKiosk/src/api/client.js`

- Methods documented: 14

### 1. `apiRequest`

- Signature: `async function apiRequest(path, param)`
- Source range: lines 5-23

#### Line-by-line

- Line 5:
  ```text
async function apiRequest(path, options = {}) {
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 6:
  ```text
  const url = path.startsWith('http') ? path : `${API_BASE_URL}${path}`;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 7:
  ```text
  const response = await fetch(url, {
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 8:
  ```text
    headers: { 'Content-Type': 'application/json', ...(options.headers || {}) },
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 9:
  ```text
    ...options,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 10:
  ```text
  });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 11:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 12:
  ```text
  if (!response.ok) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 13:
  ```text
    let detail = `HTTP ${response.status}`;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 14:
  ```text
    try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 15:
  ```text
      const err = await response.json();
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 16:
  ```text
      detail = err.detail || JSON.stringify(err);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 17:
  ```text
    } catch (_) {}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 18:
  ```text
    throw new Error(detail);
  ```
  Explanation: Raises an error/exception to signal failure to the caller.
- Line 19:
  ```text
  }
  ```
  Explanation: Closes the current block/scope.
- Line 20:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 21:
  ```text
  if (response.status === 204) return null;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 22:
  ```text
  return response.json();
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 23:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 2. `fetchAllPages`

- Signature: `async function fetchAllPages(path)`
- Source range: lines 32-53

#### Line-by-line

- Line 32:
  ```text
async function fetchAllPages(path) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 33:
  ```text
  const first = await apiRequest(path);
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 34:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 35:
  ```text
  // Flat array — pagination disabled on this viewset (pagination_class = None)
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 36:
  ```text
  if (Array.isArray(first)) return first;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 37:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 38:
  ```text
  // Paginated envelope — follow all pages
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 39:
  ```text
  if (first && Array.isArray(first.results)) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 40:
  ```text
    let items = [...first.results];
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 41:
  ```text
    let nextUrl = first.next;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 42:
  ```text
    while (nextUrl) {
  ```
  Explanation: Repeats execution while the condition remains true.
- Line 43:
  ```text
      const page = await apiRequest(nextUrl);
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 44:
  ```text
      if (Array.isArray(page)) { items = items.concat(page); break; }
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 45:
  ```text
      if (page && Array.isArray(page.results)) items = items.concat(page.results);
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 46:
  ```text
      nextUrl = page ? page.next : null;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 47:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 48:
  ```text
    return items;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 49:
  ```text
  }
  ```
  Explanation: Closes the current block/scope.
- Line 50:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 51:
  ```text
  // Fallback
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 52:
  ```text
  return Array.isArray(first) ? first : [];
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 53:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 3. `fetchPackages`

- Signature: `async function fetchPackages(param)`
- Source range: lines 56-61

#### Line-by-line

- Line 56:
  ```text
export async function fetchPackages(categoryName = null) {
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 57:
  ```text
  const data = await fetchAllPages('/packages/');
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 58:
  ```text
  if (!categoryName) return data;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 59:
  ```text
  const lower = categoryName.toLowerCase();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 60:
  ```text
  return data.filter((p) => p.category && p.category.toLowerCase() === lower);
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 61:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 4. `fetchCategories`

- Signature: `async function fetchCategories()`
- Source range: lines 63-87

#### Line-by-line

- Line 63:
  ```text
export async function fetchCategories() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 64:
  ```text
  try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 65:
  ```text
    const categories = await fetchAllPages('/categories/');
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 66:
  ```text
    if (Array.isArray(categories) && categories.length) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 67:
  ```text
      return categories.map((category) => ({
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 68:
  ```text
        ...category,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 69:
  ```text
        image: resolveCategoryImage(category),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 70:
  ```text
      }));
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 71:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 72:
  ```text
  } catch (_) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 73:
  ```text
    // Fallback to deriving categories from packages for older backend deployments.
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 74:
  ```text
  }
  ```
  Explanation: Closes the current block/scope.
- Line 75:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 76:
  ```text
  const packages = await fetchAllPages('/packages/');
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 77:
  ```text
  const seen = new Map();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 78:
  ```text
  packages.forEach((pkg) => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 79:
  ```text
    if (!pkg.category || seen.has(pkg.category)) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 80:
  ```text
    const category = { id: pkg.category, name: pkg.category, image: null };
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 81:
  ```text
    seen.set(pkg.category, {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 82:
  ```text
      ...category,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 83:
  ```text
      image: resolveCategoryImage(category),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 84:
  ```text
    });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 85:
  ```text
  });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 86:
  ```text
  return Array.from(seen.values());
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 87:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 5. `fetchAddons`

- Signature: `async function fetchAddons(param)`
- Source range: lines 90-101

#### Line-by-line

- Line 90:
  ```text
export async function fetchAddons(categoryName = null) {
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 91:
  ```text
  const data = await fetchAllPages('/addons/');
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 92:
  ```text
  if (!categoryName) return data;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 93:
  ```text
  const lower = categoryName.toLowerCase();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 94:
  ```text
  return data.filter((a) => {
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 95:
  ```text
    if (!a.applies_to) return true;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 96:
  ```text
    const applies = Array.isArray(a.applies_to)
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 97:
  ```text
      ? a.applies_to.join(',')
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 98:
  ```text
      : String(a.applies_to);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 99:
  ```text
    return applies.toLowerCase().includes(lower) || applies === '*';
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 100:
  ```text
  });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 101:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 6. `fetchPopularPackage`

- Signature: `async function fetchPopularPackage(categoryName)`
- Source range: lines 104-125

#### Line-by-line

- Line 104:
  ```text
export async function fetchPopularPackage(categoryName) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 105:
  ```text
  try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 106:
  ```text
    const bookings = await fetchAllPages('/bookings/');
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 107:
  ```text
    const countMap = {};
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 108:
  ```text
    bookings.forEach((b) => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 109:
  ```text
      if (!b.package_id) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 110:
  ```text
      countMap[b.package_id] = (countMap[b.package_id] || 0) + 1;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 111:
  ```text
    });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 112:
  ```text
    const packages = await fetchPackages(categoryName);
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 113:
  ```text
    const pkgIds = new Set(packages.map((p) => p.id));
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 114:
  ```text
    let topId = null, topCount = 0;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 115:
  ```text
    Object.entries(countMap).forEach(([id, count]) => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 116:
  ```text
      if (pkgIds.has(Number(id)) && count > topCount) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 117:
  ```text
        topCount = count;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 118:
  ```text
        topId = Number(id);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 119:
  ```text
      }
  ```
  Explanation: Closes the current block/scope.
- Line 120:
  ```text
    });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 121:
  ```text
    return { top_package_id: topId };
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 122:
  ```text
  } catch (_) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 123:
  ```text
    return { top_package_id: null };
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 124:
  ```text
  }
  ```
  Explanation: Closes the current block/scope.
- Line 125:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 7. `fetchPopularAddons`

- Signature: `async function fetchPopularAddons(categoryName)`
- Source range: lines 127-149

#### Line-by-line

- Line 127:
  ```text
export async function fetchPopularAddons(categoryName) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 128:
  ```text
  try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 129:
  ```text
    const scopedAddons = await fetchAddons(categoryName);
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 130:
  ```text
    const allowedAddonIds = new Set(scopedAddons.map((a) => Number(a.id)));
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 131:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 132:
  ```text
    const bookings = await fetchAllPages('/bookings/');
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 133:
  ```text
    const countMap = {};
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 134:
  ```text
    bookings.forEach((b) => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 135:
  ```text
      (b.addons || []).forEach((a) => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 136:
  ```text
        const key = Number(a.addonId || a.id);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 137:
  ```text
        if (!allowedAddonIds.has(key)) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 138:
  ```text
        countMap[key] = (countMap[key] || 0) + (a.quantity || 1);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 139:
  ```text
      });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 140:
  ```text
    });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 141:
  ```text
    const sorted = Object.entries(countMap)
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 142:
  ```text
      .sort((a, b) => b[1] - a[1])
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 143:
  ```text
      .slice(0, 3)
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 144:
  ```text
      .map(([id]) => Number(id));
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 145:
  ```text
    return { top_addon_ids: sorted };
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 146:
  ```text
  } catch (_) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 147:
  ```text
    return { top_addon_ids: [] };
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 148:
  ```text
  }
  ```
  Explanation: Closes the current block/scope.
- Line 149:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 8. `findCustomerByEmail`

- Signature: `async function findCustomerByEmail(email)`
- Source range: lines 152-161

#### Line-by-line

- Line 152:
  ```text
export async function findCustomerByEmail(email) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 153:
  ```text
  try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 154:
  ```text
    // /customers/all/ is unpaginated by design in your CustomerViewSet
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 155:
  ```text
    const all = await apiRequest('/customers/all/');
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 156:
  ```text
    const list = Array.isArray(all) ? all : (all.results || []);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 157:
  ```text
    return list.find((c) => c.email && c.email.toLowerCase() === email.toLowerCase()) || null;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 158:
  ```text
  } catch (_) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 159:
  ```text
    return null;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 160:
  ```text
  }
  ```
  Explanation: Closes the current block/scope.
- Line 161:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 9. `createCustomer`

- Signature: `async function createCustomer(data)`
- Source range: lines 163-173

#### Line-by-line

- Line 163:
  ```text
export async function createCustomer(data) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 164:
  ```text
  return apiRequest('/customers/', {
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 165:
  ```text
    method: 'POST',
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 166:
  ```text
    body: JSON.stringify({
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 167:
  ```text
      name:      data.full_name,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 168:
  ```text
      email:     data.email,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 169:
  ```text
      contactNo: data.contact_number,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 170:
  ```text
      consent:   data.consent_given ? 'I Agree' : 'I Disagree',
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 171:
  ```text
    }),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 172:
  ```text
  });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 173:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 10. `submitBooking`

- Signature: `async function submitBooking(payload)`
- Source range: lines 176-205

#### Line-by-line

- Line 176:
  ```text
export async function submitBooking(payload) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 177:
  ```text
  let customer = await findCustomerByEmail(payload.customer.email);
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 178:
  ```text
  if (!customer) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 179:
  ```text
    customer = await createCustomer(payload.customer);
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 180:
  ```text
  }
  ```
  Explanation: Closes the current block/scope.
- Line 181:
  ```text
  const customerId = customer.id || customer.customer_id;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 182:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 183:
  ```text
  const addonsInput = (payload.addon_ids || []).map((id) => ({
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 184:
  ```text
    addonId: id,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 185:
  ```text
    quantity: 1,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 186:
  ```text
  }));
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 187:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 188:
  ```text
  const bookingData = {
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 189:
  ```text
    customer_id:    customerId,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 190:
  ```text
    package_id:     payload.package_id,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 191:
  ```text
    addons_input:   addonsInput,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 192:
  ```text
    session_status: 'Pending',
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 193:
  ```text
    total_price:    payload.total_amount,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 194:
  ```text
    session_date:   payload.preferred_date
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 195:
  ```text
      ? new Date(payload.preferred_date).toISOString()
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 196:
  ```text
      : null,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 197:
  ```text
  };
  ```
  Explanation: Closes the current block/scope.
- Line 198:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 199:
  ```text
  const booking = await apiRequest(`/customers/${customerId}/bookings/`, {
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 200:
  ```text
    method: 'POST',
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 201:
  ```text
    body: JSON.stringify(bookingData),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 202:
  ```text
  });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 203:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 204:
  ```text
  return { customer, booking };
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 205:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 11. `fetchBookingsByStatus`

- Signature: `async function fetchBookingsByStatus(param)`
- Source range: lines 208-210

#### Line-by-line

- Line 208:
  ```text
export async function fetchBookingsByStatus(statuses = 'Pending,Ongoing') {
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 209:
  ```text
  return fetchAllPages(`/bookings/?status=${encodeURIComponent(statuses)}`);
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 210:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 12. `updateBookingStatus`

- Signature: `async function updateBookingStatus(bookingId, sessionStatus)`
- Source range: lines 212-217

#### Line-by-line

- Line 212:
  ```text
export async function updateBookingStatus(bookingId, sessionStatus) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 213:
  ```text
  return apiRequest(`/bookings/${bookingId}/status/`, {
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 214:
  ```text
    method: 'PATCH',
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 215:
  ```text
    body: JSON.stringify({ session_status: sessionStatus }),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 216:
  ```text
  });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 217:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 13. `fetchRecommendations`

- Signature: `async function fetchRecommendations(customerId, param, param)`
- Source range: lines 220-226

#### Line-by-line

- Line 220:
  ```text
export async function fetchRecommendations(customerId, targetDate = null, k = 3) {
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 221:
  ```text
  let url = `/recommendations/${customerId}/`;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 222:
  ```text
  const params = new URLSearchParams({ k: String(k) });
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 223:
  ```text
  if (targetDate) params.append('date', targetDate);
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 224:
  ```text
  url += '?' + params.toString();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 225:
  ```text
  return apiRequest(url);
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 226:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 14. `fetchPopularRecommendations`

- Signature: `async function fetchPopularRecommendations(param)`
- Source range: lines 228-230

#### Line-by-line

- Line 228:
  ```text
export async function fetchPopularRecommendations(k = 3) {
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 229:
  ```text
  return apiRequest(`/recommendations/popular/?k=${encodeURIComponent(String(k))}`);
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 230:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

## File: `HeigenKiosk/src/components/Icon.js`

- Methods documented: 1

### 1. `Icon`

- Signature: `function Icon(param)`
- Source range: lines 64-89

#### Line-by-line

- Line 64:
  ```text
export default function Icon({ name = '', size = 20, color = '#000', style }) {
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 65:
  ```text
  const char = ICONS[name] ?? '•';
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 66:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 67:
  ```text
  // Special handling for circle-wrapped icons to match the Ionicons visual weight
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 68:
  ```text
  const isCircleIcon = name.includes('circle') || name === 'reload' || name === 'sync';
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 69:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 70:
  ```text
  return (
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 71:
  ```text
    <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 72:
  ```text
      style={[
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 73:
  ```text
        {
  ```
  Explanation: Opens a new block scope.
- Line 74:
  ```text
          fontSize: isCircleIcon ? size * 1.1 : size,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 75:
  ```text
          color,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 76:
  ```text
          // Prevent text scaling from system accessibility settings
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 77:
  ```text
          // breaking layout in a kiosk context
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 78:
  ```text
          includeFontPadding: false,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 79:
  ```text
          textAlignVertical: 'center',
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 80:
  ```text
          lineHeight: size * 1.35,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 81:
  ```text
        },
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 82:
  ```text
        style,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 83:
  ```text
      ]}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 84:
  ```text
      allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 85:
  ```text
    >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 86:
  ```text
      {char}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 87:
  ```text
    </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 88:
  ```text
  );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 89:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

## File: `HeigenKiosk/src/components/ui.js`

- Methods documented: 11

### 1. `Button`

- Signature: `function Button(param)`
- Source range: lines 22-111

#### Line-by-line

- Line 22:
  ```text
export function Button({
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 23:
  ```text
    onPress,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 24:
  ```text
    label,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 25:
  ```text
    variant = "primary",
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 26:
  ```text
    disabled = false,
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 27:
  ```text
    size = "md",
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 28:
  ```text
    loading = false,
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 29:
  ```text
    icon,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 30:
  ```text
    style,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 31:
  ```text
    labelStyle,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 32:
  ```text
    fullWidth = false,
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 33:
  ```text
}) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 34:
  ```text
    const { s, fs } = useScale();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 35:
  ```text
    const isDisabled = disabled || loading;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 36:
  ```text
    const paddingV = size === "sm" ? s(1) : s(spacing.lg);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 37:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 38:
  ```text
    const containerStyles = [
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 39:
  ```text
        {
  ```
  Explanation: Opens a new block scope.
- Line 40:
  ```text
            flexDirection: "row",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 41:
  ```text
            alignItems: "center",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 42:
  ```text
            justifyContent: "center",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 43:
  ```text
            paddingHorizontal: s(spacing.xxl),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 44:
  ```text
            paddingVertical: paddingV,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 45:
  ```text
            borderRadius: s(radii.xxl),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 46:
  ```text
            minHeight: s(52),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 47:
  ```text
        },
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 48:
  ```text
        variant === "primary" && {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 49:
  ```text
            backgroundColor: colors.accent,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 50:
  ```text
            ...shadow.accent,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 51:
  ```text
        },
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 52:
  ```text
        variant === "secondary" && { backgroundColor: colors.muted },
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 53:
  ```text
        variant === "ghost" && { backgroundColor: "transparent" },
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 54:
  ```text
        variant === "danger" && {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 55:
  ```text
            backgroundColor: "#fee2e2",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 56:
  ```text
            borderWidth: 1.5,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 57:
  ```text
            borderColor: colors.error,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 58:
  ```text
        },
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 59:
  ```text
        fullWidth && { width: "100%" },
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 60:
  ```text
        isDisabled && { opacity: 0.5 },
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 61:
  ```text
        style,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 62:
  ```text
    ];
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 63:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 64:
  ```text
    const labelStyles = [
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 65:
  ```text
        { fontSize: fs(16), fontWeight: "600" },
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 66:
  ```text
        variant === "primary" && { color: "#fff" },
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 67:
  ```text
        variant === "secondary" && { color: colors.foreground },
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 68:
  ```text
        variant === "ghost" && { color: colors.mutedForeground },
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 69:
  ```text
        variant === "danger" && { color: colors.error },
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 70:
  ```text
        labelStyle,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 71:
  ```text
    ];
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 72:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 73:
  ```text
    const iconColor =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 74:
  ```text
        variant === "primary"
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 75:
  ```text
            ? "#fff"
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 76:
  ```text
            : variant === "danger"
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 77:
  ```text
              ? colors.error
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 78:
  ```text
              : colors.accent;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 79:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 80:
  ```text
    return (
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 81:
  ```text
        <TouchableOpacity
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 82:
  ```text
            onPress={onPress}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 83:
  ```text
            disabled={isDisabled}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 84:
  ```text
            activeOpacity={0.8}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 85:
  ```text
            style={containerStyles}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 86:
  ```text
        >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 87:
  ```text
            {loading ? (
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 88:
  ```text
                <ActivityIndicator
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 89:
  ```text
                    color={variant === "primary" ? "#fff" : colors.accent}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 90:
  ```text
                    size="small"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 91:
  ```text
                />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 92:
  ```text
            ) : (
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 93:
  ```text
                <>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 94:
  ```text
                    {icon && (
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 95:
  ```text
                        <Icon
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 96:
  ```text
                            name={icon}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 97:
  ```text
                            size={s(18)}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 98:
  ```text
                            color={iconColor}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 99:
  ```text
                            style={{ marginRight: label ? s(6) : 0 }}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 100:
  ```text
                        />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 101:
  ```text
                    )}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 102:
  ```text
                    {label && (
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 103:
  ```text
                        <Text style={labelStyles} allowFontScaling={false}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 104:
  ```text
                            {label}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 105:
  ```text
                        </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 106:
  ```text
                    )}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 107:
  ```text
                </>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 108:
  ```text
            )}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 109:
  ```text
        </TouchableOpacity>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 110:
  ```text
    );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 111:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 2. `Card`

- Signature: `function Card(param)`
- Source range: lines 114-150

#### Line-by-line

- Line 114:
  ```text
export function Card({
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 115:
  ```text
    children,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 116:
  ```text
    style,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 117:
  ```text
    onPress,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 118:
  ```text
    selected = false,
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 119:
  ```text
    accent = false,
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 120:
  ```text
}) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 121:
  ```text
    const { s } = useScale();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 122:
  ```text
    const cardStyles = [
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 123:
  ```text
        {
  ```
  Explanation: Opens a new block scope.
- Line 124:
  ```text
            backgroundColor: colors.card,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 125:
  ```text
            borderRadius: s(radii.xxl),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 126:
  ```text
            borderWidth: 2,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 127:
  ```text
            borderColor: colors.border,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 128:
  ```text
            padding: s(spacing.xl),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 129:
  ```text
            ...shadow.sm,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 130:
  ```text
        },
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 131:
  ```text
        selected && {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 132:
  ```text
            borderColor: colors.accent,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 133:
  ```text
            backgroundColor: "rgba(217,119,6,0.04)",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 134:
  ```text
        },
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 135:
  ```text
        accent && { borderColor: colors.accent, ...shadow.accent },
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 136:
  ```text
        style,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 137:
  ```text
    ];
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 138:
  ```text
    if (onPress) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 139:
  ```text
        return (
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 140:
  ```text
            <TouchableOpacity
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 141:
  ```text
                onPress={onPress}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 142:
  ```text
                activeOpacity={0.85}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 143:
  ```text
                style={cardStyles}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 144:
  ```text
            >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 145:
  ```text
                {children}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 146:
  ```text
            </TouchableOpacity>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 147:
  ```text
        );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 148:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 149:
  ```text
    return <View style={cardStyles}>{children}</View>;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 150:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 3. `AccentCard`

- Signature: `function AccentCard(param)`
- Source range: lines 153-179

#### Line-by-line

- Line 153:
  ```text
export function AccentCard({ children, style, onPress }) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 154:
  ```text
    const { s } = useScale();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 155:
  ```text
    const inner = (
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 156:
  ```text
        <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 157:
  ```text
            style={[
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 158:
  ```text
                {
  ```
  Explanation: Opens a new block scope.
- Line 159:
  ```text
                    borderRadius: s(radii.xxl),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 160:
  ```text
                    borderWidth: 2,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 161:
  ```text
                    borderColor: colors.accent,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 162:
  ```text
                    backgroundColor: "rgba(217,119,6,0.08)",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 163:
  ```text
                    padding: s(spacing.xl),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 164:
  ```text
                    ...shadow.accent,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 165:
  ```text
                },
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 166:
  ```text
                style,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 167:
  ```text
            ]}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 168:
  ```text
        >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 169:
  ```text
            {children}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 170:
  ```text
        </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 171:
  ```text
    );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 172:
  ```text
    if (onPress)
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 173:
  ```text
        return (
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 174:
  ```text
            <TouchableOpacity onPress={onPress} activeOpacity={0.85}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 175:
  ```text
                {inner}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 176:
  ```text
            </TouchableOpacity>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 177:
  ```text
        );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 178:
  ```text
    return inner;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 179:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 4. `Badge`

- Signature: `function Badge(param)`
- Source range: lines 182-223

#### Line-by-line

- Line 182:
  ```text
export function Badge({ label, variant = "accent", style }) {
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 183:
  ```text
    const { s, fs } = useScale();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 184:
  ```text
    const bgMap = {
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 185:
  ```text
        accent: colors.accent,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 186:
  ```text
        success: colors.success,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 187:
  ```text
        warning: colors.warning,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 188:
  ```text
        muted: colors.muted,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 189:
  ```text
    };
  ```
  Explanation: Closes the current block/scope.
- Line 190:
  ```text
    const txtMap = {
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 191:
  ```text
        accent: "#fff",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 192:
  ```text
        success: "#fff",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 193:
  ```text
        warning: colors.warningText,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 194:
  ```text
        muted: colors.mutedForeground,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 195:
  ```text
    };
  ```
  Explanation: Closes the current block/scope.
- Line 196:
  ```text
    return (
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 197:
  ```text
        <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 198:
  ```text
            style={[
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 199:
  ```text
                {
  ```
  Explanation: Opens a new block scope.
- Line 200:
  ```text
                    paddingHorizontal: s(spacing.md),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 201:
  ```text
                    paddingVertical: s(3),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 202:
  ```text
                    borderRadius: s(radii.full),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 203:
  ```text
                    backgroundColor: bgMap[variant] || colors.accent,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 204:
  ```text
                    ...(variant === "warning"
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 205:
  ```text
                        ? { borderWidth: 1, borderColor: "#fde68a" }
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 206:
  ```text
                        : {}),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 207:
  ```text
                },
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 208:
  ```text
                style,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 209:
  ```text
            ]}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 210:
  ```text
        >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 211:
  ```text
            <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 212:
  ```text
                style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 213:
  ```text
                    fontSize: fs(12),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 214:
  ```text
                    fontWeight: "600",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 215:
  ```text
                    color: txtMap[variant] || "#fff",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 216:
  ```text
                }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 217:
  ```text
                allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 218:
  ```text
            >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 219:
  ```text
                {label}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 220:
  ```text
            </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 221:
  ```text
        </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 222:
  ```text
    );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 223:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 5. `SectionHeader`

- Signature: `function SectionHeader(param)`
- Source range: lines 226-267

#### Line-by-line

- Line 226:
  ```text
export function SectionHeader({ title, icon, style }) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 227:
  ```text
    const { s, fs } = useScale();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 228:
  ```text
    return (
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 229:
  ```text
        <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 230:
  ```text
            style={[
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 231:
  ```text
                {
  ```
  Explanation: Opens a new block scope.
- Line 232:
  ```text
                    flexDirection: "row",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 233:
  ```text
                    alignItems: "center",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 234:
  ```text
                    justifyContent: "center",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 235:
  ```text
                },
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 236:
  ```text
                style,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 237:
  ```text
            ]}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 238:
  ```text
        >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 239:
  ```text
            {icon && (
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 240:
  ```text
                <Icon
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 241:
  ```text
                    name={icon}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 242:
  ```text
                    size={s(18)}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 243:
  ```text
                    color={colors.accent}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 244:
  ```text
                    style={{ marginRight: s(6) }}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 245:
  ```text
                />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 246:
  ```text
            )}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 247:
  ```text
            <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 248:
  ```text
                style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 249:
  ```text
                    fontSize: fs(20),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 250:
  ```text
                    fontWeight: "700",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 251:
  ```text
                    color: colors.accent,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 252:
  ```text
                }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 253:
  ```text
                allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 254:
  ```text
            >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 255:
  ```text
                {title}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 256:
  ```text
            </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 257:
  ```text
            {icon && (
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 258:
  ```text
                <Icon
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 259:
  ```text
                    name={icon}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 260:
  ```text
                    size={s(18)}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 261:
  ```text
                    color={colors.accent}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 262:
  ```text
                    style={{ marginLeft: s(6) }}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 263:
  ```text
                />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 264:
  ```text
            )}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 265:
  ```text
        </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 266:
  ```text
    );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 267:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 6. `LoadingScreen`

- Signature: `function LoadingScreen(param)`
- Source range: lines 270-294

#### Line-by-line

- Line 270:
  ```text
export function LoadingScreen({ message = "Loading..." }) {
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 271:
  ```text
    const { s, fs } = useScale();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 272:
  ```text
    return (
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 273:
  ```text
        <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 274:
  ```text
            style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 275:
  ```text
                flex: 1,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 276:
  ```text
                alignItems: "center",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 277:
  ```text
                justifyContent: "center",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 278:
  ```text
                padding: s(spacing.xxl),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 279:
  ```text
            }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 280:
  ```text
        >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 281:
  ```text
            <ActivityIndicator size="large" color={colors.accent} />
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 282:
  ```text
            <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 283:
  ```text
                style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 284:
  ```text
                    fontSize: fs(16),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 285:
  ```text
                    color: colors.mutedForeground,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 286:
  ```text
                    marginTop: s(spacing.lg),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 287:
  ```text
                }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 288:
  ```text
                allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 289:
  ```text
            >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 290:
  ```text
                {message}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 291:
  ```text
            </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 292:
  ```text
        </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 293:
  ```text
    );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 294:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 7. `ErrorScreen`

- Signature: `function ErrorScreen(param)`
- Source range: lines 297-339

#### Line-by-line

- Line 297:
  ```text
export function ErrorScreen({ message, onRetry }) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 298:
  ```text
    const { s, fs } = useScale();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 299:
  ```text
    return (
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 300:
  ```text
        <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 301:
  ```text
            style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 302:
  ```text
                flex: 1,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 303:
  ```text
                alignItems: "center",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 304:
  ```text
                justifyContent: "center",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 305:
  ```text
                padding: s(spacing.xxl),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 306:
  ```text
            }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 307:
  ```text
        >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 308:
  ```text
            <Text style={{ fontSize: s(56), color: colors.mutedForeground }}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 309:
  ```text
                ✗
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 310:
  ```text
            </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 311:
  ```text
            <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 312:
  ```text
                style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 313:
  ```text
                    fontSize: fs(20),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 314:
  ```text
                    fontWeight: "700",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 315:
  ```text
                    color: colors.error,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 316:
  ```text
                    marginTop: s(spacing.lg),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 317:
  ```text
                    marginBottom: s(spacing.sm),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 318:
  ```text
                    textAlign: "center",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 319:
  ```text
                }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 320:
  ```text
                allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 321:
  ```text
            >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 322:
  ```text
                Something went wrong
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 323:
  ```text
            </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 324:
  ```text
            <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 325:
  ```text
                style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 326:
  ```text
                    fontSize: fs(16),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 327:
  ```text
                    color: colors.mutedForeground,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 328:
  ```text
                    textAlign: "center",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 329:
  ```text
                    marginHorizontal: s(spacing.xxl),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 330:
  ```text
                    marginBottom: s(spacing.xxl),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 331:
  ```text
                }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 332:
  ```text
                allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 333:
  ```text
            >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 334:
  ```text
                {message}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 335:
  ```text
            </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 336:
  ```text
            {onRetry && <Button label="Try Again" onPress={onRetry} />}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 337:
  ```text
        </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 338:
  ```text
    );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 339:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 8. `ModalSheet`

- Signature: `function ModalSheet(param)`
- Source range: lines 342-421

#### Line-by-line

- Line 342:
  ```text
export function ModalSheet({
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 343:
  ```text
    visible,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 344:
  ```text
    onClose,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 345:
  ```text
    title,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 346:
  ```text
    children,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 347:
  ```text
    maxHeight = "90%",
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 348:
  ```text
}) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 349:
  ```text
    const { s, fs } = useScale();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 350:
  ```text
    return (
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 351:
  ```text
        <Modal
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 352:
  ```text
            visible={visible}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 353:
  ```text
            transparent
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 354:
  ```text
            animationType="slide"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 355:
  ```text
            onRequestClose={onClose}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 356:
  ```text
        >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 357:
  ```text
            <Pressable
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 358:
  ```text
                style={{ flex: 1, backgroundColor: "rgba(0,0,0,0.5)" }}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 359:
  ```text
                onPress={onClose}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 360:
  ```text
            />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 361:
  ```text
            <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 362:
  ```text
                style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 363:
  ```text
                    position: "absolute",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 364:
  ```text
                    bottom: 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 365:
  ```text
                    left: 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 366:
  ```text
                    right: 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 367:
  ```text
                    maxHeight,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 368:
  ```text
                    backgroundColor: "#fff",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 369:
  ```text
                    borderTopLeftRadius: s(radii.xxl),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 370:
  ```text
                    borderTopRightRadius: s(radii.xxl),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 371:
  ```text
                    ...shadow.lg,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 372:
  ```text
                }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 373:
  ```text
            >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 374:
  ```text
                <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 375:
  ```text
                    style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 376:
  ```text
                        width: s(40),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 377:
  ```text
                        height: s(4),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 378:
  ```text
                        borderRadius: s(2),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 379:
  ```text
                        backgroundColor: colors.border,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 380:
  ```text
                        alignSelf: "center",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 381:
  ```text
                        marginTop: s(spacing.md),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 382:
  ```text
                    }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 383:
  ```text
                />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 384:
  ```text
                <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 385:
  ```text
                    style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 386:
  ```text
                        flexDirection: "row",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 387:
  ```text
                        alignItems: "center",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 388:
  ```text
                        justifyContent: "space-between",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 389:
  ```text
                        paddingHorizontal: s(spacing.xxl),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 390:
  ```text
                        paddingVertical: s(spacing.lg),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 391:
  ```text
                        borderBottomWidth: 1,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 392:
  ```text
                        borderBottomColor: colors.border,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 393:
  ```text
                    }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 394:
  ```text
                >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 395:
  ```text
                    <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 396:
  ```text
                        style={{ fontSize: fs(20), fontWeight: "700" }}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 397:
  ```text
                        allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 398:
  ```text
                    >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 399:
  ```text
                        {title}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 400:
  ```text
                    </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 401:
  ```text
                    <TouchableOpacity
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 402:
  ```text
                        onPress={onClose}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 403:
  ```text
                        style={{ padding: s(spacing.sm) }}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 404:
  ```text
                    >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 405:
  ```text
                        <Icon
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 406:
  ```text
                            name="close"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 407:
  ```text
                            size={s(22)}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 408:
  ```text
                            color={colors.mutedForeground}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 409:
  ```text
                        />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 410:
  ```text
                    </TouchableOpacity>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 411:
  ```text
                </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 412:
  ```text
                <ScrollView
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 413:
  ```text
                    showsVerticalScrollIndicator={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 414:
  ```text
                    contentContainerStyle={{ padding: s(spacing.xxl) }}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 415:
  ```text
                >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 416:
  ```text
                    {children}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 417:
  ```text
                </ScrollView>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 418:
  ```text
            </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 419:
  ```text
        </Modal>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 420:
  ```text
    );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 421:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 9. `StepIndicator`

- Signature: `function StepIndicator(param)`
- Source range: lines 425-572

#### Line-by-line

- Line 425:
  ```text
export function StepIndicator({ steps, currentStep, compact = false }) {
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 426:
  ```text
    const { s, fs, W } = useScale();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 427:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 428:
  ```text
    if (compact) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 429:
  ```text
        // Tablet compact: numbered circles in a row, connector lines between them,
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 430:
  ```text
        // step name as a single trailing label showing the current step only.
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 431:
  ```text
        // Using fixed sizes (no scaling) so nothing balloons on tablet.
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 432:
  ```text
        const CIRCLE = 22;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 433:
  ```text
        const GAP = 40; // connector width between circles
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 434:
  ```text
        return (
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 435:
  ```text
            <View style={{ flexDirection: "row", alignItems: "center" }}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 436:
  ```text
                {steps.map((label, i) => (
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 437:
  ```text
                    <React.Fragment key={i}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 438:
  ```text
                        <View style={{ alignItems: "center", width: CIRCLE }}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 439:
  ```text
                            <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 440:
  ```text
                                style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 441:
  ```text
                                    width: CIRCLE,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 442:
  ```text
                                    height: CIRCLE,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 443:
  ```text
                                    borderRadius: CIRCLE / 2,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 444:
  ```text
                                    backgroundColor:
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 445:
  ```text
                                        i <= currentStep
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 446:
  ```text
                                            ? colors.accent
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 447:
  ```text
                                            : colors.muted,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 448:
  ```text
                                    alignItems: "center",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 449:
  ```text
                                    justifyContent: "center",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 450:
  ```text
                                }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 451:
  ```text
                            >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 452:
  ```text
                                <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 453:
  ```text
                                    style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 454:
  ```text
                                        fontSize: 11,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 455:
  ```text
                                        fontWeight: "700",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 456:
  ```text
                                        color:
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 457:
  ```text
                                            i <= currentStep
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 458:
  ```text
                                                ? "#fff"
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 459:
  ```text
                                                : colors.mutedForeground,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 460:
  ```text
                                    }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 461:
  ```text
                                    allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 462:
  ```text
                                >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 463:
  ```text
                                    {i + 1}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 464:
  ```text
                                </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 465:
  ```text
                            </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 466:
  ```text
                        </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 467:
  ```text
                        {i < steps.length - 1 && (
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 468:
  ```text
                            <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 469:
  ```text
                                style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 470:
  ```text
                                    width: GAP,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 471:
  ```text
                                    height: 2,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 472:
  ```text
                                    backgroundColor:
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 473:
  ```text
                                        i < currentStep
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 474:
  ```text
                                            ? colors.accent
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 475:
  ```text
                                            : colors.muted,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 476:
  ```text
                                }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 477:
  ```text
                            />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 478:
  ```text
                        )}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 479:
  ```text
                    </React.Fragment>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 480:
  ```text
                ))}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 481:
  ```text
                {/* Current step name shown once, after all circles */}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 482:
  ```text
                <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 483:
  ```text
                    style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 484:
  ```text
                        marginLeft: 12,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 485:
  ```text
                        fontSize: 12,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 486:
  ```text
                        fontWeight: "700",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 487:
  ```text
                        color: colors.accent,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 488:
  ```text
                    }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 489:
  ```text
                    allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 490:
  ```text
                >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 491:
  ```text
                    {steps[currentStep]}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 492:
  ```text
                </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 493:
  ```text
            </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 494:
  ```text
        );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 495:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 496:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 497:
  ```text
    // Default full indicator
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 498:
  ```text
    const lineW = Math.max(
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 499:
  ```text
        s(24),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 500:
  ```text
        (W - steps.length * s(64)) / (steps.length - 1) - s(8),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 501:
  ```text
    );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 502:
  ```text
    return (
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 503:
  ```text
        <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 504:
  ```text
            style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 505:
  ```text
                flexDirection: "row",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 506:
  ```text
                alignItems: "center",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 507:
  ```text
                justifyContent: "center",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 508:
  ```text
            }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 509:
  ```text
        >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 510:
  ```text
            {steps.map((label, i) => (
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 511:
  ```text
                <React.Fragment key={i}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 512:
  ```text
                    <View style={{ alignItems: "center", gap: s(4) }}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 513:
  ```text
                        <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 514:
  ```text
                            style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 515:
  ```text
                                width: s(34),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 516:
  ```text
                                height: s(34),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 517:
  ```text
                                borderRadius: s(17),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 518:
  ```text
                                backgroundColor:
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 519:
  ```text
                                    i <= currentStep
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 520:
  ```text
                                        ? colors.accent
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 521:
  ```text
                                        : colors.muted,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 522:
  ```text
                                alignItems: "center",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 523:
  ```text
                                justifyContent: "center",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 524:
  ```text
                            }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 525:
  ```text
                        >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 526:
  ```text
                            <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 527:
  ```text
                                style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 528:
  ```text
                                    fontSize: fs(14),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 529:
  ```text
                                    fontWeight: "600",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 530:
  ```text
                                    color:
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 531:
  ```text
                                        i <= currentStep
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 532:
  ```text
                                            ? "#fff"
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 533:
  ```text
                                            : colors.mutedForeground,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 534:
  ```text
                                }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 535:
  ```text
                                allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 536:
  ```text
                            >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 537:
  ```text
                                {i + 1}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 538:
  ```text
                            </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 539:
  ```text
                        </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 540:
  ```text
                        <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 541:
  ```text
                            style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 542:
  ```text
                                fontSize: fs(12),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 543:
  ```text
                                color:
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 544:
  ```text
                                    i <= currentStep
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 545:
  ```text
                                        ? colors.foreground
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 546:
  ```text
                                        : colors.mutedForeground,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 547:
  ```text
                                fontWeight: i <= currentStep ? "600" : "400",
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 548:
  ```text
                            }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 549:
  ```text
                            allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 550:
  ```text
                        >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 551:
  ```text
                            {label}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 552:
  ```text
                        </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 553:
  ```text
                    </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 554:
  ```text
                    {i < steps.length - 1 && (
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 555:
  ```text
                        <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 556:
  ```text
                            style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 557:
  ```text
                                width: lineW,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 558:
  ```text
                                height: s(4),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 559:
  ```text
                                borderRadius: s(2),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 560:
  ```text
                                backgroundColor:
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 561:
  ```text
                                    i < currentStep
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 562:
  ```text
                                        ? colors.accent
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 563:
  ```text
                                        : colors.muted,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 564:
  ```text
                                marginBottom: s(20),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 565:
  ```text
                            }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 566:
  ```text
                        />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 567:
  ```text
                    )}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 568:
  ```text
                </React.Fragment>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 569:
  ```text
            ))}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 570:
  ```text
        </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 571:
  ```text
    );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 572:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 10. `FormInput`

- Signature: `function FormInput(param)`
- Source range: lines 575-615

#### Line-by-line

- Line 575:
  ```text
export function FormInput({ label, required, style, ...inputProps }) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 576:
  ```text
    const [focused, setFocused] = React.useState(false);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 577:
  ```text
    const { s, fs } = useScale();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 578:
  ```text
    return (
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 579:
  ```text
        <View style={[{ marginBottom: s(spacing.xl) }, style]}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 580:
  ```text
            {label && (
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 581:
  ```text
                <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 582:
  ```text
                    style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 583:
  ```text
                        fontSize: fs(16),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 584:
  ```text
                        fontWeight: "600",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 585:
  ```text
                        marginBottom: s(spacing.sm),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 586:
  ```text
                    }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 587:
  ```text
                    allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 588:
  ```text
                >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 589:
  ```text
                    {label}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 590:
  ```text
                    {required && (
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 591:
  ```text
                        <Text style={{ color: colors.error }}> *</Text>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 592:
  ```text
                    )}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 593:
  ```text
                </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 594:
  ```text
            )}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 595:
  ```text
            <TextInput
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 596:
  ```text
                style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 597:
  ```text
                    borderWidth: 2,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 598:
  ```text
                    borderColor: focused ? colors.accent : colors.border,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 599:
  ```text
                    borderRadius: s(radii.lg),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 600:
  ```text
                    paddingHorizontal: s(spacing.xl),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 601:
  ```text
                    paddingVertical: s(spacing.lg),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 602:
  ```text
                    fontSize: fs(16),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 603:
  ```text
                    color: colors.foreground,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 604:
  ```text
                    backgroundColor: "#fff",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 605:
  ```text
                    minHeight: s(52),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 606:
  ```text
                }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 607:
  ```text
                onFocus={() => setFocused(true)}
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 608:
  ```text
                onBlur={() => setFocused(false)}
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 609:
  ```text
                placeholderTextColor={colors.mutedForeground}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 610:
  ```text
                allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 611:
  ```text
                {...inputProps}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 612:
  ```text
            />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 613:
  ```text
        </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 614:
  ```text
    );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 615:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 11. `Divider`

- Signature: `function Divider(param)`
- Source range: lines 618-622

#### Line-by-line

- Line 618:
  ```text
export function Divider({ style }) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 619:
  ```text
    return (
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 620:
  ```text
        <View style={[{ height: 1, backgroundColor: colors.border }, style]} />
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 621:
  ```text
    );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 622:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

## File: `HeigenKiosk/src/constants/assets.js`

- Methods documented: 3

### 1. `normalizeKey`

- Signature: `function normalizeKey(param)`
- Source range: lines 33-35

#### Line-by-line

- Line 33:
  ```text
function normalizeKey(value = "") {
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 34:
  ```text
    return String(value || "").toLowerCase().trim();
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 35:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 2. `resolveCategoryImage`

- Signature: `function resolveCategoryImage(category)`
- Source range: lines 37-43

#### Line-by-line

- Line 37:
  ```text
export function resolveCategoryImage(category) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 38:
  ```text
    const explicit = category?.image_url || category?.image;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 39:
  ```text
    if (explicit) return explicit;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 40:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 41:
  ```text
    const key = normalizeKey(category?.name).split(" ")[0];
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 42:
  ```text
    return CATEGORY_IMAGE_OVERRIDES[key] || DEFAULT_CATEGORY_IMAGE_URL;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 43:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 3. `resolvePackageImage`

- Signature: `function resolvePackageImage(pkg)`
- Source range: lines 45-51

#### Line-by-line

- Line 45:
  ```text
export function resolvePackageImage(pkg) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 46:
  ```text
    const explicit = pkg?.image_url || pkg?.image;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 47:
  ```text
    if (explicit) return explicit;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 48:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 49:
  ```text
    const key = normalizeKey(pkg?.name);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 50:
  ```text
    return PACKAGE_IMAGE_OVERRIDES[key] || DEFAULT_PACKAGE_IMAGE_URL;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 51:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

## File: `HeigenKiosk/src/hooks/useApi.js`

- Methods documented: 2

### 1. `useApi`

- Signature: `function useApi(fetchFn, param, param)`
- Source range: lines 10-47

#### Line-by-line

- Line 10:
  ```text
export function useApi(fetchFn, args = [], options = {}) {
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 11:
  ```text
  const { enabled = true, initialData = null } = options;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 12:
  ```text
  const [data, setData]       = useState(initialData);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 13:
  ```text
  const [loading, setLoading] = useState(enabled);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 14:
  ```text
  const [error, setError]     = useState(null);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 15:
  ```text
  const mountedRef = useRef(true);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 16:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 17:
  ```text
  useEffect(() => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 18:
  ```text
    mountedRef.current = true;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 19:
  ```text
    return () => { mountedRef.current = false; };
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 20:
  ```text
  }, []);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 21:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 22:
  ```text
  const execute = useCallback(async () => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 23:
  ```text
    if (!enabled) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 24:
  ```text
    setLoading(true);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 25:
  ```text
    setError(null);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 26:
  ```text
    try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 27:
  ```text
      const result = await fetchFn(...args);
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 28:
  ```text
      if (mountedRef.current) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 29:
  ```text
        setData(result);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 30:
  ```text
      }
  ```
  Explanation: Closes the current block/scope.
- Line 31:
  ```text
    } catch (err) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 32:
  ```text
      if (mountedRef.current) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 33:
  ```text
        setError(err.message || 'An error occurred');
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 34:
  ```text
      }
  ```
  Explanation: Closes the current block/scope.
- Line 35:
  ```text
    } finally {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 36:
  ```text
      if (mountedRef.current) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 37:
  ```text
        setLoading(false);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 38:
  ```text
      }
  ```
  Explanation: Closes the current block/scope.
- Line 39:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 40:
  ```text
  }, [fetchFn, JSON.stringify(args), enabled]); // eslint-disable-line
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 41:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 42:
  ```text
  useEffect(() => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 43:
  ```text
    execute();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 44:
  ```text
  }, [execute]);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 45:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 46:
  ```text
  return { data, loading, error, refetch: execute };
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 47:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 2. `useMutation`

- Signature: `function useMutation(mutateFn)`
- Source range: lines 50-70

#### Line-by-line

- Line 50:
  ```text
export function useMutation(mutateFn) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 51:
  ```text
  const [loading, setLoading] = useState(false);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 52:
  ```text
  const [error, setError]     = useState(null);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 53:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 54:
  ```text
  const mutate = useCallback(async (...args) => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 55:
  ```text
    setLoading(true);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 56:
  ```text
    setError(null);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 57:
  ```text
    try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 58:
  ```text
      const result = await mutateFn(...args);
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 59:
  ```text
      return result;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 60:
  ```text
    } catch (err) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 61:
  ```text
      const msg = err.message || 'Request failed';
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 62:
  ```text
      setError(msg);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 63:
  ```text
      throw err;
  ```
  Explanation: Raises an error/exception to signal failure to the caller.
- Line 64:
  ```text
    } finally {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 65:
  ```text
      setLoading(false);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 66:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 67:
  ```text
  }, [mutateFn]);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 68:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 69:
  ```text
  return { mutate, loading, error };
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 70:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

## File: `HeigenKiosk/src/hooks/useScale.js`

- Methods documented: 3

### 1. `useScale`

- Signature: `function useScale()`
- Source range: lines 18-35

#### Line-by-line

- Line 18:
  ```text
export function useScale() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 19:
  ```text
  const { width: W, height: H } = useWindowDimensions();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 20:
  ```text
  const isLandscape = W > H;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 21:
  ```text
  const isTablet    = W >= 768;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 22:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 23:
  ```text
  // Use the shorter dimension as reference so landscape doesn't over-scale
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 24:
  ```text
  const refDim = isLandscape ? Math.min(W, H) : W;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 25:
  ```text
  const raw    = refDim / BASE_WIDTH;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 26:
  ```text
  const scale  = Math.min(raw, MAX_SCALE);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 27:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 28:
  ```text
  /** Scale a spacing / dimension value */
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 29:
  ```text
  const s  = (n) => Math.round(n * scale);
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 30:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 31:
  ```text
  /** Scale a font size — gentler curve so text doesn't get enormous */
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 32:
  ```text
  const fs = (n) => Math.round(n * Math.min(scale, 1.35));
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 33:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 34:
  ```text
  return { s, fs, isTablet, isLandscape, W, H, scale };
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 35:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 2. `s`

- Signature: `const s = (n) =>`
- Source range: lines 29-29

#### Line-by-line

- Line 29:
  ```text
  const s  = (n) => Math.round(n * scale);
  ```
  Explanation: Declares the callable signature and entry point for this method.

### 3. `fs`

- Signature: `const fs = (n) =>`
- Source range: lines 32-32

#### Line-by-line

- Line 32:
  ```text
  const fs = (n) => Math.round(n * Math.min(scale, 1.35));
  ```
  Explanation: Declares the callable signature and entry point for this method.

## File: `HeigenKiosk/src/screens/AddonsScreen.js`

- Methods documented: 3

### 1. `AddonsScreen`

- Signature: `function AddonsScreen(param)`
- Source range: lines 11-477

#### Line-by-line

- Line 11:
  ```text
export default function AddonsScreen({
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 12:
  ```text
    category,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 13:
  ```text
    selectedPackage,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 14:
  ```text
    selectedAddons,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 15:
  ```text
    onToggleAddon,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 16:
  ```text
    onNext,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 17:
  ```text
    onBack,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 18:
  ```text
}) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 19:
  ```text
    const { s, fs, isTablet, W } = useScale();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 20:
  ```text
    const [scrollY, setScrollY] = useState(0);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 21:
  ```text
    const [contentHeight, setContentHeight] = useState(0);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 22:
  ```text
    const [viewportHeight, setViewportHeight] = useState(0);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 23:
  ```text
    const catName = category?.name;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 24:
  ```text
    const fetchAddonsFn = useCallback(() => fetchAddons(catName), [catName]);
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 25:
  ```text
    const fetchPopFn = useCallback(
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 26:
  ```text
        () => fetchPopularAddons(catName),
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 27:
  ```text
        [catName],
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 28:
  ```text
    );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 29:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 30:
  ```text
    const { data: addons, loading, error, refetch } = useApi(fetchAddonsFn);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 31:
  ```text
    const { data: popularData } = useApi(fetchPopFn);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 32:
  ```text
    const popularIds = popularData?.top_addon_ids ?? [];
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 33:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 34:
  ```text
    if (loading) return <LoadingScreen message="Loading add-ons..." />;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 35:
  ```text
    if (error) return <ErrorScreen message={error} onRetry={refetch} />;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 36:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 37:
  ```text
    const popularAddons =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 38:
  ```text
        addons?.filter((a) => popularIds.includes(a.id)) ?? [];
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 39:
  ```text
    const otherAddons = addons?.filter((a) => !popularIds.includes(a.id)) ?? [];
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 40:
  ```text
    const displayPopular =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 41:
  ```text
        popularAddons.length > 0 ? popularAddons : (addons?.slice(0, 2) ?? []);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 42:
  ```text
    const displayOther =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 43:
  ```text
        popularAddons.length > 0 ? otherAddons : (addons?.slice(2) ?? []);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 44:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 45:
  ```text
    const pkgPrice = Number(
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 46:
  ```text
        selectedPackage?.promo_price
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 47:
  ```text
            ? selectedPackage.promo_price
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 48:
  ```text
            : selectedPackage?.price
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 49:
  ```text
              ? selectedPackage.price
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 50:
  ```text
              : 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 51:
  ```text
    );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 52:
  ```text
    const addonsTotal = selectedAddons.reduce(
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 53:
  ```text
        (sum, a) => sum + Number(a.price),
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 54:
  ```text
        0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 55:
  ```text
    );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 56:
  ```text
    const grandTotal = pkgPrice + addonsTotal;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 57:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 58:
  ```text
    const hPad = s(spacing.xl);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 59:
  ```text
    const colGap = s(spacing.md);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 60:
  ```text
    const cardWidth = isTablet ? (W - hPad * 2 - colGap) / 2 : undefined;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 61:
  ```text
    const footerH = isTablet
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 62:
  ```text
        ? 68
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 63:
  ```text
        : s(52) + s(spacing.lg) * 2 + s(spacing.xl) * 2 + s(52);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 64:
  ```text
    const extraBottomSpace = s(spacing.xxxl);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 65:
  ```text
    const maxScroll = Math.max(0, contentHeight - viewportHeight);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 66:
  ```text
    const canScrollDown = maxScroll > s(8);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 67:
  ```text
    const isNearBottom = scrollY >= maxScroll - s(20);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 68:
  ```text
    const showScrollHint = canScrollDown && !isNearBottom;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 69:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 70:
  ```text
    function renderGrid(list, isPopular) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 71:
  ```text
        return (
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 72:
  ```text
            <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 73:
  ```text
                style={
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 74:
  ```text
                    isTablet
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 75:
  ```text
                        ? { flexDirection: "row", flexWrap: "wrap", gap: colGap }
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 76:
  ```text
                        : { gap: s(spacing.sm) }
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 77:
  ```text
                }
  ```
  Explanation: Closes the current block/scope.
- Line 78:
  ```text
            >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 79:
  ```text
                {list.map((addon) => (
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 80:
  ```text
                    <AddonCard
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 81:
  ```text
                        key={addon.id}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 82:
  ```text
                        addon={addon}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 83:
  ```text
                        isPopular={isPopular}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 84:
  ```text
                        isSelected={selectedAddons.some(
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 85:
  ```text
                            (a) => a.id === addon.id,
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 86:
  ```text
                        )}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 87:
  ```text
                        onToggle={() => onToggleAddon(addon)}
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 88:
  ```text
                        s={s}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 89:
  ```text
                        fs={fs}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 90:
  ```text
                        width={cardWidth}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 91:
  ```text
                    />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 92:
  ```text
                ))}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 93:
  ```text
            </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 94:
  ```text
        );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 95:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 96:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 97:
  ```text
    return (
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 98:
  ```text
        <View style={{ flex: 1, backgroundColor: colors.background }}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 99:
  ```text
            <ScrollView
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 100:
  ```text
                contentContainerStyle={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 101:
  ```text
                    padding: hPad,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 102:
  ```text
                    paddingBottom: footerH + extraBottomSpace,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 103:
  ```text
                }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 104:
  ```text
                showsVerticalScrollIndicator={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 105:
  ```text
                onLayout={(e) => setViewportHeight(e.nativeEvent.layout.height)}
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 106:
  ```text
                onContentSizeChange={(_, h) => setContentHeight(h)}
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 107:
  ```text
                onScroll={(e) => setScrollY(e.nativeEvent.contentOffset.y)}
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 108:
  ```text
                scrollEventThrottle={16}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 109:
  ```text
            >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 110:
  ```text
                <Button
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 111:
  ```text
                    label="Back to Packages"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 112:
  ```text
                    icon="arrow-back"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 113:
  ```text
                    variant="ghost"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 114:
  ```text
                    onPress={onBack}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 115:
  ```text
                    style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 116:
  ```text
                        alignSelf: "flex-start",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 117:
  ```text
                        marginBottom: s(spacing.md),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 118:
  ```text
                    }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 119:
  ```text
                    labelStyle={{ color: colors.mutedForeground }}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 120:
  ```text
                />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 121:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 122:
  ```text
                <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 123:
  ```text
                    style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 124:
  ```text
                        fontSize: fs(22),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 125:
  ```text
                        fontWeight: "700",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 126:
  ```text
                        textAlign: "center",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 127:
  ```text
                        marginBottom: s(2),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 128:
  ```text
                    }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 129:
  ```text
                    allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 130:
  ```text
                >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 131:
  ```text
                    Enhance Your Photoshoot
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 132:
  ```text
                </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 133:
  ```text
                <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 134:
  ```text
                    style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 135:
  ```text
                        fontSize: fs(14),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 136:
  ```text
                        color: colors.mutedForeground,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 137:
  ```text
                        textAlign: "center",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 138:
  ```text
                        marginBottom: s(spacing.xl),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 139:
  ```text
                    }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 140:
  ```text
                    allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 141:
  ```text
                >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 142:
  ```text
                    Add optional upgrades to make your experience even better
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 143:
  ```text
                </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 144:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 145:
  ```text
                {displayPopular.length > 0 && (
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 146:
  ```text
                    <View style={{ marginBottom: s(spacing.xl) }}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 147:
  ```text
                        <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 148:
  ```text
                            style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 149:
  ```text
                                fontSize: fs(12),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 150:
  ```text
                                color: colors.accent,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 151:
  ```text
                                fontWeight: "700",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 152:
  ```text
                                marginBottom: s(spacing.sm),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 153:
  ```text
                                textTransform: "uppercase",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 154:
  ```text
                                letterSpacing: 0.6,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 155:
  ```text
                            }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 156:
  ```text
                            allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 157:
  ```text
                        >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 158:
  ```text
                            Popular Add-ons For {catName}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 159:
  ```text
                        </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 160:
  ```text
                        {renderGrid(displayPopular, true)}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 161:
  ```text
                    </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 162:
  ```text
                )}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 163:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 164:
  ```text
                {displayOther.length > 0 && (
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 165:
  ```text
                    <View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 166:
  ```text
                        <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 167:
  ```text
                            style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 168:
  ```text
                                fontSize: fs(12),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 169:
  ```text
                                color: colors.mutedForeground,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 170:
  ```text
                                fontWeight: "700",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 171:
  ```text
                                marginBottom: s(spacing.sm),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 172:
  ```text
                                textTransform: "uppercase",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 173:
  ```text
                                letterSpacing: 0.6,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 174:
  ```text
                            }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 175:
  ```text
                            allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 176:
  ```text
                        >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 177:
  ```text
                            More Add-ons
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 178:
  ```text
                        </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 179:
  ```text
                        {renderGrid(displayOther, false)}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 180:
  ```text
                    </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 181:
  ```text
                )}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 182:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 183:
  ```text
                {(!addons || addons.length === 0) && (
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 184:
  ```text
                    <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 185:
  ```text
                        style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 186:
  ```text
                            alignItems: "center",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 187:
  ```text
                            padding: s(spacing.xxxl),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 188:
  ```text
                        }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 189:
  ```text
                    >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 190:
  ```text
                        <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 191:
  ```text
                            style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 192:
  ```text
                                fontSize: fs(16),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 193:
  ```text
                                color: colors.mutedForeground,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 194:
  ```text
                                textAlign: "center",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 195:
  ```text
                            }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 196:
  ```text
                            allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 197:
  ```text
                        >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 198:
  ```text
                            No add-ons available for this category.
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 199:
  ```text
                        </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 200:
  ```text
                    </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 201:
  ```text
                )}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 202:
  ```text
            </ScrollView>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 203:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 204:
  ```text
            {showScrollHint && (
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 205:
  ```text
                <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 206:
  ```text
                    pointerEvents="none"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 207:
  ```text
                    style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 208:
  ```text
                        position: "absolute",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 209:
  ```text
                        left: 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 210:
  ```text
                        right: 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 211:
  ```text
                        bottom: footerH + s(spacing.sm),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 212:
  ```text
                        alignItems: "center",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 213:
  ```text
                    }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 214:
  ```text
                >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 215:
  ```text
                    <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 216:
  ```text
                        style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 217:
  ```text
                            width: s(34),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 218:
  ```text
                            height: s(34),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 219:
  ```text
                            borderRadius: s(17),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 220:
  ```text
                            alignItems: "center",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 221:
  ```text
                            justifyContent: "center",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 222:
  ```text
                            backgroundColor: "rgba(255,255,255,0.74)",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 223:
  ```text
                            borderWidth: 1,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 224:
  ```text
                            borderColor: "rgba(0,0,0,0.08)",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 225:
  ```text
                            ...shadow.sm,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 226:
  ```text
                        }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 227:
  ```text
                    >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 228:
  ```text
                        <Icon
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 229:
  ```text
                            name="chevron-down"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 230:
  ```text
                            size={s(16)}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 231:
  ```text
                            color={colors.mutedForeground}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 232:
  ```text
                        />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 233:
  ```text
                    </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 234:
  ```text
                </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 235:
  ```text
            )}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 236:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 237:
  ```text
            {/* Sticky footer — tablet: single row; phone: two rows */}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 238:
  ```text
            <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 239:
  ```text
                style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 240:
  ```text
                    position: "absolute",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 241:
  ```text
                    bottom: 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 242:
  ```text
                    left: 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 243:
  ```text
                    right: 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 244:
  ```text
                    backgroundColor: "#fff",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 245:
  ```text
                    borderTopWidth: 1,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 246:
  ```text
                    borderTopColor: colors.border,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 247:
  ```text
                    paddingHorizontal: s(spacing.xl),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 248:
  ```text
                    paddingVertical: isTablet ? 14 : s(spacing.lg),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 249:
  ```text
                    ...shadow.lg,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 250:
  ```text
                }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 251:
  ```text
            >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 252:
  ```text
                {isTablet ? (
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 253:
  ```text
                    <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 254:
  ```text
                        style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 255:
  ```text
                            flexDirection: "row",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 256:
  ```text
                            alignItems: "center",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 257:
  ```text
                            gap: s(spacing.xl),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 258:
  ```text
                        }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 259:
  ```text
                    >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 260:
  ```text
                        <View style={{ flex: 2 }}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 261:
  ```text
                            <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 262:
  ```text
                                style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 263:
  ```text
                                    fontSize: 11,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 264:
  ```text
                                    color: colors.mutedForeground,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 265:
  ```text
                                    fontWeight: "600",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 266:
  ```text
                                    textTransform: "uppercase",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 267:
  ```text
                                    letterSpacing: 0.4,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 268:
  ```text
                                }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 269:
  ```text
                                allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 270:
  ```text
                            >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 271:
  ```text
                                Package
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 272:
  ```text
                            </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 273:
  ```text
                            <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 274:
  ```text
                                style={{ fontSize: 13, fontWeight: "600" }}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 275:
  ```text
                                numberOfLines={1}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 276:
  ```text
                                allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 277:
  ```text
                            >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 278:
  ```text
                                {selectedPackage?.name}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 279:
  ```text
                                <Text style={{ color: colors.accent }}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 280:
  ```text
                                    {"  "}₱{pkgPrice.toLocaleString()}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 281:
  ```text
                                </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 282:
  ```text
                            </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 283:
  ```text
                        </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 284:
  ```text
                        {selectedAddons.length > 0 && (
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 285:
  ```text
                            <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 286:
  ```text
                                style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 287:
  ```text
                                    flex: 1,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 288:
  ```text
                                    borderLeftWidth: 1,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 289:
  ```text
                                    borderLeftColor: colors.border,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 290:
  ```text
                                    paddingLeft: s(spacing.lg),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 291:
  ```text
                                }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 292:
  ```text
                            >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 293:
  ```text
                                <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 294:
  ```text
                                    style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 295:
  ```text
                                        fontSize: 11,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 296:
  ```text
                                        color: colors.mutedForeground,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 297:
  ```text
                                        fontWeight: "600",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 298:
  ```text
                                        textTransform: "uppercase",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 299:
  ```text
                                        letterSpacing: 0.4,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 300:
  ```text
                                    }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 301:
  ```text
                                    allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 302:
  ```text
                                >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 303:
  ```text
                                    Add-ons
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 304:
  ```text
                                </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 305:
  ```text
                                <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 306:
  ```text
                                    style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 307:
  ```text
                                        fontSize: 13,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 308:
  ```text
                                        fontWeight: "600",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 309:
  ```text
                                        color: colors.accent,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 310:
  ```text
                                    }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 311:
  ```text
                                    allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 312:
  ```text
                                >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 313:
  ```text
                                    +₱{addonsTotal.toLocaleString()} (
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 314:
  ```text
                                    {selectedAddons.length})
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 315:
  ```text
                                </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 316:
  ```text
                            </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 317:
  ```text
                        )}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 318:
  ```text
                        <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 319:
  ```text
                            style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 320:
  ```text
                                flex: 1,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 321:
  ```text
                                borderLeftWidth: 1,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 322:
  ```text
                                borderLeftColor: colors.border,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 323:
  ```text
                                paddingLeft: s(spacing.lg),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 324:
  ```text
                            }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 325:
  ```text
                        >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 326:
  ```text
                            <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 327:
  ```text
                                style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 328:
  ```text
                                    fontSize: 11,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 329:
  ```text
                                    color: colors.mutedForeground,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 330:
  ```text
                                    fontWeight: "600",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 331:
  ```text
                                    textTransform: "uppercase",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 332:
  ```text
                                    letterSpacing: 0.4,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 333:
  ```text
                                }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 334:
  ```text
                                allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 335:
  ```text
                            >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 336:
  ```text
                                Total
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 337:
  ```text
                            </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 338:
  ```text
                            <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 339:
  ```text
                                style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 340:
  ```text
                                    fontSize: 17,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 341:
  ```text
                                    fontWeight: "700",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 342:
  ```text
                                    color: colors.accent,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 343:
  ```text
                                }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 344:
  ```text
                                allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 345:
  ```text
                            >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 346:
  ```text
                                ₱{grandTotal.toLocaleString()}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 347:
  ```text
                            </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 348:
  ```text
                        </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 349:
  ```text
                        <Button
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 350:
  ```text
                            label="Book Now"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 351:
  ```text
                            onPress={onNext}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 352:
  ```text
                            style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 353:
  ```text
                                flexShrink: 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 354:
  ```text
                                paddingHorizontal: s(spacing.xxl),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 355:
  ```text
                                height: 40,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 356:
  ```text
                            }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 357:
  ```text
                        />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 358:
  ```text
                    </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 359:
  ```text
                ) : (
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 360:
  ```text
                    <>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 361:
  ```text
                        <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 362:
  ```text
                            style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 363:
  ```text
                                flexDirection: "row",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 364:
  ```text
                                marginBottom: s(spacing.md),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 365:
  ```text
                            }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 366:
  ```text
                        >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 367:
  ```text
                            <View style={{ flex: 1 }}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 368:
  ```text
                                <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 369:
  ```text
                                    style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 370:
  ```text
                                        fontSize: fs(11),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 371:
  ```text
                                        color: colors.mutedForeground,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 372:
  ```text
                                    }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 373:
  ```text
                                    allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 374:
  ```text
                                >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 375:
  ```text
                                    Package
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 376:
  ```text
                                </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 377:
  ```text
                                <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 378:
  ```text
                                    style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 379:
  ```text
                                        fontSize: fs(13),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 380:
  ```text
                                        fontWeight: "600",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 381:
  ```text
                                    }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 382:
  ```text
                                    numberOfLines={1}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 383:
  ```text
                                    allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 384:
  ```text
                                >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 385:
  ```text
                                    {selectedPackage?.name}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 386:
  ```text
                                </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 387:
  ```text
                                <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 388:
  ```text
                                    style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 389:
  ```text
                                        fontSize: fs(12),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 390:
  ```text
                                        color: colors.accent,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 391:
  ```text
                                        fontWeight: "600",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 392:
  ```text
                                    }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 393:
  ```text
                                    allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 394:
  ```text
                                >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 395:
  ```text
                                    ₱{pkgPrice.toLocaleString()}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 396:
  ```text
                                </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 397:
  ```text
                            </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 398:
  ```text
                            {selectedAddons.length > 0 && (
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 399:
  ```text
                                <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 400:
  ```text
                                    style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 401:
  ```text
                                        flex: 1,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 402:
  ```text
                                        borderLeftWidth: 1,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 403:
  ```text
                                        borderLeftColor: colors.border,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 404:
  ```text
                                        paddingLeft: s(spacing.lg),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 405:
  ```text
                                    }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 406:
  ```text
                                >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 407:
  ```text
                                    <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 408:
  ```text
                                        style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 409:
  ```text
                                            fontSize: fs(11),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 410:
  ```text
                                            color: colors.mutedForeground,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 411:
  ```text
                                        }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 412:
  ```text
                                        allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 413:
  ```text
                                    >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 414:
  ```text
                                        Add-ons
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 415:
  ```text
                                    </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 416:
  ```text
                                    <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 417:
  ```text
                                        style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 418:
  ```text
                                            fontSize: fs(13),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 419:
  ```text
                                            fontWeight: "600",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 420:
  ```text
                                        }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 421:
  ```text
                                        allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 422:
  ```text
                                    >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 423:
  ```text
                                        {selectedAddons.length} item(s)
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 424:
  ```text
                                    </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 425:
  ```text
                                    <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 426:
  ```text
                                        style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 427:
  ```text
                                            fontSize: fs(12),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 428:
  ```text
                                            color: colors.accent,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 429:
  ```text
                                            fontWeight: "600",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 430:
  ```text
                                        }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 431:
  ```text
                                        allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 432:
  ```text
                                    >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 433:
  ```text
                                        +₱{addonsTotal.toLocaleString()}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 434:
  ```text
                                    </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 435:
  ```text
                                </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 436:
  ```text
                            )}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 437:
  ```text
                            <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 438:
  ```text
                                style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 439:
  ```text
                                    flex: 1,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 440:
  ```text
                                    borderLeftWidth: 1,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 441:
  ```text
                                    borderLeftColor: colors.border,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 442:
  ```text
                                    paddingLeft: s(spacing.lg),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 443:
  ```text
                                }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 444:
  ```text
                            >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 445:
  ```text
                                <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 446:
  ```text
                                    style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 447:
  ```text
                                        fontSize: fs(11),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 448:
  ```text
                                        color: colors.mutedForeground,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 449:
  ```text
                                    }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 450:
  ```text
                                    allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 451:
  ```text
                                >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 452:
  ```text
                                    Total
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 453:
  ```text
                                </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 454:
  ```text
                                <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 455:
  ```text
                                    style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 456:
  ```text
                                        fontSize: fs(20),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 457:
  ```text
                                        fontWeight: "700",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 458:
  ```text
                                        color: colors.accent,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 459:
  ```text
                                    }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 460:
  ```text
                                    allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 461:
  ```text
                                >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 462:
  ```text
                                    ₱{grandTotal.toLocaleString()}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 463:
  ```text
                                </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 464:
  ```text
                            </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 465:
  ```text
                        </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 466:
  ```text
                        <Button
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 467:
  ```text
                            label="Book Now"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 468:
  ```text
                            onPress={onNext}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 469:
  ```text
                            fullWidth
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 470:
  ```text
                            style={{ height: s(48) }}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 471:
  ```text
                        />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 472:
  ```text
                    </>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 473:
  ```text
                )}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 474:
  ```text
            </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 475:
  ```text
        </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 476:
  ```text
    );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 477:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 2. `renderGrid`

- Signature: `function renderGrid(list, isPopular)`
- Source range: lines 70-95

#### Line-by-line

- Line 70:
  ```text
    function renderGrid(list, isPopular) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 71:
  ```text
        return (
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 72:
  ```text
            <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 73:
  ```text
                style={
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 74:
  ```text
                    isTablet
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 75:
  ```text
                        ? { flexDirection: "row", flexWrap: "wrap", gap: colGap }
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 76:
  ```text
                        : { gap: s(spacing.sm) }
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 77:
  ```text
                }
  ```
  Explanation: Closes the current block/scope.
- Line 78:
  ```text
            >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 79:
  ```text
                {list.map((addon) => (
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 80:
  ```text
                    <AddonCard
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 81:
  ```text
                        key={addon.id}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 82:
  ```text
                        addon={addon}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 83:
  ```text
                        isPopular={isPopular}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 84:
  ```text
                        isSelected={selectedAddons.some(
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 85:
  ```text
                            (a) => a.id === addon.id,
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 86:
  ```text
                        )}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 87:
  ```text
                        onToggle={() => onToggleAddon(addon)}
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 88:
  ```text
                        s={s}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 89:
  ```text
                        fs={fs}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 90:
  ```text
                        width={cardWidth}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 91:
  ```text
                    />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 92:
  ```text
                ))}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 93:
  ```text
            </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 94:
  ```text
        );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 95:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 3. `AddonCard`

- Signature: `function AddonCard(param)`
- Source range: lines 480-599

#### Line-by-line

- Line 480:
  ```text
function AddonCard({ addon, isPopular, isSelected, onToggle, s, fs, width }) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 481:
  ```text
    const borderColor = isSelected
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 482:
  ```text
        ? colors.accent
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 483:
  ```text
        : isPopular
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 484:
  ```text
          ? "rgba(217,119,6,0.7)" // slightly darker for contrast
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 485:
  ```text
          : colors.border;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 486:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 487:
  ```text
    return (
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 488:
  ```text
        <TouchableOpacity
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 489:
  ```text
            onPress={onToggle}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 490:
  ```text
            activeOpacity={0.85}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 491:
  ```text
            style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 492:
  ```text
                borderRadius: s(radii.lg),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 493:
  ```text
                borderWidth: isSelected ? 2 : 1.5,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 494:
  ```text
                borderColor: borderColor,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 495:
  ```text
                backgroundColor: isSelected
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 496:
  ```text
                    ? "#fef3c7"
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 497:
  ```text
                    : isPopular
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 498:
  ```text
                      ? "#fffbf5"
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 499:
  ```text
                      : "#ffffff",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 500:
  ```text
                padding: s(spacing.md),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 501:
  ```text
                ...(isSelected ? shadow.accent : shadow.sm),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 502:
  ```text
                position: "relative",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 503:
  ```text
                width: width || undefined,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 504:
  ```text
            }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 505:
  ```text
        >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 506:
  ```text
            {/* Popular star at top center */}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 507:
  ```text
            {isPopular && !isSelected && (
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 508:
  ```text
                <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 509:
  ```text
                    style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 510:
  ```text
                        position: "absolute",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 511:
  ```text
                        top: -s(10),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 512:
  ```text
                        left: "50%",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 513:
  ```text
                        transform: [{ translateX: -s(10) }],
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 514:
  ```text
                        width: s(20),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 515:
  ```text
                        height: s(20),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 516:
  ```text
                        borderRadius: s(10),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 517:
  ```text
                        backgroundColor: borderColor,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 518:
  ```text
                        alignItems: "center",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 519:
  ```text
                        justifyContent: "center",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 520:
  ```text
                        elevation: 3,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 521:
  ```text
                    }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 522:
  ```text
                >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 523:
  ```text
                    <Icon name="star" size={s(12)} color="#fff" />
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 524:
  ```text
                </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 525:
  ```text
            )}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 526:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 527:
  ```text
            <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 528:
  ```text
                style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 529:
  ```text
                    flexDirection: "row",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 530:
  ```text
                    alignItems: "center",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 531:
  ```text
                    gap: s(spacing.lg),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 532:
  ```text
                }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 533:
  ```text
            >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 534:
  ```text
                <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 535:
  ```text
                    style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 536:
  ```text
                        width: s(28),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 537:
  ```text
                        height: s(28),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 538:
  ```text
                        borderRadius: s(14),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 539:
  ```text
                        flexShrink: 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 540:
  ```text
                        borderWidth: 2,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 541:
  ```text
                        borderColor: borderColor,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 542:
  ```text
                        backgroundColor: isSelected ? colors.accent : "#ffffff",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 543:
  ```text
                        alignItems: "center",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 544:
  ```text
                        justifyContent: "center",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 545:
  ```text
                    }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 546:
  ```text
                >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 547:
  ```text
                    <Icon
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 548:
  ```text
                        name={isSelected ? "checkmark" : "add"}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 549:
  ```text
                        size={s(13)}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 550:
  ```text
                        color={
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 551:
  ```text
                            isSelected
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 552:
  ```text
                                ? "#fff"
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 553:
  ```text
                                : isPopular
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 554:
  ```text
                                  ? colors.accent
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 555:
  ```text
                                  : colors.mutedForeground
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 556:
  ```text
                        }
  ```
  Explanation: Closes the current block/scope.
- Line 557:
  ```text
                    />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 558:
  ```text
                </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 559:
  ```text
                <View style={{ flex: 1 }}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 560:
  ```text
                    <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 561:
  ```text
                        style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 562:
  ```text
                            fontSize: fs(13),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 563:
  ```text
                            fontWeight: "600",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 564:
  ```text
                            color: isSelected
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 565:
  ```text
                                ? colors.accent
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 566:
  ```text
                                : colors.foreground,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 567:
  ```text
                        }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 568:
  ```text
                        allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 569:
  ```text
                    >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 570:
  ```text
                        {addon.name}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 571:
  ```text
                    </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 572:
  ```text
                    {!!addon.additional_info && (
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 573:
  ```text
                        <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 574:
  ```text
                            style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 575:
  ```text
                                fontSize: fs(11),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 576:
  ```text
                                color: colors.mutedForeground,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 577:
  ```text
                                marginTop: s(2),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 578:
  ```text
                            }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 579:
  ```text
                            allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 580:
  ```text
                        >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 581:
  ```text
                            {String(addon.additional_info)}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 582:
  ```text
                        </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 583:
  ```text
                    )}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 584:
  ```text
                </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 585:
  ```text
                <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 586:
  ```text
                    style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 587:
  ```text
                        fontSize: fs(13),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 588:
  ```text
                        fontWeight: "700",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 589:
  ```text
                        color: colors.accent,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 590:
  ```text
                        flexShrink: 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 591:
  ```text
                    }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 592:
  ```text
                    allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 593:
  ```text
                >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 594:
  ```text
                    +₱{Number(addon.price).toLocaleString()}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 595:
  ```text
                </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 596:
  ```text
            </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 597:
  ```text
        </TouchableOpacity>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 598:
  ```text
    );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 599:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

## File: `HeigenKiosk/src/screens/AdminBookingQueue.js`

- Methods documented: 8

### 1. `AdminBookingQueue`

- Signature: `function AdminBookingQueue()`
- Source range: lines 28-161

#### Line-by-line

- Line 28:
  ```text
export default function AdminBookingQueue() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 29:
  ```text
  const [selectedBooking, setSelectedBooking] = useState(null);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 30:
  ```text
  const [refreshing, setRefreshing]           = useState(false);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 31:
  ```text
  const [recommenderMsg, setRecommenderMsg]   = useState(null);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 32:
  ```text
  const [refreshingRec, setRefreshingRec]     = useState(false);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 33:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 34:
  ```text
  const fetchActive = useCallback(() => fetchBookingsByStatus('Pending,Ongoing'), []);
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 35:
  ```text
  const { data: bookings, loading, error, refetch } = useApi(fetchActive);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 36:
  ```text
  const { mutate: updateStatus, loading: updating }  = useMutation(
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 37:
  ```text
    (id, status) => updateBookingStatus(id, status),
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 38:
  ```text
  );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 39:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 40:
  ```text
  async function handleRefresh() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 41:
  ```text
    setRefreshing(true);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 42:
  ```text
    await refetch();
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 43:
  ```text
    setRefreshing(false);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 44:
  ```text
  }
  ```
  Explanation: Closes the current block/scope.
- Line 45:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 46:
  ```text
  async function handleStatusChange(booking, newStatus) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 47:
  ```text
    const labels = {
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 48:
  ```text
      [BOOKING_STATUS.ONGOING]:   'Accept this booking?',
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 49:
  ```text
      [BOOKING_STATUS.DONE]:      'Mark session as done?',
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 50:
  ```text
      [BOOKING_STATUS.CANCELLED]: 'Cancel this booking?',
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 51:
  ```text
    };
  ```
  Explanation: Closes the current block/scope.
- Line 52:
  ```text
    Alert.alert('Confirm', labels[newStatus] || 'Update status?', [
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 53:
  ```text
      { text: 'No', style: 'cancel' },
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 54:
  ```text
      {
  ```
  Explanation: Opens a new block scope.
- Line 55:
  ```text
        text: 'Yes',
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 56:
  ```text
        style: newStatus === BOOKING_STATUS.CANCELLED ? 'destructive' : 'default',
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 57:
  ```text
        onPress: async () => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 58:
  ```text
          try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 59:
  ```text
            await updateStatus(booking.id, newStatus);
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 60:
  ```text
            setSelectedBooking(null);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 61:
  ```text
            refetch();
  ```
  Explanation: Triggers an external API/network request as part of this flow.
- Line 62:
  ```text
          } catch (err) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 63:
  ```text
            Alert.alert('Error', err.message || 'Failed to update status.');
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 64:
  ```text
          }
  ```
  Explanation: Closes the current block/scope.
- Line 65:
  ```text
        },
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 66:
  ```text
      },
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 67:
  ```text
    ]);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 68:
  ```text
  }
  ```
  Explanation: Closes the current block/scope.
- Line 69:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 70:
  ```text
  async function handleRefreshRecommender() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 71:
  ```text
    setRefreshingRec(true);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 72:
  ```text
    setRecommenderMsg(null);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 73:
  ```text
    try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 74:
  ```text
      const res = await fetch('http://localhost:8000/api/recommendations/1/?k=3');
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 75:
  ```text
      setRecommenderMsg({
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 76:
  ```text
        type: res.ok ? 'success' : 'warn',
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 77:
  ```text
        text: res.ok
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 78:
  ```text
          ? 'Recommender refreshed! Popular choices are now up to date.'
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 79:
  ```text
          : 'Recommender updates automatically as bookings are added.',
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 80:
  ```text
      });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 81:
  ```text
    } catch {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 82:
  ```text
      setRecommenderMsg({
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 83:
  ```text
        type: 'warn',
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 84:
  ```text
        text: 'Recommender uses live booking counts — it updates automatically.',
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 85:
  ```text
      });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 86:
  ```text
    } finally {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 87:
  ```text
      setRefreshingRec(false);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 88:
  ```text
      setTimeout(() => setRecommenderMsg(null), 5000);
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 89:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 90:
  ```text
  }
  ```
  Explanation: Closes the current block/scope.
- Line 91:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 92:
  ```text
  if (loading && !refreshing) return <LoadingScreen message="Loading booking queue..." />;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 93:
  ```text
  if (error)                  return <ErrorScreen message={error} onRetry={refetch} />;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 94:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 95:
  ```text
  const pending = (bookings || []).filter((b) => b.session_status === BOOKING_STATUS.PENDING);
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 96:
  ```text
  const ongoing = (bookings || []).filter((b) => b.session_status === BOOKING_STATUS.ONGOING);
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 97:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 98:
  ```text
  return (
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 99:
  ```text
    <SafeAreaView style={styles.safe}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 100:
  ```text
      <StatusBar style="dark" />
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 101:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 102:
  ```text
      {/* Header */}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 103:
  ```text
      <View style={styles.header}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 104:
  ```text
        <View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 105:
  ```text
          <Text style={styles.headerTitle}>Booking Queue</Text>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 106:
  ```text
          <Text style={styles.headerSub}>{pending.length} pending · {ongoing.length} ongoing</Text>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 107:
  ```text
        </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 108:
  ```text
        <View style={styles.headerActions}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 109:
  ```text
          <TouchableOpacity onPress={handleRefreshRecommender} disabled={refreshingRec} style={styles.recBtn}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 110:
  ```text
            <Icon name={refreshingRec ? 'sync' : 'sparkles-outline'} size={16} color={colors.accent} />
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 111:
  ```text
            <Text style={styles.recBtnText}>Refresh AI</Text>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 112:
  ```text
          </TouchableOpacity>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 113:
  ```text
          <TouchableOpacity onPress={handleRefresh} style={styles.reloadBtn}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 114:
  ```text
            <Icon name="reload" size={20} color={colors.foreground} />
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 115:
  ```text
          </TouchableOpacity>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 116:
  ```text
        </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 117:
  ```text
      </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 118:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 119:
  ```text
      {/* Recommender toast */}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 120:
  ```text
      {recommenderMsg && (
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 121:
  ```text
        <View style={[styles.recMsg, recommenderMsg.type === 'success' ? styles.recMsgSuccess : styles.recMsgWarn]}>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 122:
  ```text
          <Icon
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 123:
  ```text
            name={recommenderMsg.type === 'success' ? 'checkmark-circle' : 'information-circle'}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 124:
  ```text
            size={14}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 125:
  ```text
            color={recommenderMsg.type === 'success' ? colors.success : colors.accent}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 126:
  ```text
          />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 127:
  ```text
          <Text style={styles.recMsgText}>{recommenderMsg.text}</Text>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 128:
  ```text
        </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 129:
  ```text
      )}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 130:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 131:
  ```text
      <FlatList
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 132:
  ```text
        data={[...pending, ...ongoing]}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 133:
  ```text
        keyExtractor={(item) => String(item.id)}
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 134:
  ```text
        renderItem={({ item }) => (
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 135:
  ```text
          <BookingCard booking={item} onPress={() => setSelectedBooking(item)} />
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 136:
  ```text
        )}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 137:
  ```text
        contentContainerStyle={styles.list}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 138:
  ```text
        refreshControl={
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 139:
  ```text
          <RefreshControl refreshing={refreshing} onRefresh={handleRefresh} tintColor={colors.accent} />
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 140:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 141:
  ```text
        ItemSeparatorComponent={() => <View style={{ height: spacing.md }} />}
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 142:
  ```text
        ListEmptyComponent={
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 143:
  ```text
          <View style={styles.empty}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 144:
  ```text
            <Text style={styles.emptyIcon}>◻</Text>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 145:
  ```text
            <Text style={styles.emptyTitle}>No Active Bookings</Text>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 146:
  ```text
            <Text style={styles.emptyText}>Pull down to refresh</Text>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 147:
  ```text
          </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 148:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 149:
  ```text
      />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 150:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 151:
  ```text
      {selectedBooking && (
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 152:
  ```text
        <BookingDetailModal
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 153:
  ```text
          booking={selectedBooking}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 154:
  ```text
          onClose={() => setSelectedBooking(null)}
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 155:
  ```text
          onStatusChange={handleStatusChange}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 156:
  ```text
          updating={updating}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 157:
  ```text
        />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 158:
  ```text
      )}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 159:
  ```text
    </SafeAreaView>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 160:
  ```text
  );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 161:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 2. `handleRefresh`

- Signature: `async function handleRefresh()`
- Source range: lines 40-44

#### Line-by-line

- Line 40:
  ```text
  async function handleRefresh() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 41:
  ```text
    setRefreshing(true);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 42:
  ```text
    await refetch();
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 43:
  ```text
    setRefreshing(false);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 44:
  ```text
  }
  ```
  Explanation: Closes the current block/scope.

### 3. `handleStatusChange`

- Signature: `async function handleStatusChange(booking, newStatus)`
- Source range: lines 46-68

#### Line-by-line

- Line 46:
  ```text
  async function handleStatusChange(booking, newStatus) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 47:
  ```text
    const labels = {
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 48:
  ```text
      [BOOKING_STATUS.ONGOING]:   'Accept this booking?',
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 49:
  ```text
      [BOOKING_STATUS.DONE]:      'Mark session as done?',
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 50:
  ```text
      [BOOKING_STATUS.CANCELLED]: 'Cancel this booking?',
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 51:
  ```text
    };
  ```
  Explanation: Closes the current block/scope.
- Line 52:
  ```text
    Alert.alert('Confirm', labels[newStatus] || 'Update status?', [
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 53:
  ```text
      { text: 'No', style: 'cancel' },
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 54:
  ```text
      {
  ```
  Explanation: Opens a new block scope.
- Line 55:
  ```text
        text: 'Yes',
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 56:
  ```text
        style: newStatus === BOOKING_STATUS.CANCELLED ? 'destructive' : 'default',
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 57:
  ```text
        onPress: async () => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 58:
  ```text
          try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 59:
  ```text
            await updateStatus(booking.id, newStatus);
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 60:
  ```text
            setSelectedBooking(null);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 61:
  ```text
            refetch();
  ```
  Explanation: Triggers an external API/network request as part of this flow.
- Line 62:
  ```text
          } catch (err) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 63:
  ```text
            Alert.alert('Error', err.message || 'Failed to update status.');
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 64:
  ```text
          }
  ```
  Explanation: Closes the current block/scope.
- Line 65:
  ```text
        },
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 66:
  ```text
      },
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 67:
  ```text
    ]);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 68:
  ```text
  }
  ```
  Explanation: Closes the current block/scope.

### 4. `handleRefreshRecommender`

- Signature: `async function handleRefreshRecommender()`
- Source range: lines 70-90

#### Line-by-line

- Line 70:
  ```text
  async function handleRefreshRecommender() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 71:
  ```text
    setRefreshingRec(true);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 72:
  ```text
    setRecommenderMsg(null);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 73:
  ```text
    try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 74:
  ```text
      const res = await fetch('http://localhost:8000/api/recommendations/1/?k=3');
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 75:
  ```text
      setRecommenderMsg({
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 76:
  ```text
        type: res.ok ? 'success' : 'warn',
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 77:
  ```text
        text: res.ok
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 78:
  ```text
          ? 'Recommender refreshed! Popular choices are now up to date.'
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 79:
  ```text
          : 'Recommender updates automatically as bookings are added.',
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 80:
  ```text
      });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 81:
  ```text
    } catch {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 82:
  ```text
      setRecommenderMsg({
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 83:
  ```text
        type: 'warn',
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 84:
  ```text
        text: 'Recommender uses live booking counts — it updates automatically.',
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 85:
  ```text
      });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 86:
  ```text
    } finally {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 87:
  ```text
      setRefreshingRec(false);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 88:
  ```text
      setTimeout(() => setRecommenderMsg(null), 5000);
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 89:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 90:
  ```text
  }
  ```
  Explanation: Closes the current block/scope.

### 5. `BookingCard`

- Signature: `function BookingCard(param)`
- Source range: lines 163-193

#### Line-by-line

- Line 163:
  ```text
function BookingCard({ booking, onPress }) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 164:
  ```text
  const cfg = STATUS_CONFIG[booking.session_status] || STATUS_CONFIG[BOOKING_STATUS.PENDING];
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 165:
  ```text
  return (
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 166:
  ```text
    <TouchableOpacity onPress={onPress} activeOpacity={0.85} style={styles.card}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 167:
  ```text
      <View style={[styles.statusStrip, { backgroundColor: cfg.color }]} />
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 168:
  ```text
      <View style={styles.cardBody}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 169:
  ```text
        <View style={styles.cardTop}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 170:
  ```text
          <View style={{ flex: 1 }}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 171:
  ```text
            <Text style={styles.customerName}>{booking.customer_name || 'Unknown Customer'}</Text>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 172:
  ```text
            <Text style={styles.packageName}>{booking.packageName || '—'}</Text>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 173:
  ```text
          </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 174:
  ```text
          <View style={{ alignItems: 'flex-end', gap: spacing.sm }}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 175:
  ```text
            <View style={[styles.statusPill, { backgroundColor: cfg.bg }]}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 176:
  ```text
              <Icon name={cfg.icon} size={11} color={cfg.color} />
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 177:
  ```text
              <Text style={[styles.statusPillText, { color: cfg.color }]}>{cfg.label}</Text>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 178:
  ```text
            </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 179:
  ```text
            <Text style={styles.cardPrice}>₱{Number(booking.total || 0).toLocaleString()}</Text>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 180:
  ```text
          </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 181:
  ```text
        </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 182:
  ```text
        <Divider style={{ marginVertical: spacing.sm }} />
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 183:
  ```text
        <View style={styles.cardMeta}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 184:
  ```text
          <Text style={styles.metaText}>◻ {booking.date || 'No date'}</Text>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 185:
  ```text
          {booking.addons?.length > 0 && (
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 186:
  ```text
            <Text style={styles.metaText}>+ {booking.addons.length} add-on(s)</Text>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 187:
  ```text
          )}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 188:
  ```text
          <Text style={[styles.metaText, { color: colors.accent }]}>Tap to manage →</Text>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 189:
  ```text
        </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 190:
  ```text
      </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 191:
  ```text
    </TouchableOpacity>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 192:
  ```text
  );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 193:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 6. `BookingDetailModal`

- Signature: `function BookingDetailModal(param)`
- Source range: lines 195-270

#### Line-by-line

- Line 195:
  ```text
function BookingDetailModal({ booking, onClose, onStatusChange, updating }) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 196:
  ```text
  const cfg     = STATUS_CONFIG[booking.session_status] || STATUS_CONFIG[BOOKING_STATUS.PENDING];
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 197:
  ```text
  const isPend  = booking.session_status === BOOKING_STATUS.PENDING;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 198:
  ```text
  const isGoing = booking.session_status === BOOKING_STATUS.ONGOING;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 199:
  ```text
  const addons  = Array.isArray(booking.addons) ? booking.addons : [];
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 200:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 201:
  ```text
  return (
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 202:
  ```text
    <Modal visible transparent animationType="slide" onRequestClose={onClose}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 203:
  ```text
      <Pressable style={styles.backdrop} onPress={onClose} />
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 204:
  ```text
      <View style={styles.detailSheet}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 205:
  ```text
        <View style={styles.handle} />
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 206:
  ```text
        <View style={[styles.detailHeader, { backgroundColor: cfg.bg }]}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 207:
  ```text
          <View style={styles.detailHeaderLeft}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 208:
  ```text
            <Icon name={cfg.icon} size={26} color={cfg.color} />
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 209:
  ```text
            <View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 210:
  ```text
              <Text style={[styles.detailStatus, { color: cfg.color }]}>{cfg.label}</Text>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 211:
  ```text
              <Text style={styles.detailId}>Booking #{booking.id}</Text>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 212:
  ```text
            </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 213:
  ```text
          </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 214:
  ```text
          <TouchableOpacity onPress={onClose}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 215:
  ```text
            <Icon name="close" size={22} color={colors.mutedForeground} />
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 216:
  ```text
          </TouchableOpacity>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 217:
  ```text
        </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 218:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 219:
  ```text
        <ScrollView contentContainerStyle={styles.detailBody}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 220:
  ```text
          <Section title="Customer">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 221:
  ```text
            <DetailRow icon="person-outline"   label={booking.customer_name || '—'} />
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 222:
  ```text
            <DetailRow icon="calendar-outline" label={booking.date || 'No preferred date'} />
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 223:
  ```text
          </Section>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 224:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 225:
  ```text
          <Section title="Package">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 226:
  ```text
            <View style={styles.pkgBox}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 227:
  ```text
              <Text style={styles.pkgBoxName}>{booking.packageName}</Text>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 228:
  ```text
              <Text style={styles.pkgBoxPrice}>₱{Number(booking.packagePrice || 0).toLocaleString()}</Text>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 229:
  ```text
            </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 230:
  ```text
          </Section>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 231:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 232:
  ```text
          {addons.length > 0 && (
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 233:
  ```text
            <Section title="Add-ons">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 234:
  ```text
              {addons.map((a, i) => (
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 235:
  ```text
                <View key={i} style={styles.addonRow}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 236:
  ```text
                  <Text style={styles.addonName}>{a.name}</Text>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 237:
  ```text
                  <Text style={styles.addonPrice}>₱{Number(a.price).toLocaleString()} × {a.quantity}</Text>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 238:
  ```text
                </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 239:
  ```text
              ))}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 240:
  ```text
            </Section>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 241:
  ```text
          )}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 242:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 243:
  ```text
          <View style={styles.totalRow}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 244:
  ```text
            <Text style={styles.totalLabel}>Total</Text>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 245:
  ```text
            <Text style={styles.totalPrice}>₱{Number(booking.total || 0).toLocaleString()}</Text>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 246:
  ```text
          </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 247:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 248:
  ```text
          <Divider style={{ marginVertical: spacing.xl }} />
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 249:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 250:
  ```text
          <View style={styles.actionGroup}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 251:
  ```text
            {isPend && (
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 252:
  ```text
              <Button label="Accept → Ongoing" icon="checkmark-circle"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 253:
  ```text
                onPress={() => onStatusChange(booking, BOOKING_STATUS.ONGOING)}
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 254:
  ```text
                loading={updating} fullWidth style={{ marginBottom: spacing.lg }} />
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 255:
  ```text
            )}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 256:
  ```text
            {isGoing && (
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 257:
  ```text
              <Button label="Mark Session as Done" icon="checkmark-circle"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 258:
  ```text
                onPress={() => onStatusChange(booking, BOOKING_STATUS.DONE)}
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 259:
  ```text
                loading={updating} fullWidth
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 260:
  ```text
                style={{ marginBottom: spacing.lg, backgroundColor: colors.success }} />
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 261:
  ```text
            )}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 262:
  ```text
            <Button label="Cancel Booking" variant="danger" icon="close-circle-outline"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 263:
  ```text
              onPress={() => onStatusChange(booking, BOOKING_STATUS.CANCELLED)}
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 264:
  ```text
              loading={updating} fullWidth />
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 265:
  ```text
          </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 266:
  ```text
        </ScrollView>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 267:
  ```text
      </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 268:
  ```text
    </Modal>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 269:
  ```text
  );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 270:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 7. `Section`

- Signature: `function Section(param)`
- Source range: lines 272-279

#### Line-by-line

- Line 272:
  ```text
function Section({ title, children }) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 273:
  ```text
  return (
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 274:
  ```text
    <View style={styles.section}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 275:
  ```text
      <Text style={styles.sectionTitle}>{title}</Text>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 276:
  ```text
      {children}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 277:
  ```text
    </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 278:
  ```text
  );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 279:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 8. `DetailRow`

- Signature: `function DetailRow(param)`
- Source range: lines 281-288

#### Line-by-line

- Line 281:
  ```text
function DetailRow({ icon, label }) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 282:
  ```text
  return (
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 283:
  ```text
    <View style={styles.detailRow}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 284:
  ```text
      <Icon name={icon} size={15} color={colors.mutedForeground} />
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 285:
  ```text
      <Text style={styles.detailRowText}>{label}</Text>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 286:
  ```text
    </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 287:
  ```text
  );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 288:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

## File: `HeigenKiosk/src/screens/BookingSummaryModal.js`

- Methods documented: 3

### 1. `BookingSummaryModal`

- Signature: `function BookingSummaryModal(param)`
- Source range: lines 16-757

#### Line-by-line

- Line 16:
  ```text
export default function BookingSummaryModal({
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 17:
  ```text
    visible,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 18:
  ```text
    onClose,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 19:
  ```text
    onEdit,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 20:
  ```text
    onConfirm,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 21:
  ```text
    category,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 22:
  ```text
    selectedPackage,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 23:
  ```text
    selectedAddons,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 24:
  ```text
    customerInfo,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 25:
  ```text
    loading,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 26:
  ```text
}) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 27:
  ```text
    const { s, fs, isTablet, W } = useScale();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 28:
  ```text
    if (!selectedPackage || !customerInfo) return null;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 29:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 30:
  ```text
    const pkgPrice = Number(
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 31:
  ```text
        selectedPackage?.promo_price
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 32:
  ```text
            ? selectedPackage.promo_price
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 33:
  ```text
            : selectedPackage?.price
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 34:
  ```text
              ? selectedPackage.price
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 35:
  ```text
              : 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 36:
  ```text
    );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 37:
  ```text
    const addonsTotal = selectedAddons.reduce(
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 38:
  ```text
        (sum, a) => sum + Number(a.price),
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 39:
  ```text
        0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 40:
  ```text
    );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 41:
  ```text
    const grandTotal = pkgPrice + addonsTotal;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 42:
  ```text
    const inclusions = Array.isArray(selectedPackage.inclusions)
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 43:
  ```text
        ? selectedPackage.inclusions
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 44:
  ```text
        : [];
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 45:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 46:
  ```text
    // ── TABLET: centered fixed dialog, no scroll, raw dp sizing ─────────────────
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 47:
  ```text
    if (isTablet) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 48:
  ```text
        const dialogW = Math.min(720, W * 0.86);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 49:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 50:
  ```text
        return (
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 51:
  ```text
            <Modal
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 52:
  ```text
                visible={visible}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 53:
  ```text
                transparent
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 54:
  ```text
                animationType="fade"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 55:
  ```text
                onRequestClose={onClose}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 56:
  ```text
            >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 57:
  ```text
                <Pressable
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 58:
  ```text
                    style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 59:
  ```text
                        flex: 1,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 60:
  ```text
                        backgroundColor: "rgba(0,0,0,0.55)",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 61:
  ```text
                        justifyContent: "center",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 62:
  ```text
                        alignItems: "center",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 63:
  ```text
                    }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 64:
  ```text
                    onPress={onClose}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 65:
  ```text
                >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 66:
  ```text
                    <Pressable onPress={(e) => e.stopPropagation()}>
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 67:
  ```text
                        <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 68:
  ```text
                            style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 69:
  ```text
                                width: dialogW,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 70:
  ```text
                                backgroundColor: "#fff",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 71:
  ```text
                                borderRadius: 16,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 72:
  ```text
                                overflow: "hidden",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 73:
  ```text
                                ...shadow.lg,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 74:
  ```text
                            }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 75:
  ```text
                        >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 76:
  ```text
                            {/* ── Dialog header ── */}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 77:
  ```text
                            <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 78:
  ```text
                                style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 79:
  ```text
                                    flexDirection: "row",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 80:
  ```text
                                    alignItems: "center",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 81:
  ```text
                                    justifyContent: "space-between",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 82:
  ```text
                                    paddingHorizontal: 20,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 83:
  ```text
                                    paddingVertical: 14,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 84:
  ```text
                                    borderBottomWidth: 1,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 85:
  ```text
                                    borderBottomColor: colors.border,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 86:
  ```text
                                }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 87:
  ```text
                            >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 88:
  ```text
                                <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 89:
  ```text
                                    style={{ fontSize: 15, fontWeight: "700" }}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 90:
  ```text
                                    allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 91:
  ```text
                                >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 92:
  ```text
                                    Booking Summary
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 93:
  ```text
                                </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 94:
  ```text
                                <TouchableOpacity
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 95:
  ```text
                                    onPress={onClose}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 96:
  ```text
                                    style={{ padding: 6 }}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 97:
  ```text
                                >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 98:
  ```text
                                    <Icon
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 99:
  ```text
                                        name="close"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 100:
  ```text
                                        size={18}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 101:
  ```text
                                        color={colors.mutedForeground}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 102:
  ```text
                                    />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 103:
  ```text
                                </TouchableOpacity>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 104:
  ```text
                            </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 105:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 106:
  ```text
                            {/* ── Two-column body ── */}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 107:
  ```text
                            <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 108:
  ```text
                                style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 109:
  ```text
                                    flexDirection: "row",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 110:
  ```text
                                    padding: 20,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 111:
  ```text
                                    gap: 20,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 112:
  ```text
                                }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 113:
  ```text
                            >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 114:
  ```text
                                {/* Left column: customer info */}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 115:
  ```text
                                <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 116:
  ```text
                                    style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 117:
  ```text
                                        flex: 1,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 118:
  ```text
                                        borderRightWidth: 1,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 119:
  ```text
                                        borderRightColor: colors.border,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 120:
  ```text
                                        paddingRight: 20,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 121:
  ```text
                                    }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 122:
  ```text
                                >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 123:
  ```text
                                    <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 124:
  ```text
                                        style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 125:
  ```text
                                            fontSize: 10,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 126:
  ```text
                                            fontWeight: "700",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 127:
  ```text
                                            color: colors.mutedForeground,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 128:
  ```text
                                            textTransform: "uppercase",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 129:
  ```text
                                            letterSpacing: 0.8,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 130:
  ```text
                                            marginBottom: 10,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 131:
  ```text
                                        }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 132:
  ```text
                                        allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 133:
  ```text
                                    >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 134:
  ```text
                                        Customer
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 135:
  ```text
                                    </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 136:
  ```text
                                    <SRow
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 137:
  ```text
                                        label="Name"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 138:
  ```text
                                        value={customerInfo.fullName}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 139:
  ```text
                                    />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 140:
  ```text
                                    <SRow
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 141:
  ```text
                                        label="Email"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 142:
  ```text
                                        value={customerInfo.email}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 143:
  ```text
                                    />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 144:
  ```text
                                    <SRow
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 145:
  ```text
                                        label="Contact"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 146:
  ```text
                                        value={customerInfo.contactNumber}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 147:
  ```text
                                    />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 148:
  ```text
                                    {customerInfo.preferredDate ? (
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 149:
  ```text
                                        <SRow
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 150:
  ```text
                                            label="Date"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 151:
  ```text
                                            value={customerInfo.preferredDate}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 152:
  ```text
                                        />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 153:
  ```text
                                    ) : null}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 154:
  ```text
                                </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 155:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 156:
  ```text
                                {/* Right column: booking details */}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 157:
  ```text
                                <View style={{ flex: 1.3 }}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 158:
  ```text
                                    <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 159:
  ```text
                                        style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 160:
  ```text
                                            fontSize: 10,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 161:
  ```text
                                            fontWeight: "700",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 162:
  ```text
                                            color: colors.mutedForeground,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 163:
  ```text
                                            textTransform: "uppercase",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 164:
  ```text
                                            letterSpacing: 0.8,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 165:
  ```text
                                            marginBottom: 10,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 166:
  ```text
                                        }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 167:
  ```text
                                        allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 168:
  ```text
                                    >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 169:
  ```text
                                        Booking
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 170:
  ```text
                                    </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 171:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 172:
  ```text
                                    <SRow
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 173:
  ```text
                                        label="Category"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 174:
  ```text
                                        value={category?.name}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 175:
  ```text
                                    />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 176:
  ```text
                                    <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 177:
  ```text
                                        style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 178:
  ```text
                                            height: 1,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 179:
  ```text
                                            backgroundColor: colors.border,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 180:
  ```text
                                            marginVertical: 8,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 181:
  ```text
                                        }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 182:
  ```text
                                    />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 183:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 184:
  ```text
                                    {/* Package */}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 185:
  ```text
                                    <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 186:
  ```text
                                        style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 187:
  ```text
                                            flexDirection: "row",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 188:
  ```text
                                            justifyContent: "space-between",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 189:
  ```text
                                            alignItems: "flex-start",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 190:
  ```text
                                            marginBottom: 4,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 191:
  ```text
                                        }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 192:
  ```text
                                    >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 193:
  ```text
                                        <View style={{ flex: 1 }}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 194:
  ```text
                                            <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 195:
  ```text
                                                style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 196:
  ```text
                                                    fontSize: 10,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 197:
  ```text
                                                    color: colors.mutedForeground,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 198:
  ```text
                                                    marginBottom: 2,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 199:
  ```text
                                                }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 200:
  ```text
                                                allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 201:
  ```text
                                            >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 202:
  ```text
                                                Package
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 203:
  ```text
                                            </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 204:
  ```text
                                            <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 205:
  ```text
                                                style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 206:
  ```text
                                                    fontSize: 13,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 207:
  ```text
                                                    fontWeight: "600",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 208:
  ```text
                                                }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 209:
  ```text
                                                allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 210:
  ```text
                                            >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 211:
  ```text
                                                {selectedPackage.name}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 212:
  ```text
                                            </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 213:
  ```text
                                            {inclusions
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 214:
  ```text
                                                .slice(0, 3)
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 215:
  ```text
                                                .map((item, i) => (
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 216:
  ```text
                                                    <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 217:
  ```text
                                                        key={i}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 218:
  ```text
                                                        style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 219:
  ```text
                                                            fontSize: 11,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 220:
  ```text
                                                            color: colors.mutedForeground,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 221:
  ```text
                                                            marginLeft: 4,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 222:
  ```text
                                                        }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 223:
  ```text
                                                        allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 224:
  ```text
                                                    >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 225:
  ```text
                                                        • {item}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 226:
  ```text
                                                    </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 227:
  ```text
                                                ))}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 228:
  ```text
                                            {inclusions.length > 3 ? (
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 229:
  ```text
                                                <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 230:
  ```text
                                                    style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 231:
  ```text
                                                        fontSize: 11,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 232:
  ```text
                                                        color: colors.mutedForeground,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 233:
  ```text
                                                        marginLeft: 4,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 234:
  ```text
                                                    }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 235:
  ```text
                                                    allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 236:
  ```text
                                                >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 237:
  ```text
                                                    +{inclusions.length - 3}{" "}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 238:
  ```text
                                                    more
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 239:
  ```text
                                                </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 240:
  ```text
                                            ) : null}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 241:
  ```text
                                        </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 242:
  ```text
                                        <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 243:
  ```text
                                            style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 244:
  ```text
                                                fontSize: 14,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 245:
  ```text
                                                fontWeight: "700",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 246:
  ```text
                                                color: colors.accent,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 247:
  ```text
                                                marginLeft: 8,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 248:
  ```text
                                            }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 249:
  ```text
                                            allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 250:
  ```text
                                        >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 251:
  ```text
                                            ₱{pkgPrice.toLocaleString()}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 252:
  ```text
                                        </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 253:
  ```text
                                    </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 254:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 255:
  ```text
                                    {/* Add-ons */}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 256:
  ```text
                                    {selectedAddons.length > 0 && (
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 257:
  ```text
                                        <>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 258:
  ```text
                                            <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 259:
  ```text
                                                style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 260:
  ```text
                                                    height: 1,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 261:
  ```text
                                                    backgroundColor:
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 262:
  ```text
                                                        colors.border,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 263:
  ```text
                                                    marginVertical: 8,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 264:
  ```text
                                                }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 265:
  ```text
                                            />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 266:
  ```text
                                            <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 267:
  ```text
                                                style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 268:
  ```text
                                                    fontSize: 10,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 269:
  ```text
                                                    color: colors.mutedForeground,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 270:
  ```text
                                                    marginBottom: 4,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 271:
  ```text
                                                }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 272:
  ```text
                                                allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 273:
  ```text
                                            >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 274:
  ```text
                                                Add-ons
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 275:
  ```text
                                            </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 276:
  ```text
                                            {selectedAddons.map((addon) => (
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 277:
  ```text
                                                <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 278:
  ```text
                                                    key={addon.id}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 279:
  ```text
                                                    style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 280:
  ```text
                                                        flexDirection: "row",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 281:
  ```text
                                                        justifyContent:
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 282:
  ```text
                                                            "space-between",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 283:
  ```text
                                                        marginBottom: 3,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 284:
  ```text
                                                    }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 285:
  ```text
                                                >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 286:
  ```text
                                                    <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 287:
  ```text
                                                        style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 288:
  ```text
                                                            fontSize: 12,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 289:
  ```text
                                                            fontWeight: "500",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 290:
  ```text
                                                            flex: 1,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 291:
  ```text
                                                        }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 292:
  ```text
                                                        allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 293:
  ```text
                                                    >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 294:
  ```text
                                                        {addon.name}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 295:
  ```text
                                                    </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 296:
  ```text
                                                    <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 297:
  ```text
                                                        style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 298:
  ```text
                                                            fontSize: 12,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 299:
  ```text
                                                            fontWeight: "600",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 300:
  ```text
                                                            color: colors.accent,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 301:
  ```text
                                                        }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 302:
  ```text
                                                        allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 303:
  ```text
                                                    >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 304:
  ```text
                                                        +₱
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 305:
  ```text
                                                        {Number(
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 306:
  ```text
                                                            addon.price,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 307:
  ```text
                                                        ).toLocaleString()}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 308:
  ```text
                                                    </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 309:
  ```text
                                                </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 310:
  ```text
                                            ))}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 311:
  ```text
                                        </>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 312:
  ```text
                                    )}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 313:
  ```text
                                </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 314:
  ```text
                            </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 315:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 316:
  ```text
                            {/* ── Total + notice + actions ── */}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 317:
  ```text
                            <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 318:
  ```text
                                style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 319:
  ```text
                                    paddingHorizontal: 20,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 320:
  ```text
                                    paddingBottom: 20,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 321:
  ```text
                                    gap: 12,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 322:
  ```text
                                }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 323:
  ```text
                            >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 324:
  ```text
                                {/* Total bar */}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 325:
  ```text
                                <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 326:
  ```text
                                    style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 327:
  ```text
                                        flexDirection: "row",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 328:
  ```text
                                        alignItems: "center",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 329:
  ```text
                                        justifyContent: "space-between",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 330:
  ```text
                                        backgroundColor: "rgba(217,119,6,0.08)",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 331:
  ```text
                                        borderWidth: 1.5,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 332:
  ```text
                                        borderColor: colors.accent,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 333:
  ```text
                                        borderRadius: 10,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 334:
  ```text
                                        paddingHorizontal: 16,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 335:
  ```text
                                        paddingVertical: 10,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 336:
  ```text
                                    }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 337:
  ```text
                                >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 338:
  ```text
                                    <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 339:
  ```text
                                        style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 340:
  ```text
                                            flexDirection: "row",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 341:
  ```text
                                            alignItems: "center",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 342:
  ```text
                                            gap: 8,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 343:
  ```text
                                            flex: 1,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 344:
  ```text
                                        }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 345:
  ```text
                                    >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 346:
  ```text
                                        <Icon
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 347:
  ```text
                                            name="time-outline"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 348:
  ```text
                                            size={13}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 349:
  ```text
                                            color={colors.accent}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 350:
  ```text
                                        />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 351:
  ```text
                                        <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 352:
  ```text
                                            style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 353:
  ```text
                                                fontSize: 11,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 354:
  ```text
                                                color: colors.mutedForeground,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 355:
  ```text
                                            }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 356:
  ```text
                                            allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 357:
  ```text
                                        >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 358:
  ```text
                                            Will be{" "}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 359:
  ```text
                                            <Text style={{ fontWeight: "700" }}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 360:
  ```text
                                                Pending
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 361:
  ```text
                                            </Text>{" "}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 362:
  ```text
                                            until confirmed by staff
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 363:
  ```text
                                        </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 364:
  ```text
                                    </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 365:
  ```text
                                    <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 366:
  ```text
                                        style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 367:
  ```text
                                            alignItems: "flex-end",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 368:
  ```text
                                            marginLeft: 16,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 369:
  ```text
                                        }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 370:
  ```text
                                    >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 371:
  ```text
                                        <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 372:
  ```text
                                            style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 373:
  ```text
                                                fontSize: 10,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 374:
  ```text
                                                color: colors.mutedForeground,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 375:
  ```text
                                                textTransform: "uppercase",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 376:
  ```text
                                                letterSpacing: 0.4,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 377:
  ```text
                                            }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 378:
  ```text
                                            allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 379:
  ```text
                                        >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 380:
  ```text
                                            Total
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 381:
  ```text
                                        </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 382:
  ```text
                                        <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 383:
  ```text
                                            style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 384:
  ```text
                                                fontSize: 20,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 385:
  ```text
                                                fontWeight: "700",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 386:
  ```text
                                                color: colors.accent,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 387:
  ```text
                                            }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 388:
  ```text
                                            allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 389:
  ```text
                                        >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 390:
  ```text
                                            ₱{grandTotal.toLocaleString()}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 391:
  ```text
                                        </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 392:
  ```text
                                    </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 393:
  ```text
                                </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 394:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 395:
  ```text
                                {/* Actions */}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 396:
  ```text
                                <View style={{ flexDirection: "row", gap: 10 }}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 397:
  ```text
                                    <Button
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 398:
  ```text
                                        label="Edit Selection"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 399:
  ```text
                                        icon="create-outline"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 400:
  ```text
                                        variant="secondary"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 401:
  ```text
                                        size="sm"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 402:
  ```text
                                        onPress={onEdit}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 403:
  ```text
                                        style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 404:
  ```text
                                            flex: 1,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 405:
  ```text
                                            height: 40,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 406:
  ```text
                                            minHeight: 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 407:
  ```text
                                        }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 408:
  ```text
                                        disabled={loading}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 409:
  ```text
                                    />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 410:
  ```text
                                    <Button
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 411:
  ```text
                                        label="Confirm Booking"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 412:
  ```text
                                        onPress={onConfirm}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 413:
  ```text
                                        size="sm"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 414:
  ```text
                                        loading={loading}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 415:
  ```text
                                        style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 416:
  ```text
                                            flex: 1,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 417:
  ```text
                                            height: 40,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 418:
  ```text
                                            minHeight: 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 419:
  ```text
                                        }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 420:
  ```text
                                    />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 421:
  ```text
                                </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 422:
  ```text
                            </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 423:
  ```text
                        </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 424:
  ```text
                    </Pressable>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 425:
  ```text
                </Pressable>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 426:
  ```text
            </Modal>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 427:
  ```text
        );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 428:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 429:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 430:
  ```text
    // ── PHONE: bottom sheet with scroll ──────────────────────────────────────────
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 431:
  ```text
    return (
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 432:
  ```text
        <Modal
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 433:
  ```text
            visible={visible}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 434:
  ```text
            transparent
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 435:
  ```text
            animationType="slide"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 436:
  ```text
            onRequestClose={onClose}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 437:
  ```text
        >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 438:
  ```text
            <Pressable
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 439:
  ```text
                style={{ flex: 1, backgroundColor: "rgba(0,0,0,0.5)" }}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 440:
  ```text
                onPress={onClose}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 441:
  ```text
            />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 442:
  ```text
            <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 443:
  ```text
                style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 444:
  ```text
                    position: "absolute",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 445:
  ```text
                    bottom: 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 446:
  ```text
                    left: 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 447:
  ```text
                    right: 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 448:
  ```text
                    backgroundColor: "#fff",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 449:
  ```text
                    borderTopLeftRadius: s(radii.xxl),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 450:
  ```text
                    borderTopRightRadius: s(radii.xxl),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 451:
  ```text
                    maxHeight: "92%",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 452:
  ```text
                    ...shadow.lg,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 453:
  ```text
                }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 454:
  ```text
            >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 455:
  ```text
                <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 456:
  ```text
                    style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 457:
  ```text
                        width: s(40),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 458:
  ```text
                        height: s(4),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 459:
  ```text
                        borderRadius: s(2),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 460:
  ```text
                        backgroundColor: colors.border,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 461:
  ```text
                        alignSelf: "center",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 462:
  ```text
                        marginTop: s(spacing.md),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 463:
  ```text
                    }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 464:
  ```text
                />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 465:
  ```text
                <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 466:
  ```text
                    style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 467:
  ```text
                        flexDirection: "row",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 468:
  ```text
                        alignItems: "center",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 469:
  ```text
                        justifyContent: "space-between",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 470:
  ```text
                        paddingHorizontal: s(spacing.xxl),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 471:
  ```text
                        paddingVertical: s(spacing.lg),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 472:
  ```text
                        borderBottomWidth: 1,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 473:
  ```text
                        borderBottomColor: colors.border,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 474:
  ```text
                    }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 475:
  ```text
                >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 476:
  ```text
                    <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 477:
  ```text
                        style={{ fontSize: fs(18), fontWeight: "700" }}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 478:
  ```text
                        allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 479:
  ```text
                    >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 480:
  ```text
                        Booking Summary
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 481:
  ```text
                    </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 482:
  ```text
                    <TouchableOpacity
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 483:
  ```text
                        onPress={onClose}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 484:
  ```text
                        style={{ padding: s(spacing.sm) }}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 485:
  ```text
                    >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 486:
  ```text
                        <Icon
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 487:
  ```text
                            name="close"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 488:
  ```text
                            size={s(22)}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 489:
  ```text
                            color={colors.mutedForeground}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 490:
  ```text
                        />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 491:
  ```text
                    </TouchableOpacity>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 492:
  ```text
                </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 493:
  ```text
                <ScrollView
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 494:
  ```text
                    contentContainerStyle={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 495:
  ```text
                        padding: s(spacing.xxl),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 496:
  ```text
                        paddingBottom: s(spacing.xxxl),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 497:
  ```text
                    }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 498:
  ```text
                    showsVerticalScrollIndicator={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 499:
  ```text
                >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 500:
  ```text
                    <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 501:
  ```text
                        style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 502:
  ```text
                            fontSize: fs(11),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 503:
  ```text
                            fontWeight: "700",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 504:
  ```text
                            color: colors.mutedForeground,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 505:
  ```text
                            textTransform: "uppercase",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 506:
  ```text
                            letterSpacing: 0.6,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 507:
  ```text
                            marginBottom: s(spacing.md),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 508:
  ```text
                        }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 509:
  ```text
                        allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 510:
  ```text
                    >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 511:
  ```text
                        Customer
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 512:
  ```text
                    </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 513:
  ```text
                    <PhoneRow
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 514:
  ```text
                        label="Name"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 515:
  ```text
                        value={customerInfo.fullName}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 516:
  ```text
                        s={s}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 517:
  ```text
                        fs={fs}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 518:
  ```text
                    />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 519:
  ```text
                    <PhoneRow
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 520:
  ```text
                        label="Email"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 521:
  ```text
                        value={customerInfo.email}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 522:
  ```text
                        s={s}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 523:
  ```text
                        fs={fs}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 524:
  ```text
                    />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 525:
  ```text
                    <PhoneRow
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 526:
  ```text
                        label="Contact"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 527:
  ```text
                        value={customerInfo.contactNumber}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 528:
  ```text
                        s={s}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 529:
  ```text
                        fs={fs}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 530:
  ```text
                    />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 531:
  ```text
                    {customerInfo.preferredDate ? (
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 532:
  ```text
                        <PhoneRow
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 533:
  ```text
                            label="Date"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 534:
  ```text
                            value={customerInfo.preferredDate}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 535:
  ```text
                            s={s}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 536:
  ```text
                            fs={fs}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 537:
  ```text
                        />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 538:
  ```text
                    ) : null}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 539:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 540:
  ```text
                    <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 541:
  ```text
                        style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 542:
  ```text
                            height: 1,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 543:
  ```text
                            backgroundColor: colors.border,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 544:
  ```text
                            marginVertical: s(spacing.lg),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 545:
  ```text
                        }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 546:
  ```text
                    />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 547:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 548:
  ```text
                    <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 549:
  ```text
                        style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 550:
  ```text
                            fontSize: fs(11),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 551:
  ```text
                            fontWeight: "700",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 552:
  ```text
                            color: colors.mutedForeground,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 553:
  ```text
                            textTransform: "uppercase",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 554:
  ```text
                            letterSpacing: 0.6,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 555:
  ```text
                            marginBottom: s(spacing.md),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 556:
  ```text
                        }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 557:
  ```text
                        allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 558:
  ```text
                    >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 559:
  ```text
                        Booking
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 560:
  ```text
                    </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 561:
  ```text
                    <PhoneRow
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 562:
  ```text
                        label="Category"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 563:
  ```text
                        value={category?.name}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 564:
  ```text
                        s={s}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 565:
  ```text
                        fs={fs}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 566:
  ```text
                    />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 567:
  ```text
                    <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 568:
  ```text
                        style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 569:
  ```text
                            height: 1,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 570:
  ```text
                            backgroundColor: colors.border,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 571:
  ```text
                            marginVertical: s(spacing.md),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 572:
  ```text
                        }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 573:
  ```text
                    />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 574:
  ```text
                    <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 575:
  ```text
                        style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 576:
  ```text
                            flexDirection: "row",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 577:
  ```text
                            justifyContent: "space-between",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 578:
  ```text
                            alignItems: "flex-start",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 579:
  ```text
                            marginBottom: s(spacing.sm),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 580:
  ```text
                        }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 581:
  ```text
                    >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 582:
  ```text
                        <View style={{ flex: 1 }}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 583:
  ```text
                            <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 584:
  ```text
                                style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 585:
  ```text
                                    fontSize: fs(12),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 586:
  ```text
                                    color: colors.mutedForeground,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 587:
  ```text
                                }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 588:
  ```text
                                allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 589:
  ```text
                            >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 590:
  ```text
                                Package
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 591:
  ```text
                            </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 592:
  ```text
                            <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 593:
  ```text
                                style={{ fontSize: fs(15), fontWeight: "600" }}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 594:
  ```text
                                allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 595:
  ```text
                            >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 596:
  ```text
                                {selectedPackage.name}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 597:
  ```text
                            </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 598:
  ```text
                            {inclusions.map((item, i) => (
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 599:
  ```text
                                <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 600:
  ```text
                                    key={i}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 601:
  ```text
                                    style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 602:
  ```text
                                        fontSize: fs(12),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 603:
  ```text
                                        color: colors.mutedForeground,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 604:
  ```text
                                        marginLeft: s(spacing.sm),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 605:
  ```text
                                    }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 606:
  ```text
                                    allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 607:
  ```text
                                >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 608:
  ```text
                                    • {item}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 609:
  ```text
                                </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 610:
  ```text
                            ))}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 611:
  ```text
                        </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 612:
  ```text
                        <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 613:
  ```text
                            style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 614:
  ```text
                                fontSize: fs(16),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 615:
  ```text
                                fontWeight: "700",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 616:
  ```text
                                color: colors.accent,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 617:
  ```text
                            }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 618:
  ```text
                            allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 619:
  ```text
                        >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 620:
  ```text
                            ₱{pkgPrice.toLocaleString()}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 621:
  ```text
                        </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 622:
  ```text
                    </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 623:
  ```text
                    {selectedAddons.length > 0 && (
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 624:
  ```text
                        <>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 625:
  ```text
                            <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 626:
  ```text
                                style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 627:
  ```text
                                    height: 1,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 628:
  ```text
                                    backgroundColor: colors.border,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 629:
  ```text
                                    marginVertical: s(spacing.md),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 630:
  ```text
                                }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 631:
  ```text
                            />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 632:
  ```text
                            <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 633:
  ```text
                                style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 634:
  ```text
                                    fontSize: fs(12),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 635:
  ```text
                                    color: colors.mutedForeground,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 636:
  ```text
                                    marginBottom: s(spacing.sm),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 637:
  ```text
                                }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 638:
  ```text
                                allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 639:
  ```text
                            >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 640:
  ```text
                                Add-ons
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 641:
  ```text
                            </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 642:
  ```text
                            {selectedAddons.map((addon) => (
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 643:
  ```text
                                <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 644:
  ```text
                                    key={addon.id}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 645:
  ```text
                                    style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 646:
  ```text
                                        flexDirection: "row",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 647:
  ```text
                                        justifyContent: "space-between",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 648:
  ```text
                                        marginBottom: s(spacing.sm),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 649:
  ```text
                                    }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 650:
  ```text
                                >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 651:
  ```text
                                    <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 652:
  ```text
                                        style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 653:
  ```text
                                            fontSize: fs(14),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 654:
  ```text
                                            fontWeight: "500",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 655:
  ```text
                                            flex: 1,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 656:
  ```text
                                        }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 657:
  ```text
                                        allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 658:
  ```text
                                    >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 659:
  ```text
                                        {addon.name}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 660:
  ```text
                                    </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 661:
  ```text
                                    <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 662:
  ```text
                                        style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 663:
  ```text
                                            fontSize: fs(14),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 664:
  ```text
                                            fontWeight: "600",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 665:
  ```text
                                            color: colors.accent,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 666:
  ```text
                                        }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 667:
  ```text
                                        allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 668:
  ```text
                                    >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 669:
  ```text
                                        +₱{Number(addon.price).toLocaleString()}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 670:
  ```text
                                    </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 671:
  ```text
                                </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 672:
  ```text
                            ))}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 673:
  ```text
                        </>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 674:
  ```text
                    )}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 675:
  ```text
                    <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 676:
  ```text
                        style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 677:
  ```text
                            flexDirection: "row",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 678:
  ```text
                            alignItems: "center",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 679:
  ```text
                            justifyContent: "space-between",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 680:
  ```text
                            backgroundColor: "rgba(217,119,6,0.08)",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 681:
  ```text
                            borderWidth: 2,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 682:
  ```text
                            borderColor: colors.accent,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 683:
  ```text
                            borderRadius: s(radii.xl),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 684:
  ```text
                            padding: s(spacing.xl),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 685:
  ```text
                            marginVertical: s(spacing.lg),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 686:
  ```text
                        }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 687:
  ```text
                    >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 688:
  ```text
                        <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 689:
  ```text
                            style={{ fontSize: fs(15), fontWeight: "600" }}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 690:
  ```text
                            allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 691:
  ```text
                        >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 692:
  ```text
                            Total Amount
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 693:
  ```text
                        </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 694:
  ```text
                        <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 695:
  ```text
                            style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 696:
  ```text
                                fontSize: fs(28),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 697:
  ```text
                                fontWeight: "700",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 698:
  ```text
                                color: colors.accent,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 699:
  ```text
                            }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 700:
  ```text
                            allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 701:
  ```text
                        >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 702:
  ```text
                            ₱{grandTotal.toLocaleString()}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 703:
  ```text
                        </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 704:
  ```text
                    </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 705:
  ```text
                    <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 706:
  ```text
                        style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 707:
  ```text
                            flexDirection: "row",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 708:
  ```text
                            alignItems: "flex-start",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 709:
  ```text
                            gap: s(spacing.sm),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 710:
  ```text
                            backgroundColor: "rgba(217,119,6,0.06)",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 711:
  ```text
                            borderRadius: s(radii.lg),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 712:
  ```text
                            padding: s(spacing.lg),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 713:
  ```text
                            marginBottom: s(spacing.xl),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 714:
  ```text
                        }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 715:
  ```text
                    >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 716:
  ```text
                        <Icon
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 717:
  ```text
                            name="time-outline"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 718:
  ```text
                            size={s(16)}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 719:
  ```text
                            color={colors.accent}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 720:
  ```text
                        />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 721:
  ```text
                        <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 722:
  ```text
                            style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 723:
  ```text
                                fontSize: fs(13),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 724:
  ```text
                                color: colors.mutedForeground,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 725:
  ```text
                                flex: 1,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 726:
  ```text
                                lineHeight: fs(19),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 727:
  ```text
                            }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 728:
  ```text
                            allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 729:
  ```text
                        >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 730:
  ```text
                            Your booking will be{" "}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 731:
  ```text
                            <Text style={{ fontWeight: "700" }}>Pending</Text>{" "}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 732:
  ```text
                            until confirmed by staff.
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 733:
  ```text
                        </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 734:
  ```text
                    </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 735:
  ```text
                    <View style={{ flexDirection: "row", gap: s(spacing.lg) }}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 736:
  ```text
                        <Button
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 737:
  ```text
                            label="Edit Selection"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 738:
  ```text
                            icon="create-outline"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 739:
  ```text
                            variant="secondary"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 740:
  ```text
                            size="sm"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 741:
  ```text
                            onPress={onEdit}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 742:
  ```text
                            style={{ flex: 1 }}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 743:
  ```text
                            disabled={loading}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 744:
  ```text
                        />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 745:
  ```text
                        <Button
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 746:
  ```text
                            label="Confirm Booking"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 747:
  ```text
                            onPress={onConfirm}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 748:
  ```text
                            size="sm"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 749:
  ```text
                            loading={loading}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 750:
  ```text
                            style={{ flex: 1 }}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 751:
  ```text
                        />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 752:
  ```text
                    </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 753:
  ```text
                </ScrollView>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 754:
  ```text
            </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 755:
  ```text
        </Modal>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 756:
  ```text
    );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 757:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 2. `SRow`

- Signature: `function SRow(param)`
- Source range: lines 759-780

#### Line-by-line

- Line 759:
  ```text
function SRow({ label, value }) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 760:
  ```text
    return (
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 761:
  ```text
        <View style={{ flexDirection: "row", marginBottom: 7 }}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 762:
  ```text
            <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 763:
  ```text
                style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 764:
  ```text
                    fontSize: 11,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 765:
  ```text
                    color: colors.mutedForeground,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 766:
  ```text
                    width: 58,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 767:
  ```text
                }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 768:
  ```text
                allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 769:
  ```text
            >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 770:
  ```text
                {label}:
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 771:
  ```text
            </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 772:
  ```text
            <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 773:
  ```text
                style={{ fontSize: 12, fontWeight: "600", flex: 1 }}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 774:
  ```text
                allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 775:
  ```text
            >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 776:
  ```text
                {value}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 777:
  ```text
            </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 778:
  ```text
        </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 779:
  ```text
    );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 780:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 3. `PhoneRow`

- Signature: `function PhoneRow(param)`
- Source range: lines 781-802

#### Line-by-line

- Line 781:
  ```text
function PhoneRow({ label, value, s, fs }) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 782:
  ```text
    return (
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 783:
  ```text
        <View style={{ flexDirection: "row", marginBottom: s(spacing.sm) }}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 784:
  ```text
            <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 785:
  ```text
                style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 786:
  ```text
                    fontSize: fs(13),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 787:
  ```text
                    color: colors.mutedForeground,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 788:
  ```text
                    width: s(90),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 789:
  ```text
                }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 790:
  ```text
                allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 791:
  ```text
            >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 792:
  ```text
                {label}:
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 793:
  ```text
            </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 794:
  ```text
            <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 795:
  ```text
                style={{ fontSize: fs(13), fontWeight: "600", flex: 1 }}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 796:
  ```text
                allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 797:
  ```text
            >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 798:
  ```text
                {value}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 799:
  ```text
            </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 800:
  ```text
        </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 801:
  ```text
    );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 802:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

## File: `HeigenKiosk/src/screens/CategoryScreen.js`

- Methods documented: 3

### 1. `CategoryScreen`

- Signature: `function CategoryScreen(param)`
- Source range: lines 21-153

#### Line-by-line

- Line 21:
  ```text
export default function CategoryScreen({
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 22:
  ```text
  onSelectCategory,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 23:
  ```text
  recommendationData,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 24:
  ```text
  onSelectRecommendation,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 25:
  ```text
}) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 26:
  ```text
  const { data: categories, loading, error, refetch } = useApi(fetchCategories);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 27:
  ```text
  const { s, fs, isTablet, W } = useScale();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 28:
  ```text
  const [scrollY, setScrollY] = useState(0);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 29:
  ```text
  const [contentHeight, setContentHeight] = useState(0);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 30:
  ```text
  const [viewportHeight, setViewportHeight] = useState(0);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 31:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 32:
  ```text
  if (loading) return <LoadingScreen message="Loading categories..." />;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 33:
  ```text
  if (error)   return <ErrorScreen message={error} onRetry={refetch} />;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 34:
  ```text
  if (!categories?.length) return (
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 35:
  ```text
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 36:
  ```text
      <Text style={{ fontSize: fs(16), color: colors.mutedForeground }}>No categories available.</Text>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 37:
  ```text
    </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 38:
  ```text
  );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 39:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 40:
  ```text
  // On tablet landscape: two-column grid; on phone: single column list
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 41:
  ```text
  const numCols = isTablet ? 2 : 1;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 42:
  ```text
  const colGap  = s(spacing.lg);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 43:
  ```text
  const hPad    = s(spacing.xl);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 44:
  ```text
  const cardW   = numCols === 2 ? (W - hPad * 2 - colGap) / 2 : undefined;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 45:
  ```text
  const recs = recommendationData?.recommendations ?? [];
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 46:
  ```text
  const hasRecs = recs.length > 0;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 47:
  ```text
  const isHistoryBased = (recommendationData?.total_bookings ?? 0) > 0;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 48:
  ```text
  const maxScroll = Math.max(0, contentHeight - viewportHeight);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 49:
  ```text
  const canScrollDown = maxScroll > s(8);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 50:
  ```text
  const isNearBottom = scrollY >= maxScroll - s(20);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 51:
  ```text
  const showScrollHint = canScrollDown && !isNearBottom;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 52:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 53:
  ```text
  return (
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 54:
  ```text
    <View style={{ flex: 1, backgroundColor: colors.background }}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 55:
  ```text
      <ScrollView
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 56:
  ```text
        style={{ flex: 1, backgroundColor: colors.background }}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 57:
  ```text
        contentContainerStyle={{ padding: hPad, paddingBottom: s(spacing.xxxl * 2) }}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 58:
  ```text
        showsVerticalScrollIndicator={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 59:
  ```text
        onLayout={(e) => setViewportHeight(e.nativeEvent.layout.height)}
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 60:
  ```text
        onContentSizeChange={(_, h) => setContentHeight(h)}
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 61:
  ```text
        onScroll={(e) => setScrollY(e.nativeEvent.contentOffset.y)}
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 62:
  ```text
        scrollEventThrottle={16}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 63:
  ```text
      >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 64:
  ```text
        {hasRecs && (
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 65:
  ```text
        <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 66:
  ```text
          style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 67:
  ```text
            backgroundColor: "#fff",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 68:
  ```text
            borderRadius: s(radii.xl),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 69:
  ```text
            borderWidth: 1.5,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 70:
  ```text
            borderColor: colors.border,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 71:
  ```text
            padding: s(spacing.lg),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 72:
  ```text
            marginBottom: s(spacing.xl),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 73:
  ```text
            ...shadow.sm,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 74:
  ```text
          }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 75:
  ```text
        >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 76:
  ```text
          <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 77:
  ```text
            style={{ fontSize: fs(18), fontWeight: "700", marginBottom: s(4) }}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 78:
  ```text
            allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 79:
  ```text
          >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 80:
  ```text
            {isHistoryBased ? "Recommended for You" : "Popular Right Now"}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 81:
  ```text
          </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 82:
  ```text
          <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 83:
  ```text
            style={{ fontSize: fs(13), color: colors.mutedForeground, marginBottom: s(spacing.md) }}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 84:
  ```text
            allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 85:
  ```text
          >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 86:
  ```text
            {isHistoryBased
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 87:
  ```text
              ? "Based on your booking history. Tap a recommendation to skip straight to booking summary."
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 88:
  ```text
              : "New customer fallback. Tap a recommendation to book instantly, or continue by category below."}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 89:
  ```text
          </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 90:
  ```text
          <View style={{ gap: s(spacing.md) }}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 91:
  ```text
            {recs.slice(0, 3).map((rec, idx) => (
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 92:
  ```text
              <RecommendationCard
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 93:
  ```text
                key={`${rec?.package?.id ?? idx}-${idx}`}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 94:
  ```text
                rec={rec}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 95:
  ```text
                onPress={() => onSelectRecommendation?.(rec)}
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 96:
  ```text
                s={s}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 97:
  ```text
                fs={fs}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 98:
  ```text
              />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 99:
  ```text
            ))}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 100:
  ```text
          </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 101:
  ```text
        </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 102:
  ```text
      )}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 103:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 104:
  ```text
      <Text style={{ fontSize: fs(typography.h2.fontSize), fontWeight: '700', textAlign: 'center', marginBottom: s(spacing.xxl), marginTop: s(spacing.md) }}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 105:
  ```text
        allowFontScaling={false}>Choose a Photoshoot Category</Text>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 106:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 107:
  ```text
        {numCols === 2 ? (
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 108:
  ```text
        // Tablet: two-column grid
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 109:
  ```text
        <View style={{ flexDirection: 'row', flexWrap: 'wrap', gap: colGap }}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 110:
  ```text
          {categories.map((cat) => (
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 111:
  ```text
            <CategoryCard key={cat.id} category={cat} onPress={() => onSelectCategory(cat)} width={cardW} s={s} fs={fs} />
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 112:
  ```text
          ))}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 113:
  ```text
        </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 114:
  ```text
        ) : (
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 115:
  ```text
        // Phone: stacked list
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 116:
  ```text
        <View style={{ gap: s(spacing.lg) }}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 117:
  ```text
          {categories.map((cat) => (
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 118:
  ```text
            <CategoryCard key={cat.id} category={cat} onPress={() => onSelectCategory(cat)} s={s} fs={fs} />
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 119:
  ```text
          ))}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 120:
  ```text
        </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 121:
  ```text
        )}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 122:
  ```text
      </ScrollView>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 123:
  ```text
      {showScrollHint && (
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 124:
  ```text
        <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 125:
  ```text
          pointerEvents="none"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 126:
  ```text
          style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 127:
  ```text
            position: 'absolute',
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 128:
  ```text
            left: 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 129:
  ```text
            right: 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 130:
  ```text
            bottom: s(spacing.xl),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 131:
  ```text
            alignItems: 'center',
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 132:
  ```text
          }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 133:
  ```text
        >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 134:
  ```text
          <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 135:
  ```text
            style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 136:
  ```text
              width: s(34),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 137:
  ```text
              height: s(34),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 138:
  ```text
              borderRadius: s(17),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 139:
  ```text
              alignItems: 'center',
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 140:
  ```text
              justifyContent: 'center',
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 141:
  ```text
              backgroundColor: 'rgba(255,255,255,0.74)',
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 142:
  ```text
              borderWidth: 1,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 143:
  ```text
              borderColor: 'rgba(0,0,0,0.08)',
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 144:
  ```text
              ...shadow.sm,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 145:
  ```text
            }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 146:
  ```text
          >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 147:
  ```text
            <Icon name="chevron-down" size={s(16)} color={colors.mutedForeground} />
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 148:
  ```text
          </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 149:
  ```text
        </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 150:
  ```text
      )}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 151:
  ```text
    </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 152:
  ```text
  );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 153:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 2. `RecommendationCard`

- Signature: `function RecommendationCard(param)`
- Source range: lines 155-218

#### Line-by-line

- Line 155:
  ```text
function RecommendationCard({ rec, onPress, s, fs }) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 156:
  ```text
  const pkg = rec?.package;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 157:
  ```text
  if (!pkg) return null;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 158:
  ```text
  const addons = Array.isArray(rec?.addons) ? rec.addons : [];
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 159:
  ```text
  const basePrice = Number(rec?.base_price ?? pkg.promo_price ?? pkg.price ?? 0);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 160:
  ```text
  const totalPrice = Number(rec?.total_price ?? basePrice);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 161:
  ```text
  const sourceLabel = SOURCE_LABELS[rec?.source] || "Recommended";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 162:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 163:
  ```text
  return (
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 164:
  ```text
    <TouchableOpacity
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 165:
  ```text
      onPress={onPress}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 166:
  ```text
      activeOpacity={0.86}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 167:
  ```text
      style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 168:
  ```text
        borderWidth: 1.5,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 169:
  ```text
        borderColor: "rgba(217,119,6,0.35)",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 170:
  ```text
        borderRadius: s(radii.lg),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 171:
  ```text
        backgroundColor: "#fffaf4",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 172:
  ```text
        padding: s(spacing.md),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 173:
  ```text
      }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 174:
  ```text
    >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 175:
  ```text
      <View style={{ flexDirection: "row", justifyContent: "space-between", alignItems: "flex-start", marginBottom: s(6) }}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 176:
  ```text
        <View style={{ flex: 1, marginRight: s(spacing.sm) }}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 177:
  ```text
          <Text style={{ fontSize: fs(15), fontWeight: "700", color: colors.foreground }} allowFontScaling={false}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 178:
  ```text
            {pkg.name}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 179:
  ```text
          </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 180:
  ```text
          <Text style={{ fontSize: fs(12), color: colors.mutedForeground, marginTop: 1 }} allowFontScaling={false}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 181:
  ```text
            {pkg.category}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 182:
  ```text
          </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 183:
  ```text
        </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 184:
  ```text
        <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 185:
  ```text
          style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 186:
  ```text
            backgroundColor: "rgba(217,119,6,0.14)",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 187:
  ```text
            borderRadius: s(999),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 188:
  ```text
            paddingHorizontal: s(10),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 189:
  ```text
            paddingVertical: s(4),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 190:
  ```text
          }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 191:
  ```text
        >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 192:
  ```text
          <Text style={{ fontSize: fs(10), color: colors.accent, fontWeight: "700" }} allowFontScaling={false}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 193:
  ```text
            {sourceLabel}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 194:
  ```text
          </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 195:
  ```text
        </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 196:
  ```text
      </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 197:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 198:
  ```text
      {addons.length > 0 && (
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 199:
  ```text
        <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 200:
  ```text
          style={{ fontSize: fs(12), color: colors.mutedForeground, marginBottom: s(8) }}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 201:
  ```text
          allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 202:
  ```text
          numberOfLines={2}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 203:
  ```text
        >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 204:
  ```text
          Add-ons: {addons.map((a) => a.name).join(", ")}
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 205:
  ```text
        </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 206:
  ```text
      )}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 207:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 208:
  ```text
      <View style={{ flexDirection: "row", justifyContent: "space-between", alignItems: "center" }}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 209:
  ```text
        <Text style={{ fontSize: fs(14), color: colors.accent, fontWeight: "700" }} allowFontScaling={false}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 210:
  ```text
          Total: ₱{totalPrice.toLocaleString()}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 211:
  ```text
        </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 212:
  ```text
        <Text style={{ fontSize: fs(12), color: colors.mutedForeground }} allowFontScaling={false}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 213:
  ```text
          Base ₱{basePrice.toLocaleString()}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 214:
  ```text
        </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 215:
  ```text
      </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 216:
  ```text
    </TouchableOpacity>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 217:
  ```text
  );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 218:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 3. `CategoryCard`

- Signature: `function CategoryCard(param)`
- Source range: lines 220-260

#### Line-by-line

- Line 220:
  ```text
function CategoryCard({ category, onPress, width, s, fs }) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 221:
  ```text
  const iconSize = s(72);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 222:
  ```text
  const imageSource = resolveCategoryImage(category);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 223:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 224:
  ```text
  return (
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 225:
  ```text
    <TouchableOpacity
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 226:
  ```text
      onPress={onPress} activeOpacity={0.8}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 227:
  ```text
      style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 228:
  ```text
        flexDirection: 'row', alignItems: 'center',
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 229:
  ```text
        backgroundColor: colors.card, borderRadius: s(radii.xxl),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 230:
  ```text
        borderWidth: 2, borderColor: colors.border,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 231:
  ```text
        padding: s(spacing.xl), gap: s(spacing.lg), ...shadow.md,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 232:
  ```text
        width: width || undefined,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 233:
  ```text
      }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 234:
  ```text
    >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 235:
  ```text
      <View style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 236:
  ```text
        width: iconSize, height: iconSize, borderRadius: s(radii.xl),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 237:
  ```text
        overflow: 'hidden', alignItems: 'center', justifyContent: 'center',
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 238:
  ```text
        backgroundColor: colors.muted, flexShrink: 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 239:
  ```text
      }}>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 240:
  ```text
        <Image
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 241:
  ```text
          source={{ uri: imageSource }}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 242:
  ```text
          style={{ width: iconSize, height: iconSize }}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 243:
  ```text
          resizeMode="cover"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 244:
  ```text
        />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 245:
  ```text
      </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 246:
  ```text
      <View style={{ flex: 1 }}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 247:
  ```text
        <Text style={{ fontSize: fs(typography.h4.fontSize), fontWeight: '600', marginBottom: s(4) }}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 248:
  ```text
          allowFontScaling={false}>{category.name}</Text>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 249:
  ```text
        <Text style={{ fontSize: fs(typography.sm.fontSize), color: colors.mutedForeground }}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 250:
  ```text
          allowFontScaling={false}>{category.description || 'Professional photography session'}</Text>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 251:
  ```text
      </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 252:
  ```text
      <View style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 253:
  ```text
        width: s(36), height: s(36), borderRadius: s(18),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 254:
  ```text
        backgroundColor: 'rgba(217,119,6,0.1)', alignItems: 'center', justifyContent: 'center', flexShrink: 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 255:
  ```text
      }}>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 256:
  ```text
        <Icon name="chevron-forward" size={s(20)} color={colors.accent} />
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 257:
  ```text
      </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 258:
  ```text
    </TouchableOpacity>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 259:
  ```text
  );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 260:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

## File: `HeigenKiosk/src/screens/ConfirmationScreen.js`

- Methods documented: 1

### 1. `ConfirmationScreen`

- Signature: `function ConfirmationScreen(param)`
- Source range: lines 7-77

#### Line-by-line

- Line 7:
  ```text
export default function ConfirmationScreen({ customerInfo, onReset }) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 8:
  ```text
  const { s, fs, isTablet } = useScale();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 9:
  ```text
  const scaleAnim   = useRef(new Animated.Value(0)).current;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 10:
  ```text
  const opacityAnim = useRef(new Animated.Value(0)).current;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 11:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 12:
  ```text
  useEffect(() => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 13:
  ```text
    Animated.parallel([
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 14:
  ```text
      Animated.spring(scaleAnim,   { toValue: 1, tension: 80, friction: 8, useNativeDriver: true }),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 15:
  ```text
      Animated.timing(opacityAnim, { toValue: 1, duration: 400, useNativeDriver: true }),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 16:
  ```text
    ]).start();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 17:
  ```text
    const t = setTimeout(onReset, 4000);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 18:
  ```text
    return () => clearTimeout(t);
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 19:
  ```text
  }, []);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 20:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 21:
  ```text
  const cardMaxW = isTablet ? s(500) : s(440);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 22:
  ```text
  const iconSize = isTablet ? s(112) : s(120);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 23:
  ```text
  const cardPadding = isTablet ? s(spacing.xxl) : s(spacing.xxxl);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 24:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 25:
  ```text
  return (
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 26:
  ```text
    <Animated.View style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 27:
  ```text
      flex: 1, backgroundColor: colors.background,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 28:
  ```text
      alignItems: 'center', justifyContent: 'center',
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 29:
  ```text
      padding: s(spacing.xl), opacity: opacityAnim,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 30:
  ```text
    }}>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 31:
  ```text
      <Animated.View style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 32:
  ```text
        backgroundColor: '#fff', borderRadius: s(radii.xxl),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 33:
  ```text
        padding: cardPadding, alignItems: 'center',
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 34:
  ```text
        maxWidth: cardMaxW, width: '100%', ...shadow.lg,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 35:
  ```text
        transform: [{ scale: scaleAnim }],
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 36:
  ```text
      }}>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 37:
  ```text
        <View style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 38:
  ```text
          width: iconSize, height: iconSize, borderRadius: iconSize / 2,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 39:
  ```text
          backgroundColor: 'rgba(217,119,6,0.1)',
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 40:
  ```text
          alignItems: 'center', justifyContent: 'center',
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 41:
  ```text
          marginBottom: isTablet ? s(spacing.xl) : s(spacing.xxl),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 42:
  ```text
        }}>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 43:
  ```text
          <Text style={{ fontSize: s(64), color: colors.accent, lineHeight: s(80) }}>✓</Text>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 44:
  ```text
        </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 45:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 46:
  ```text
        <Text style={{ fontSize: fs(isTablet ? 21 : 24), fontWeight: '700', textAlign: 'center', marginBottom: s(spacing.lg) }}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 47:
  ```text
          allowFontScaling={false}>Booking Submitted!</Text>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 48:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 49:
  ```text
        <Text style={{ fontSize: fs(16), textAlign: 'center', color: colors.mutedForeground, marginBottom: s(spacing.xl), lineHeight: fs(24) }}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 50:
  ```text
          allowFontScaling={false}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 51:
  ```text
          Your booking is{' '}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 52:
  ```text
          <Text style={{ fontWeight: '700', color: colors.accent }}>pending confirmation</Text>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 53:
  ```text
          {' '}by our staff.
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 54:
  ```text
        </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 55:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 56:
  ```text
        {customerInfo?.email && (
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 57:
  ```text
          <Text style={{ fontSize: fs(14), textAlign: 'center', color: colors.mutedForeground, marginBottom: s(spacing.xl), lineHeight: fs(22) }}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 58:
  ```text
            allowFontScaling={false}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 59:
  ```text
            Confirmation will be sent to{'\n'}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 60:
  ```text
            <Text style={{ fontWeight: '700', color: colors.foreground }}>{customerInfo.email}</Text>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 61:
  ```text
          </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 62:
  ```text
        )}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 63:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 64:
  ```text
        <View style={{ width: '100%', height: 1, backgroundColor: colors.border, marginVertical: s(spacing.xl) }} />
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 65:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 66:
  ```text
        <View style={{ flexDirection: 'row', alignItems: 'flex-start', gap: s(spacing.md), marginBottom: s(spacing.xxl) }}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 67:
  ```text
          <Text style={{ fontSize: s(18), color: colors.mutedForeground, lineHeight: s(22) }}>◷</Text>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 68:
  ```text
          <Text style={{ fontSize: fs(14), color: colors.mutedForeground, flex: 1, lineHeight: fs(20) }}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 69:
  ```text
            allowFontScaling={false}>Please wait for a staff member to confirm your session.</Text>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 70:
  ```text
        </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 71:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 72:
  ```text
        <Text style={{ fontSize: fs(12), color: colors.mutedForeground, fontStyle: 'italic' }}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 73:
  ```text
          allowFontScaling={false}>Returning to home screen...</Text>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 74:
  ```text
      </Animated.View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 75:
  ```text
    </Animated.View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 76:
  ```text
  );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 77:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

## File: `HeigenKiosk/src/screens/CustomerFormModal.js`

- Methods documented: 7

### 1. `CustomerFormModal`

- Signature: `function CustomerFormModal(param)`
- Source range: lines 19-607

#### Line-by-line

- Line 19:
  ```text
export default function CustomerFormModal({
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 20:
  ```text
    visible,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 21:
  ```text
    onClose,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 22:
  ```text
    onSubmit,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 23:
  ```text
    loading,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 24:
  ```text
    requireSubmit = false,
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 25:
  ```text
    resetToken = 0,
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 26:
  ```text
}) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 27:
  ```text
    const { s, fs, isTablet, W, H } = useScale();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 28:
  ```text
    const [form, setForm] = useState({
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 29:
  ```text
        fullName: "",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 30:
  ```text
        email: "",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 31:
  ```text
        contactNumber: "",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 32:
  ```text
        preferredDate: "",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 33:
  ```text
        consentGiven: false,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 34:
  ```text
    });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 35:
  ```text
    const [errors, setErrors] = useState({});
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 36:
  ```text
    const [focused, setFocused] = useState("");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 37:
  ```text
    const canClose = !requireSubmit && !loading;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 38:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 39:
  ```text
    useEffect(() => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 40:
  ```text
        setForm({
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 41:
  ```text
            fullName: "",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 42:
  ```text
            email: "",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 43:
  ```text
            contactNumber: "",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 44:
  ```text
            preferredDate: "",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 45:
  ```text
            consentGiven: false,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 46:
  ```text
        });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 47:
  ```text
        setErrors({});
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 48:
  ```text
        setFocused("");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 49:
  ```text
    }, [resetToken]);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 50:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 51:
  ```text
    const set = (field) => (value) => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 52:
  ```text
        setForm((prev) => ({ ...prev, [field]: value }));
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 53:
  ```text
        if (errors[field]) setErrors((prev) => ({ ...prev, [field]: null }));
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 54:
  ```text
    };
  ```
  Explanation: Closes the current block/scope.
- Line 55:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 56:
  ```text
    function validate() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 57:
  ```text
        const e = {};
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 58:
  ```text
        if (!form.fullName.trim()) e.fullName = "Required";
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 59:
  ```text
        if (!form.email.trim()) e.email = "Required";
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 60:
  ```text
        else if (!/\S+@\S+\.\S+/.test(form.email)) e.email = "Invalid email";
  ```
  Explanation: Defines the fallback path when previous conditions are not satisfied.
- Line 61:
  ```text
        if (!form.contactNumber.trim()) e.contactNumber = "Required";
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 62:
  ```text
        setErrors(e);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 63:
  ```text
        return Object.keys(e).length === 0;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 64:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 65:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 66:
  ```text
    function handleSubmit() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 67:
  ```text
        if (!validate()) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 68:
  ```text
        onSubmit({
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 69:
  ```text
            fullName: form.fullName.trim(),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 70:
  ```text
            email: form.email.trim().toLowerCase(),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 71:
  ```text
            contactNumber: form.contactNumber.trim(),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 72:
  ```text
            preferredDate: form.preferredDate.trim() || null,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 73:
  ```text
            consentGiven: form.consentGiven,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 74:
  ```text
        });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 75:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 76:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 77:
  ```text
    const isValid = form.fullName && form.email && form.contactNumber;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 78:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 79:
  ```text
    // ── TABLET: centered fixed dialog, no scroll ─────────────────────────────────
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 80:
  ```text
    // All sizes are raw dp values (not scaled) so they don't balloon on large screens
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 81:
  ```text
    if (isTablet) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 82:
  ```text
        const dialogW = Math.min(600, W * 0.75);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 83:
  ```text
        const inp = (field) => ({
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 84:
  ```text
            borderWidth: 1.5,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 85:
  ```text
            borderColor: focused === field ? colors.accent : colors.border,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 86:
  ```text
            borderRadius: 8,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 87:
  ```text
            paddingHorizontal: 12,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 88:
  ```text
            paddingVertical: 9,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 89:
  ```text
            fontSize: 14,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 90:
  ```text
            color: colors.foreground,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 91:
  ```text
            backgroundColor: "#fff",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 92:
  ```text
        });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 93:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 94:
  ```text
        return (
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 95:
  ```text
            <Modal
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 96:
  ```text
                visible={visible}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 97:
  ```text
                transparent
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 98:
  ```text
                animationType="fade"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 99:
  ```text
                onRequestClose={canClose ? onClose : undefined}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 100:
  ```text
            >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 101:
  ```text
                <Pressable
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 102:
  ```text
                    style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 103:
  ```text
                        flex: 1,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 104:
  ```text
                        backgroundColor: "rgba(0,0,0,0.55)",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 105:
  ```text
                        justifyContent: "center",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 106:
  ```text
                        alignItems: "center",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 107:
  ```text
                    }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 108:
  ```text
                    onPress={canClose ? onClose : undefined}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 109:
  ```text
                >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 110:
  ```text
                    <Pressable onPress={(e) => e.stopPropagation()}>
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 111:
  ```text
                        <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 112:
  ```text
                            style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 113:
  ```text
                                width: dialogW,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 114:
  ```text
                                backgroundColor: "#fff",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 115:
  ```text
                                borderRadius: 16,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 116:
  ```text
                                overflow: "hidden",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 117:
  ```text
                                ...shadow.lg,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 118:
  ```text
                            }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 119:
  ```text
                        >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 120:
  ```text
                            {/* ── Header ── */}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 121:
  ```text
                            <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 122:
  ```text
                                style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 123:
  ```text
                                    flexDirection: "row",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 124:
  ```text
                                    alignItems: "center",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 125:
  ```text
                                    justifyContent: "space-between",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 126:
  ```text
                                    paddingHorizontal: 20,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 127:
  ```text
                                    paddingVertical: 14,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 128:
  ```text
                                    borderBottomWidth: 1,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 129:
  ```text
                                    borderBottomColor: colors.border,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 130:
  ```text
                                }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 131:
  ```text
                            >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 132:
  ```text
                                <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 133:
  ```text
                                    style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 134:
  ```text
                                        fontSize: 15,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 135:
  ```text
                                        fontWeight: "700",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 136:
  ```text
                                        color: colors.foreground,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 137:
  ```text
                                    }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 138:
  ```text
                                    allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 139:
  ```text
                                >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 140:
  ```text
                                    Customer Information
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 141:
  ```text
                                </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 142:
  ```text
                                {canClose ? (
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 143:
  ```text
                                    <TouchableOpacity
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 144:
  ```text
                                        onPress={onClose}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 145:
  ```text
                                        style={{ padding: 6 }}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 146:
  ```text
                                    >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 147:
  ```text
                                        <Icon
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 148:
  ```text
                                            name="close"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 149:
  ```text
                                            size={18}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 150:
  ```text
                                            color={colors.mutedForeground}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 151:
  ```text
                                        />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 152:
  ```text
                                    </TouchableOpacity>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 153:
  ```text
                                ) : null}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 154:
  ```text
                            </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 155:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 156:
  ```text
                            {/* ── Body ── */}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 157:
  ```text
                            <View style={{ padding: 20, gap: 12 }}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 158:
  ```text
                                {/* Row 1: Full Name + Email */}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 159:
  ```text
                                <View style={{ flexDirection: "row", gap: 12 }}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 160:
  ```text
                                    <View style={{ flex: 1 }}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 161:
  ```text
                                        <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 162:
  ```text
                                            style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 163:
  ```text
                                                fontSize: 11,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 164:
  ```text
                                                fontWeight: "600",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 165:
  ```text
                                                color: colors.mutedForeground,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 166:
  ```text
                                                marginBottom: 5,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 167:
  ```text
                                                textTransform: "uppercase",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 168:
  ```text
                                                letterSpacing: 0.4,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 169:
  ```text
                                            }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 170:
  ```text
                                            allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 171:
  ```text
                                        >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 172:
  ```text
                                            Full Name{" "}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 173:
  ```text
                                            <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 174:
  ```text
                                                style={{ color: colors.error }}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 175:
  ```text
                                            >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 176:
  ```text
                                                *
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 177:
  ```text
                                            </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 178:
  ```text
                                        </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 179:
  ```text
                                        <TextInput
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 180:
  ```text
                                            style={inp("name")}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 181:
  ```text
                                            value={form.fullName}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 182:
  ```text
                                            onChangeText={set("fullName")}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 183:
  ```text
                                            placeholder="Enter your full name"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 184:
  ```text
                                            autoCapitalize="words"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 185:
  ```text
                                            placeholderTextColor={
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 186:
  ```text
                                                colors.mutedForeground
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 187:
  ```text
                                            }
  ```
  Explanation: Closes the current block/scope.
- Line 188:
  ```text
                                            allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 189:
  ```text
                                            onFocus={() => setFocused("name")}
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 190:
  ```text
                                            onBlur={() => setFocused("")}
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 191:
  ```text
                                        />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 192:
  ```text
                                        {errors.fullName ? (
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 193:
  ```text
                                            <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 194:
  ```text
                                                style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 195:
  ```text
                                                    fontSize: 10,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 196:
  ```text
                                                    color: colors.error,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 197:
  ```text
                                                    marginTop: 3,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 198:
  ```text
                                                }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 199:
  ```text
                                            >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 200:
  ```text
                                                {errors.fullName}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 201:
  ```text
                                            </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 202:
  ```text
                                        ) : null}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 203:
  ```text
                                    </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 204:
  ```text
                                    <View style={{ flex: 1 }}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 205:
  ```text
                                        <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 206:
  ```text
                                            style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 207:
  ```text
                                                fontSize: 11,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 208:
  ```text
                                                fontWeight: "600",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 209:
  ```text
                                                color: colors.mutedForeground,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 210:
  ```text
                                                marginBottom: 5,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 211:
  ```text
                                                textTransform: "uppercase",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 212:
  ```text
                                                letterSpacing: 0.4,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 213:
  ```text
                                            }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 214:
  ```text
                                            allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 215:
  ```text
                                        >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 216:
  ```text
                                            Email{" "}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 217:
  ```text
                                            <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 218:
  ```text
                                                style={{ color: colors.error }}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 219:
  ```text
                                            >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 220:
  ```text
                                                *
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 221:
  ```text
                                            </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 222:
  ```text
                                        </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 223:
  ```text
                                        <TextInput
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 224:
  ```text
                                            style={inp("email")}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 225:
  ```text
                                            value={form.email}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 226:
  ```text
                                            onChangeText={set("email")}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 227:
  ```text
                                            placeholder="example@email.com"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 228:
  ```text
                                            keyboardType="email-address"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 229:
  ```text
                                            autoCapitalize="none"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 230:
  ```text
                                            placeholderTextColor={
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 231:
  ```text
                                                colors.mutedForeground
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 232:
  ```text
                                            }
  ```
  Explanation: Closes the current block/scope.
- Line 233:
  ```text
                                            allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 234:
  ```text
                                            onFocus={() => setFocused("email")}
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 235:
  ```text
                                            onBlur={() => setFocused("")}
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 236:
  ```text
                                        />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 237:
  ```text
                                        {errors.email ? (
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 238:
  ```text
                                            <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 239:
  ```text
                                                style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 240:
  ```text
                                                    fontSize: 10,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 241:
  ```text
                                                    color: colors.error,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 242:
  ```text
                                                    marginTop: 3,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 243:
  ```text
                                                }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 244:
  ```text
                                            >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 245:
  ```text
                                                {errors.email}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 246:
  ```text
                                            </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 247:
  ```text
                                        ) : null}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 248:
  ```text
                                    </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 249:
  ```text
                                </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 250:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 251:
  ```text
                                {/* Row 2: Contact + Preferred Date */}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 252:
  ```text
                                <View style={{ flexDirection: "row", gap: 12 }}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 253:
  ```text
                                    <View style={{ flex: 1 }}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 254:
  ```text
                                        <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 255:
  ```text
                                            style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 256:
  ```text
                                                fontSize: 11,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 257:
  ```text
                                                fontWeight: "600",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 258:
  ```text
                                                color: colors.mutedForeground,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 259:
  ```text
                                                marginBottom: 5,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 260:
  ```text
                                                textTransform: "uppercase",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 261:
  ```text
                                                letterSpacing: 0.4,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 262:
  ```text
                                            }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 263:
  ```text
                                            allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 264:
  ```text
                                        >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 265:
  ```text
                                            Contact Number{" "}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 266:
  ```text
                                            <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 267:
  ```text
                                                style={{ color: colors.error }}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 268:
  ```text
                                            >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 269:
  ```text
                                                *
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 270:
  ```text
                                            </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 271:
  ```text
                                        </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 272:
  ```text
                                        <TextInput
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 273:
  ```text
                                            style={inp("contact")}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 274:
  ```text
                                            value={form.contactNumber}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 275:
  ```text
                                            onChangeText={set("contactNumber")}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 276:
  ```text
                                            placeholder="+63 9XX XXX XXXX"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 277:
  ```text
                                            keyboardType="phone-pad"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 278:
  ```text
                                            placeholderTextColor={
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 279:
  ```text
                                                colors.mutedForeground
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 280:
  ```text
                                            }
  ```
  Explanation: Closes the current block/scope.
- Line 281:
  ```text
                                            allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 282:
  ```text
                                            onFocus={() =>
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 283:
  ```text
                                                setFocused("contact")
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 284:
  ```text
                                            }
  ```
  Explanation: Closes the current block/scope.
- Line 285:
  ```text
                                            onBlur={() => setFocused("")}
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 286:
  ```text
                                        />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 287:
  ```text
                                        {errors.contactNumber ? (
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 288:
  ```text
                                            <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 289:
  ```text
                                                style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 290:
  ```text
                                                    fontSize: 10,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 291:
  ```text
                                                    color: colors.error,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 292:
  ```text
                                                    marginTop: 3,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 293:
  ```text
                                                }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 294:
  ```text
                                            >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 295:
  ```text
                                                {errors.contactNumber}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 296:
  ```text
                                            </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 297:
  ```text
                                        ) : null}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 298:
  ```text
                                    </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 299:
  ```text
                                    <View style={{ flex: 1 }}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 300:
  ```text
                                        <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 301:
  ```text
                                            style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 302:
  ```text
                                                fontSize: 11,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 303:
  ```text
                                                fontWeight: "600",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 304:
  ```text
                                                color: colors.mutedForeground,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 305:
  ```text
                                                marginBottom: 5,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 306:
  ```text
                                                textTransform: "uppercase",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 307:
  ```text
                                                letterSpacing: 0.4,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 308:
  ```text
                                            }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 309:
  ```text
                                            allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 310:
  ```text
                                        >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 311:
  ```text
                                            Preferred Date (optional)
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 312:
  ```text
                                        </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 313:
  ```text
                                        <TextInput
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 314:
  ```text
                                            style={inp("date")}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 315:
  ```text
                                            value={form.preferredDate}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 316:
  ```text
                                            onChangeText={set("preferredDate")}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 317:
  ```text
                                            placeholder="YYYY-MM-DD"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 318:
  ```text
                                            placeholderTextColor={
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 319:
  ```text
                                                colors.mutedForeground
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 320:
  ```text
                                            }
  ```
  Explanation: Closes the current block/scope.
- Line 321:
  ```text
                                            allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 322:
  ```text
                                            onFocus={() => setFocused("date")}
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 323:
  ```text
                                            onBlur={() => setFocused("")}
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 324:
  ```text
                                        />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 325:
  ```text
                                    </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 326:
  ```text
                                </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 327:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 328:
  ```text
                                {/* Consent */}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 329:
  ```text
                                <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 330:
  ```text
                                    style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 331:
  ```text
                                        flexDirection: "row",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 332:
  ```text
                                        alignItems: "flex-start",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 333:
  ```text
                                        gap: 12,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 334:
  ```text
                                        borderWidth: 1.5,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 335:
  ```text
                                        borderColor: colors.border,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 336:
  ```text
                                        borderRadius: 10,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 337:
  ```text
                                        padding: 12,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 338:
  ```text
                                        backgroundColor:
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 339:
  ```text
                                            "rgba(236,236,240,0.25)",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 340:
  ```text
                                    }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 341:
  ```text
                                >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 342:
  ```text
                                    <Switch
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 343:
  ```text
                                        value={form.consentGiven}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 344:
  ```text
                                        onValueChange={set("consentGiven")}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 345:
  ```text
                                        trackColor={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 346:
  ```text
                                            false: colors.muted,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 347:
  ```text
                                            true: colors.accent,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 348:
  ```text
                                        }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 349:
  ```text
                                        thumbColor="#fff"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 350:
  ```text
                                    />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 351:
  ```text
                                    <View style={{ flex: 1 }}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 352:
  ```text
                                        <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 353:
  ```text
                                            style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 354:
  ```text
                                                fontSize: 12,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 355:
  ```text
                                                color: colors.mutedForeground,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 356:
  ```text
                                                lineHeight: 17,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 357:
  ```text
                                            }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 358:
  ```text
                                            allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 359:
  ```text
                                        >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 360:
  ```text
                                            I hereby consent to Heigen Studio
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 361:
  ```text
                                            releasing my photos on public and
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 362:
  ```text
                                            social media platforms.
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 363:
  ```text
                                        </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 364:
  ```text
                                    </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 365:
  ```text
                                </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 366:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 367:
  ```text
                                {/* Actions */}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 368:
  ```text
                                <View style={{ flexDirection: "row", gap: 10 }}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 369:
  ```text
                                    {canClose ? (
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 370:
  ```text
                                        <Button
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 371:
  ```text
                                            label="Cancel"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 372:
  ```text
                                            variant="secondary"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 373:
  ```text
                                            onPress={onClose}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 374:
  ```text
                                            size="sm"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 375:
  ```text
                                            style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 376:
  ```text
                                                flex: 1,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 377:
  ```text
                                                height: 40,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 378:
  ```text
                                                minHeight: 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 379:
  ```text
                                            }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 380:
  ```text
                                            disabled={loading}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 381:
  ```text
                                        />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 382:
  ```text
                                    ) : null}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 383:
  ```text
                                    <Button
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 384:
  ```text
                                        label="Continue"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 385:
  ```text
                                        onPress={handleSubmit}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 386:
  ```text
                                        disabled={!isValid || loading}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 387:
  ```text
                                        size="sm"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 388:
  ```text
                                        loading={loading}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 389:
  ```text
                                        style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 390:
  ```text
                                            flex: canClose ? 1 : 2,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 391:
  ```text
                                            height: 40,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 392:
  ```text
                                            minHeight: 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 393:
  ```text
                                        }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 394:
  ```text
                                    />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 395:
  ```text
                                </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 396:
  ```text
                            </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 397:
  ```text
                        </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 398:
  ```text
                    </Pressable>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 399:
  ```text
                </Pressable>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 400:
  ```text
            </Modal>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 401:
  ```text
        );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 402:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 403:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 404:
  ```text
    // ── PHONE: bottom sheet with scroll ──────────────────────────────────────────
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 405:
  ```text
    return (
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 406:
  ```text
        <Modal
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 407:
  ```text
            visible={visible}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 408:
  ```text
            transparent
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 409:
  ```text
            animationType="slide"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 410:
  ```text
            onRequestClose={canClose ? onClose : undefined}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 411:
  ```text
        >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 412:
  ```text
            <Pressable
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 413:
  ```text
                style={{ flex: 1, backgroundColor: "rgba(0,0,0,0.5)" }}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 414:
  ```text
                onPress={canClose ? onClose : undefined}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 415:
  ```text
            />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 416:
  ```text
            <KeyboardAvoidingView
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 417:
  ```text
                behavior={Platform.OS === "ios" ? "padding" : "height"}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 418:
  ```text
                style={{ position: "absolute", bottom: 0, left: 0, right: 0 }}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 419:
  ```text
            >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 420:
  ```text
                <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 421:
  ```text
                    style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 422:
  ```text
                        backgroundColor: "#fff",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 423:
  ```text
                        borderTopLeftRadius: s(radii.xxl),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 424:
  ```text
                        borderTopRightRadius: s(radii.xxl),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 425:
  ```text
                        maxHeight: "92%",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 426:
  ```text
                        ...shadow.lg,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 427:
  ```text
                    }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 428:
  ```text
                >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 429:
  ```text
                    <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 430:
  ```text
                        style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 431:
  ```text
                            width: s(40),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 432:
  ```text
                            height: s(4),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 433:
  ```text
                            borderRadius: s(2),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 434:
  ```text
                            backgroundColor: colors.border,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 435:
  ```text
                            alignSelf: "center",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 436:
  ```text
                            marginTop: s(spacing.md),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 437:
  ```text
                        }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 438:
  ```text
                    />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 439:
  ```text
                    <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 440:
  ```text
                        style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 441:
  ```text
                            flexDirection: "row",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 442:
  ```text
                            alignItems: "center",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 443:
  ```text
                            justifyContent: "space-between",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 444:
  ```text
                            paddingHorizontal: s(spacing.xxl),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 445:
  ```text
                            paddingVertical: s(spacing.lg),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 446:
  ```text
                            borderBottomWidth: 1,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 447:
  ```text
                            borderBottomColor: colors.border,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 448:
  ```text
                        }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 449:
  ```text
                    >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 450:
  ```text
                        <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 451:
  ```text
                            style={{ fontSize: fs(18), fontWeight: "700" }}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 452:
  ```text
                            allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 453:
  ```text
                        >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 454:
  ```text
                            Customer Information
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 455:
  ```text
                        </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 456:
  ```text
                        {canClose ? (
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 457:
  ```text
                            <TouchableOpacity
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 458:
  ```text
                                onPress={onClose}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 459:
  ```text
                                style={{ padding: s(spacing.sm) }}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 460:
  ```text
                            >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 461:
  ```text
                                <Icon
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 462:
  ```text
                                    name="close"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 463:
  ```text
                                    size={s(22)}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 464:
  ```text
                                    color={colors.mutedForeground}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 465:
  ```text
                                />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 466:
  ```text
                            </TouchableOpacity>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 467:
  ```text
                        ) : null}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 468:
  ```text
                    </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 469:
  ```text
                    <ScrollView
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 470:
  ```text
                        contentContainerStyle={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 471:
  ```text
                            padding: s(spacing.xxl),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 472:
  ```text
                            paddingBottom: s(spacing.xxxl),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 473:
  ```text
                        }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 474:
  ```text
                        showsVerticalScrollIndicator={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 475:
  ```text
                        keyboardShouldPersistTaps="handled"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 476:
  ```text
                    >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 477:
  ```text
                        <PhoneField
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 478:
  ```text
                            label="Full Name"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 479:
  ```text
                            required
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 480:
  ```text
                            error={errors.fullName}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 481:
  ```text
                            s={s}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 482:
  ```text
                            fs={fs}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 483:
  ```text
                        >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 484:
  ```text
                            <PhoneInput
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 485:
  ```text
                                value={form.fullName}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 486:
  ```text
                                onChangeText={set("fullName")}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 487:
  ```text
                                placeholder="Enter your full name"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 488:
  ```text
                                autoCapitalize="words"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 489:
  ```text
                                s={s}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 490:
  ```text
                                fs={fs}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 491:
  ```text
                            />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 492:
  ```text
                        </PhoneField>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 493:
  ```text
                        <PhoneField
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 494:
  ```text
                            label="Email Address"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 495:
  ```text
                            required
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 496:
  ```text
                            error={errors.email}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 497:
  ```text
                            s={s}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 498:
  ```text
                            fs={fs}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 499:
  ```text
                        >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 500:
  ```text
                            <PhoneInput
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 501:
  ```text
                                value={form.email}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 502:
  ```text
                                onChangeText={set("email")}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 503:
  ```text
                                placeholder="example@email.com"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 504:
  ```text
                                keyboardType="email-address"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 505:
  ```text
                                autoCapitalize="none"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 506:
  ```text
                                s={s}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 507:
  ```text
                                fs={fs}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 508:
  ```text
                            />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 509:
  ```text
                        </PhoneField>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 510:
  ```text
                        <PhoneField
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 511:
  ```text
                            label="Contact Number"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 512:
  ```text
                            required
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 513:
  ```text
                            error={errors.contactNumber}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 514:
  ```text
                            s={s}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 515:
  ```text
                            fs={fs}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 516:
  ```text
                        >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 517:
  ```text
                            <PhoneInput
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 518:
  ```text
                                value={form.contactNumber}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 519:
  ```text
                                onChangeText={set("contactNumber")}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 520:
  ```text
                                placeholder="+63 9XX XXX XXXX"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 521:
  ```text
                                keyboardType="phone-pad"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 522:
  ```text
                                s={s}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 523:
  ```text
                                fs={fs}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 524:
  ```text
                            />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 525:
  ```text
                        </PhoneField>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 526:
  ```text
                        <PhoneField
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 527:
  ```text
                            label="Preferred Date (optional)"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 528:
  ```text
                            s={s}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 529:
  ```text
                            fs={fs}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 530:
  ```text
                        >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 531:
  ```text
                            <PhoneInput
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 532:
  ```text
                                value={form.preferredDate}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 533:
  ```text
                                onChangeText={set("preferredDate")}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 534:
  ```text
                                placeholder="YYYY-MM-DD"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 535:
  ```text
                                s={s}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 536:
  ```text
                                fs={fs}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 537:
  ```text
                            />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 538:
  ```text
                        </PhoneField>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 539:
  ```text
                        <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 540:
  ```text
                            style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 541:
  ```text
                                backgroundColor: "rgba(236,236,240,0.4)",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 542:
  ```text
                                borderWidth: 2,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 543:
  ```text
                                borderColor: colors.border,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 544:
  ```text
                                borderRadius: s(radii.xl),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 545:
  ```text
                                padding: s(spacing.xl),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 546:
  ```text
                                marginBottom: s(spacing.xl),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 547:
  ```text
                            }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 548:
  ```text
                        >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 549:
  ```text
                            <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 550:
  ```text
                                style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 551:
  ```text
                                    flexDirection: "row",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 552:
  ```text
                                    alignItems: "flex-start",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 553:
  ```text
                                    gap: s(spacing.lg),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 554:
  ```text
                                }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 555:
  ```text
                            >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 556:
  ```text
                                <Switch
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 557:
  ```text
                                    value={form.consentGiven}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 558:
  ```text
                                    onValueChange={set("consentGiven")}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 559:
  ```text
                                    trackColor={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 560:
  ```text
                                        false: colors.muted,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 561:
  ```text
                                        true: colors.accent,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 562:
  ```text
                                    }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 563:
  ```text
                                    thumbColor="#fff"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 564:
  ```text
                                />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 565:
  ```text
                                <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 566:
  ```text
                                    style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 567:
  ```text
                                        fontSize: fs(13),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 568:
  ```text
                                        color: colors.mutedForeground,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 569:
  ```text
                                        flex: 1,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 570:
  ```text
                                        lineHeight: fs(19),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 571:
  ```text
                                    }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 572:
  ```text
                                    allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 573:
  ```text
                                >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 574:
  ```text
                                    I hereby consent to Heigen Studio releasing
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 575:
  ```text
                                    my photos on public and social media
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 576:
  ```text
                                    platforms.
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 577:
  ```text
                                </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 578:
  ```text
                            </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 579:
  ```text
                        </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 580:
  ```text
                        <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 581:
  ```text
                            style={{ flexDirection: "row", gap: s(spacing.lg) }}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 582:
  ```text
                        >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 583:
  ```text
                            {canClose ? (
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 584:
  ```text
                                <Button
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 585:
  ```text
                                    label="Cancel"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 586:
  ```text
                                    variant="secondary"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 587:
  ```text
                                    onPress={onClose}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 588:
  ```text
                                    size="sm"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 589:
  ```text
                                    style={{ flex: 1 }}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 590:
  ```text
                                    disabled={loading}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 591:
  ```text
                                />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 592:
  ```text
                            ) : null}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 593:
  ```text
                            <Button
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 594:
  ```text
                                label="Continue"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 595:
  ```text
                                onPress={handleSubmit}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 596:
  ```text
                                disabled={!isValid || loading}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 597:
  ```text
                                size="sm"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 598:
  ```text
                                loading={loading}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 599:
  ```text
                                style={{ flex: canClose ? 1 : 2 }}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 600:
  ```text
                            />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 601:
  ```text
                        </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 602:
  ```text
                    </ScrollView>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 603:
  ```text
                </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 604:
  ```text
            </KeyboardAvoidingView>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 605:
  ```text
        </Modal>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 606:
  ```text
    );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 607:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 2. `set`

- Signature: `const set = (field) =>`
- Source range: lines 51-54

#### Line-by-line

- Line 51:
  ```text
    const set = (field) => (value) => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 52:
  ```text
        setForm((prev) => ({ ...prev, [field]: value }));
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 53:
  ```text
        if (errors[field]) setErrors((prev) => ({ ...prev, [field]: null }));
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 54:
  ```text
    };
  ```
  Explanation: Closes the current block/scope.

### 3. `validate`

- Signature: `function validate()`
- Source range: lines 56-64

#### Line-by-line

- Line 56:
  ```text
    function validate() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 57:
  ```text
        const e = {};
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 58:
  ```text
        if (!form.fullName.trim()) e.fullName = "Required";
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 59:
  ```text
        if (!form.email.trim()) e.email = "Required";
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 60:
  ```text
        else if (!/\S+@\S+\.\S+/.test(form.email)) e.email = "Invalid email";
  ```
  Explanation: Defines the fallback path when previous conditions are not satisfied.
- Line 61:
  ```text
        if (!form.contactNumber.trim()) e.contactNumber = "Required";
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 62:
  ```text
        setErrors(e);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 63:
  ```text
        return Object.keys(e).length === 0;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 64:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 4. `handleSubmit`

- Signature: `function handleSubmit()`
- Source range: lines 66-75

#### Line-by-line

- Line 66:
  ```text
    function handleSubmit() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 67:
  ```text
        if (!validate()) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 68:
  ```text
        onSubmit({
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 69:
  ```text
            fullName: form.fullName.trim(),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 70:
  ```text
            email: form.email.trim().toLowerCase(),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 71:
  ```text
            contactNumber: form.contactNumber.trim(),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 72:
  ```text
            preferredDate: form.preferredDate.trim() || null,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 73:
  ```text
            consentGiven: form.consentGiven,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 74:
  ```text
        });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 75:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 5. `inp`

- Signature: `const inp = (field) =>`
- Source range: lines 83-92

#### Line-by-line

- Line 83:
  ```text
        const inp = (field) => ({
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 84:
  ```text
            borderWidth: 1.5,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 85:
  ```text
            borderColor: focused === field ? colors.accent : colors.border,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 86:
  ```text
            borderRadius: 8,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 87:
  ```text
            paddingHorizontal: 12,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 88:
  ```text
            paddingVertical: 9,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 89:
  ```text
            fontSize: 14,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 90:
  ```text
            color: colors.foreground,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 91:
  ```text
            backgroundColor: "#fff",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 92:
  ```text
        });
  ```
  Explanation: Executes a core operation in this method's control flow.

### 6. `PhoneField`

- Signature: `function PhoneField(param)`
- Source range: lines 609-639

#### Line-by-line

- Line 609:
  ```text
function PhoneField({ label, required, error, children, s, fs }) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 610:
  ```text
    return (
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 611:
  ```text
        <View style={{ marginBottom: s(spacing.xl) }}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 612:
  ```text
            <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 613:
  ```text
                style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 614:
  ```text
                    fontSize: fs(14),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 615:
  ```text
                    fontWeight: "600",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 616:
  ```text
                    marginBottom: s(spacing.sm),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 617:
  ```text
                }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 618:
  ```text
                allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 619:
  ```text
            >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 620:
  ```text
                {label}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 621:
  ```text
                {required ? (
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 622:
  ```text
                    <Text style={{ color: colors.error }}> *</Text>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 623:
  ```text
                ) : null}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 624:
  ```text
            </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 625:
  ```text
            {children}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 626:
  ```text
            {error ? (
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 627:
  ```text
                <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 628:
  ```text
                    style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 629:
  ```text
                        fontSize: fs(11),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 630:
  ```text
                        color: colors.error,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 631:
  ```text
                        marginTop: s(4),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 632:
  ```text
                    }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 633:
  ```text
                >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 634:
  ```text
                    {error}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 635:
  ```text
                </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 636:
  ```text
            ) : null}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 637:
  ```text
        </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 638:
  ```text
    );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 639:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 7. `PhoneInput`

- Signature: `function PhoneInput(param)`
- Source range: lines 641-662

#### Line-by-line

- Line 641:
  ```text
function PhoneInput({ s, fs, ...rest }) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 642:
  ```text
    const [focused, setFocused] = useState(false);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 643:
  ```text
    return (
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 644:
  ```text
        <TextInput
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 645:
  ```text
            style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 646:
  ```text
                borderWidth: 2,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 647:
  ```text
                borderColor: focused ? colors.accent : colors.border,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 648:
  ```text
                borderRadius: s(radii.lg),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 649:
  ```text
                paddingHorizontal: s(spacing.xl),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 650:
  ```text
                paddingVertical: s(spacing.lg),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 651:
  ```text
                fontSize: fs(15),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 652:
  ```text
                color: colors.foreground,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 653:
  ```text
                backgroundColor: "#fff",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 654:
  ```text
            }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 655:
  ```text
            onFocus={() => setFocused(true)}
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 656:
  ```text
            onBlur={() => setFocused(false)}
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 657:
  ```text
            placeholderTextColor={colors.mutedForeground}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 658:
  ```text
            allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 659:
  ```text
            {...rest}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 660:
  ```text
        />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 661:
  ```text
    );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 662:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

## File: `HeigenKiosk/src/screens/KioskApp.js`

- Methods documented: 17

### 1. `createInitialState`

- Signature: `function createInitialState()`
- Source range: lines 31-47

#### Line-by-line

- Line 31:
  ```text
function createInitialState() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 32:
  ```text
    return {
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 33:
  ```text
        step: 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 34:
  ```text
        selectedCategory: null,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 35:
  ```text
        selectedPackage: null,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 36:
  ```text
        selectedAddons: [],
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 37:
  ```text
        customerInfo: null,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 38:
  ```text
        showCustomerForm: true,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 39:
  ```text
        showSummary: false,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 40:
  ```text
        showExitPage: false,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 41:
  ```text
        requestSummaryAfterCustomerForm: false,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 42:
  ```text
        loadingCustomerCheck: false,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 43:
  ```text
        recommendationData: null,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 44:
  ```text
        formResetToken: 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 45:
  ```text
        submitting: false,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 46:
  ```text
    };
  ```
  Explanation: Closes the current block/scope.
- Line 47:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 2. `KioskApp`

- Signature: `function KioskApp()`
- Source range: lines 49-453

#### Line-by-line

- Line 49:
  ```text
export default function KioskApp() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 50:
  ```text
    const [state, setState] = useState(createInitialState());
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 51:
  ```text
    const { s, fs, isTablet } = useScale();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 52:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 53:
  ```text
    const update = useCallback(
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 54:
  ```text
        (patch) => setState((prev) => ({ ...prev, ...patch })),
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 55:
  ```text
        [],
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 56:
  ```text
    );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 57:
  ```text
    function reset() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 58:
  ```text
        setState(createInitialState());
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 59:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 60:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 61:
  ```text
    function openExitPage() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 62:
  ```text
        update({ showExitPage: true });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 63:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 64:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 65:
  ```text
    function closeExitPage() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 66:
  ```text
        update({ showExitPage: false });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 67:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 68:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 69:
  ```text
    function confirmExitSession() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 70:
  ```text
        setState({
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 71:
  ```text
            ...createInitialState(),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 72:
  ```text
            formResetToken: state.formResetToken + 1,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 73:
  ```text
        });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 74:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 75:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 76:
  ```text
    function handleSelectCategory(cat) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 77:
  ```text
        update({ selectedCategory: cat, step: 1 });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 78:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 79:
  ```text
    function handleSelectPackage(pkg) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 80:
  ```text
        update({ selectedPackage: pkg, selectedAddons: [], step: 2 });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 81:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 82:
  ```text
    function handleToggleAddon(addon) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 83:
  ```text
        setState((prev) => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 84:
  ```text
            const already = prev.selectedAddons.some((a) => a.id === addon.id);
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 85:
  ```text
            return {
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 86:
  ```text
                ...prev,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 87:
  ```text
                selectedAddons: already
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 88:
  ```text
                    ? prev.selectedAddons.filter((a) => a.id !== addon.id)
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 89:
  ```text
                    : [...prev.selectedAddons, addon],
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 90:
  ```text
            };
  ```
  Explanation: Closes the current block/scope.
- Line 91:
  ```text
        });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 92:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 93:
  ```text
    function handleBackToCategory() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 94:
  ```text
        update({
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 95:
  ```text
            step: 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 96:
  ```text
            selectedCategory: null,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 97:
  ```text
            selectedPackage: null,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 98:
  ```text
            selectedAddons: [],
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 99:
  ```text
        });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 100:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 101:
  ```text
    function handleBackToPackages() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 102:
  ```text
        update({ step: 1, selectedAddons: [] });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 103:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 104:
  ```text
    function handleProceedToBookNow() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 105:
  ```text
        if (state.customerInfo) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 106:
  ```text
            update({ showSummary: true });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 107:
  ```text
            return;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 108:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 109:
  ```text
        update({
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 110:
  ```text
            showCustomerForm: true,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 111:
  ```text
            requestSummaryAfterCustomerForm: true,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 112:
  ```text
        });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 113:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 114:
  ```text
    async function handleCustomerFormSubmit(info) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 115:
  ```text
        update({
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 116:
  ```text
            loadingCustomerCheck: true,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 117:
  ```text
            customerInfo: info,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 118:
  ```text
        });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 119:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 120:
  ```text
        let recommendationData = null;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 121:
  ```text
        try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 122:
  ```text
            const existingCustomer = await findCustomerByEmail(info.email);
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 123:
  ```text
            if (existingCustomer) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 124:
  ```text
                const customerId =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 125:
  ```text
                    existingCustomer.id || existingCustomer.customer_id;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 126:
  ```text
                recommendationData = await fetchRecommendations(
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 127:
  ```text
                    customerId,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 128:
  ```text
                    info.preferredDate || null,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 129:
  ```text
                    3,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 130:
  ```text
                );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 131:
  ```text
            } else {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 132:
  ```text
                recommendationData = await fetchPopularRecommendations(3);
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 133:
  ```text
            }
  ```
  Explanation: Closes the current block/scope.
- Line 134:
  ```text
        } catch (_) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 135:
  ```text
            try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 136:
  ```text
                recommendationData = await fetchPopularRecommendations(3);
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 137:
  ```text
            } catch (__) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 138:
  ```text
                recommendationData = { recommendations: [] };
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 139:
  ```text
            }
  ```
  Explanation: Closes the current block/scope.
- Line 140:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 141:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 142:
  ```text
        update({
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 143:
  ```text
            recommendationData,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 144:
  ```text
            showCustomerForm: false,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 145:
  ```text
            showSummary: state.requestSummaryAfterCustomerForm,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 146:
  ```text
            requestSummaryAfterCustomerForm: false,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 147:
  ```text
            loadingCustomerCheck: false,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 148:
  ```text
        });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 149:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 150:
  ```text
    function handleEditSelection() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 151:
  ```text
        update({ showSummary: false });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 152:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 153:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 154:
  ```text
    function handleQuickBookRecommendation(rec) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 155:
  ```text
        const pkg = rec?.package;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 156:
  ```text
        if (!pkg?.id) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 157:
  ```text
        update({
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 158:
  ```text
            selectedCategory: {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 159:
  ```text
                id: pkg.category || pkg.id,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 160:
  ```text
                name: pkg.category || "Recommended",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 161:
  ```text
            },
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 162:
  ```text
            selectedPackage: pkg,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 163:
  ```text
            selectedAddons: Array.isArray(rec.addons) ? rec.addons : [],
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 164:
  ```text
            showSummary: true,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 165:
  ```text
        });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 166:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 167:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 168:
  ```text
    async function handleConfirmBooking() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 169:
  ```text
        update({ submitting: true });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 170:
  ```text
        try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 171:
  ```text
            await submitBooking({
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 172:
  ```text
                customer: {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 173:
  ```text
                    full_name: state.customerInfo.fullName,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 174:
  ```text
                    email: state.customerInfo.email,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 175:
  ```text
                    contact_number: state.customerInfo.contactNumber,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 176:
  ```text
                    consent_given: state.customerInfo.consentGiven ?? true,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 177:
  ```text
                },
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 178:
  ```text
                category_id:
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 179:
  ```text
                    state.selectedCategory?.id ?? state.selectedCategory?.name,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 180:
  ```text
                package_id: state.selectedPackage?.id,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 181:
  ```text
                addon_ids: state.selectedAddons.map((a) => a.id),
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 182:
  ```text
                preferred_date: state.customerInfo.preferredDate,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 183:
  ```text
                total_amount: calcTotal(),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 184:
  ```text
            });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 185:
  ```text
            update({
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 186:
  ```text
                submitting: false,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 187:
  ```text
                showSummary: false,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 188:
  ```text
                step: 3,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 189:
  ```text
                formResetToken: state.formResetToken + 1,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 190:
  ```text
            });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 191:
  ```text
        } catch (err) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 192:
  ```text
            update({ submitting: false });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 193:
  ```text
            Alert.alert(
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 194:
  ```text
                "Booking Failed",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 195:
  ```text
                err.message || "Could not submit booking.",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 196:
  ```text
                [{ text: "OK" }],
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 197:
  ```text
            );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 198:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 199:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 200:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 201:
  ```text
    function calcTotal() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 202:
  ```text
        const base = Number(
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 203:
  ```text
            state.selectedPackage?.promo_price
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 204:
  ```text
                ? state.selectedPackage.promo_price
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 205:
  ```text
                : state.selectedPackage?.price
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 206:
  ```text
                  ? state.selectedPackage.price
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 207:
  ```text
                  : 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 208:
  ```text
        );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 209:
  ```text
        return (
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 210:
  ```text
            base +
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 211:
  ```text
            state.selectedAddons.reduce((sum, a) => sum + Number(a.price), 0)
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 212:
  ```text
        );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 213:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 214:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 215:
  ```text
    return (
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 216:
  ```text
        <SafeAreaView style={{ flex: 1, backgroundColor: "#fff" }}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 217:
  ```text
            <StatusBar style="dark" />
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 218:
  ```text
            {state.step < 3 &&
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 219:
  ```text
                (isTablet ? (
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 220:
  ```text
                    // ── TABLET header: two compact rows, ~72dp total ───────────────
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 221:
  ```text
                    // ── TABLET header: two compact rows, ~72dp total ───────────────────
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 222:
  ```text
                    //Row 1: brand name (no subtitle to save height)
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 223:
  ```text
                    // Row 2: full step indicator — gets the full width, no clipping
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 224:
  ```text
                    <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 225:
  ```text
                        style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 226:
  ```text
                            backgroundColor: "#fff",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 227:
  ```text
                            borderBottomWidth: 1,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 228:
  ```text
                            borderBottomColor: "rgba(0,0,0,0.08)",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 229:
  ```text
                            paddingHorizontal: 24,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 230:
  ```text
                            paddingTop: 10,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 231:
  ```text
                            paddingBottom: 10,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 232:
  ```text
                            elevation: 2,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 233:
  ```text
                        }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 234:
  ```text
                    >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 235:
  ```text
                        <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 236:
  ```text
                            style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 237:
  ```text
                                fontSize: 13,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 238:
  ```text
                                fontWeight: "800",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 239:
  ```text
                                color: colors.foreground,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 240:
  ```text
                                letterSpacing: 0.3,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 241:
  ```text
                                marginBottom: 8,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 242:
  ```text
                            }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 243:
  ```text
                            allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 244:
  ```text
                        >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 245:
  ```text
                            Heigen Studio
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 246:
  ```text
                        </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 247:
  ```text
                        <View style={{ alignItems: "center" }}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 248:
  ```text
                            <StepIndicator
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 249:
  ```text
                                steps={STEPS}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 250:
  ```text
                                currentStep={state.step}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 251:
  ```text
                                compact
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 252:
  ```text
                            />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 253:
  ```text
                        </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 254:
  ```text
                        <TouchableOpacity
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 255:
  ```text
                            onPress={openExitPage}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 256:
  ```text
                            style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 257:
  ```text
                                position: "absolute",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 258:
  ```text
                                right: 24,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 259:
  ```text
                                top: 10,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 260:
  ```text
                                paddingVertical: 6,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 261:
  ```text
                                paddingHorizontal: 10,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 262:
  ```text
                                borderRadius: 12,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 263:
  ```text
                                borderWidth: 1,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 264:
  ```text
                                borderColor: "rgba(22,81,102,0.25)",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 265:
  ```text
                                backgroundColor: "rgba(22,81,102,0.06)",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 266:
  ```text
                            }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 267:
  ```text
                        >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 268:
  ```text
                            <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 269:
  ```text
                                style={{ fontSize: 11, fontWeight: "700", color: colors.foreground }}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 270:
  ```text
                                allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 271:
  ```text
                            >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 272:
  ```text
                                Exit
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 273:
  ```text
                            </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 274:
  ```text
                        </TouchableOpacity>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 275:
  ```text
                    </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 276:
  ```text
                ) : (
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 277:
  ```text
                    // ── PHONE header: stacked brand + subtitle + full step indicator ─
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 278:
  ```text
                    <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 279:
  ```text
                        style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 280:
  ```text
                            backgroundColor: "#fff",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 281:
  ```text
                            borderBottomWidth: 1,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 282:
  ```text
                            borderBottomColor: "rgba(0,0,0,0.08)",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 283:
  ```text
                            paddingHorizontal: s(spacing.xl),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 284:
  ```text
                            paddingTop: s(spacing.lg),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 285:
  ```text
                            paddingBottom: s(spacing.xl),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 286:
  ```text
                            elevation: 2,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 287:
  ```text
                        }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 288:
  ```text
                    >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 289:
  ```text
                        <View style={{ marginBottom: s(spacing.lg) }}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 290:
  ```text
                            <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 291:
  ```text
                                style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 292:
  ```text
                                    fontSize: fs(typography.h2.fontSize),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 293:
  ```text
                                    fontWeight: "700",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 294:
  ```text
                                }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 295:
  ```text
                                allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 296:
  ```text
                            >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 297:
  ```text
                                Heigen Studio
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 298:
  ```text
                            </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 299:
  ```text
                            <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 300:
  ```text
                                style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 301:
  ```text
                                    fontSize: fs(typography.sm.fontSize),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 302:
  ```text
                                    color: colors.mutedForeground,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 303:
  ```text
                                    marginTop: 2,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 304:
  ```text
                                }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 305:
  ```text
                                allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 306:
  ```text
                            >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 307:
  ```text
                                Book Your Photoshoot Appointment
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 308:
  ```text
                            </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 309:
  ```text
                        </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 310:
  ```text
                        <View style={{ alignItems: "center" }}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 311:
  ```text
                            <StepIndicator
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 312:
  ```text
                                steps={STEPS}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 313:
  ```text
                                currentStep={state.step}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 314:
  ```text
                            />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 315:
  ```text
                        </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 316:
  ```text
                        <TouchableOpacity
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 317:
  ```text
                            onPress={openExitPage}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 318:
  ```text
                            style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 319:
  ```text
                                position: "absolute",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 320:
  ```text
                                right: s(spacing.xl),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 321:
  ```text
                                top: s(spacing.lg),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 322:
  ```text
                                paddingVertical: s(6),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 323:
  ```text
                                paddingHorizontal: s(10),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 324:
  ```text
                                borderRadius: s(12),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 325:
  ```text
                                borderWidth: 1,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 326:
  ```text
                                borderColor: "rgba(22,81,102,0.25)",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 327:
  ```text
                                backgroundColor: "rgba(22,81,102,0.06)",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 328:
  ```text
                            }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 329:
  ```text
                        >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 330:
  ```text
                            <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 331:
  ```text
                                style={{ fontSize: fs(11), fontWeight: "700", color: colors.foreground }}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 332:
  ```text
                                allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 333:
  ```text
                            >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 334:
  ```text
                                Exit
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 335:
  ```text
                            </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 336:
  ```text
                        </TouchableOpacity>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 337:
  ```text
                    </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 338:
  ```text
                ))}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 339:
  ```text
            <View style={{ flex: 1 }}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 340:
  ```text
                {state.showExitPage && (
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 341:
  ```text
                    <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 342:
  ```text
                        style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 343:
  ```text
                            position: "absolute",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 344:
  ```text
                            top: 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 345:
  ```text
                            left: 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 346:
  ```text
                            right: 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 347:
  ```text
                            bottom: 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 348:
  ```text
                            backgroundColor: "rgba(0,0,0,0.45)",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 349:
  ```text
                            zIndex: 30,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 350:
  ```text
                            alignItems: "center",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 351:
  ```text
                            justifyContent: "center",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 352:
  ```text
                            padding: s(spacing.xl),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 353:
  ```text
                        }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 354:
  ```text
                    >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 355:
  ```text
                        <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 356:
  ```text
                            style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 357:
  ```text
                                width: "100%",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 358:
  ```text
                                maxWidth: isTablet ? 520 : 420,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 359:
  ```text
                                backgroundColor: "#fff",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 360:
  ```text
                                borderRadius: 16,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 361:
  ```text
                                padding: s(spacing.xl),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 362:
  ```text
                                gap: s(spacing.md),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 363:
  ```text
                            }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 364:
  ```text
                        >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 365:
  ```text
                            <Text style={{ fontSize: fs(20), fontWeight: "700", color: colors.foreground }} allowFontScaling={false}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 366:
  ```text
                                Exit Session
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 367:
  ```text
                            </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 368:
  ```text
                            <Text style={{ fontSize: fs(14), color: colors.mutedForeground }} allowFontScaling={false}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 369:
  ```text
                                Exit without booking? Current selections will be cleared and the kiosk will reset for the next customer.
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 370:
  ```text
                            </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 371:
  ```text
                            <View style={{ flexDirection: "row", gap: s(spacing.md), marginTop: s(8) }}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 372:
  ```text
                                <TouchableOpacity
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 373:
  ```text
                                    onPress={closeExitPage}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 374:
  ```text
                                    style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 375:
  ```text
                                        flex: 1,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 376:
  ```text
                                        borderRadius: 12,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 377:
  ```text
                                        borderWidth: 1,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 378:
  ```text
                                        borderColor: colors.border,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 379:
  ```text
                                        paddingVertical: s(12),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 380:
  ```text
                                        alignItems: "center",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 381:
  ```text
                                    }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 382:
  ```text
                                >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 383:
  ```text
                                    <Text style={{ color: colors.foreground, fontWeight: "600", fontSize: fs(14) }} allowFontScaling={false}>Continue</Text>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 384:
  ```text
                                </TouchableOpacity>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 385:
  ```text
                                <TouchableOpacity
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 386:
  ```text
                                    onPress={confirmExitSession}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 387:
  ```text
                                    style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 388:
  ```text
                                        flex: 1,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 389:
  ```text
                                        borderRadius: 12,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 390:
  ```text
                                        backgroundColor: colors.accent,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 391:
  ```text
                                        paddingVertical: s(12),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 392:
  ```text
                                        alignItems: "center",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 393:
  ```text
                                    }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 394:
  ```text
                                >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 395:
  ```text
                                    <Text style={{ color: "#fff", fontWeight: "700", fontSize: fs(14) }} allowFontScaling={false}>Exit</Text>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 396:
  ```text
                                </TouchableOpacity>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 397:
  ```text
                            </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 398:
  ```text
                        </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 399:
  ```text
                    </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 400:
  ```text
                )}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 401:
  ```text
                {state.step === 0 && (
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 402:
  ```text
                    <CategoryScreen
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 403:
  ```text
                        onSelectCategory={handleSelectCategory}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 404:
  ```text
                        recommendationData={state.recommendationData}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 405:
  ```text
                        onSelectRecommendation={handleQuickBookRecommendation}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 406:
  ```text
                    />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 407:
  ```text
                )}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 408:
  ```text
                {state.step === 1 && (
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 409:
  ```text
                    <PackageScreen
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 410:
  ```text
                        category={state.selectedCategory}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 411:
  ```text
                        onSelectPackage={handleSelectPackage}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 412:
  ```text
                        onBack={handleBackToCategory}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 413:
  ```text
                    />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 414:
  ```text
                )}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 415:
  ```text
                {state.step === 2 && (
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 416:
  ```text
                    <AddonsScreen
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 417:
  ```text
                        category={state.selectedCategory}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 418:
  ```text
                        selectedPackage={state.selectedPackage}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 419:
  ```text
                        selectedAddons={state.selectedAddons}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 420:
  ```text
                        onToggleAddon={handleToggleAddon}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 421:
  ```text
                        onNext={handleProceedToBookNow}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 422:
  ```text
                        onBack={handleBackToPackages}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 423:
  ```text
                    />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 424:
  ```text
                )}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 425:
  ```text
                {state.step === 3 && (
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 426:
  ```text
                    <ConfirmationScreen
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 427:
  ```text
                        customerInfo={state.customerInfo}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 428:
  ```text
                        onReset={reset}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 429:
  ```text
                    />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 430:
  ```text
                )}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 431:
  ```text
            </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 432:
  ```text
            <CustomerFormModal
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 433:
  ```text
                visible={state.showCustomerForm}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 434:
  ```text
                onClose={() => update({ showCustomerForm: false })}
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 435:
  ```text
                onSubmit={handleCustomerFormSubmit}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 436:
  ```text
                loading={state.loadingCustomerCheck}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 437:
  ```text
                requireSubmit={!state.customerInfo}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 438:
  ```text
                resetToken={state.formResetToken}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 439:
  ```text
            />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 440:
  ```text
            <BookingSummaryModal
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 441:
  ```text
                visible={state.showSummary}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 442:
  ```text
                onClose={() => update({ showSummary: false })}
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 443:
  ```text
                onEdit={handleEditSelection}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 444:
  ```text
                onConfirm={handleConfirmBooking}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 445:
  ```text
                category={state.selectedCategory}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 446:
  ```text
                selectedPackage={state.selectedPackage}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 447:
  ```text
                selectedAddons={state.selectedAddons}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 448:
  ```text
                customerInfo={state.customerInfo}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 449:
  ```text
                loading={state.submitting}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 450:
  ```text
            />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 451:
  ```text
        </SafeAreaView>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 452:
  ```text
    );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 453:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 3. `reset`

- Signature: `function reset()`
- Source range: lines 57-59

#### Line-by-line

- Line 57:
  ```text
    function reset() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 58:
  ```text
        setState(createInitialState());
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 59:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 4. `openExitPage`

- Signature: `function openExitPage()`
- Source range: lines 61-63

#### Line-by-line

- Line 61:
  ```text
    function openExitPage() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 62:
  ```text
        update({ showExitPage: true });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 63:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 5. `closeExitPage`

- Signature: `function closeExitPage()`
- Source range: lines 65-67

#### Line-by-line

- Line 65:
  ```text
    function closeExitPage() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 66:
  ```text
        update({ showExitPage: false });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 67:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 6. `confirmExitSession`

- Signature: `function confirmExitSession()`
- Source range: lines 69-74

#### Line-by-line

- Line 69:
  ```text
    function confirmExitSession() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 70:
  ```text
        setState({
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 71:
  ```text
            ...createInitialState(),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 72:
  ```text
            formResetToken: state.formResetToken + 1,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 73:
  ```text
        });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 74:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 7. `handleSelectCategory`

- Signature: `function handleSelectCategory(cat)`
- Source range: lines 76-78

#### Line-by-line

- Line 76:
  ```text
    function handleSelectCategory(cat) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 77:
  ```text
        update({ selectedCategory: cat, step: 1 });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 78:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 8. `handleSelectPackage`

- Signature: `function handleSelectPackage(pkg)`
- Source range: lines 79-81

#### Line-by-line

- Line 79:
  ```text
    function handleSelectPackage(pkg) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 80:
  ```text
        update({ selectedPackage: pkg, selectedAddons: [], step: 2 });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 81:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 9. `handleToggleAddon`

- Signature: `function handleToggleAddon(addon)`
- Source range: lines 82-92

#### Line-by-line

- Line 82:
  ```text
    function handleToggleAddon(addon) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 83:
  ```text
        setState((prev) => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 84:
  ```text
            const already = prev.selectedAddons.some((a) => a.id === addon.id);
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 85:
  ```text
            return {
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 86:
  ```text
                ...prev,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 87:
  ```text
                selectedAddons: already
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 88:
  ```text
                    ? prev.selectedAddons.filter((a) => a.id !== addon.id)
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 89:
  ```text
                    : [...prev.selectedAddons, addon],
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 90:
  ```text
            };
  ```
  Explanation: Closes the current block/scope.
- Line 91:
  ```text
        });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 92:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 10. `handleBackToCategory`

- Signature: `function handleBackToCategory()`
- Source range: lines 93-100

#### Line-by-line

- Line 93:
  ```text
    function handleBackToCategory() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 94:
  ```text
        update({
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 95:
  ```text
            step: 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 96:
  ```text
            selectedCategory: null,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 97:
  ```text
            selectedPackage: null,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 98:
  ```text
            selectedAddons: [],
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 99:
  ```text
        });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 100:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 11. `handleBackToPackages`

- Signature: `function handleBackToPackages()`
- Source range: lines 101-103

#### Line-by-line

- Line 101:
  ```text
    function handleBackToPackages() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 102:
  ```text
        update({ step: 1, selectedAddons: [] });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 103:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 12. `handleProceedToBookNow`

- Signature: `function handleProceedToBookNow()`
- Source range: lines 104-113

#### Line-by-line

- Line 104:
  ```text
    function handleProceedToBookNow() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 105:
  ```text
        if (state.customerInfo) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 106:
  ```text
            update({ showSummary: true });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 107:
  ```text
            return;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 108:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 109:
  ```text
        update({
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 110:
  ```text
            showCustomerForm: true,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 111:
  ```text
            requestSummaryAfterCustomerForm: true,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 112:
  ```text
        });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 113:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 13. `handleCustomerFormSubmit`

- Signature: `async function handleCustomerFormSubmit(info)`
- Source range: lines 114-149

#### Line-by-line

- Line 114:
  ```text
    async function handleCustomerFormSubmit(info) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 115:
  ```text
        update({
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 116:
  ```text
            loadingCustomerCheck: true,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 117:
  ```text
            customerInfo: info,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 118:
  ```text
        });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 119:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 120:
  ```text
        let recommendationData = null;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 121:
  ```text
        try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 122:
  ```text
            const existingCustomer = await findCustomerByEmail(info.email);
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 123:
  ```text
            if (existingCustomer) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 124:
  ```text
                const customerId =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 125:
  ```text
                    existingCustomer.id || existingCustomer.customer_id;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 126:
  ```text
                recommendationData = await fetchRecommendations(
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 127:
  ```text
                    customerId,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 128:
  ```text
                    info.preferredDate || null,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 129:
  ```text
                    3,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 130:
  ```text
                );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 131:
  ```text
            } else {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 132:
  ```text
                recommendationData = await fetchPopularRecommendations(3);
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 133:
  ```text
            }
  ```
  Explanation: Closes the current block/scope.
- Line 134:
  ```text
        } catch (_) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 135:
  ```text
            try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 136:
  ```text
                recommendationData = await fetchPopularRecommendations(3);
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 137:
  ```text
            } catch (__) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 138:
  ```text
                recommendationData = { recommendations: [] };
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 139:
  ```text
            }
  ```
  Explanation: Closes the current block/scope.
- Line 140:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 141:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 142:
  ```text
        update({
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 143:
  ```text
            recommendationData,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 144:
  ```text
            showCustomerForm: false,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 145:
  ```text
            showSummary: state.requestSummaryAfterCustomerForm,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 146:
  ```text
            requestSummaryAfterCustomerForm: false,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 147:
  ```text
            loadingCustomerCheck: false,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 148:
  ```text
        });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 149:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 14. `handleEditSelection`

- Signature: `function handleEditSelection()`
- Source range: lines 150-152

#### Line-by-line

- Line 150:
  ```text
    function handleEditSelection() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 151:
  ```text
        update({ showSummary: false });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 152:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 15. `handleQuickBookRecommendation`

- Signature: `function handleQuickBookRecommendation(rec)`
- Source range: lines 154-166

#### Line-by-line

- Line 154:
  ```text
    function handleQuickBookRecommendation(rec) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 155:
  ```text
        const pkg = rec?.package;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 156:
  ```text
        if (!pkg?.id) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 157:
  ```text
        update({
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 158:
  ```text
            selectedCategory: {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 159:
  ```text
                id: pkg.category || pkg.id,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 160:
  ```text
                name: pkg.category || "Recommended",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 161:
  ```text
            },
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 162:
  ```text
            selectedPackage: pkg,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 163:
  ```text
            selectedAddons: Array.isArray(rec.addons) ? rec.addons : [],
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 164:
  ```text
            showSummary: true,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 165:
  ```text
        });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 166:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 16. `handleConfirmBooking`

- Signature: `async function handleConfirmBooking()`
- Source range: lines 168-199

#### Line-by-line

- Line 168:
  ```text
    async function handleConfirmBooking() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 169:
  ```text
        update({ submitting: true });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 170:
  ```text
        try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 171:
  ```text
            await submitBooking({
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 172:
  ```text
                customer: {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 173:
  ```text
                    full_name: state.customerInfo.fullName,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 174:
  ```text
                    email: state.customerInfo.email,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 175:
  ```text
                    contact_number: state.customerInfo.contactNumber,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 176:
  ```text
                    consent_given: state.customerInfo.consentGiven ?? true,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 177:
  ```text
                },
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 178:
  ```text
                category_id:
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 179:
  ```text
                    state.selectedCategory?.id ?? state.selectedCategory?.name,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 180:
  ```text
                package_id: state.selectedPackage?.id,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 181:
  ```text
                addon_ids: state.selectedAddons.map((a) => a.id),
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 182:
  ```text
                preferred_date: state.customerInfo.preferredDate,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 183:
  ```text
                total_amount: calcTotal(),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 184:
  ```text
            });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 185:
  ```text
            update({
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 186:
  ```text
                submitting: false,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 187:
  ```text
                showSummary: false,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 188:
  ```text
                step: 3,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 189:
  ```text
                formResetToken: state.formResetToken + 1,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 190:
  ```text
            });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 191:
  ```text
        } catch (err) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 192:
  ```text
            update({ submitting: false });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 193:
  ```text
            Alert.alert(
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 194:
  ```text
                "Booking Failed",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 195:
  ```text
                err.message || "Could not submit booking.",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 196:
  ```text
                [{ text: "OK" }],
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 197:
  ```text
            );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 198:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 199:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 17. `calcTotal`

- Signature: `function calcTotal()`
- Source range: lines 201-213

#### Line-by-line

- Line 201:
  ```text
    function calcTotal() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 202:
  ```text
        const base = Number(
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 203:
  ```text
            state.selectedPackage?.promo_price
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 204:
  ```text
                ? state.selectedPackage.promo_price
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 205:
  ```text
                : state.selectedPackage?.price
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 206:
  ```text
                  ? state.selectedPackage.price
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 207:
  ```text
                  : 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 208:
  ```text
        );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 209:
  ```text
        return (
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 210:
  ```text
            base +
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 211:
  ```text
            state.selectedAddons.reduce((sum, a) => sum + Number(a.price), 0)
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 212:
  ```text
        );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 213:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

## File: `HeigenKiosk/src/screens/PackageScreen.js`

- Methods documented: 5

### 1. `PackageScreen`

- Signature: `function PackageScreen(param)`
- Source range: lines 12-158

#### Line-by-line

- Line 12:
  ```text
export default function PackageScreen({ category, onSelectPackage, onBack }) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 13:
  ```text
    const { s, fs, isTablet, W } = useScale();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 14:
  ```text
    const [scrollY, setScrollY] = useState(0);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 15:
  ```text
    const [contentHeight, setContentHeight] = useState(0);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 16:
  ```text
    const [viewportHeight, setViewportHeight] = useState(0);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 17:
  ```text
    const catName = category?.name;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 18:
  ```text
    const fetchPkgs = useCallback(() => fetchPackages(catName), [catName]);
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 19:
  ```text
    const fetchPop = useCallback(() => fetchPopularPackage(catName), [catName]);
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 20:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 21:
  ```text
    const { data: packages, loading, error, refetch } = useApi(fetchPkgs);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 22:
  ```text
    const { data: popularData } = useApi(fetchPop);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 23:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 24:
  ```text
    if (loading) return <LoadingScreen message="Loading packages..." />;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 25:
  ```text
    if (error) return <ErrorScreen message={error} onRetry={refetch} />;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 26:
  ```text
    if (!packages?.length)
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 27:
  ```text
        return (
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 28:
  ```text
            <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 29:
  ```text
                style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 30:
  ```text
                    flex: 1,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 31:
  ```text
                    alignItems: "center",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 32:
  ```text
                    justifyContent: "center",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 33:
  ```text
                }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 34:
  ```text
            >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 35:
  ```text
                <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 36:
  ```text
                    style={{ fontSize: fs(16), color: colors.mutedForeground }}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 37:
  ```text
                >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 38:
  ```text
                    No packages found for {catName}.
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 39:
  ```text
                </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 40:
  ```text
                <Button
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 41:
  ```text
                    label="Go Back"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 42:
  ```text
                    onPress={onBack}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 43:
  ```text
                    variant="ghost"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 44:
  ```text
                    style={{ marginTop: s(spacing.xl) }}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 45:
  ```text
                />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 46:
  ```text
            </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 47:
  ```text
        );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 48:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 49:
  ```text
    const popularId = popularData?.top_package_id ?? packages[0]?.id;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 50:
  ```text
    const popularPkg = packages.find((p) => p.id === popularId) ?? packages[0];
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 51:
  ```text
    const otherPkgs = packages.filter((p) => p.id !== popularPkg?.id);
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 52:
  ```text
    const orderedPackages = [popularPkg, ...otherPkgs].filter(Boolean);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 53:
  ```text
    const hPad = s(spacing.xl);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 54:
  ```text
    const colGap = s(spacing.md);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 55:
  ```text
    const cardWidth = isTablet ? (W - hPad * 2 - colGap) / 2 : undefined;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 56:
  ```text
    const maxScroll = Math.max(0, contentHeight - viewportHeight);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 57:
  ```text
    const canScrollDown = maxScroll > s(8);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 58:
  ```text
    const isNearBottom = scrollY >= maxScroll - s(20);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 59:
  ```text
    const showScrollHint = canScrollDown && !isNearBottom;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 60:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 61:
  ```text
    return (
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 62:
  ```text
        <View style={{ flex: 1, backgroundColor: colors.background }}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 63:
  ```text
            <ScrollView
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 64:
  ```text
                style={{ flex: 1, backgroundColor: colors.background }}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 65:
  ```text
                contentContainerStyle={{ padding: hPad, paddingBottom: s(64) }}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 66:
  ```text
                showsVerticalScrollIndicator={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 67:
  ```text
                onLayout={(e) => setViewportHeight(e.nativeEvent.layout.height)}
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 68:
  ```text
                onContentSizeChange={(_, h) => setContentHeight(h)}
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 69:
  ```text
                onScroll={(e) => setScrollY(e.nativeEvent.contentOffset.y)}
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 70:
  ```text
                scrollEventThrottle={16}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 71:
  ```text
            >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 72:
  ```text
            <Button
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 73:
  ```text
                label="Back to Categories"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 74:
  ```text
                icon="arrow-back"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 75:
  ```text
                variant="ghost"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 76:
  ```text
                onPress={onBack}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 77:
  ```text
                style={{ alignSelf: "flex-start", marginBottom: s(spacing.md) }}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 78:
  ```text
                labelStyle={{ color: colors.mutedForeground }}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 79:
  ```text
            />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 80:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 81:
  ```text
            <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 82:
  ```text
                style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 83:
  ```text
                    fontSize: fs(22),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 84:
  ```text
                    fontWeight: "700",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 85:
  ```text
                    textAlign: "center",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 86:
  ```text
                    marginBottom: s(2),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 87:
  ```text
                }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 88:
  ```text
                allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 89:
  ```text
            >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 90:
  ```text
                Select a Package
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 91:
  ```text
            </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 92:
  ```text
            <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 93:
  ```text
                style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 94:
  ```text
                    fontSize: fs(14),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 95:
  ```text
                    color: colors.mutedForeground,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 96:
  ```text
                    textAlign: "center",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 97:
  ```text
                    marginBottom: s(spacing.xl),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 98:
  ```text
                }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 99:
  ```text
                allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 100:
  ```text
            >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 101:
  ```text
                for {catName}
  ```
  Explanation: Iterates over a collection to apply repeated processing.
- Line 102:
  ```text
            </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 103:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 104:
  ```text
                <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 105:
  ```text
                    style={
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 106:
  ```text
                        isTablet
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 107:
  ```text
                            ? { flexDirection: "row", flexWrap: "wrap", gap: colGap }
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 108:
  ```text
                            : { gap: s(spacing.sm) }
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 109:
  ```text
                    }
  ```
  Explanation: Closes the current block/scope.
- Line 110:
  ```text
                >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 111:
  ```text
                    {orderedPackages.map((pkg, idx) => (
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 112:
  ```text
                        <PackageCard
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 113:
  ```text
                            key={pkg.id}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 114:
  ```text
                            pkg={pkg}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 115:
  ```text
                            onPress={() => onSelectPackage(pkg)}
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 116:
  ```text
                            popular={idx === 0}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 117:
  ```text
                            s={s}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 118:
  ```text
                            fs={fs}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 119:
  ```text
                            width={cardWidth}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 120:
  ```text
                        />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 121:
  ```text
                    ))}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 122:
  ```text
                </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 123:
  ```text
            </ScrollView>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 124:
  ```text
            {showScrollHint && (
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 125:
  ```text
                <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 126:
  ```text
                    pointerEvents="none"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 127:
  ```text
                    style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 128:
  ```text
                        position: "absolute",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 129:
  ```text
                        left: 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 130:
  ```text
                        right: 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 131:
  ```text
                        bottom: s(spacing.xl),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 132:
  ```text
                        alignItems: "center",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 133:
  ```text
                    }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 134:
  ```text
                >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 135:
  ```text
                    <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 136:
  ```text
                        style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 137:
  ```text
                            width: s(34),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 138:
  ```text
                            height: s(34),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 139:
  ```text
                            borderRadius: s(17),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 140:
  ```text
                            alignItems: "center",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 141:
  ```text
                            justifyContent: "center",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 142:
  ```text
                            backgroundColor: "rgba(255,255,255,0.74)",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 143:
  ```text
                            borderWidth: 1,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 144:
  ```text
                            borderColor: "rgba(0,0,0,0.08)",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 145:
  ```text
                            ...shadow.sm,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 146:
  ```text
                        }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 147:
  ```text
                    >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 148:
  ```text
                        <Icon
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 149:
  ```text
                            name="chevron-down"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 150:
  ```text
                            size={s(16)}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 151:
  ```text
                            color={colors.mutedForeground}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 152:
  ```text
                        />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 153:
  ```text
                    </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 154:
  ```text
                </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 155:
  ```text
            )}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 156:
  ```text
        </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 157:
  ```text
    );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 158:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 2. `toStringArray`

- Signature: `function toStringArray(val)`
- Source range: lines 160-165

#### Line-by-line

- Line 160:
  ```text
function toStringArray(val) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 161:
  ```text
    if (!val) return [];
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 162:
  ```text
    if (Array.isArray(val)) return val.map(String);
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 163:
  ```text
    if (typeof val === "object") return Object.values(val).map(String);
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 164:
  ```text
    return [String(val)];
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 165:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 3. `hasPromo`

- Signature: `function hasPromo(pkg)`
- Source range: lines 166-168

#### Line-by-line

- Line 166:
  ```text
function hasPromo(pkg) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 167:
  ```text
    return pkg.promo_price != null && Number(pkg.promo_price) > 0;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 168:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 4. `IncludeRow`

- Signature: `function IncludeRow(param)`
- Source range: lines 170-197

#### Line-by-line

- Line 170:
  ```text
function IncludeRow({ text, color, s, fs }) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 171:
  ```text
    return (
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 172:
  ```text
        <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 173:
  ```text
            style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 174:
  ```text
                flexDirection: "row",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 175:
  ```text
                alignItems: "flex-start",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 176:
  ```text
                gap: s(spacing.sm),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 177:
  ```text
                marginBottom: s(4),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 178:
  ```text
            }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 179:
  ```text
        >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 180:
  ```text
            <Icon
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 181:
  ```text
                name="checkmark"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 182:
  ```text
                size={s(13)}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 183:
  ```text
                color={color || colors.accent}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 184:
  ```text
            />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 185:
  ```text
            <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 186:
  ```text
                style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 187:
  ```text
                    fontSize: fs(13),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 188:
  ```text
                    color: color || colors.mutedForeground,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 189:
  ```text
                    flex: 1,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 190:
  ```text
                }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 191:
  ```text
                allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 192:
  ```text
            >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 193:
  ```text
                {text}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 194:
  ```text
            </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 195:
  ```text
        </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 196:
  ```text
    );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 197:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 5. `PackageCard`

- Signature: `function PackageCard(param)`
- Source range: lines 200-368

#### Line-by-line

- Line 200:
  ```text
function PackageCard({ pkg, onPress, popular = false, s, fs, width }) {
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 201:
  ```text
    const borderColor = popular ? "rgba(217,119,6,0.7)" : colors.border;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 202:
  ```text
    const bgColor = "#ffffff";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 203:
  ```text
    const price = hasPromo(pkg) ? pkg.promo_price : pkg.price;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 204:
  ```text
    const inclusions = toStringArray(pkg.inclusions);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 205:
  ```text
    const freebies = toStringArray(pkg.freebies);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 206:
  ```text
    const packageImage = resolvePackageImage(pkg);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 207:
  ```text
    const imageHeight = popular ? s(156) : s(132);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 208:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 209:
  ```text
    return (
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 210:
  ```text
        <TouchableOpacity
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 211:
  ```text
            onPress={onPress}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 212:
  ```text
            activeOpacity={0.85}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 213:
  ```text
            style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 214:
  ```text
                borderRadius: s(radii.xl),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 215:
  ```text
                borderWidth: popular ? 2 : 1.5,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 216:
  ```text
                borderColor: borderColor,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 217:
  ```text
                backgroundColor: bgColor,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 218:
  ```text
                ...(popular ? shadow.accent : shadow.sm),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 219:
  ```text
                position: "relative",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 220:
  ```text
                overflow: "hidden",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 221:
  ```text
                width: width || undefined,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 222:
  ```text
            }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 223:
  ```text
        >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 224:
  ```text
            <View style={{ height: imageHeight, backgroundColor: colors.muted }}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 225:
  ```text
                <Image
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 226:
  ```text
                    source={{ uri: packageImage }}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 227:
  ```text
                    style={{ width: "100%", height: "100%" }}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 228:
  ```text
                    resizeMode="cover"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 229:
  ```text
                />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 230:
  ```text
                <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 231:
  ```text
                    style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 232:
  ```text
                        position: "absolute",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 233:
  ```text
                        left: 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 234:
  ```text
                        right: 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 235:
  ```text
                        bottom: 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 236:
  ```text
                        paddingHorizontal: s(spacing.md),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 237:
  ```text
                        paddingVertical: s(spacing.sm),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 238:
  ```text
                        backgroundColor: "rgba(15,23,42,0.52)",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 239:
  ```text
                    }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 240:
  ```text
                >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 241:
  ```text
                    <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 242:
  ```text
                        style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 243:
  ```text
                            fontSize: fs(popular ? 16 : 14),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 244:
  ```text
                            fontWeight: popular ? "700" : "600",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 245:
  ```text
                            color: "#fff",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 246:
  ```text
                        }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 247:
  ```text
                        allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 248:
  ```text
                    >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 249:
  ```text
                        {pkg.name}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 250:
  ```text
                    </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 251:
  ```text
                </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 252:
  ```text
                {popular && (
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 253:
  ```text
                    <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 254:
  ```text
                        style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 255:
  ```text
                            position: "absolute",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 256:
  ```text
                            top: s(spacing.sm),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 257:
  ```text
                            left: s(spacing.sm),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 258:
  ```text
                            flexDirection: "row",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 259:
  ```text
                            alignItems: "center",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 260:
  ```text
                            gap: s(4),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 261:
  ```text
                            backgroundColor: "rgba(217,119,6,0.92)",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 262:
  ```text
                            borderRadius: s(radii.full),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 263:
  ```text
                            paddingHorizontal: s(7),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 264:
  ```text
                            paddingVertical: s(3),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 265:
  ```text
                        }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 266:
  ```text
                    >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 267:
  ```text
                        <Icon name="star" size={s(11)} color="#fff" />
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 268:
  ```text
                        <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 269:
  ```text
                            style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 270:
  ```text
                                fontSize: fs(10),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 271:
  ```text
                                color: "#fff",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 272:
  ```text
                                fontWeight: "700",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 273:
  ```text
                            }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 274:
  ```text
                            allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 275:
  ```text
                        >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 276:
  ```text
                            Most Booked
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 277:
  ```text
                        </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 278:
  ```text
                    </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 279:
  ```text
                )}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 280:
  ```text
            </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 281:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 282:
  ```text
            <View style={{ padding: s(spacing.md) }}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 283:
  ```text
                <View style={{ flexDirection: "row", alignItems: "flex-start" }}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 284:
  ```text
                    <View style={{ flex: 1 }}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 285:
  ```text
                        {pkg.included_portraits != null && (
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 286:
  ```text
                            <IncludeRow
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 287:
  ```text
                                text={`${pkg.included_portraits} portrait(s) included`}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 288:
  ```text
                                s={s}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 289:
  ```text
                                fs={fs}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 290:
  ```text
                            />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 291:
  ```text
                        )}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 292:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 293:
  ```text
                        {inclusions.slice(0, 2).map((item, i) => (
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 294:
  ```text
                            <IncludeRow key={i} text={item} s={s} fs={fs} />
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 295:
  ```text
                        ))}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 296:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 297:
  ```text
                        {freebies.slice(0, 1).map((f, i) => (
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 298:
  ```text
                            <IncludeRow
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 299:
  ```text
                                key={"f" + i}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 300:
  ```text
                                text={"Bonus: " + f}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 301:
  ```text
                                color={colors.success}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 302:
  ```text
                                s={s}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 303:
  ```text
                                fs={fs}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 304:
  ```text
                            />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 305:
  ```text
                        ))}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 306:
  ```text
                    </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 307:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 308:
  ```text
                    <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 309:
  ```text
                        style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 310:
  ```text
                            alignItems: "flex-end",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 311:
  ```text
                            marginLeft: s(spacing.md),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 312:
  ```text
                            flexShrink: 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 313:
  ```text
                        }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 314:
  ```text
                    >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 315:
  ```text
                        {hasPromo(pkg) && (
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 316:
  ```text
                            <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 317:
  ```text
                                style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 318:
  ```text
                                    fontSize: fs(12),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 319:
  ```text
                                    color: colors.mutedForeground,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 320:
  ```text
                                    textDecorationLine: "line-through",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 321:
  ```text
                                }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 322:
  ```text
                                allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 323:
  ```text
                            >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 324:
  ```text
                                ₱{Number(pkg.price).toLocaleString()}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 325:
  ```text
                            </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 326:
  ```text
                        )}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 327:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 328:
  ```text
                        <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 329:
  ```text
                            style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 330:
  ```text
                                fontSize: fs(popular ? 22 : 19),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 331:
  ```text
                                fontWeight: "700",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 332:
  ```text
                                color: colors.accent,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 333:
  ```text
                            }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 334:
  ```text
                            allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 335:
  ```text
                        >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 336:
  ```text
                            ₱{Number(price).toLocaleString()}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 337:
  ```text
                        </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 338:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 339:
  ```text
                        {!!pkg.promo_price_condition && (
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 340:
  ```text
                            <View
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 341:
  ```text
                                style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 342:
  ```text
                                    backgroundColor: colors.warning,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 343:
  ```text
                                    borderWidth: 1,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 344:
  ```text
                                    borderColor: "#fde68a",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 345:
  ```text
                                    borderRadius: s(radii.sm),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 346:
  ```text
                                    paddingHorizontal: s(spacing.sm),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 347:
  ```text
                                    paddingVertical: s(2),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 348:
  ```text
                                    marginTop: s(4),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 349:
  ```text
                                }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 350:
  ```text
                            >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 351:
  ```text
                                <Text
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 352:
  ```text
                                    style={{
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 353:
  ```text
                                        fontSize: fs(11),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 354:
  ```text
                                        fontWeight: "600",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 355:
  ```text
                                        color: colors.warningText,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 356:
  ```text
                                    }}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 357:
  ```text
                                    allowFontScaling={false}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 358:
  ```text
                                >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 359:
  ```text
                                    {String(pkg.promo_price_condition)}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 360:
  ```text
                                </Text>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 361:
  ```text
                            </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 362:
  ```text
                        )}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 363:
  ```text
                    </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 364:
  ```text
                </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 365:
  ```text
            </View>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 366:
  ```text
        </TouchableOpacity>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 367:
  ```text
    );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 368:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

## File: `backend/admin.py`

- Methods documented: 5

### 1. `UserCreationNoPasswordForm.save`

- Signature: `save(self, commit)`
- Source range: lines 36-41

#### Line-by-line

- Line 36:
  ```text
    def save(self, commit=True):
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 37:
  ```text
        user = super().save(commit=False)
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 38:
  ```text
        if commit:
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 39:
  ```text
            user.save()
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 40:
  ```text
            self.save_m2m()
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 41:
  ```text
        return user
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.

### 2. `UserAdmin.save_model`

- Signature: `save_model(self, request, obj, form, change)`
- Source range: lines 66-76

#### Line-by-line

- Line 66:
  ```text
    def save_model(self, request, obj, form, change):
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 67:
  ```text
        is_new = not change
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 68:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 69:
  ```text
        super().save_model(request, obj, form, change)
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 70:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 71:
  ```text
        if is_new:
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 72:
  ```text
            issue_staff_temporary_password(
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 73:
  ```text
                obj,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 74:
  ```text
                can_send_email=bool(request.user and request.user.is_superuser),
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 75:
  ```text
                reset_profile_completed=True,
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 76:
  ```text
            )
  ```
  Explanation: Executes a core operation in this method's control flow.

### 3. `PasswordResetRequestAdmin._can_review`

- Signature: `_can_review(self, request)`
- Source range: lines 104-105

#### Line-by-line

- Line 104:
  ```text
    def _can_review(self, request):
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 105:
  ```text
        return bool(request.user and request.user.is_superuser)
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.

### 4. `PasswordResetRequestAdmin.approve_requests`

- Signature: `approve_requests(self, request, queryset)`
- Source range: lines 108-139

#### Line-by-line

- Line 108:
  ```text
    def approve_requests(self, request, queryset):
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 109:
  ```text
        if not self._can_review(request):
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 110:
  ```text
            self.message_user(
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 111:
  ```text
                request,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 112:
  ```text
                "Only superusers can approve requests.",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 113:
  ```text
                level=messages.ERROR,
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 114:
  ```text
            )
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 115:
  ```text
            return
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 116:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 117:
  ```text
        pending = queryset.filter(status=PasswordResetRequest.STATUS_PENDING).select_related("user")
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 118:
  ```text
        approved_count = 0
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 119:
  ```text
        emailed_count = 0
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 120:
  ```text
        for row in pending:
  ```
  Explanation: Iterates over a collection to apply repeated processing.
- Line 121:
  ```text
            if row.user and (row.user.is_staff or row.user.is_superuser):
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 122:
  ```text
                result = issue_staff_temporary_password(
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 123:
  ```text
                    row.user,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 124:
  ```text
                    can_send_email=True,
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 125:
  ```text
                    reset_profile_completed=False,
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 126:
  ```text
                )
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 127:
  ```text
                if result.get("email_sent"):
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 128:
  ```text
                    emailed_count += 1
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 129:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 130:
  ```text
            row.status = PasswordResetRequest.STATUS_APPROVED
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 131:
  ```text
            row.reviewed_at = timezone.now()
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 132:
  ```text
            row.reviewed_by = request.user
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 133:
  ```text
            row.save(update_fields=["status", "reviewed_at", "reviewed_by"])
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 134:
  ```text
            approved_count += 1
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 135:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 136:
  ```text
        self.message_user(
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 137:
  ```text
            request,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 138:
  ```text
            f"Approved {approved_count} request(s). Email sent for {emailed_count} account(s).",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 139:
  ```text
        )
  ```
  Explanation: Executes a core operation in this method's control flow.

### 5. `PasswordResetRequestAdmin.reject_requests`

- Signature: `reject_requests(self, request, queryset)`
- Source range: lines 142-156

#### Line-by-line

- Line 142:
  ```text
    def reject_requests(self, request, queryset):
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 143:
  ```text
        if not self._can_review(request):
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 144:
  ```text
            self.message_user(
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 145:
  ```text
                request,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 146:
  ```text
                "Only superusers can reject requests.",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 147:
  ```text
                level=messages.ERROR,
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 148:
  ```text
            )
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 149:
  ```text
            return
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 150:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 151:
  ```text
        rejected = queryset.filter(status=PasswordResetRequest.STATUS_PENDING).update(
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 152:
  ```text
            status=PasswordResetRequest.STATUS_REJECTED,
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 153:
  ```text
            reviewed_at=timezone.now(),
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 154:
  ```text
            reviewed_by=request.user,
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 155:
  ```text
        )
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 156:
  ```text
        self.message_user(request, f"Rejected {rejected} request(s).")
  ```
  Explanation: Executes a core operation in this method's control flow.

## File: `backend/apps.py`

- Methods documented: 1

### 1. `BackendConfig.ready`

- Signature: `ready(self)`
- Source range: lines 8-9

#### Line-by-line

- Line 8:
  ```text
    def ready(self):
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 9:
  ```text
        from . import signals  # noqa: F401
  ```
  Explanation: Imports dependencies required by this method's implementation.

## File: `backend/auth_utils.py`

- Methods documented: 1

### 1. `issue_staff_temporary_password`

- Signature: `issue_staff_temporary_password(user, *, can_send_email, reset_profile_completed)`
- Source range: lines 8-56

#### Line-by-line

- Line 8:
  ```text
def issue_staff_temporary_password(
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 9:
  ```text
    user,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 10:
  ```text
    *,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 11:
  ```text
    can_send_email=False,
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 12:
  ```text
    reset_profile_completed=False,
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 13:
  ```text
):
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 14:
  ```text
    """
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 15:
  ```text
    Set a new random password for a staff account and optionally email it.
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 16:
  ```text
    """
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 17:
  ```text
    temporary_password = get_random_string(12)
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 18:
  ```text
    user.set_password(temporary_password)
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 19:
  ```text
    user.save(update_fields=["password"])
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 20:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 21:
  ```text
    profile, _ = StaffProfile.objects.get_or_create(user=user)
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 22:
  ```text
    profile.must_change_password = True
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 23:
  ```text
    if reset_profile_completed:
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 24:
  ```text
        profile.profile_completed = False
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 25:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 26:
  ```text
    update_fields = ["must_change_password"]
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 27:
  ```text
    if reset_profile_completed:
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 28:
  ```text
        update_fields.append("profile_completed")
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 29:
  ```text
    profile.save(update_fields=update_fields)
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 30:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 31:
  ```text
    email_sent = False
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 32:
  ```text
    if can_send_email and user.email and (user.is_staff or user.is_superuser):
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 33:
  ```text
        subject = "Your Snaplytics account credentials"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 34:
  ```text
        display_name = user.first_name or user.username or "User"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 35:
  ```text
        username_or_email = user.email or user.username
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 36:
  ```text
        message = (
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 37:
  ```text
            f"Hi {display_name},\n\n"
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 38:
  ```text
            "A temporary password has been generated for your Snaplytics account.\n\n"
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 39:
  ```text
            f"Login email/username: {username_or_email}\n"
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 40:
  ```text
            f"Temporary password: {temporary_password}\n\n"
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 41:
  ```text
            "Please log in and change this password immediately.\n\n"
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 42:
  ```text
            "If you did not request this, please contact your administrator."
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 43:
  ```text
        )
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 44:
  ```text
        send_mail(
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 45:
  ```text
            subject,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 46:
  ```text
            message,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 47:
  ```text
            getattr(settings, "DEFAULT_FROM_EMAIL", None),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 48:
  ```text
            [user.email],
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 49:
  ```text
            fail_silently=True,
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 50:
  ```text
        )
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 51:
  ```text
        email_sent = True
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 52:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 53:
  ```text
    return {
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 54:
  ```text
        "temporary_password": temporary_password,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 55:
  ```text
        "email_sent": email_sent,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 56:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

## File: `backend/management/commands/run_etl.py`

- Methods documented: 2

### 1. `Command.add_arguments`

- Signature: `add_arguments(self, parser)`
- Source range: lines 7-8

#### Line-by-line

- Line 7:
  ```text
    def add_arguments(self, parser):
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 8:
  ```text
        parser.add_argument("--merge", action="store_true")
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.

### 2. `Command.handle`

- Signature: `handle(self, *args, **options)`
- Source range: lines 10-13

#### Line-by-line

- Line 10:
  ```text
    def handle(self, *args, **options):
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 11:
  ```text
        merge = options["merge"]
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 12:
  ```text
        run_etl(merge=merge)
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 13:
  ```text
        self.stdout.write(self.style.SUCCESS("ETL completed"))
  ```
  Explanation: Executes a core operation in this method's control flow.

## File: `backend/models.py`

- Methods documented: 6

### 1. `Category.__str__`

- Signature: `__str__(self)`
- Source range: lines 14-15

#### Line-by-line

- Line 14:
  ```text
    def __str__(self):
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 15:
  ```text
        return self.name
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.

### 2. `Package.__str__`

- Signature: `__str__(self)`
- Source range: lines 33-34

#### Line-by-line

- Line 33:
  ```text
    def __str__(self):
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 34:
  ```text
        return self.name
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.

### 3. `Customer.__str__`

- Signature: `__str__(self)`
- Source range: lines 52-53

#### Line-by-line

- Line 52:
  ```text
    def __str__(self):
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 53:
  ```text
        return self.full_name or "Customer"
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.

### 4. `Addon.__str__`

- Signature: `__str__(self)`
- Source range: lines 65-66

#### Line-by-line

- Line 65:
  ```text
    def __str__(self):
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 66:
  ```text
        return self.name
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.

### 5. `StaffProfile.__str__`

- Signature: `__str__(self)`
- Source range: lines 132-133

#### Line-by-line

- Line 132:
  ```text
    def __str__(self):
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 133:
  ```text
        return f"StaffProfile<{self.user_id}>"
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.

### 6. `PasswordResetRequest.__str__`

- Signature: `__str__(self)`
- Source range: lines 170-171

#### Line-by-line

- Line 170:
  ```text
    def __str__(self):
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 171:
  ```text
        return f"PasswordResetRequest<{self.requested_email}:{self.status}>"
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.

## File: `backend/renewal_utils.py`

- Methods documented: 1

### 1. `recompute_customer_renewal_profile`

- Signature: `recompute_customer_renewal_profile(customer)`
- Source range: lines 10-91

#### Line-by-line

- Line 10:
  ```text
def recompute_customer_renewal_profile(customer):
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 11:
  ```text
    """
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 12:
  ```text
    Rebuild Renewal metrics from live booking data for one customer.
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 13:
  ```text
    """
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 14:
  ```text
    bookings_qs = (
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 15:
  ```text
        Booking.objects.filter(
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 16:
  ```text
            customer=customer, session_status__in=ACCEPTED_BOOKING_STATUSES
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 17:
  ```text
        )
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 18:
  ```text
        .select_related("package")
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 19:
  ```text
        .order_by("session_date", "created_at", "id")
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 20:
  ```text
    )
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 21:
  ```text
    total_bookings = bookings_qs.count()
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 22:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 23:
  ```text
    if total_bookings == 0:
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 24:
  ```text
        renewal, _ = Renewal.objects.update_or_create(
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 25:
  ```text
            customer=customer,
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 26:
  ```text
            defaults={
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 27:
  ```text
                "total_bookings": 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 28:
  ```text
                "avg_booking_value": 0.0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 29:
  ```text
                "booking_frequency": 0.0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 30:
  ```text
                "renewed_within_366": False,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 31:
  ```text
                "total_spent": 0.0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 32:
  ```text
                "preferred_package_type": "",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 33:
  ```text
            },
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 34:
  ```text
        )
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 35:
  ```text
        return renewal
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 36:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 37:
  ```text
    agg = bookings_qs.aggregate(
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 38:
  ```text
        avg_booking_value=Avg("total_price"),
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 39:
  ```text
        total_spent=Sum("total_price"),
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 40:
  ```text
    )
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 41:
  ```text
    avg_booking_value = float(agg.get("avg_booking_value") or 0.0)
  ```
  Explanation: Triggers an external API/network request as part of this flow.
- Line 42:
  ```text
    total_spent = float(agg.get("total_spent") or 0.0)
  ```
  Explanation: Triggers an external API/network request as part of this flow.
- Line 43:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 44:
  ```text
    preferred = (
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 45:
  ```text
        bookings_qs.values("package__category")
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 46:
  ```text
        .annotate(count=Count("id"))
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 47:
  ```text
        .order_by("-count")
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 48:
  ```text
        .first()
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 49:
  ```text
    )
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 50:
  ```text
    preferred_package_type = (
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 51:
  ```text
        str(preferred.get("package__category")).strip()
  ```
  Explanation: Triggers an external API/network request as part of this flow.
- Line 52:
  ```text
        if preferred and preferred.get("package__category")
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 53:
  ```text
        else ""
  ```
  Explanation: Defines the fallback path when previous conditions are not satisfied.
- Line 54:
  ```text
    )
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 55:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 56:
  ```text
    booking_dates = []
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 57:
  ```text
    for booking in bookings_qs:
  ```
  Explanation: Iterates over a collection to apply repeated processing.
- Line 58:
  ```text
        dt = booking.session_date or booking.created_at
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 59:
  ```text
        if dt:
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 60:
  ```text
            booking_dates.append(dt.date())
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 61:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 62:
  ```text
    if booking_dates:
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 63:
  ```text
        # Use trailing 365-day booking count for a stable, non-explosive yearly rate.
  ```
  Explanation: Comment line documenting developer intent or context.
- Line 64:
  ```text
        # This prevents unrealistic values like 365/year for newly created accounts.
  ```
  Explanation: Comment line documenting developer intent or context.
- Line 65:
  ```text
        one_year_ago = timezone.now().date() - timedelta(days=365)
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 66:
  ```text
        trailing_365_count = sum(1 for d in booking_dates if d >= one_year_ago)
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 67:
  ```text
        booking_frequency = float(trailing_365_count)
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 68:
  ```text
    else:
  ```
  Explanation: Defines the fallback path when previous conditions are not satisfied.
- Line 69:
  ```text
        booking_frequency = 0.0
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 70:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 71:
  ```text
    renewed_within_366 = False
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 72:
  ```text
    if len(booking_dates) >= 2:
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 73:
  ```text
        sorted_dates = sorted(set(booking_dates))
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 74:
  ```text
        for prev, curr in zip(sorted_dates, sorted_dates[1:]):
  ```
  Explanation: Iterates over a collection to apply repeated processing.
- Line 75:
  ```text
            gap_days = (curr - prev).days
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 76:
  ```text
            if 1 <= gap_days <= 366:
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 77:
  ```text
                renewed_within_366 = True
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 78:
  ```text
                break
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 79:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 80:
  ```text
    renewal, _ = Renewal.objects.update_or_create(
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 81:
  ```text
        customer=customer,
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 82:
  ```text
        defaults={
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 83:
  ```text
            "total_bookings": int(total_bookings),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 84:
  ```text
            "avg_booking_value": avg_booking_value,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 85:
  ```text
            "booking_frequency": booking_frequency,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 86:
  ```text
            "renewed_within_366": renewed_within_366,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 87:
  ```text
            "total_spent": total_spent,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 88:
  ```text
            "preferred_package_type": preferred_package_type,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 89:
  ```text
        },
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 90:
  ```text
    )
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 91:
  ```text
    return renewal
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.

## File: `backend/signals.py`

- Methods documented: 1

### 1. `ensure_staff_profile`

- Signature: `ensure_staff_profile(sender, instance, created, **kwargs)`
- Source range: lines 11-15

#### Line-by-line

- Line 11:
  ```text
def ensure_staff_profile(sender, instance, created, **kwargs):
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 12:
  ```text
    if created:
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 13:
  ```text
        StaffProfile.objects.get_or_create(user=instance)
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 14:
  ```text
        return
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 15:
  ```text
    StaffProfile.objects.get_or_create(user=instance)
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.

## File: `electron-app/main.js`

- Methods documented: 2

### 1. `startDjango`

- Signature: `function startDjango()`
- Source range: lines 10-21

#### Line-by-line

- Line 10:
  ```text
function startDjango() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 11:
  ```text
    // Adjust path to your Django project
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 12:
  ```text
    djangoProcess = spawn(
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 13:
  ```text
        "python",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 14:
  ```text
        [path.join(__dirname, "../manage.py"), "runserver"],
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 15:
  ```text
        { cwd: path.join(__dirname, "../backend") },
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 16:
  ```text
    );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 17:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 18:
  ```text
    djangoProcess.stdout.on("data", (data) => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 19:
  ```text
        console.log(`Django: ${data}`);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 20:
  ```text
    });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 21:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 2. `createWindow`

- Signature: `function createWindow()`
- Source range: lines 23-36

#### Line-by-line

- Line 23:
  ```text
function createWindow() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 24:
  ```text
    mainWindow = new BrowserWindow({
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 25:
  ```text
        width: 1400,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 26:
  ```text
        height: 900,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 27:
  ```text
        webPreferences: {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 28:
  ```text
            nodeIntegration: false,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 29:
  ```text
            contextIsolation: true,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 30:
  ```text
            preload: path.join(__dirname, "preload.js"),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 31:
  ```text
        },
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 32:
  ```text
    });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 33:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 34:
  ```text
    mainWindow.loadFile("index.html");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 35:
  ```text
    mainWindow.webContents.openDevTools(); // Remove in production
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 36:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

## File: `electron-app/pages/guest/js/bookingform_packageinfo.js`

- Methods documented: 12

### 1. `getGuestContext`

- Signature: `function getGuestContext()`
- Source range: lines 18-24

#### Line-by-line

- Line 18:
  ```text
    function getGuestContext() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 19:
  ```text
        try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 20:
  ```text
            return JSON.parse(sessionStorage.getItem("guestCustomer") || "{}");
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 21:
  ```text
        } catch (_) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 22:
  ```text
            return {};
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 23:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 24:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 2. `ensureLoadingOverlay`

- Signature: `function ensureLoadingOverlay()`
- Source range: lines 35-46

#### Line-by-line

- Line 35:
  ```text
    function ensureLoadingOverlay() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 36:
  ```text
        let el = document.getElementById("guestLoadingOverlay");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 37:
  ```text
        if (el) return el;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 38:
  ```text
        el = document.createElement("div");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 39:
  ```text
        el.id = "guestLoadingOverlay";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 40:
  ```text
        el.style.cssText =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 41:
  ```text
            "position:fixed;inset:0;background:rgba(15,23,42,0.28);display:none;align-items:center;justify-content:center;z-index:9999;";
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 42:
  ```text
        el.innerHTML =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 43:
  ```text
            '<div style="background:#fff;padding:14px 18px;border-radius:12px;font-weight:700;color:#165166;box-shadow:0 8px 24px rgba(0,0,0,0.18);">Loading...</div>';
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 44:
  ```text
        document.body.appendChild(el);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 45:
  ```text
        return el;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 46:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 3. `setLoading`

- Signature: `function setLoading(on, param)`
- Source range: lines 48-59

#### Line-by-line

- Line 48:
  ```text
    function setLoading(on, text = "Loading...") {
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 49:
  ```text
        const overlay = ensureLoadingOverlay();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 50:
  ```text
        const box = overlay.firstElementChild;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 51:
  ```text
        if (box) box.textContent = text;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 52:
  ```text
        overlay.style.display = on ? "flex" : "none";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 53:
  ```text
        console.debug("[GUEST_DEBUG] packageinfo.setLoading", {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 54:
  ```text
            on,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 55:
  ```text
            text,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 56:
  ```text
            overlayDisplay: overlay.style.display,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 57:
  ```text
            bodyPointerEvents: document.body.style.pointerEvents || "(unset)",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 58:
  ```text
        });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 59:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 4. `ensureToast`

- Signature: `function ensureToast()`
- Source range: lines 62-75

#### Line-by-line

- Line 62:
  ```text
    function ensureToast() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 63:
  ```text
        let el = document.getElementById("guestToast");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 64:
  ```text
        if (el) return el;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 65:
  ```text
        el = document.createElement("div");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 66:
  ```text
        el.id = "guestToast";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 67:
  ```text
        el.style.cssText =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 68:
  ```text
            "position:fixed;bottom:32px;left:50%;transform:translateX(-50%);" +
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 69:
  ```text
            "background:#165166;color:#fff;padding:14px 24px;border-radius:10px;" +
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 70:
  ```text
            "font-weight:700;font-size:14px;z-index:10000;opacity:0;" +
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 71:
  ```text
            "transition:opacity 0.25s;pointer-events:none;text-align:center;" +
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 72:
  ```text
            "max-width:90vw;box-shadow:0 4px 16px rgba(0,0,0,0.25);";
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 73:
  ```text
        document.body.appendChild(el);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 74:
  ```text
        return el;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 75:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 5. `showToast`

- Signature: `function showToast(message, param)`
- Source range: lines 77-87

#### Line-by-line

- Line 77:
  ```text
    function showToast(message, durationMs = 2500) {
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 78:
  ```text
        const el = ensureToast();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 79:
  ```text
        el.textContent = message;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 80:
  ```text
        el.style.opacity = "1";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 81:
  ```text
        return new Promise((resolve) => {
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 82:
  ```text
            setTimeout(() => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 83:
  ```text
                el.style.opacity = "0";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 84:
  ```text
                setTimeout(resolve, 260);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 85:
  ```text
            }, durationMs);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 86:
  ```text
        });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 87:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 6. `showError`

- Signature: `function showError(message)`
- Source range: lines 89-95

#### Line-by-line

- Line 89:
  ```text
    function showError(message) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 90:
  ```text
        const el = ensureToast();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 91:
  ```text
        el.style.background = "#b91c1c";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 92:
  ```text
        el.textContent = message;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 93:
  ```text
        el.style.opacity = "1";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 94:
  ```text
        setTimeout(() => { el.style.opacity = "0"; }, 3500);
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 95:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 7. `apiRequest`

- Signature: `async function apiRequest(path, param)`
- Source range: lines 97-116

#### Line-by-line

- Line 97:
  ```text
    async function apiRequest(path, options = {}) {
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 98:
  ```text
        const response = await fetch(`${API_BASE}${path}`, {
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 99:
  ```text
            headers: { "Content-Type": "application/json" },
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 100:
  ```text
            ...options,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 101:
  ```text
        });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 102:
  ```text
        const text = await response.text();
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 103:
  ```text
        let data = null;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 104:
  ```text
        try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 105:
  ```text
            data = text ? JSON.parse(text) : null;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 106:
  ```text
        } catch (_) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 107:
  ```text
            data = null;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 108:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 109:
  ```text
        if (!response.ok) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 110:
  ```text
            throw new Error(
  ```
  Explanation: Raises an error/exception to signal failure to the caller.
- Line 111:
  ```text
                (data && (data.detail || data.message || data.error)) ||
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 112:
  ```text
                    `HTTP ${response.status}`,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 113:
  ```text
            );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 114:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 115:
  ```text
        return data;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 116:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 8. `packagePrice`

- Signature: `function packagePrice(pkg)`
- Source range: lines 118-122

#### Line-by-line

- Line 118:
  ```text
    function packagePrice(pkg) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 119:
  ```text
        const promo = Number(pkg?.promo_price || 0);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 120:
  ```text
        if (promo > 0) return promo;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 121:
  ```text
        return Number(pkg?.price || 0);
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 122:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 9. `setPackageOptions`

- Signature: `function setPackageOptions()`
- Source range: lines 124-136

#### Line-by-line

- Line 124:
  ```text
    function setPackageOptions() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 125:
  ```text
        packageSelect.innerHTML =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 126:
  ```text
            '<option value="">Select Package</option>' +
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 127:
  ```text
            packages
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 128:
  ```text
                .map(
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 129:
  ```text
                    (pkg) =>
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 130:
  ```text
                        `<option value="${pkg.id}">${pkg.name} – ₱${packagePrice(pkg).toLocaleString("en-PH", { minimumFractionDigits: 2 })}</option>`,
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 131:
  ```text
                )
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 132:
  ```text
                .join("");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 133:
  ```text
        if (preselectedPackageId) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 134:
  ```text
            packageSelect.value = String(preselectedPackageId);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 135:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 136:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 10. `renderAddonRows`

- Signature: `function renderAddonRows()`
- Source range: lines 138-170

#### Line-by-line

- Line 138:
  ```text
    function renderAddonRows() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 139:
  ```text
        if (!activePackage) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 140:
  ```text
            addonsContainer.innerHTML =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 141:
  ```text
                '<div class="empty-state">Select a package to view available add-ons.</div>';
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 142:
  ```text
            return;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 143:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 144:
  ```text
        const category = String(activePackage.category || "").toLowerCase();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 145:
  ```text
        const applicable = addons.filter((a) => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 146:
  ```text
            if (!a.applies_to) return true;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 147:
  ```text
            const applies = String(a.applies_to || "").toLowerCase();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 148:
  ```text
            return applies.includes(category) || applies.includes("*");
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 149:
  ```text
        });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 150:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 151:
  ```text
        if (!applicable.length) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 152:
  ```text
            addonsContainer.innerHTML =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 153:
  ```text
                '<div class="empty-state">No add-ons for this package.</div>';
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 154:
  ```text
            return;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 155:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 156:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 157:
  ```text
        addonsContainer.innerHTML = applicable
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 158:
  ```text
            .map((addon) => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 159:
  ```text
                const prechecked = preselectedAddonIds.includes(Number(addon.id));
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 160:
  ```text
                return `
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 161:
  ```text
                <label class="addon-row" data-addon-row="${addon.id}" style="display:flex;align-items:center;gap:12px;padding:8px 0;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 162:
  ```text
                  <input type="checkbox" data-addon-id="${addon.id}" ${
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 163:
  ```text
                      prechecked ? "checked" : ""
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 164:
  ```text
                  } />
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 165:
  ```text
                  <span style="flex:1;">${addon.name}</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 166:
  ```text
                  <span style="font-weight:700;color:#165166;">+₱${Number(addon.price || 0).toLocaleString("en-PH", { minimumFractionDigits: 2 })}</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 167:
  ```text
                </label>`;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 168:
  ```text
            })
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 169:
  ```text
            .join("");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 170:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 11. `getSelectedAddons`

- Signature: `function getSelectedAddons()`
- Source range: lines 172-178

#### Line-by-line

- Line 172:
  ```text
    function getSelectedAddons() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 173:
  ```text
        const checks = addonsContainer.querySelectorAll("input[data-addon-id]:checked");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 174:
  ```text
        const ids = Array.from(checks).map((c) => Number(c.getAttribute("data-addon-id")));
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 175:
  ```text
        return ids
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 176:
  ```text
            .map((id) => addons.find((a) => Number(a.id) === id))
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 177:
  ```text
            .filter(Boolean);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 178:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 12. `loadData`

- Signature: `async function loadData()`
- Source range: lines 180-197

#### Line-by-line

- Line 180:
  ```text
    async function loadData() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 181:
  ```text
        setLoading(true, "Loading package details...");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 182:
  ```text
        try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 183:
  ```text
            const [pkgData, addonData] = await Promise.all([
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 184:
  ```text
                apiRequest("/packages/"),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 185:
  ```text
                apiRequest("/addons/"),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 186:
  ```text
            ]);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 187:
  ```text
            packages = Array.isArray(pkgData) ? pkgData : [];
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 188:
  ```text
            addons = Array.isArray(addonData) ? addonData : [];
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 189:
  ```text
            setPackageOptions();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 190:
  ```text
            activePackage =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 191:
  ```text
                packages.find((p) => Number(p.id) === Number(packageSelect.value)) ||
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 192:
  ```text
                null;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 193:
  ```text
            renderAddonRows();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 194:
  ```text
        } finally {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 195:
  ```text
            setLoading(false);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 196:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 197:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

## File: `electron-app/pages/guest/js/bookingform_packagelist.js`

- Methods documented: 9

### 1. `getGuestContext`

- Signature: `function getGuestContext()`
- Source range: lines 10-16

#### Line-by-line

- Line 10:
  ```text
    function getGuestContext() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 11:
  ```text
        try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 12:
  ```text
            return JSON.parse(sessionStorage.getItem("guestCustomer") || "{}");
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 13:
  ```text
        } catch (_) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 14:
  ```text
            return {};
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 15:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 16:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 2. `apiRequest`

- Signature: `async function apiRequest(path, param)`
- Source range: lines 24-43

#### Line-by-line

- Line 24:
  ```text
    async function apiRequest(path, options = {}) {
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 25:
  ```text
        const response = await fetch(`${API_BASE}${path}`, {
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 26:
  ```text
            headers: { "Content-Type": "application/json" },
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 27:
  ```text
            ...options,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 28:
  ```text
        });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 29:
  ```text
        const text = await response.text();
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 30:
  ```text
        let data = null;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 31:
  ```text
        try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 32:
  ```text
            data = text ? JSON.parse(text) : null;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 33:
  ```text
        } catch (_) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 34:
  ```text
            data = null;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 35:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 36:
  ```text
        if (!response.ok) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 37:
  ```text
            throw new Error(
  ```
  Explanation: Raises an error/exception to signal failure to the caller.
- Line 38:
  ```text
                (data && (data.detail || data.message || data.error)) ||
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 39:
  ```text
                    `HTTP ${response.status}`,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 40:
  ```text
            );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 41:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 42:
  ```text
        return data;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 43:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 3. `ensureLoadingOverlay`

- Signature: `function ensureLoadingOverlay()`
- Source range: lines 45-56

#### Line-by-line

- Line 45:
  ```text
    function ensureLoadingOverlay() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 46:
  ```text
        let el = document.getElementById("guestLoadingOverlay");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 47:
  ```text
        if (el) return el;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 48:
  ```text
        el = document.createElement("div");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 49:
  ```text
        el.id = "guestLoadingOverlay";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 50:
  ```text
        el.style.cssText =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 51:
  ```text
            "position:fixed;inset:0;background:rgba(15,23,42,0.28);display:none;align-items:center;justify-content:center;z-index:9999;";
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 52:
  ```text
        el.innerHTML =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 53:
  ```text
            '<div style="background:#fff;padding:14px 18px;border-radius:12px;font-weight:700;color:#165166;box-shadow:0 8px 24px rgba(0,0,0,0.18);">Loading...</div>';
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 54:
  ```text
        document.body.appendChild(el);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 55:
  ```text
        return el;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 56:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 4. `setLoading`

- Signature: `function setLoading(on, param)`
- Source range: lines 57-62

#### Line-by-line

- Line 57:
  ```text
    function setLoading(on, text = "Loading...") {
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 58:
  ```text
        const overlay = ensureLoadingOverlay();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 59:
  ```text
        const box = overlay.firstElementChild;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 60:
  ```text
        if (box) box.textContent = text;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 61:
  ```text
        overlay.style.display = on ? "flex" : "none";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 62:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 5. `mergeCategories`

- Signature: `function mergeCategories(categories, packages)`
- Source range: lines 65-81

#### Line-by-line

- Line 65:
  ```text
    function mergeCategories(categories, packages) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 66:
  ```text
        const pkgByCategory = new Map();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 67:
  ```text
        (packages || []).forEach((pkg) => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 68:
  ```text
            const k = String(pkg.category || "").trim().toLowerCase();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 69:
  ```text
            if (!k || pkgByCategory.has(k)) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 70:
  ```text
            pkgByCategory.set(k, pkg);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 71:
  ```text
        });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 72:
  ```text
        return (categories || []).map((cat) => {
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 73:
  ```text
            const key = String(cat.name || "").trim().toLowerCase();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 74:
  ```text
            const pkg = pkgByCategory.get(key);
  ```
  Explanation: Triggers an external API/network request as part of this flow.
- Line 75:
  ```text
            return {
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 76:
  ```text
                key,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 77:
  ```text
                name: cat.name,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 78:
  ```text
                image: cat.image_url || pkg?.image_url || "",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 79:
  ```text
            };
  ```
  Explanation: Closes the current block/scope.
- Line 80:
  ```text
        });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 81:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 6. `renderCategoryCards`

- Signature: `function renderCategoryCards(categories)`
- Source range: lines 83-105

#### Line-by-line

- Line 83:
  ```text
    function renderCategoryCards(categories) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 84:
  ```text
        if (!categories.length) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 85:
  ```text
            categoryGrid.innerHTML =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 86:
  ```text
                '<div class="empty-state">No categories available right now.</div>';
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 87:
  ```text
            return;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 88:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 89:
  ```text
        categoryGrid.innerHTML = categories
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 90:
  ```text
            .map(
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 91:
  ```text
                (cat) => `
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 92:
  ```text
                <div class="package-card"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 93:
  ```text
                   style="cursor:pointer;"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 94:
  ```text
                   onclick="setGuestLoading(true,'Opening category...');window.location.href='bookingform_packagelist2.html?category=${encodeURIComponent(cat.name)}'">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 95:
  ```text
                <img src="${cat.image || "https://images.unsplash.com/photo-1520854221256-17451cc331bf?w=1000"}"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 96:
  ```text
                     alt="${cat.name}"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 97:
  ```text
                     class="package-card-image"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 98:
  ```text
                     style="background-color:#f0f4f5;height:220px;object-fit:cover;"/>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 99:
  ```text
                <div class="package-card-content">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 100:
  ```text
                  <div class="package-card-text" style="font-size:18px;line-height:1.35;white-space:normal;">${cat.name.toUpperCase()}</div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 101:
  ```text
                </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 102:
  ```text
              </div>`,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 103:
  ```text
            )
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 104:
  ```text
            .join("");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 105:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 7. `sourceBadge`

- Signature: `function sourceBadge(source)`
- Source range: lines 107-111

#### Line-by-line

- Line 107:
  ```text
    function sourceBadge(source) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 108:
  ```text
        if (source === "customer_booking_history") return "Booking Loyalty";
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 109:
  ```text
        if (source === "popular_fallback") return "Popular Choice";
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 110:
  ```text
        return "Recommended";
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 111:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 8. `renderRecommendationCards`

- Signature: `function renderRecommendationCards(data)`
- Source range: lines 113-166

#### Line-by-line

- Line 113:
  ```text
    function renderRecommendationCards(data) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 114:
  ```text
        if (!recGrid) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 115:
  ```text
        const recs = Array.isArray(data?.recommendations)
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 116:
  ```text
            ? data.recommendations
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 117:
  ```text
            : [];
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 118:
  ```text
        if (!recs.length) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 119:
  ```text
            recGrid.innerHTML =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 120:
  ```text
                '<div class="empty-state">No recommendations available.</div>';
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 121:
  ```text
            return;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 122:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 123:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 124:
  ```text
        recGrid.innerHTML = recs
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 125:
  ```text
            .slice(0, 3)
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 126:
  ```text
            .map((rec) => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 127:
  ```text
                const pkg = rec.package || {};
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 128:
  ```text
                const addons = Array.isArray(rec.addons) ? rec.addons : [];
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 129:
  ```text
                const addonRows = addons
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 130:
  ```text
                    .map(
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 131:
  ```text
                        (a) => `
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 132:
  ```text
                    <div class="addon-item">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 133:
  ```text
                      <span>${a.name}</span>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 134:
  ```text
                      <span>₱${Number(a.price || 0).toLocaleString("en-PH", { minimumFractionDigits: 2 })}</span>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 135:
  ```text
                    </div>`,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 136:
  ```text
                    )
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 137:
  ```text
                    .join("");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 138:
  ```text
                const addonIds = addons
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 139:
  ```text
                    .map((a) => a.id)
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 140:
  ```text
                    .filter((x) => Number.isFinite(Number(x)))
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 141:
  ```text
                    .join(",");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 142:
  ```text
                return `
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 143:
  ```text
                <div class="recommendation-card">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 144:
  ```text
                  <h3>${pkg.name || "Recommended Package"}</h3>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 145:
  ```text
                  <p style="font-size:12px;color:#6b7280;margin-bottom:8px;">${pkg.category || ""} • ${sourceBadge(rec.source)}</p>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 146:
  ```text
                  ${
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 147:
  ```text
                      Array.isArray(pkg.inclusions) && pkg.inclusions.length
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 148:
  ```text
                          ? `<ul>${pkg.inclusions
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 149:
  ```text
                                .slice(0, 4)
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 150:
  ```text
                                .map((inc) => `<li>${inc}</li>`)
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 151:
  ```text
                                .join("")}</ul>`
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 152:
  ```text
                          : "<ul><li>Popular customer pick</li></ul>"
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 153:
  ```text
                  }
  ```
  Explanation: Closes the current block/scope.
- Line 154:
  ```text
                  <h4>Add-ons</h4>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 155:
  ```text
                  ${addonRows || '<div class="addon-item"><span>No add-ons</span><span>₱0.00</span></div>'}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 156:
  ```text
                  <div class="card-footer">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 157:
  ```text
                    <div class="total-price">₱${Number(rec.total_price || 0).toLocaleString("en-PH", { minimumFractionDigits: 2 })}</div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 158:
  ```text
                    <button class="btn btn-primary"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 159:
  ```text
                            onclick="setGuestLoading(true,'Opening booking form...');window.location.href='bookingform_packageinfo.html?packageId=${encodeURIComponent(pkg.id || "")}&addonIds=${encodeURIComponent(addonIds)}'">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 160:
  ```text
                      Book
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 161:
  ```text
                    </button>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 162:
  ```text
                  </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 163:
  ```text
                </div>`;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 164:
  ```text
            })
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 165:
  ```text
            .join("");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 166:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 9. `loadPageData`

- Signature: `async function loadPageData()`
- Source range: lines 168-196

#### Line-by-line

- Line 168:
  ```text
    async function loadPageData() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 169:
  ```text
        try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 170:
  ```text
            setLoading(true, "Loading booking options...");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 171:
  ```text
            categoryGrid.innerHTML =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 172:
  ```text
                '<div class="empty-state">Loading categories...</div>';
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 173:
  ```text
            if (recGrid) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 174:
  ```text
                recGrid.innerHTML =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 175:
  ```text
                    '<div class="empty-state">Loading recommendations...</div>';
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 176:
  ```text
            }
  ```
  Explanation: Closes the current block/scope.
- Line 177:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 178:
  ```text
            const [packages, categories, recs] = await Promise.all([
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 179:
  ```text
                apiRequest("/packages/"),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 180:
  ```text
                apiRequest("/categories/"),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 181:
  ```text
                apiRequest(`/recommendations/${guest.id}/?k=3`),
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 182:
  ```text
            ]);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 183:
  ```text
            renderCategoryCards(mergeCategories(categories, packages));
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 184:
  ```text
            renderRecommendationCards(recs);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 185:
  ```text
        } catch (err) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 186:
  ```text
            console.error("Guest package list load error:", err);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 187:
  ```text
            categoryGrid.innerHTML =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 188:
  ```text
                '<div class="empty-state">Failed to load categories.</div>';
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 189:
  ```text
            if (recGrid) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 190:
  ```text
                recGrid.innerHTML =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 191:
  ```text
                    '<div class="empty-state">Failed to load recommendations.</div>';
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 192:
  ```text
            }
  ```
  Explanation: Closes the current block/scope.
- Line 193:
  ```text
        } finally {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 194:
  ```text
            setLoading(false);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 195:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 196:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

## File: `electron-app/pages/guest/js/bookingform_packagelist2.js`

- Methods documented: 6

### 1. `getGuestContext`

- Signature: `function getGuestContext()`
- Source range: lines 13-19

#### Line-by-line

- Line 13:
  ```text
    function getGuestContext() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 14:
  ```text
        try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 15:
  ```text
            return JSON.parse(sessionStorage.getItem("guestCustomer") || "{}");
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 16:
  ```text
        } catch (_) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 17:
  ```text
            return {};
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 18:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 19:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 2. `apiRequest`

- Signature: `async function apiRequest(path)`
- Source range: lines 25-43

#### Line-by-line

- Line 25:
  ```text
    async function apiRequest(path) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 26:
  ```text
        const response = await fetch(`${API_BASE}${path}`, {
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 27:
  ```text
            headers: { "Content-Type": "application/json" },
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 28:
  ```text
        });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 29:
  ```text
        const text = await response.text();
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 30:
  ```text
        let data = null;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 31:
  ```text
        try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 32:
  ```text
            data = text ? JSON.parse(text) : null;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 33:
  ```text
        } catch (_) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 34:
  ```text
            data = null;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 35:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 36:
  ```text
        if (!response.ok) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 37:
  ```text
            throw new Error(
  ```
  Explanation: Raises an error/exception to signal failure to the caller.
- Line 38:
  ```text
                (data && (data.detail || data.message || data.error)) ||
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 39:
  ```text
                    `HTTP ${response.status}`,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 40:
  ```text
            );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 41:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 42:
  ```text
        return data;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 43:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 3. `ensureLoadingOverlay`

- Signature: `function ensureLoadingOverlay()`
- Source range: lines 45-56

#### Line-by-line

- Line 45:
  ```text
    function ensureLoadingOverlay() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 46:
  ```text
        let el = document.getElementById("guestLoadingOverlay");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 47:
  ```text
        if (el) return el;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 48:
  ```text
        el = document.createElement("div");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 49:
  ```text
        el.id = "guestLoadingOverlay";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 50:
  ```text
        el.style.cssText =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 51:
  ```text
            "position:fixed;inset:0;background:rgba(15,23,42,0.28);display:none;align-items:center;justify-content:center;z-index:9999;";
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 52:
  ```text
        el.innerHTML =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 53:
  ```text
            '<div style="background:#fff;padding:14px 18px;border-radius:12px;font-weight:700;color:#165166;box-shadow:0 8px 24px rgba(0,0,0,0.18);">Loading...</div>';
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 54:
  ```text
        document.body.appendChild(el);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 55:
  ```text
        return el;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 56:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 4. `setLoading`

- Signature: `function setLoading(on, param)`
- Source range: lines 57-62

#### Line-by-line

- Line 57:
  ```text
    function setLoading(on, text = "Loading...") {
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 58:
  ```text
        const overlay = ensureLoadingOverlay();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 59:
  ```text
        const box = overlay.firstElementChild;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 60:
  ```text
        if (box) box.textContent = text;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 61:
  ```text
        overlay.style.display = on ? "flex" : "none";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 62:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 5. `renderPackages`

- Signature: `function renderPackages(packages)`
- Source range: lines 65-103

#### Line-by-line

- Line 65:
  ```text
    function renderPackages(packages) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 66:
  ```text
        if (!packages.length) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 67:
  ```text
            grid.innerHTML =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 68:
  ```text
                '<div class="empty-state">No packages available for this category.</div>';
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 69:
  ```text
            return;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 70:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 71:
  ```text
        grid.innerHTML = packages
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 72:
  ```text
            .map((pkg) => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 73:
  ```text
                const displayPrice =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 74:
  ```text
                    Number(pkg.promo_price || 0) > 0 ? pkg.promo_price : pkg.price;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 75:
  ```text
                return `
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 76:
  ```text
                <div class="package-card">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 77:
  ```text
                  <img src="${pkg.image_url || "https://images.unsplash.com/photo-1554941426-5eb1f0fbc37d?w=1000"}"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 78:
  ```text
                       alt="${pkg.name || "Package"}"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 79:
  ```text
                       class="package-image">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 80:
  ```text
                  <div class="package-content">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 81:
  ```text
                    <div class="package-header">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 82:
  ```text
                      <div class="package-name">${pkg.name || "Package"}</div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 83:
  ```text
                      <div class="package-actions">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 84:
  ```text
                        <button class="btn-book"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 85:
  ```text
                                onclick="setGuestLoading(true,'Opening package details...');window.location.href='bookingform_packageinfo.html?packageId=${encodeURIComponent(pkg.id)}'">Book</button>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 86:
  ```text
                      </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 87:
  ```text
                    </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 88:
  ```text
                    <div class="package-price">₱${Number(displayPrice || 0).toLocaleString("en-PH", { minimumFractionDigits: 2 })}</div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 89:
  ```text
                    <div class="package-pax">${
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 90:
  ```text
                        pkg.max_people
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 91:
  ```text
                            ? `Up to ${pkg.max_people} pax`
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 92:
  ```text
                            : "Studio Session"
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 93:
  ```text
                    }</div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 94:
  ```text
                    <div class="package-details">${
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 95:
  ```text
                        Array.isArray(pkg.inclusions) && pkg.inclusions.length
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 96:
  ```text
                            ? pkg.inclusions.slice(0, 4).join(" • ")
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 97:
  ```text
                            : pkg.notes || "Tap Book to continue"
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 98:
  ```text
                    }</div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 99:
  ```text
                  </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 100:
  ```text
                </div>`;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 101:
  ```text
            })
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 102:
  ```text
            .join("");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 103:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 6. `loadPackages`

- Signature: `async function loadPackages()`
- Source range: lines 105-129

#### Line-by-line

- Line 105:
  ```text
    async function loadPackages() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 106:
  ```text
        try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 107:
  ```text
            setLoading(true, "Loading packages...");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 108:
  ```text
            grid.innerHTML = '<div class="empty-state">Loading packages...</div>';
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 109:
  ```text
            const all = await apiRequest("/packages/");
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 110:
  ```text
            const list = Array.isArray(all) ? all : [];
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 111:
  ```text
            const filtered = category
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 112:
  ```text
                ? list.filter(
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 113:
  ```text
                      (p) =>
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 114:
  ```text
                          String(p.category || "").toLowerCase() ===
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 115:
  ```text
                          category.toLowerCase(),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 116:
  ```text
                  )
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 117:
  ```text
                : list;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 118:
  ```text
            if (titleEl && category) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 119:
  ```text
                titleEl.textContent = `Booking Form - ${category} Packages`;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 120:
  ```text
            }
  ```
  Explanation: Closes the current block/scope.
- Line 121:
  ```text
            renderPackages(filtered);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 122:
  ```text
        } catch (err) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 123:
  ```text
            console.error("Guest category package load error:", err);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 124:
  ```text
            grid.innerHTML =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 125:
  ```text
                '<div class="empty-state">Failed to load packages.</div>';
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 126:
  ```text
        } finally {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 127:
  ```text
            setLoading(false);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 128:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 129:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

## File: `electron-app/pages/guest/js/customer-info.js`

- Methods documented: 9

### 1. `apiRequest`

- Signature: `async function apiRequest(path, param)`
- Source range: lines 9-28

#### Line-by-line

- Line 9:
  ```text
    async function apiRequest(path, options = {}) {
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 10:
  ```text
        const response = await fetch(`${API_BASE}${path}`, {
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 11:
  ```text
            headers: { "Content-Type": "application/json" },
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 12:
  ```text
            ...options,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 13:
  ```text
        });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 14:
  ```text
        const text = await response.text();
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 15:
  ```text
        let data = null;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 16:
  ```text
        try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 17:
  ```text
            data = text ? JSON.parse(text) : null;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 18:
  ```text
        } catch (_) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 19:
  ```text
            data = null;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 20:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 21:
  ```text
        if (!response.ok) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 22:
  ```text
            const detail =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 23:
  ```text
                (data && (data.detail || data.message || data.error)) ||
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 24:
  ```text
                `HTTP ${response.status}`;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 25:
  ```text
            throw new Error(detail);
  ```
  Explanation: Raises an error/exception to signal failure to the caller.
- Line 26:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 27:
  ```text
        return data;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 28:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 2. `normalizeConsent`

- Signature: `function normalizeConsent(checked)`
- Source range: lines 30-32

#### Line-by-line

- Line 30:
  ```text
    function normalizeConsent(checked) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 31:
  ```text
        return checked ? "I Agree" : "I Disagree";
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 32:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 3. `findCustomerByEmail`

- Signature: `async function findCustomerByEmail(email)`
- Source range: lines 34-44

#### Line-by-line

- Line 34:
  ```text
    async function findCustomerByEmail(email) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 35:
  ```text
        const all = await apiRequest("/customers/all/");
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 36:
  ```text
        const list = Array.isArray(all) ? all : all?.results || [];
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 37:
  ```text
        return (
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 38:
  ```text
            list.find(
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 39:
  ```text
                (c) =>
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 40:
  ```text
                    c.email &&
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 41:
  ```text
                    String(c.email).toLowerCase() === String(email).toLowerCase(),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 42:
  ```text
            ) || null
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 43:
  ```text
        );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 44:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 4. `upsertCustomer`

- Signature: `async function upsertCustomer(payload)`
- Source range: lines 46-59

#### Line-by-line

- Line 46:
  ```text
    async function upsertCustomer(payload) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 47:
  ```text
        const existing = await findCustomerByEmail(payload.email);
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 48:
  ```text
        if (existing && existing.id) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 49:
  ```text
            const updated = await apiRequest(`/customers/${existing.id}/`, {
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 50:
  ```text
                method: "PUT",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 51:
  ```text
                body: JSON.stringify(payload),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 52:
  ```text
            });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 53:
  ```text
            return updated;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 54:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 55:
  ```text
        return apiRequest("/customers/", {
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 56:
  ```text
            method: "POST",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 57:
  ```text
            body: JSON.stringify(payload),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 58:
  ```text
        });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 59:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 5. `setSubmitState`

- Signature: `function setSubmitState(isSubmitting)`
- Source range: lines 61-67

#### Line-by-line

- Line 61:
  ```text
    function setSubmitState(isSubmitting) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 62:
  ```text
        const submitBtn = form.querySelector('button[type="submit"]');
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 63:
  ```text
        if (!submitBtn) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 64:
  ```text
        submitBtn.disabled = isSubmitting;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 65:
  ```text
        submitBtn.style.opacity = isSubmitting ? "0.7" : "1";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 66:
  ```text
        submitBtn.textContent = isSubmitting ? "Saving..." : "Next";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 67:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 6. `ensureLoadingOverlay`

- Signature: `function ensureLoadingOverlay()`
- Source range: lines 69-80

#### Line-by-line

- Line 69:
  ```text
    function ensureLoadingOverlay() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 70:
  ```text
        let el = document.getElementById("guestLoadingOverlay");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 71:
  ```text
        if (el) return el;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 72:
  ```text
        el = document.createElement("div");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 73:
  ```text
        el.id = "guestLoadingOverlay";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 74:
  ```text
        el.style.cssText =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 75:
  ```text
            "position:fixed;inset:0;background:rgba(15,23,42,0.28);display:none;align-items:center;justify-content:center;z-index:9999;";
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 76:
  ```text
        el.innerHTML =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 77:
  ```text
            '<div style="background:#fff;padding:14px 18px;border-radius:12px;font-weight:700;color:#165166;box-shadow:0 8px 24px rgba(0,0,0,0.18);">Loading...</div>';
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 78:
  ```text
        document.body.appendChild(el);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 79:
  ```text
        return el;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 80:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 7. `setLoading`

- Signature: `function setLoading(on, param)`
- Source range: lines 82-87

#### Line-by-line

- Line 82:
  ```text
    function setLoading(on, text = "Loading...") {
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 83:
  ```text
        const overlay = ensureLoadingOverlay();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 84:
  ```text
        const box = overlay.firstElementChild;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 85:
  ```text
        if (box) box.textContent = text;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 86:
  ```text
        overlay.style.display = on ? "flex" : "none";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 87:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 8. `ensureToast`

- Signature: `function ensureToast()`
- Source range: lines 90-103

#### Line-by-line

- Line 90:
  ```text
    function ensureToast() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 91:
  ```text
        let el = document.getElementById("guestToast");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 92:
  ```text
        if (el) return el;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 93:
  ```text
        el = document.createElement("div");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 94:
  ```text
        el.id = "guestToast";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 95:
  ```text
        el.style.cssText =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 96:
  ```text
            "position:fixed;bottom:32px;left:50%;transform:translateX(-50%);" +
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 97:
  ```text
            "background:#b91c1c;color:#fff;padding:14px 24px;border-radius:10px;" +
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 98:
  ```text
            "font-weight:700;font-size:14px;z-index:10000;opacity:0;" +
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 99:
  ```text
            "transition:opacity 0.25s;pointer-events:none;text-align:center;" +
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 100:
  ```text
            "max-width:90vw;box-shadow:0 4px 16px rgba(0,0,0,0.25);";
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 101:
  ```text
        document.body.appendChild(el);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 102:
  ```text
        return el;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 103:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 9. `showError`

- Signature: `function showError(message)`
- Source range: lines 105-110

#### Line-by-line

- Line 105:
  ```text
    function showError(message) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 106:
  ```text
        const el = ensureToast();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 107:
  ```text
        el.textContent = message;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 108:
  ```text
        el.style.opacity = "1";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 109:
  ```text
        setTimeout(() => { el.style.opacity = "0"; }, 3500);
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 110:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

## File: `electron-app/scripts/api-client.js`

- Methods documented: 2

### 1. `_request`

- Signature: `async function _request(method, path, param)`
- Source range: lines 9-29

#### Line-by-line

- Line 9:
  ```text
async function _request(method, path, body = null) {
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 10:
  ```text
    const opts = { method, headers: { "Content-Type": "application/json" } };
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 11:
  ```text
    if (body !== null) opts.body = JSON.stringify(body);
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 12:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 13:
  ```text
    const res = await fetch(`${API_BASE}${path}`, opts);
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 14:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 15:
  ```text
    if (!res.ok) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 16:
  ```text
        let detail = res.statusText;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 17:
  ```text
        try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 18:
  ```text
            const err = await res.json();
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 19:
  ```text
            detail = err.detail || err.message || JSON.stringify(err);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 20:
  ```text
        } catch (_) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 21:
  ```text
            try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 22:
  ```text
                const text = await res.text();
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 23:
  ```text
                if (text && text.trim()) detail = text.trim().slice(0, 600);
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 24:
  ```text
            } catch (_) {}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 25:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 26:
  ```text
        throw new Error(`[${method} ${path}] ${res.status}: ${detail}`);
  ```
  Explanation: Raises an error/exception to signal failure to the caller.
- Line 27:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 28:
  ```text
    return res.status === 204 ? null : res.json();
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 29:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 2. `_unwrap`

- Signature: `function _unwrap(data)`
- Source range: lines 32-37

#### Line-by-line

- Line 32:
  ```text
function _unwrap(data) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 33:
  ```text
    if (data && !Array.isArray(data) && Array.isArray(data.results)) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 34:
  ```text
        return data.results;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 35:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 36:
  ```text
    return data;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 37:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

## File: `electron-app/scripts/app.js`

- Methods documented: 7

### 1. `toggleMobileSidebar`

- Signature: `function toggleMobileSidebar(show)`
- Source range: lines 26-36

#### Line-by-line

- Line 26:
  ```text
function toggleMobileSidebar(show) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 27:
  ```text
    const sidebar = document.getElementById("sidebar");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 28:
  ```text
    const overlay = document.getElementById("mobileOverlay");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 29:
  ```text
    if (show) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 30:
  ```text
        sidebar.classList.add("mobile-open");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 31:
  ```text
        overlay.classList.add("show");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 32:
  ```text
    } else {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 33:
  ```text
        sidebar.classList.remove("mobile-open");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 34:
  ```text
        overlay.classList.remove("show");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 35:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 36:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 2. `navigateTo`

- Signature: `function navigateTo(path)`
- Source range: lines 38-40

#### Line-by-line

- Line 38:
  ```text
function navigateTo(path) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 39:
  ```text
    window.location.href = path;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 40:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 3. `openLogoutModal`

- Signature: `function openLogoutModal(e)`
- Source range: lines 42-46

#### Line-by-line

- Line 42:
  ```text
function openLogoutModal(e) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 43:
  ```text
    e.preventDefault();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 44:
  ```text
    document.getElementById("logoutModal").classList.remove("hidden");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 45:
  ```text
    document.getElementById("logoutModal").classList.add("flex");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 46:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 4. `closeLogoutModal`

- Signature: `function closeLogoutModal()`
- Source range: lines 48-51

#### Line-by-line

- Line 48:
  ```text
function closeLogoutModal() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 49:
  ```text
    document.getElementById("logoutModal").classList.add("hidden");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 50:
  ```text
    document.getElementById("logoutModal").classList.remove("flex");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 51:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 5. `confirmLogout`

- Signature: `function confirmLogout()`
- Source range: lines 53-55

#### Line-by-line

- Line 53:
  ```text
function confirmLogout() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 54:
  ```text
    window.location.href = "../../index.html";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 55:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 6. `isDateInRange`

- Signature: `function isDateInRange(dateToCheck, filterType)`
- Source range: lines 58-84

#### Line-by-line

- Line 58:
  ```text
function isDateInRange(dateToCheck, filterType) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 59:
  ```text
    if (!dateToCheck || !filterType) return true;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 60:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 61:
  ```text
    const checkDate = new Date(dateToCheck);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 62:
  ```text
    const today = new Date();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 63:
  ```text
    today.setHours(0, 0, 0, 0);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 64:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 65:
  ```text
    switch (filterType) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 66:
  ```text
        case "Today":
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 67:
  ```text
            const todayEnd = new Date(today);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 68:
  ```text
            todayEnd.setHours(23, 59, 59, 999);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 69:
  ```text
            return checkDate >= today && checkDate <= todayEnd;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 70:
  ```text
        case "Last 7 Days":
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 71:
  ```text
            const sevenDaysAgo = new Date(today);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 72:
  ```text
            sevenDaysAgo.setDate(sevenDaysAgo.getDate() - 7);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 73:
  ```text
            return checkDate >= sevenDaysAgo;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 74:
  ```text
        case "Last 30 Days":
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 75:
  ```text
            const thirtyDaysAgo = new Date(today);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 76:
  ```text
            thirtyDaysAgo.setDate(thirtyDaysAgo.getDate() - 30);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 77:
  ```text
            return checkDate >= thirtyDaysAgo;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 78:
  ```text
        case "This year":
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 79:
  ```text
            const thisYearStart = new Date(today.getFullYear(), 0, 1);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 80:
  ```text
            return checkDate >= thisYearStart;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 81:
  ```text
        default:
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 82:
  ```text
            return true;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 83:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 84:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 7. `getFilteredAndSortedCustomers`

- Signature: `function getFilteredAndSortedCustomers(customers, searchTerm, selectedModified, selectedSort)`
- Source range: lines 87-137

#### Line-by-line

- Line 87:
  ```text
function getFilteredAndSortedCustomers(
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 88:
  ```text
    customers,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 89:
  ```text
    searchTerm,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 90:
  ```text
    selectedModified,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 91:
  ```text
    selectedSort,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 92:
  ```text
) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 93:
  ```text
    // Filter by search term
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 94:
  ```text
    let filtered = customers;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 95:
  ```text
    if (searchTerm.trim()) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 96:
  ```text
        const lowerSearchTerm = searchTerm.toLowerCase();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 97:
  ```text
        filtered = customers.filter(
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 98:
  ```text
            (customer) =>
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 99:
  ```text
                customer.name.toLowerCase().includes(lowerSearchTerm) ||
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 100:
  ```text
                customer.email.toLowerCase().includes(lowerSearchTerm) ||
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 101:
  ```text
                customer.contactNo.toLowerCase().includes(lowerSearchTerm),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 102:
  ```text
        );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 103:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 104:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 105:
  ```text
    // Filter by date range
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 106:
  ```text
    if (selectedModified) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 107:
  ```text
        filtered = filtered.filter((customer) =>
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 108:
  ```text
            isDateInRange(customer.updatedAt, selectedModified),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 109:
  ```text
        );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 110:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 111:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 112:
  ```text
    // Sort
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 113:
  ```text
    const sorted = [...filtered];
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 114:
  ```text
    switch (selectedSort) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 115:
  ```text
        case "name-asc":
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 116:
  ```text
            sorted.sort((a, b) => a.name.localeCompare(b.name));
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 117:
  ```text
            break;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 118:
  ```text
        case "name-desc":
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 119:
  ```text
            sorted.sort((a, b) => b.name.localeCompare(a.name));
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 120:
  ```text
            break;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 121:
  ```text
        case "bookings-asc":
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 122:
  ```text
            sorted.sort((a, b) => a.bookings.length - b.bookings.length);
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 123:
  ```text
            break;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 124:
  ```text
        case "bookings-desc":
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 125:
  ```text
            sorted.sort((a, b) => b.bookings.length - a.bookings.length);
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 126:
  ```text
            break;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 127:
  ```text
        case "id-asc":
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 128:
  ```text
            sorted.sort((a, b) => a.id - b.id);
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 129:
  ```text
            break;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 130:
  ```text
        case "id-desc":
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 131:
  ```text
            sorted.sort((a, b) => b.id - a.id);
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 132:
  ```text
            break;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 133:
  ```text
        default:
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 134:
  ```text
            sorted.sort((a, b) => a.id - b.id);
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 135:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 136:
  ```text
    return sorted;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 137:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

## File: `electron-app/scripts/components.js`

- Methods documented: 1

### 1. `loadComponent`

- Signature: `function loadComponent(elementId, html)`
- Source range: lines 133-138

#### Line-by-line

- Line 133:
  ```text
function loadComponent(elementId, html) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 134:
  ```text
    const element = document.getElementById(elementId);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 135:
  ```text
    if (element) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 136:
  ```text
        element.innerHTML = html;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 137:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 138:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

## File: `electron-app/scripts/customer-api.js`

- Methods documented: 6

### 1. `toggleMobileSidebar`

- Signature: `function toggleMobileSidebar(show)`
- Source range: lines 3-14

#### Line-by-line

- Line 3:
  ```text
function toggleMobileSidebar(show) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 4:
  ```text
    const sidebar = document.getElementById("sidebar");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 5:
  ```text
    const overlay = document.getElementById("mobileOverlay");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 6:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 7:
  ```text
    if (show) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 8:
  ```text
        sidebar.classList.add("mobile-open");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 9:
  ```text
        overlay.classList.add("show");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 10:
  ```text
    } else {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 11:
  ```text
        sidebar.classList.remove("mobile-open");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 12:
  ```text
        overlay.classList.remove("show");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 13:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 14:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 2. `navigateTo`

- Signature: `function navigateTo(section)`
- Source range: lines 16-58

#### Line-by-line

- Line 16:
  ```text
function navigateTo(section) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 17:
  ```text
    // Hide all sections
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 18:
  ```text
    document.querySelectorAll(".section").forEach((s) => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 19:
  ```text
        s.classList.remove("active");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 20:
  ```text
    });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 21:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 22:
  ```text
    // Remove active from all nav items
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 23:
  ```text
    document.querySelectorAll(".nav-item").forEach((item) => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 24:
  ```text
        item.classList.remove("active");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 25:
  ```text
    });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 26:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 27:
  ```text
    // Show selected section
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 28:
  ```text
    const selectedSection = document.getElementById(section);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 29:
  ```text
    if (selectedSection) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 30:
  ```text
        selectedSection.classList.add("active");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 31:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 32:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 33:
  ```text
    // Set active nav item
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 34:
  ```text
    const navItems = document.querySelectorAll(".nav-item");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 35:
  ```text
    navItems.forEach((item, index) => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 36:
  ```text
        if (section === "dashboard" && index === 0) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 37:
  ```text
            item.classList.add("active");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 38:
  ```text
        } else if (section === "customer-data" && index === 1) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 39:
  ```text
            item.classList.add("active");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 40:
  ```text
        } else if (section === "package-list" && index === 2) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 41:
  ```text
            item.classList.add("active");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 42:
  ```text
        } else if (section === "survey-form" && index === 3) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 43:
  ```text
            item.classList.add("active");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 44:
  ```text
        } else if (section === "edit-profile" && index === 5) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 45:
  ```text
            item.classList.add("active");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 46:
  ```text
        } else if (section === "logout" && index === 6) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 47:
  ```text
            item.classList.add("active");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 48:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 49:
  ```text
    });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 50:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 51:
  ```text
    // Close mobile sidebar
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 52:
  ```text
    toggleMobileSidebar(false);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 53:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 54:
  ```text
    // Initialize charts when dashboard is shown
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 55:
  ```text
    if (section === "dashboard") {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 56:
  ```text
        setTimeout(initCharts, 100);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 57:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 58:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 3. `openLogoutModal`

- Signature: `function openLogoutModal(e)`
- Source range: lines 59-63

#### Line-by-line

- Line 59:
  ```text
function openLogoutModal(e) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 60:
  ```text
    e.preventDefault();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 61:
  ```text
    document.getElementById("logoutModal").classList.remove("hidden");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 62:
  ```text
    document.getElementById("logoutModal").classList.add("flex");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 63:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 4. `closeLogoutModal`

- Signature: `function closeLogoutModal()`
- Source range: lines 65-68

#### Line-by-line

- Line 65:
  ```text
function closeLogoutModal() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 66:
  ```text
    document.getElementById("logoutModal").classList.add("hidden");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 67:
  ```text
    document.getElementById("logoutModal").classList.remove("flex");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 68:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 5. `confirmLogout`

- Signature: `function confirmLogout()`
- Source range: lines 70-73

#### Line-by-line

- Line 70:
  ```text
function confirmLogout() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 71:
  ```text
    // ðŸ” CHANGE THIS PATH IF NEEDED
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 72:
  ```text
    window.location.href = "../index.html";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 73:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 6. `normalizeConsent`

- Signature: `function normalizeConsent(value)`
- Source range: lines 75-86

#### Line-by-line

- Line 75:
  ```text
function normalizeConsent(value) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 76:
  ```text
    if (value === true) return "I Agree";
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 77:
  ```text
    if (value === false || value === null || value === undefined) return "I Disagree";
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 78:
  ```text
    const v = String(value).trim().toLowerCase();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 79:
  ```text
    if (["i agree", "agree", "yes", "approved", "true"].includes(v)) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 80:
  ```text
        return "I Agree";
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 81:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 82:
  ```text
    if (["i disagree", "disagree", "no", "not approved", "false"].includes(v)) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 83:
  ```text
        return "I Disagree";
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 84:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 85:
  ```text
    return value;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 86:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

## File: `electron-app/scripts/customer-data.js`

- Methods documented: 20

### 1. `normalizeConsent`

- Signature: `function normalizeConsent(value)`
- Source range: lines 21-33

#### Line-by-line

- Line 21:
  ```text
function normalizeConsent(value) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 22:
  ```text
    if (value === true) return "I Agree";
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 23:
  ```text
    if (value === false || value === null || value === undefined)
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 24:
  ```text
        return "I Disagree";
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 25:
  ```text
    const v = String(value).trim().toLowerCase();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 26:
  ```text
    if (["i agree", "agree", "yes", "approved", "true"].includes(v)) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 27:
  ```text
        return "I Agree";
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 28:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 29:
  ```text
    if (["i disagree", "disagree", "no", "not approved", "false"].includes(v)) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 30:
  ```text
        return "I Disagree";
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 31:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 32:
  ```text
    return value;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 33:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 2. `loadCustomers`

- Signature: `async function loadCustomers()`
- Source range: lines 37-54

#### Line-by-line

- Line 37:
  ```text
async function loadCustomers() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 38:
  ```text
    const overlay = document.getElementById("pageLoadingOverlay");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 39:
  ```text
    if (overlay) overlay.style.display = "flex";
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 40:
  ```text
    try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 41:
  ```text
        const data = await window.apiClient.customers.listAll();
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 42:
  ```text
        pageState.customers = Array.isArray(data) ? data : (data.results ?? []);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 43:
  ```text
        renderTable();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 44:
  ```text
    } catch (err) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 45:
  ```text
        console.error("loadCustomers:", err);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 46:
  ```text
        document.getElementById("tableBody").innerHTML =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 47:
  ```text
            `<div style="padding:20px;color:#c00;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 48:
  ```text
               Could not load customers. Is the Django server running?<br>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 49:
  ```text
               <small>${err.message}</small>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 50:
  ```text
             </div>`;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 51:
  ```text
    } finally {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 52:
  ```text
        if (overlay) overlay.style.display = "none";
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 53:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 54:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 3. `getFilteredAndSortedCustomers`

- Signature: `function getFilteredAndSortedCustomers(customers, searchTerm, modified, sort)`
- Source range: lines 58-99

#### Line-by-line

- Line 58:
  ```text
function getFilteredAndSortedCustomers(customers, searchTerm, modified, sort) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 59:
  ```text
    let list = [...customers];
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 60:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 61:
  ```text
    if (searchTerm) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 62:
  ```text
        const q = searchTerm.toLowerCase();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 63:
  ```text
        list = list.filter(
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 64:
  ```text
            (c) =>
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 65:
  ```text
                (c.name ?? "").toLowerCase().includes(q) ||
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 66:
  ```text
                (c.email ?? "").toLowerCase().includes(q) ||
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 67:
  ```text
                String(c.id).includes(q),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 68:
  ```text
        );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 69:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 70:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 71:
  ```text
    if (modified) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 72:
  ```text
        const now = new Date();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 73:
  ```text
        list = list.filter((c) => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 74:
  ```text
            if (!c.updatedAt) return false;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 75:
  ```text
            const d = new Date(c.updatedAt);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 76:
  ```text
            if (modified === "Today")
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 77:
  ```text
                return d.toDateString() === now.toDateString();
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 78:
  ```text
            if (modified === "Last 7 Days") return (now - d) / 86400000 <= 7;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 79:
  ```text
            if (modified === "Last 30 Days") return (now - d) / 86400000 <= 30;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 80:
  ```text
            if (modified === "This year")
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 81:
  ```text
                return d.getFullYear() === now.getFullYear();
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 82:
  ```text
            return true;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 83:
  ```text
        });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 84:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 85:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 86:
  ```text
    const [field, dir] = (sort ?? "id-asc").split("-");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 87:
  ```text
    const asc = dir !== "desc";
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 88:
  ```text
    list.sort((a, b) => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 89:
  ```text
        let va = field === "bookings" ? (a.bookings ?? 0) : (a[field] ?? "");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 90:
  ```text
        let vb = field === "bookings" ? (b.bookings ?? 0) : (b[field] ?? "");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 91:
  ```text
        if (typeof va === "string") va = va.toLowerCase();
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 92:
  ```text
        if (typeof vb === "string") vb = vb.toLowerCase();
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 93:
  ```text
        if (va < vb) return asc ? -1 : 1;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 94:
  ```text
        if (va > vb) return asc ? 1 : -1;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 95:
  ```text
        return 0;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 96:
  ```text
    });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 97:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 98:
  ```text
    return list;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 99:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 4. `renderTable`

- Signature: `function renderTable()`
- Source range: lines 103-170

#### Line-by-line

- Line 103:
  ```text
function renderTable() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 104:
  ```text
    const sorted = getFilteredAndSortedCustomers(
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 105:
  ```text
        pageState.customers,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 106:
  ```text
        pageState.searchTerm,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 107:
  ```text
        pageState.selectedModified,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 108:
  ```text
        pageState.selectedSort,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 109:
  ```text
    );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 110:
  ```text
    const totalPages = Math.max(1, Math.ceil(sorted.length / ITEMS_PER_PAGE));
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 111:
  ```text
    if (pageState.currentPage > totalPages) pageState.currentPage = totalPages;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 112:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 113:
  ```text
    const start = (pageState.currentPage - 1) * ITEMS_PER_PAGE;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 114:
  ```text
    const visible = sorted.slice(start, start + ITEMS_PER_PAGE);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 115:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 116:
  ```text
    // ── header checkbox ───────────────────────────────────────────────────────
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 117:
  ```text
    const header = document.getElementById("tableHeader");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 118:
  ```text
    const existCb = header.querySelector("input[type='checkbox']");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 119:
  ```text
    if (pageState.viewMode === "select") {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 120:
  ```text
        if (!existCb) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 121:
  ```text
            const wrap = document.createElement("div");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 122:
  ```text
            wrap.style.cssText =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 123:
  ```text
                "width:20px;height:20px;flex-shrink:0;" +
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 124:
  ```text
                "display:flex;align-items:center;justify-content:center;";
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 125:
  ```text
            const cb = document.createElement("input");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 126:
  ```text
            cb.type = "checkbox";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 127:
  ```text
            cb.style.cssText =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 128:
  ```text
                "width:20px;height:20px;border-radius:4px;" +
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 129:
  ```text
                "border:2px solid #37352F;cursor:pointer;";
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 130:
  ```text
            cb.onchange = handleSelectAll;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 131:
  ```text
            wrap.appendChild(cb);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 132:
  ```text
            header.insertBefore(wrap, header.firstChild);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 133:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 134:
  ```text
    } else if (existCb) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 135:
  ```text
        existCb.parentElement.remove();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 136:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 137:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 138:
  ```text
    // ── rows ──────────────────────────────────────────────────────────────────
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 139:
  ```text
    // bookings field from the API is already the integer count
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 140:
  ```text
    document.getElementById("tableBody").innerHTML = visible
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 141:
  ```text
        .map((c) => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 142:
  ```text
            let row = '<div class="table-row">';
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 143:
  ```text
            if (pageState.viewMode === "select") {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 144:
  ```text
                row += `
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 145:
  ```text
              <div style="width:20px;height:20px;flex-shrink:0;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 146:
  ```text
                          display:flex;align-items:center;justify-content:center;">
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 147:
  ```text
                <input type="checkbox"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 148:
  ```text
                       ${pageState.selectedRows.has(c.id) ? "checked" : ""}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 149:
  ```text
                       onchange="handleSelectRow(${c.id})"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 150:
  ```text
                       style="width:20px;height:20px;border-radius:4px;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 151:
  ```text
                              border:2px solid #37352F;cursor:pointer;">
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 152:
  ```text
              </div>`;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 153:
  ```text
            }
  ```
  Explanation: Closes the current block/scope.
- Line 154:
  ```text
            row += `
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 155:
  ```text
          <div style="flex:0.5;">${c.id}</div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 156:
  ```text
          <div style="flex:2;">${c.name ?? ""}</div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 157:
  ```text
          <div style="flex:2;">${c.email ?? ""}</div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 158:
  ```text
          <div style="flex:1;">${c.contactNo ?? ""}</div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 159:
  ```text
          <div style="flex:0.75;">${normalizeConsent(c.consent ?? "")}</div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 160:
  ```text
          <div style="flex:0.75;">${c.bookings ?? 0}</div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 161:
  ```text
          <div style="flex:0.8;color:#006FC9;cursor:pointer;"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 162:
  ```text
               onclick="navigateToCustomerDetails(${c.id})">View Details</div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 163:
  ```text
        </div>`;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 164:
  ```text
            return row;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 165:
  ```text
        })
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 166:
  ```text
        .join("");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 167:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 168:
  ```text
    renderActionButtons();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 169:
  ```text
    renderPagination(totalPages);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 170:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 5. `renderActionButtons`

- Signature: `function renderActionButtons()`
- Source range: lines 172-193

#### Line-by-line

- Line 172:
  ```text
function renderActionButtons() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 173:
  ```text
    const container = document.getElementById("actionButtons");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 174:
  ```text
    if (!container) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 175:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 176:
  ```text
    if (pageState.viewMode === "default") {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 177:
  ```text
        container.innerHTML = `
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 178:
  ```text
          <button class="btn btn-select" onclick="handleSelectMode()">Select</button>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 179:
  ```text
          <button class="btn btn-add"    onclick="handleAddCustomer()">Add</button>`;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 180:
  ```text
    } else if (pageState.viewMode === "select") {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 181:
  ```text
        const none = pageState.selectedRows.size === 0;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 182:
  ```text
        const many = pageState.selectedRows.size !== 1;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 183:
  ```text
        container.innerHTML = `
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 184:
  ```text
          <button class="btn btn-delete" onclick="handleDeleteCustomer()">Delete</button>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 185:
  ```text
          <button class="btn btn-edit ${many ? "disabled" : ""}"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 186:
  ```text
                  onclick="handleEditCustomer()" ${many ? "disabled" : ""}>Edit</button>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 187:
  ```text
          <button class="btn btn-cancel" onclick="handleCancel()">Cancel</button>`;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 188:
  ```text
    } else if (pageState.viewMode === "edit") {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 189:
  ```text
        container.innerHTML = `
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 190:
  ```text
          <button class="btn btn-save"   onclick="handleSave()">Save</button>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 191:
  ```text
          <button class="btn btn-cancel" onclick="handleCancel()">Cancel</button>`;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 192:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 193:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 6. `renderPagination`

- Signature: `function renderPagination(totalPages)`
- Source range: lines 195-210

#### Line-by-line

- Line 195:
  ```text
function renderPagination(totalPages) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 196:
  ```text
    const pg = document.getElementById("pagination");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 197:
  ```text
    if (!pg) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 198:
  ```text
    let opts = "";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 199:
  ```text
    for (let i = 1; i <= totalPages; i++) {
  ```
  Explanation: Iterates over a collection to apply repeated processing.
- Line 200:
  ```text
        opts += `<option value="${i}"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 201:
  ```text
                         ${i === pageState.currentPage ? "selected" : ""}>${i}</option>`;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 202:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 203:
  ```text
    pg.innerHTML = `
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 204:
  ```text
      <span class="pagination-label">Page</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 205:
  ```text
      <select class="pagination-select"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 206:
  ```text
              onchange="pageState.currentPage = parseInt(this.value); renderTable();">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 207:
  ```text
        ${opts}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 208:
  ```text
      </select>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 209:
  ```text
      <span class="pagination-label">of ${totalPages}</span>`;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 210:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 7. `handleSelectAll`

- Signature: `function handleSelectAll()`
- Source range: lines 214-230

#### Line-by-line

- Line 214:
  ```text
function handleSelectAll() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 215:
  ```text
    const sorted = getFilteredAndSortedCustomers(
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 216:
  ```text
        pageState.customers,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 217:
  ```text
        pageState.searchTerm,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 218:
  ```text
        pageState.selectedModified,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 219:
  ```text
        pageState.selectedSort,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 220:
  ```text
    );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 221:
  ```text
    const start = (pageState.currentPage - 1) * ITEMS_PER_PAGE;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 222:
  ```text
    const pageIds = sorted
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 223:
  ```text
        .slice(start, start + ITEMS_PER_PAGE)
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 224:
  ```text
        .map((c) => c.id);
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 225:
  ```text
    pageState.selectedRows.size === pageIds.length
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 226:
  ```text
        ? pageState.selectedRows.clear()
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 227:
  ```text
        : (pageState.selectedRows.clear(),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 228:
  ```text
          pageIds.forEach((id) => pageState.selectedRows.add(id)));
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 229:
  ```text
    renderTable();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 230:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 8. `handleSelectRow`

- Signature: `function handleSelectRow(id)`
- Source range: lines 232-237

#### Line-by-line

- Line 232:
  ```text
function handleSelectRow(id) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 233:
  ```text
    pageState.selectedRows.has(id)
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 234:
  ```text
        ? pageState.selectedRows.delete(id)
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 235:
  ```text
        : pageState.selectedRows.add(id);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 236:
  ```text
    renderTable();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 237:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 9. `handleSelectMode`

- Signature: `function handleSelectMode()`
- Source range: lines 239-243

#### Line-by-line

- Line 239:
  ```text
function handleSelectMode() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 240:
  ```text
    pageState.viewMode = "select";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 241:
  ```text
    pageState.selectedRows.clear();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 242:
  ```text
    renderTable();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 243:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 10. `handleCancel`

- Signature: `function handleCancel()`
- Source range: lines 245-253

#### Line-by-line

- Line 245:
  ```text
function handleCancel() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 246:
  ```text
    pageState.selectedRows.clear();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 247:
  ```text
    pageState.viewMode = "default";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 248:
  ```text
    pageState.showModal = false;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 249:
  ```text
    pageState.editingCustomer = null;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 250:
  ```text
    pageState.deleteConfirmation = false;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 251:
  ```text
    document.getElementById("modalsContainer").innerHTML = "";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 252:
  ```text
    renderTable();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 253:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 11. `handleSave`

- Signature: `function handleSave()`
- Source range: lines 255-257

#### Line-by-line

- Line 255:
  ```text
function handleSave() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 256:
  ```text
    handleCancel();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 257:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 12. `navigateToCustomerDetails`

- Signature: `function navigateToCustomerDetails(id)`
- Source range: lines 259-261

#### Line-by-line

- Line 259:
  ```text
function navigateToCustomerDetails(id) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 260:
  ```text
    window.location.href = `customer-details.html?id=${id}`;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 261:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 13. `handleAddCustomer`

- Signature: `function handleAddCustomer()`
- Source range: lines 265-269

#### Line-by-line

- Line 265:
  ```text
function handleAddCustomer() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 266:
  ```text
    pageState.editingCustomer = null;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 267:
  ```text
    pageState.showModal = true;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 268:
  ```text
    renderCustomerModal();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 269:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 14. `handleEditCustomer`

- Signature: `function handleEditCustomer()`
- Source range: lines 271-280

#### Line-by-line

- Line 271:
  ```text
function handleEditCustomer() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 272:
  ```text
    if (pageState.selectedRows.size !== 1) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 273:
  ```text
    const id = [...pageState.selectedRows][0];
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 274:
  ```text
    const c = pageState.customers.find((x) => x.id === id);
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 275:
  ```text
    if (!c) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 276:
  ```text
    pageState.editingCustomer = JSON.parse(JSON.stringify(c));
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 277:
  ```text
    pageState.viewMode = "edit";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 278:
  ```text
    pageState.showModal = true;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 279:
  ```text
    renderCustomerModal();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 280:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 15. `renderCustomerModal`

- Signature: `function renderCustomerModal()`
- Source range: lines 282-336

#### Line-by-line

- Line 282:
  ```text
function renderCustomerModal() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 283:
  ```text
    const c = pageState.editingCustomer || {};
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 284:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 285:
  ```text
    // Build the modal HTML without any value="" attributes
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 286:
  ```text
    document.getElementById("modalsContainer").innerHTML = `
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 287:
  ```text
      <div class="modal-overlay" id="customerModal"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 288:
  ```text
           onclick="if(event.target.id==='customerModal') closeCustomerModal()">
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 289:
  ```text
        <div class="modal-content">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 290:
  ```text
          <h2 class="modal-title">Customer Info</h2>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 291:
  ```text
          <form onsubmit="handleSaveCustomer(event)"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 292:
  ```text
                style="width:100%;display:flex;flex-direction:column;gap:32px;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 293:
  ```text
            <div class="form-group">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 294:
  ```text
              <label class="form-label">Name</label>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 295:
  ```text
              <input type="text" id="customerName"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 296:
  ```text
                     placeholder="Enter full name"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 297:
  ```text
                     class="form-input" required
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 298:
  ```text
                     autocomplete="off">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 299:
  ```text
            </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 300:
  ```text
            <div class="form-group">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 301:
  ```text
              <label class="form-label">Email</label>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 302:
  ```text
              <input type="email" id="customerEmail"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 303:
  ```text
                     placeholder="Enter email"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 304:
  ```text
                     class="form-input" required
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 305:
  ```text
                     autocomplete="off">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 306:
  ```text
            </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 307:
  ```text
            <div class="form-group">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 308:
  ```text
              <label class="form-label">Contact No.</label>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 309:
  ```text
              <input type="tel" id="customerContactNo"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 310:
  ```text
                     placeholder="Enter contact no."
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 311:
  ```text
                     class="form-input"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 312:
  ```text
                     autocomplete="off">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 313:
  ```text
            </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 314:
  ```text
            <div class="checkbox-wrapper">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 315:
  ```text
              <input type="checkbox" id="customerConsent" class="checkbox">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 316:
  ```text
              <label class="checkbox-label">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 317:
  ```text
                I hereby consent to Heigen Studio releasing my photos on
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 318:
  ```text
                public and social media platforms.
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 319:
  ```text
              </label>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 320:
  ```text
            </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 321:
  ```text
            <div class="modal-actions">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 322:
  ```text
              <button type="button" class="btn-back"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 323:
  ```text
                      onclick="closeCustomerModal()">Back</button>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 324:
  ```text
              <button type="submit" class="btn-next">Save</button>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 325:
  ```text
            </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 326:
  ```text
          </form>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 327:
  ```text
        </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 328:
  ```text
      </div>`;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 329:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 330:
  ```text
    // Set values via .value property AFTER DOM insertion — this is always editable
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 331:
  ```text
    document.getElementById("customerName").value = c.name ?? "";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 332:
  ```text
    document.getElementById("customerEmail").value = c.email ?? "";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 333:
  ```text
    document.getElementById("customerContactNo").value = c.contactNo ?? "";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 334:
  ```text
    document.getElementById("customerConsent").checked =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 335:
  ```text
        normalizeConsent(c.consent) === "I Agree";
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 336:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 16. `handleSaveCustomer`

- Signature: `async function handleSaveCustomer(event)`
- Source range: lines 338-385

#### Line-by-line

- Line 338:
  ```text
async function handleSaveCustomer(event) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 339:
  ```text
    event.preventDefault();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 340:
  ```text
    const payload = {
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 341:
  ```text
        name: document.getElementById("customerName").value.trim(),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 342:
  ```text
        email: document.getElementById("customerEmail").value.trim(),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 343:
  ```text
        contactNo: document.getElementById("customerContactNo").value.trim(),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 344:
  ```text
        consent: document.getElementById("customerConsent").checked
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 345:
  ```text
            ? "I Agree"
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 346:
  ```text
            : "I Disagree",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 347:
  ```text
    };
  ```
  Explanation: Closes the current block/scope.
- Line 348:
  ```text
    const overlay = document.getElementById("pageLoadingOverlay");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 349:
  ```text
    if (overlay) { overlay.style.display = "flex"; overlay.querySelector(".loading-label").textContent = "Saving…"; }
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 350:
  ```text
    try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 351:
  ```text
        if (pageState.editingCustomer) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 352:
  ```text
            const updated = await window.apiClient.customers.update(
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 353:
  ```text
                pageState.editingCustomer.id,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 354:
  ```text
                payload,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 355:
  ```text
            );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 356:
  ```text
            const idx = pageState.customers.findIndex(
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 357:
  ```text
                (c) => c.id === pageState.editingCustomer.id,
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 358:
  ```text
            );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 359:
  ```text
            if (idx !== -1)
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 360:
  ```text
                pageState.customers[idx] = {
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 361:
  ```text
                    ...pageState.customers[idx],
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 362:
  ```text
                    ...updated,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 363:
  ```text
                };
  ```
  Explanation: Closes the current block/scope.
- Line 364:
  ```text
        } else {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 365:
  ```text
            const created = await window.apiClient.customers.create(payload);
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 366:
  ```text
            pageState.customers.push(created);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 367:
  ```text
            const total = Math.ceil(
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 368:
  ```text
                pageState.customers.length / ITEMS_PER_PAGE,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 369:
  ```text
            );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 370:
  ```text
            pageState.currentPage = total;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 371:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 372:
  ```text
    } catch (err) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 373:
  ```text
        console.error("handleSaveCustomer:", err);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 374:
  ```text
        alert("Failed to save customer: " + err.message);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 375:
  ```text
        if (overlay) overlay.style.display = "none";
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 376:
  ```text
        return;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 377:
  ```text
    } finally {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 378:
  ```text
        if (overlay) overlay.style.display = "none";
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 379:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 380:
  ```text
    pageState.viewMode = "default";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 381:
  ```text
    pageState.showModal = false;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 382:
  ```text
    pageState.editingCustomer = null;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 383:
  ```text
    document.getElementById("modalsContainer").innerHTML = "";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 384:
  ```text
    renderTable();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 385:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 17. `closeCustomerModal`

- Signature: `function closeCustomerModal()`
- Source range: lines 387-391

#### Line-by-line

- Line 387:
  ```text
function closeCustomerModal() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 388:
  ```text
    pageState.showModal = false;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 389:
  ```text
    pageState.editingCustomer = null;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 390:
  ```text
    document.getElementById("customerModal")?.remove();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 391:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 18. `handleDeleteCustomer`

- Signature: `function handleDeleteCustomer()`
- Source range: lines 395-414

#### Line-by-line

- Line 395:
  ```text
function handleDeleteCustomer() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 396:
  ```text
    if (!pageState.selectedRows.size) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 397:
  ```text
    const count = pageState.selectedRows.size;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 398:
  ```text
    document.getElementById("modalsContainer").innerHTML = `
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 399:
  ```text
      <div class="confirmation-dialog" id="deleteConfirmModal">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 400:
  ```text
        <div class="confirmation-content">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 401:
  ```text
          <h3 class="confirmation-title">Delete Customers</h3>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 402:
  ```text
          <p class="confirmation-message">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 403:
  ```text
            Delete ${count} ${count === 1 ? "customer" : "customers"}?
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 404:
  ```text
            This cannot be undone.
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 405:
  ```text
          </p>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 406:
  ```text
          <div class="confirmation-actions">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 407:
  ```text
            <button class="btn-confirm-cancel"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 408:
  ```text
                    onclick="cancelDeleteCustomer()">Cancel</button>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 409:
  ```text
            <button class="btn-confirm-delete"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 410:
  ```text
                    onclick="confirmDeleteCustomer()">Delete</button>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 411:
  ```text
          </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 412:
  ```text
        </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 413:
  ```text
      </div>`;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 414:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 19. `confirmDeleteCustomer`

- Signature: `async function confirmDeleteCustomer()`
- Source range: lines 416-440

#### Line-by-line

- Line 416:
  ```text
async function confirmDeleteCustomer() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 417:
  ```text
    const overlay = document.getElementById("pageLoadingOverlay");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 418:
  ```text
    if (overlay) { overlay.style.display = "flex"; overlay.querySelector(".loading-label").textContent = "Deleting…"; }
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 419:
  ```text
    try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 420:
  ```text
        await window.apiClient.customers.bulkDelete(pageState.selectedRows);
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 421:
  ```text
        pageState.customers = pageState.customers.filter(
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 422:
  ```text
            (c) => !pageState.selectedRows.has(c.id),
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 423:
  ```text
        );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 424:
  ```text
    } catch (err) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 425:
  ```text
        console.error("confirmDeleteCustomer:", err);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 426:
  ```text
        alert("Delete failed: " + err.message);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 427:
  ```text
        if (overlay) overlay.style.display = "none";
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 428:
  ```text
        return;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 429:
  ```text
    } finally {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 430:
  ```text
        if (overlay) overlay.style.display = "none";
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 431:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 432:
  ```text
    const newTotal = Math.ceil(pageState.customers.length / ITEMS_PER_PAGE);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 433:
  ```text
    if (pageState.currentPage > newTotal && newTotal > 0)
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 434:
  ```text
        pageState.currentPage = newTotal;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 435:
  ```text
    pageState.selectedRows.clear();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 436:
  ```text
    pageState.viewMode = "default";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 437:
  ```text
    pageState.deleteConfirmation = false;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 438:
  ```text
    document.getElementById("modalsContainer").innerHTML = "";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 439:
  ```text
    renderTable();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 440:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 20. `cancelDeleteCustomer`

- Signature: `function cancelDeleteCustomer()`
- Source range: lines 442-445

#### Line-by-line

- Line 442:
  ```text
function cancelDeleteCustomer() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 443:
  ```text
    pageState.deleteConfirmation = false;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 444:
  ```text
    document.getElementById("modalsContainer").innerHTML = "";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 445:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

## File: `electron-app/scripts/customer-details.js`

- Methods documented: 63

### 1. `ensureRecommendationStyles`

- Signature: `function ensureRecommendationStyles()`
- Source range: lines 4-11

#### Line-by-line

- Line 4:
  ```text
function ensureRecommendationStyles() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 5:
  ```text
    if (!document.querySelector('link[href*="recommendations_styles.css"]')) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 6:
  ```text
        const link = document.createElement("link");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 7:
  ```text
        link.rel = "stylesheet";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 8:
  ```text
        link.href = "../../styles/recommendations_styles.css";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 9:
  ```text
        document.head.appendChild(link);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 10:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 11:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 2. `normalizeConsent`

- Signature: `function normalizeConsent(value)`
- Source range: lines 15-27

#### Line-by-line

- Line 15:
  ```text
function normalizeConsent(value) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 16:
  ```text
    if (value === true) return "I Agree";
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 17:
  ```text
    if (value === false || value === null || value === undefined)
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 18:
  ```text
        return "I Disagree";
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 19:
  ```text
    const v = String(value).trim().toLowerCase();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 20:
  ```text
    if (["i agree", "agree", "yes", "approved", "true"].includes(v)) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 21:
  ```text
        return "I Agree";
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 22:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 23:
  ```text
    if (["i disagree", "disagree", "no", "not approved", "false"].includes(v)) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 24:
  ```text
        return "I Disagree";
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 25:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 26:
  ```text
    return value;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 27:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 3. `getParameterByName`

- Signature: `function getParameterByName(name)`
- Source range: lines 44-49

#### Line-by-line

- Line 44:
  ```text
function getParameterByName(name) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 45:
  ```text
    const r = new RegExp("[?&]" + name + "=([^&#]*)").exec(
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 46:
  ```text
        window.location.href,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 47:
  ```text
    );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 48:
  ```text
    return r ? decodeURIComponent(r[1].replace(/\+/g, " ")) : null;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 49:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 4. `initializeCustomerDetails`

- Signature: `async function initializeCustomerDetails()`
- Source range: lines 51-91

#### Line-by-line

- Line 51:
  ```text
async function initializeCustomerDetails() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 52:
  ```text
    const customerId = parseInt(getParameterByName("id"), 10);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 53:
  ```text
    if (!customerId || isNaN(customerId)) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 54:
  ```text
        document.querySelector(".page-wrapper").innerHTML =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 55:
  ```text
            '<div class="p-10">Customer not found</div>';
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 56:
  ```text
        return;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 57:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 58:
  ```text
    detailsState.customerId = customerId;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 59:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 60:
  ```text
    const overlay = document.getElementById("pageLoadingOverlay");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 61:
  ```text
    if (overlay) overlay.style.display = "flex";
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 62:
  ```text
    try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 63:
  ```text
        const [customer, packages, addons] = await Promise.all([
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 64:
  ```text
            window.apiClient.customers.get(customerId),
  ```
  Explanation: Triggers an external API/network request as part of this flow.
- Line 65:
  ```text
            window.apiClient.packages.list(),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 66:
  ```text
            window.apiClient.addons.list(),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 67:
  ```text
        ]);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 68:
  ```text
        if (!customer) throw new Error("Customer not found");
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 69:
  ```text
        detailsState.customer = customer;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 70:
  ```text
        // Only show confirmed (BOOKED) bookings on the customer profile.
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 71:
  ```text
        // Pending/Ongoing bookings from the kiosk are managed via the
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 72:
  ```text
        // notification panel and must not appear until accepted.
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 73:
  ```text
        if (Array.isArray(customer.bookings)) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 74:
  ```text
            customer.bookings = customer.bookings.filter(
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 75:
  ```text
                (b) => !b.session_status || b.session_status === "BOOKED",
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 76:
  ```text
            );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 77:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 78:
  ```text
        detailsState.packages = Array.isArray(packages) ? packages : [];
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 79:
  ```text
        detailsState.addons = Array.isArray(addons) ? addons : [];
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 80:
  ```text
        ensureRecommendationStyles();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 81:
  ```text
        renderCustomerDetails();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 82:
  ```text
    } catch (err) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 83:
  ```text
        console.error("initializeCustomerDetails:", err);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 84:
  ```text
        document.querySelector(".page-wrapper").innerHTML =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 85:
  ```text
            `<div class="p-10" style="color:#c00;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 86:
  ```text
               Failed to load customer: ${err.message}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 87:
  ```text
             </div>`;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 88:
  ```text
    } finally {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 89:
  ```text
        if (overlay) overlay.style.display = "none";
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 90:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 91:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 5. `renderCustomerDetails`

- Signature: `function renderCustomerDetails()`
- Source range: lines 95-132

#### Line-by-line

- Line 95:
  ```text
function renderCustomerDetails() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 96:
  ```text
    const c = detailsState.customer;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 97:
  ```text
    const bookingCount = Array.isArray(c.bookings) ? c.bookings.length : 0;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 98:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 99:
  ```text
    document.getElementById("customerHeader").textContent =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 100:
  ```text
        `${c.name}'s Details`;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 101:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 102:
  ```text
    document.getElementById("accountInfo").innerHTML = `
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 103:
  ```text
      <div class="info-row">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 104:
  ```text
        <span class="info-label">Full name</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 105:
  ```text
        <span class="info-value">${c.name ?? ""}</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 106:
  ```text
      </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 107:
  ```text
      <div class="info-row">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 108:
  ```text
        <span class="info-label">Consent</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 109:
  ```text
        <span class="info-value">${normalizeConsent(c.consent ?? "")}</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 110:
  ```text
      </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 111:
  ```text
      <div class="info-row">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 112:
  ```text
        <span class="info-label">Email</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 113:
  ```text
        <span class="info-value">${c.email ?? ""}</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 114:
  ```text
      </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 115:
  ```text
      <div class="info-row">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 116:
  ```text
        <span class="info-label">Contact no.</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 117:
  ```text
        <span class="info-value">${c.contactNo ?? ""}</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 118:
  ```text
      </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 119:
  ```text
      <div class="info-row">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 120:
  ```text
        <span class="info-label">Bookings</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 121:
  ```text
        <span class="info-value">${bookingCount}</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 122:
  ```text
      </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 123:
  ```text
      <div class="info-row info-actions">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 124:
  ```text
        <button class="info-link" onclick="toggleBookingHistory()">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 125:
  ```text
          ${detailsState.isHistoryExpanded ? "Collapse history" : "View history"}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 126:
  ```text
        </button>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 127:
  ```text
        <button class="info-link" onclick="handleAddBooking()">Add</button>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 128:
  ```text
      </div>`;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 129:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 130:
  ```text
    renderBookingHistory();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 131:
  ```text
    renderViewButtons();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 132:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 6. `renderBookingHistory`

- Signature: `function renderBookingHistory()`
- Source range: lines 136-210

#### Line-by-line

- Line 136:
  ```text
function renderBookingHistory() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 137:
  ```text
    const container = document.getElementById("bookingHistory");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 138:
  ```text
    const c = detailsState.customer;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 139:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 140:
  ```text
    detailsState.isHistoryExpanded
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 141:
  ```text
        ? container.classList.add("expanded")
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 142:
  ```text
        : container.classList.remove("expanded");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 143:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 144:
  ```text
    const bookings = Array.isArray(c.bookings) ? c.bookings : [];
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 145:
  ```text
    if (!bookings.length) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 146:
  ```text
        container.innerHTML =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 147:
  ```text
            '<div class="flex flex-col gap-2-5">' +
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 148:
  ```text
            '<div class="empty-state">No booking history</div></div>';
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 149:
  ```text
        return;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 150:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 151:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 152:
  ```text
    container.innerHTML =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 153:
  ```text
        '<div class="flex flex-col gap-2-5">' +
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 154:
  ```text
        bookings
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 155:
  ```text
            .map((b) => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 156:
  ```text
                const date = b.date ?? "N/A";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 157:
  ```text
                const total = Number(b.total ?? b.totalPrice ?? 0);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 158:
  ```text
                return `
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 159:
  ```text
              <div class="booking-item">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 160:
  ```text
                <div class="booking-header">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 161:
  ```text
                  <div class="booking-date">${date}</div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 162:
  ```text
                  <div class="booking-field" style="width:150px;min-width:150px;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 163:
  ```text
                    <div class="booking-field-label">Full Name</div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 164:
  ```text
                    <div class="booking-field-value booking-text-truncate">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 165:
  ```text
                      ${b.customer_name ?? c.name ?? ""}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 166:
  ```text
                    </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 167:
  ```text
                  </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 168:
  ```text
                  <div class="booking-field" style="width:140px;min-width:140px;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 169:
  ```text
                    <div class="booking-field-label">Email</div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 170:
  ```text
                    <div class="booking-field-value booking-text-truncate">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 171:
  ```text
                      ${c.email ?? ""}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 172:
  ```text
                    </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 173:
  ```text
                  </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 174:
  ```text
                  <div class="booking-field" style="width:80px;min-width:80px;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 175:
  ```text
                    <div class="booking-field-label">Type</div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 176:
  ```text
                    <div class="booking-field-value">${b.type ?? "Walk-In"}</div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 177:
  ```text
                  </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 178:
  ```text
                  <div class="booking-field" style="width:100px;min-width:100px;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 179:
  ```text
                    <div class="booking-field-label">Package</div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 180:
  ```text
                    <div class="booking-field-value booking-text-truncate">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 181:
  ```text
                      ${b.packageName ?? ""}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 182:
  ```text
                    </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 183:
  ```text
                  </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 184:
  ```text
                  <div class="booking-field" style="width:90px;min-width:90px;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 185:
  ```text
                    <div class="booking-field-label">Total:</div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 186:
  ```text
                    <div class="booking-field-value">₱${total.toFixed(2)}</div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 187:
  ```text
                  </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 188:
  ```text
                  <div class="booking-actions">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 189:
  ```text
                    <button class="booking-action-btn"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 190:
  ```text
                            onclick="handleViewInvoice(${b.id})">Invoice</button>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 191:
  ```text
                    <button class="booking-action-btn"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 192:
  ```text
                            onclick="handleEditBooking(${b.id})">Edit</button>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 193:
  ```text
                  </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 194:
  ```text
                  <button class="booking-delete-btn" style="margin-left:auto;"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 195:
  ```text
                          onclick="handleDeleteBooking(${b.id})" title="Delete">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 196:
  ```text
                    <svg width="16" height="16" viewBox="0 0 16 16" fill="none">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 197:
  ```text
                      <path d="M11.333 2.667V1.333A1.333 1.333 0 0 0 10 0H6a1.333
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 198:
  ```text
                               1.333 0 0 0-1.333 1.333V2.667H1.333V4h1.334V14A2
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 199:
  ```text
                               2 0 0 0 4.667 16h6.666A2 2 0 0 0 13.333 14V4h1.334
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 200:
  ```text
                               V2.667H11.333ZM7.333 11.333H6V7.333h1.333v4ZM10
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 201:
  ```text
                               11.333H8.667V7.333H10v4ZM10 2.667H6V1.333h4v1.334Z"
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 202:
  ```text
                            fill="#7DA3AC"/>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 203:
  ```text
                    </svg>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 204:
  ```text
                  </button>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 205:
  ```text
                </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 206:
  ```text
              </div>`;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 207:
  ```text
            })
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 208:
  ```text
            .join("") +
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 209:
  ```text
        "</div>";
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 210:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 7. `renderViewButtons`

- Signature: `function renderViewButtons()`
- Source range: lines 214-220

#### Line-by-line

- Line 214:
  ```text
function renderViewButtons() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 215:
  ```text
    document.getElementById("viewButtons").innerHTML = `
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 216:
  ```text
      <button class="view-button ${detailsState.activeView === "predict" ? "active" : "inactive"}"
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 217:
  ```text
              onclick="togglePredictView()">Predict Renewal</button>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 218:
  ```text
      <button class="view-button ${detailsState.activeView === "recommendation" ? "active" : "inactive"}"
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 219:
  ```text
              onclick="toggleRecommendationView()">Recommendation</button>`;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 220:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 8. `debugModelMetrics`

- Signature: `function debugModelMetrics(message, param)`
- Source range: lines 236-243

#### Line-by-line

- Line 236:
  ```text
function debugModelMetrics(message, payload = undefined) {
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 237:
  ```text
    if (!MODEL_METRICS_DEBUG) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 238:
  ```text
    if (payload === undefined) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 239:
  ```text
        console.log(`[ModelMetrics] ${message}`);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 240:
  ```text
        return;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 241:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 242:
  ```text
    console.log(`[ModelMetrics] ${message}`, payload);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 243:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 9. `updateLoadingProgress`

- Signature: `function updateLoadingProgress(p)`
- Source range: lines 245-262

#### Line-by-line

- Line 245:
  ```text
function updateLoadingProgress(p) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 246:
  ```text
    loadingProgress = p;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 247:
  ```text
    const bar = document.querySelector(".loading-progress-bar-fill");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 248:
  ```text
    const val = document.querySelector(".loading-progress-value");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 249:
  ```text
    const status = document.querySelector(".loading-progress-status");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 250:
  ```text
    if (bar) bar.style.width = `${p}%`;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 251:
  ```text
    if (val) val.textContent = `${Math.round(p)}%`;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 252:
  ```text
    if (status && p < 100) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 253:
  ```text
        status.textContent =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 254:
  ```text
            p < 30
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 255:
  ```text
                ? "Analysing customer data..."
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 256:
  ```text
                : p < 60
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 257:
  ```text
                  ? "Generating recommendations..."
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 258:
  ```text
                  : p < 90
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 259:
  ```text
                    ? "Fetching package details..."
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 260:
  ```text
                    : "Almost ready...";
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 261:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 262:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 10. `updatePredictionProgress`

- Signature: `function updatePredictionProgress(p)`
- Source range: lines 264-281

#### Line-by-line

- Line 264:
  ```text
function updatePredictionProgress(p) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 265:
  ```text
    predictionProgress = p;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 266:
  ```text
    const bar = document.querySelector(".loading-progress-bar-fill");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 267:
  ```text
    const val = document.querySelector(".loading-progress-value");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 268:
  ```text
    const status = document.querySelector(".loading-progress-status");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 269:
  ```text
    if (bar) bar.style.width = `${p}%`;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 270:
  ```text
    if (val) val.textContent = `${Math.round(p)}%`;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 271:
  ```text
    if (status && p < 100) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 272:
  ```text
        status.textContent =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 273:
  ```text
            p < 30
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 274:
  ```text
                ? "Preparing renewal model..."
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 275:
  ```text
                : p < 60
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 276:
  ```text
                  ? "Analyzing booking history..."
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 277:
  ```text
                  : p < 90
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 278:
  ```text
                    ? "Calculating renewal score..."
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 279:
  ```text
                    : "Almost ready...";
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 280:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 281:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 11. `loadRecommendations`

- Signature: `async function loadRecommendations(customerId)`
- Source range: lines 282-300

#### Line-by-line

- Line 282:
  ```text
async function loadRecommendations(customerId) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 283:
  ```text
    try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 284:
  ```text
        updateLoadingProgress(10);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 285:
  ```text
        const iv = setInterval(() => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 286:
  ```text
            if (loadingProgress < 90)
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 287:
  ```text
                updateLoadingProgress(loadingProgress + 10);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 288:
  ```text
        }, 200);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 289:
  ```text
        const data = await getCustomerRecommendations(customerId, null, 3);
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 290:
  ```text
        clearInterval(iv);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 291:
  ```text
        updateLoadingProgress(100);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 292:
  ```text
        recommendationCache = data;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 293:
  ```text
        console.log("Recommendation Data:", recommendationCache);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 294:
  ```text
        await new Promise((r) => setTimeout(r, 300));
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 295:
  ```text
        return data;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 296:
  ```text
    } catch (e) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 297:
  ```text
        console.error("loadRecommendations:", e);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 298:
  ```text
        return null;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 299:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 300:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 12. `loadRenewalPrediction`

- Signature: `async function loadRenewalPrediction(customerId)`
- Source range: lines 302-318

#### Line-by-line

- Line 302:
  ```text
async function loadRenewalPrediction(customerId) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 303:
  ```text
    try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 304:
  ```text
        updatePredictionProgress(10);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 305:
  ```text
        const iv = setInterval(() => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 306:
  ```text
            if (predictionProgress < 90)
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 307:
  ```text
                updatePredictionProgress(predictionProgress + 10);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 308:
  ```text
        }, 220);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 309:
  ```text
        const data = await window.apiClient.renewal.forCustomer(customerId);
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 310:
  ```text
        clearInterval(iv);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 311:
  ```text
        updatePredictionProgress(100);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 312:
  ```text
        renewalCache = data;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 313:
  ```text
        return data;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 314:
  ```text
    } catch (e) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 315:
  ```text
        console.error("loadRenewalPrediction:", e);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 316:
  ```text
        return null;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 317:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 318:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 13. `loadModelMetrics`

- Signature: `async function loadModelMetrics()`
- Source range: lines 320-379

#### Line-by-line

- Line 320:
  ```text
async function loadModelMetrics() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 321:
  ```text
    if (modelMetricsPromise) return modelMetricsPromise;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 322:
  ```text
    modelMetricsLoadAttempted = true;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 323:
  ```text
    debugModelMetrics("loadModelMetrics:start", {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 324:
  ```text
        activeView: detailsState.activeView,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 325:
  ```text
        customerId: detailsState.customerId,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 326:
  ```text
    });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 327:
  ```text
    modelMetricsPromise = (async () => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 328:
  ```text
        try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 329:
  ```text
            const timeoutPromise = new Promise((_, reject) => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 330:
  ```text
                setTimeout(() => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 331:
  ```text
                    reject(
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 332:
  ```text
                        new Error(
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 333:
  ```text
                            `Model metrics request timed out after ${MODEL_METRICS_TIMEOUT_MS}ms`,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 334:
  ```text
                        ),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 335:
  ```text
                    );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 336:
  ```text
                }, MODEL_METRICS_TIMEOUT_MS);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 337:
  ```text
            });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 338:
  ```text
            const data = await Promise.race([
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 339:
  ```text
                window.apiClient.analytics.modelMetrics(),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 340:
  ```text
                timeoutPromise,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 341:
  ```text
            ]);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 342:
  ```text
            modelMetricsCache = data;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 343:
  ```text
            modelMetricsError = "";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 344:
  ```text
            debugModelMetrics("loadModelMetrics:success", {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 345:
  ```text
                topKeys: Object.keys(data || {}),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 346:
  ```text
                renewalKeys: Object.keys(data?.renewal || {}),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 347:
  ```text
                recommendationKeys: Object.keys(data?.recommendation || {}),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 348:
  ```text
                hasRenewalSavedModel: Boolean(data?.renewal?.saved_model),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 349:
  ```text
                hasRenewalEngineered: Boolean(
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 350:
  ```text
                    data?.renewal?.engineered_baseline,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 351:
  ```text
                ),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 352:
  ```text
                hasRecommendationSummary: Boolean(
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 353:
  ```text
                    data?.recommendation?.summary,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 354:
  ```text
                ),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 355:
  ```text
                hasRecommendationDetails: Boolean(
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 356:
  ```text
                    data?.recommendation?.details?.rows?.length,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 357:
  ```text
                ),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 358:
  ```text
                backendError: data?.error || null,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 359:
  ```text
            });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 360:
  ```text
            return data;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 361:
  ```text
        } catch (e) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 362:
  ```text
            console.error("loadModelMetrics:", e);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 363:
  ```text
            modelMetricsError = String(e?.message || e || "Unknown error");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 364:
  ```text
            debugModelMetrics("loadModelMetrics:error", {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 365:
  ```text
                error: modelMetricsError,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 366:
  ```text
                stack: e?.stack || null,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 367:
  ```text
            });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 368:
  ```text
            return null;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 369:
  ```text
        } finally {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 370:
  ```text
            debugModelMetrics("loadModelMetrics:finally", {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 371:
  ```text
                hasCache: Boolean(modelMetricsCache),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 372:
  ```text
                loadAttempted: modelMetricsLoadAttempted,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 373:
  ```text
                currentError: modelMetricsError || null,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 374:
  ```text
            });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 375:
  ```text
            modelMetricsPromise = null;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 376:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 377:
  ```text
    })();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 378:
  ```text
    return modelMetricsPromise;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 379:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 14. `ensureModelMetricsLoaded`

- Signature: `function ensureModelMetricsLoaded()`
- Source range: lines 381-400

#### Line-by-line

- Line 381:
  ```text
function ensureModelMetricsLoaded() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 382:
  ```text
    debugModelMetrics("ensureModelMetricsLoaded:check", {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 383:
  ```text
        hasCache: Boolean(modelMetricsCache),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 384:
  ```text
        inFlight: Boolean(modelMetricsPromise),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 385:
  ```text
        loadAttempted: modelMetricsLoadAttempted,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 386:
  ```text
        activeView: detailsState.activeView,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 387:
  ```text
    });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 388:
  ```text
    if (modelMetricsCache) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 389:
  ```text
    if (modelMetricsPromise) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 390:
  ```text
    loadModelMetrics().then((data) => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 391:
  ```text
        debugModelMetrics("ensureModelMetricsLoaded:resolved", {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 392:
  ```text
            hasData: Boolean(data),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 393:
  ```text
            hasCache: Boolean(modelMetricsCache),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 394:
  ```text
            error: modelMetricsError || null,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 395:
  ```text
            activeView: detailsState.activeView,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 396:
  ```text
        });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 397:
  ```text
        if (!data) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 398:
  ```text
        updateVisibleMetricsInPlace();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 399:
  ```text
    });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 400:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 15. `updateVisibleMetricsInPlace`

- Signature: `function updateVisibleMetricsInPlace()`
- Source range: lines 402-420

#### Line-by-line

- Line 402:
  ```text
function updateVisibleMetricsInPlace() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 403:
  ```text
    if (detailsState.activeView === "predict") {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 404:
  ```text
        const el = document.getElementById("renewalMetricsContainer");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 405:
  ```text
        if (el) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 406:
  ```text
            el.innerHTML = renderRenewalMetricsCollapsible();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 407:
  ```text
            debugModelMetrics("updateVisibleMetricsInPlace:renewal-updated");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 408:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 409:
  ```text
        return;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 410:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 411:
  ```text
    if (detailsState.activeView === "recommendation") {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 412:
  ```text
        const el = document.getElementById("recommendationMetricsContainer");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 413:
  ```text
        if (el) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 414:
  ```text
            el.innerHTML = renderRecommendationMetricsCollapsible();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 415:
  ```text
            debugModelMetrics(
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 416:
  ```text
                "updateVisibleMetricsInPlace:recommendation-updated",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 417:
  ```text
            );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 418:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 419:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 420:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 16. `resetModelMetrics`

- Signature: `function resetModelMetrics()`
- Source range: lines 422-428

#### Line-by-line

- Line 422:
  ```text
function resetModelMetrics() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 423:
  ```text
    debugModelMetrics("resetModelMetrics:clear-cache");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 424:
  ```text
    modelMetricsCache = null;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 425:
  ```text
    modelMetricsError = "";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 426:
  ```text
    modelMetricsLoadAttempted = false;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 427:
  ```text
    modelMetricsPromise = null;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 428:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 17. `escapeHtml`

- Signature: `function escapeHtml(value)`
- Source range: lines 430-437

#### Line-by-line

- Line 430:
  ```text
function escapeHtml(value) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 431:
  ```text
    return String(value ?? "")
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 432:
  ```text
        .replace(/&/g, "&amp;")
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 433:
  ```text
        .replace(/</g, "&lt;")
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 434:
  ```text
        .replace(/>/g, "&gt;")
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 435:
  ```text
        .replace(/"/g, "&quot;")
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 436:
  ```text
        .replace(/'/g, "&#39;");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 437:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 18. `pct`

- Signature: `function pct(value)`
- Source range: lines 439-442

#### Line-by-line

- Line 439:
  ```text
function pct(value) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 440:
  ```text
    if (!Number.isFinite(Number(value))) return "-";
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 441:
  ```text
    return `${(Number(value) * 100).toFixed(2)}%`;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 442:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 19. `isRatioMetric`

- Signature: `function isRatioMetric(key)`
- Source range: lines 444-456

#### Line-by-line

- Line 444:
  ```text
function isRatioMetric(key) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 445:
  ```text
    return (
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 446:
  ```text
        key.includes("rate") ||
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 447:
  ```text
        key.includes("auc") ||
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 448:
  ```text
        key.includes("accuracy") ||
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 449:
  ```text
        key.includes("precision") ||
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 450:
  ```text
        key.includes("recall") ||
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 451:
  ```text
        key.includes("f1") ||
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 452:
  ```text
        key.includes("mcc") ||
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 453:
  ```text
        key.includes("coverage") ||
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 454:
  ```text
        key.includes("ndcg")
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 455:
  ```text
    );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 456:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 20. `formatMetricLabel`

- Signature: `function formatMetricLabel(key)`
- Source range: lines 458-482

#### Line-by-line

- Line 458:
  ```text
function formatMetricLabel(key) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 459:
  ```text
    const labels = {
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 460:
  ```text
        accuracy: "Accuracy",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 461:
  ```text
        precision: "Precision",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 462:
  ```text
        recall: "Recall",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 463:
  ```text
        f1: "F1 Score",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 464:
  ```text
        mcc: "Matthews CC",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 465:
  ```text
        roc_auc: "ROC-AUC",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 466:
  ```text
        ndcg_at_3: "NDCG@3",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 467:
  ```text
        ndcg_at_5: "NDCG@5",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 468:
  ```text
        ndcg_at_3_std: "NDCG@3 Std Dev",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 469:
  ```text
        ndcg_at_5_std: "NDCG@5 Std Dev",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 470:
  ```text
        hit_rate_at_3: "Hit Rate@3",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 471:
  ```text
        hit_rate_at_5: "Hit Rate@5",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 472:
  ```text
        hit_rate_at_3_percent: "Hit Rate@3 (%)",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 473:
  ```text
        hit_rate_at_5_percent: "Hit Rate@5 (%)",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 474:
  ```text
        user_coverage_at_3: "User Coverage@3",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 475:
  ```text
        user_coverage_at_5: "User Coverage@5",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 476:
  ```text
        users_with_hit_at_3: "Users w/ Hit@3",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 477:
  ```text
        users_with_hit_at_5: "Users w/ Hit@5",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 478:
  ```text
        rows_evaluated: "Users Evaluated",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 479:
  ```text
    };
  ```
  Explanation: Closes the current block/scope.
- Line 480:
  ```text
    if (labels[key]) return labels[key];
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 481:
  ```text
    return key.replace(/_/g, " ").replace(/\b\w/g, (c) => c.toUpperCase());
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 482:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 21. `formatMetricValue`

- Signature: `function formatMetricValue(key, raw)`
- Source range: lines 484-490

#### Line-by-line

- Line 484:
  ```text
function formatMetricValue(key, raw) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 485:
  ```text
    if (typeof raw !== "number") return String(raw ?? "-");
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 486:
  ```text
    if (key.endsWith("_percent")) return `${raw.toFixed(2)}%`;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 487:
  ```text
    if (isRatioMetric(key)) return pct(raw);
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 488:
  ```text
    if (Number.isInteger(raw)) return raw.toLocaleString("en-US");
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 489:
  ```text
    return raw.toLocaleString("en-US", { maximumFractionDigits: 6 });
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 490:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 22. `renderMetricsRows`

- Signature: `function renderMetricsRows(metricMap, param)`
- Source range: lines 492-507

#### Line-by-line

- Line 492:
  ```text
function renderMetricsRows(metricMap, excludedKeys = []) {
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 493:
  ```text
    if (!metricMap || typeof metricMap !== "object") return "";
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 494:
  ```text
    return Object.keys(metricMap)
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 495:
  ```text
        .filter((key) => !excludedKeys.includes(key))
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 496:
  ```text
        .map((key) => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 497:
  ```text
            const raw = metricMap[key];
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 498:
  ```text
            const label = formatMetricLabel(key);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 499:
  ```text
            const value = formatMetricValue(key, raw);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 500:
  ```text
            return `
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 501:
  ```text
              <div class="model-metrics-row">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 502:
  ```text
                <span class="model-metrics-key">${escapeHtml(label)}</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 503:
  ```text
                <span class="model-metrics-value">${escapeHtml(value)}</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 504:
  ```text
              </div>`;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 505:
  ```text
        })
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 506:
  ```text
        .join("");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 507:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 23. `renderRecommendationRowsTable`

- Signature: `function renderRecommendationRowsTable(details)`
- Source range: lines 509-538

#### Line-by-line

- Line 509:
  ```text
function renderRecommendationRowsTable(details) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 510:
  ```text
    const columns = Array.isArray(details?.columns) ? details.columns : [];
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 511:
  ```text
    const rows = Array.isArray(details?.rows) ? details.rows : [];
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 512:
  ```text
    if (!columns.length || !rows.length) return "";
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 513:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 514:
  ```text
    const head = columns.map((col) => `<th>${escapeHtml(col)}</th>`).join("");
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 515:
  ```text
    const body = rows
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 516:
  ```text
        .map((row) => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 517:
  ```text
            const cells = columns
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 518:
  ```text
                .map((col) => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 519:
  ```text
                    const value = row[col];
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 520:
  ```text
                    if (typeof value === "number" && !Number.isInteger(value)) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 521:
  ```text
                        return `<td>${escapeHtml(value.toFixed(6))}</td>`;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 522:
  ```text
                    }
  ```
  Explanation: Closes the current block/scope.
- Line 523:
  ```text
                    return `<td>${escapeHtml(value)}</td>`;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 524:
  ```text
                })
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 525:
  ```text
                .join("");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 526:
  ```text
            return `<tr>${cells}</tr>`;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 527:
  ```text
        })
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 528:
  ```text
        .join("");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 529:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 530:
  ```text
    return `
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 531:
  ```text
      <div class="model-metrics-subtitle">Per-user Evaluation Details (${rows.length})</div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 532:
  ```text
      <div class="model-metrics-table-wrap">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 533:
  ```text
        <table class="model-metrics-table">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 534:
  ```text
          <thead><tr>${head}</tr></thead>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 535:
  ```text
          <tbody>${body}</tbody>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 536:
  ```text
        </table>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 537:
  ```text
      </div>`;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 538:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 24. `renderMetricsCollapsible`

- Signature: `function renderMetricsCollapsible(param)`
- Source range: lines 540-554

#### Line-by-line

- Line 540:
  ```text
function renderMetricsCollapsible({ title, subtitle, content, open = false }) {
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 541:
  ```text
    return `
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 542:
  ```text
      <details class="model-metrics-collapsible" ${open ? "open" : ""}>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 543:
  ```text
        <summary class="model-metrics-summary">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 544:
  ```text
          <div class="model-metrics-summary-main">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 545:
  ```text
            <span class="model-metrics-chevron" aria-hidden="true">▸</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 546:
  ```text
            <span>${escapeHtml(title)}</span>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 547:
  ```text
          </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 548:
  ```text
          <span class="model-metrics-summary-hint">${escapeHtml(subtitle)}</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 549:
  ```text
        </summary>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 550:
  ```text
        <div class="model-metrics-content">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 551:
  ```text
          ${content}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 552:
  ```text
        </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 553:
  ```text
      </details>`;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 554:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 25. `renderRenewalMetricsCollapsible`

- Signature: `function renderRenewalMetricsCollapsible()`
- Source range: lines 556-608

#### Line-by-line

- Line 556:
  ```text
function renderRenewalMetricsCollapsible() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 557:
  ```text
    const saved = modelMetricsCache?.renewal?.saved_model;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 558:
  ```text
    const engineered = modelMetricsCache?.renewal?.engineered_baseline;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 559:
  ```text
    const sourceSaved =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 560:
  ```text
        modelMetricsCache?.renewal?.source_files?.saved_model ||
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 561:
  ```text
        "renewal/outputs/xgboost_saved_model_results.csv";
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 562:
  ```text
    const sourceEng =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 563:
  ```text
        modelMetricsCache?.renewal?.source_files?.engineered_baseline ||
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 564:
  ```text
        "renewal/outputs/xgboost_engineered_results.csv";
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 565:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 566:
  ```text
    const hasMetrics = Boolean(saved || engineered);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 567:
  ```text
    if (!hasMetrics) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 568:
  ```text
        debugModelMetrics("renderRenewalMetricsCollapsible:unavailable", {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 569:
  ```text
            hasCache: Boolean(modelMetricsCache),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 570:
  ```text
            hasRenewal: Boolean(modelMetricsCache?.renewal),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 571:
  ```text
            hasSavedModel: Boolean(saved),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 572:
  ```text
            hasEngineeredBaseline: Boolean(engineered),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 573:
  ```text
            error: modelMetricsError || null,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 574:
  ```text
            sourceFiles: modelMetricsCache?.renewal?.source_files || null,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 575:
  ```text
        });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 576:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 577:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 578:
  ```text
    const content = hasMetrics
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 579:
  ```text
        ? `
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 580:
  ```text
          ${
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 581:
  ```text
              saved
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 582:
  ```text
                  ? `<div class="model-metrics-block">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 583:
  ```text
              <div class="model-metrics-title">Saved Model (${escapeHtml(sourceSaved)})</div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 584:
  ```text
              ${renderMetricsRows(saved)}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 585:
  ```text
            </div>`
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 586:
  ```text
                  : ""
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 587:
  ```text
          }
  ```
  Explanation: Closes the current block/scope.
- Line 588:
  ```text
          ${
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 589:
  ```text
              engineered
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 590:
  ```text
                  ? `<div class="model-metrics-block">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 591:
  ```text
              <div class="model-metrics-title">Engineered Baseline (${escapeHtml(sourceEng)})</div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 592:
  ```text
              ${renderMetricsRows(engineered)}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 593:
  ```text
            </div>`
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 594:
  ```text
                  : ""
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 595:
  ```text
          }`
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 596:
  ```text
        : `
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 597:
  ```text
            <div class="model-metrics-empty">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 598:
  ```text
              Metrics are unavailable right now. Click the refresh button on this card to reload.
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 599:
  ```text
            </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 600:
  ```text
            ${modelMetricsError ? `<div class="model-metrics-error">${escapeHtml(modelMetricsError)}</div>` : ""}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 601:
  ```text
          `;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 602:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 603:
  ```text
    return renderMetricsCollapsible({
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 604:
  ```text
        title: "Model Metrics",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 605:
  ```text
        subtitle: "Click to expand",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 606:
  ```text
        content,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 607:
  ```text
    });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 608:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 26. `renderRecommendationMetricsCollapsible`

- Signature: `function renderRecommendationMetricsCollapsible()`
- Source range: lines 610-706

#### Line-by-line

- Line 610:
  ```text
function renderRecommendationMetricsCollapsible() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 611:
  ```text
    const summary = modelMetricsCache?.recommendation?.summary;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 612:
  ```text
    const details = modelMetricsCache?.recommendation?.details;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 613:
  ```text
    const source =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 614:
  ```text
        modelMetricsCache?.recommendation?.source_files?.evaluation_results ||
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 615:
  ```text
        "recommender/results/evaluation_results.csv";
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 616:
  ```text
    const hasMetrics = Boolean(summary || details);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 617:
  ```text
    const summaryAllZero = Boolean(
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 618:
  ```text
        summary &&
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 619:
  ```text
            Number(summary.ndcg_at_3 || 0) === 0 &&
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 620:
  ```text
            Number(summary.ndcg_at_5 || 0) === 0 &&
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 621:
  ```text
            Number(summary.hit_rate_at_3 || 0) === 0 &&
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 622:
  ```text
            Number(summary.hit_rate_at_5 || 0) === 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 623:
  ```text
    );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 624:
  ```text
    if (!hasMetrics) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 625:
  ```text
        debugModelMetrics(
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 626:
  ```text
            "renderRecommendationMetricsCollapsible:unavailable",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 627:
  ```text
            {
  ```
  Explanation: Opens a new block scope.
- Line 628:
  ```text
                hasCache: Boolean(modelMetricsCache),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 629:
  ```text
                hasRecommendation: Boolean(modelMetricsCache?.recommendation),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 630:
  ```text
                hasSummary: Boolean(summary),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 631:
  ```text
                hasDetails: Boolean(details),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 632:
  ```text
                detailRowCount: details?.rows?.length || 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 633:
  ```text
                error: modelMetricsError || null,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 634:
  ```text
                sourceFiles:
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 635:
  ```text
                    modelMetricsCache?.recommendation?.source_files || null,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 636:
  ```text
            },
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 637:
  ```text
        );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 638:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 639:
  ```text
    const content = summary
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 640:
  ```text
        ? `
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 641:
  ```text
            <div class="model-metrics-actions">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 642:
  ```text
              <button
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 643:
  ```text
                class="model-metrics-recompute-btn"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 644:
  ```text
                onclick="recomputeRecommendationEvaluation()"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 645:
  ```text
                ${modelMetricsRecomputeInFlight ? "disabled" : ""}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 646:
  ```text
              >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 647:
  ```text
                ${
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 648:
  ```text
                    modelMetricsRecomputeInFlight
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 649:
  ```text
                        ? "Recomputing Evaluation..."
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 650:
  ```text
                        : "Recompute Evaluation"
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 651:
  ```text
                }
  ```
  Explanation: Closes the current block/scope.
- Line 652:
  ```text
              </button>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 653:
  ```text
            </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 654:
  ```text
            ${
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 655:
  ```text
                summaryAllZero
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 656:
  ```text
                    ? `<div class="model-metrics-warning">All recommendation evaluation metrics are currently zero in the source file. This usually means the evaluated model produced no hits on the evaluation set.</div>`
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 657:
  ```text
                    : ""
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 658:
  ```text
            }
  ```
  Explanation: Closes the current block/scope.
- Line 659:
  ```text
            <div class="model-metrics-block">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 660:
  ```text
              <div class="model-metrics-title">Evaluation Summary (${escapeHtml(source)})</div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 661:
  ```text
              ${renderMetricsRows(summary)}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 662:
  ```text
            </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 663:
  ```text
            ${
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 664:
  ```text
                Array.isArray(details?.columns)
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 665:
  ```text
                    ? `
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 666:
  ```text
            <div class="model-metrics-block">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 667:
  ```text
              <div class="model-metrics-title">Raw Fields (${details.columns.length})</div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 668:
  ```text
              <div class="model-metrics-fields">${details.columns
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 669:
  ```text
                  .map(
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 670:
  ```text
                      (col) =>
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 671:
  ```text
                          `<span class="model-metrics-field">${escapeHtml(col)}</span>`,
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 672:
  ```text
                  )
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 673:
  ```text
                  .join("")}</div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 674:
  ```text
            </div>`
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 675:
  ```text
                    : ""
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 676:
  ```text
            }
  ```
  Explanation: Closes the current block/scope.
- Line 677:
  ```text
            ${renderRecommendationRowsTable(details)}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 678:
  ```text
          `
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 679:
  ```text
        : `
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 680:
  ```text
            <div class="model-metrics-actions">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 681:
  ```text
              <button
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 682:
  ```text
                class="model-metrics-recompute-btn"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 683:
  ```text
                onclick="recomputeRecommendationEvaluation()"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 684:
  ```text
                ${modelMetricsRecomputeInFlight ? "disabled" : ""}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 685:
  ```text
              >
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 686:
  ```text
                ${
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 687:
  ```text
                    modelMetricsRecomputeInFlight
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 688:
  ```text
                        ? "Recomputing Evaluation..."
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 689:
  ```text
                        : "Recompute Evaluation"
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 690:
  ```text
                }
  ```
  Explanation: Closes the current block/scope.
- Line 691:
  ```text
              </button>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 692:
  ```text
            </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 693:
  ```text
            <div class="model-metrics-empty">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 694:
  ```text
              Metrics are unavailable right now. Click the refresh button on this card to reload.
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 695:
  ```text
            </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 696:
  ```text
            ${modelMetricsError ? `<div class="model-metrics-error">${escapeHtml(modelMetricsError)}</div>` : ""}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 697:
  ```text
          `;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 698:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 699:
  ```text
    return renderMetricsCollapsible({
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 700:
  ```text
        title: "Model Metrics",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 701:
  ```text
        subtitle: hasMetrics
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 702:
  ```text
            ? "Click to expand full evaluation fields"
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 703:
  ```text
            : "Unavailable",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 704:
  ```text
        content,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 705:
  ```text
    });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 706:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 27. `getRenewalBadgeClass`

- Signature: `function getRenewalBadgeClass(band)`
- Source range: lines 708-712

#### Line-by-line

- Line 708:
  ```text
function getRenewalBadgeClass(band) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 709:
  ```text
    if (band === "very_likely" || band === "likely") return "ai-badge";
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 710:
  ```text
    if (band === "very_unlikely") return "popular-badge";
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 711:
  ```text
    return "trending-badge";
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 712:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 28. `renderRenewalPanel`

- Signature: `function renderRenewalPanel(data)`
- Source range: lines 714-838

#### Line-by-line

- Line 714:
  ```text
function renderRenewalPanel(data) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 715:
  ```text
    const {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 716:
  ```text
        customer_name,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 717:
  ```text
        total_bookings,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 718:
  ```text
        booking_frequency,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 719:
  ```text
        preferred_package_type,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 720:
  ```text
        renewal_probability,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 721:
  ```text
        predicted_renewal,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 722:
  ```text
        status_text,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 723:
  ```text
        key_factors,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 724:
  ```text
        avg_booking_value,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 725:
  ```text
        total_spent,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 726:
  ```text
        renewal_band,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 727:
  ```text
        generated_at,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 728:
  ```text
    } = data || {};
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 729:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 730:
  ```text
    const pct = Math.max(
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 731:
  ```text
        0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 732:
  ```text
        Math.min(100, Math.round((renewal_probability || 0) * 100)),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 733:
  ```text
    );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 734:
  ```text
    const factorsHTML = (key_factors || [])
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 735:
  ```text
        .map((f) => `<li class="factor-item">${f}</li>`)
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 736:
  ```text
        .join("");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 737:
  ```text
    const radius = 78;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 738:
  ```text
    const circumference = 2 * Math.PI * radius;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 739:
  ```text
    const dash = (pct / 100) * circumference;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 740:
  ```text
    const frequencyValue = Number(booking_frequency || 0);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 741:
  ```text
    const frequencyLabel = Number.isFinite(frequencyValue)
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 742:
  ```text
        ? `${frequencyValue.toFixed(2)} / year`
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 743:
  ```text
        : "0.00 / year";
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 744:
  ```text
    const preferredPackage =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 745:
  ```text
        preferred_package_type && String(preferred_package_type).trim()
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 746:
  ```text
            ? String(preferred_package_type)
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 747:
  ```text
            : "N/A";
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 748:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 749:
  ```text
    return `
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 750:
  ```text
      <div class="flex gap-5" style="flex-direction:row;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 751:
  ```text
        <div class="recommendation-panel">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 752:
  ```text
          <div class="recommendation-header">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 753:
  ```text
            <div class="recommendation-header-title">RENEWAL PREDICTION</div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 754:
  ```text
            <button class="panel-refresh-btn"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 755:
  ```text
                    onclick="refreshRenewalPanel()"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 756:
  ```text
                    title="Refresh renewal prediction"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 757:
  ```text
                    aria-label="Refresh renewal prediction">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 758:
  ```text
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 759:
  ```text
                <polyline points="23 4 23 10 17 10"></polyline>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 760:
  ```text
                <path d="M20.49 15a9 9 0 1 1-2.12-9.36L23 10"></path>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 761:
  ```text
              </svg>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 762:
  ```text
            </button>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 763:
  ```text
          </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 764:
  ```text
          <div class="recommendation-content">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 765:
  ```text
            <div style="display:flex;justify-content:center;align-items:center;flex-direction:column;margin-bottom:24px;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 766:
  ```text
              <div style="position:relative;width:220px;height:220px;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 767:
  ```text
                <svg width="220" height="220" viewBox="0 0 220 220">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 768:
  ```text
                  <circle cx="110" cy="110" r="${radius}" fill="none" stroke="#E5EDF0" stroke-width="18"></circle>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 769:
  ```text
                  <circle cx="110" cy="110" r="${radius}" fill="none" stroke="#165166" stroke-width="18"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 770:
  ```text
                    stroke-linecap="round" transform="rotate(-90 110 110)"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 771:
  ```text
                    stroke-dasharray="${dash} ${circumference - dash}"></circle>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 772:
  ```text
                </svg>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 773:
  ```text
                <div style="position:absolute;inset:0;display:flex;flex-direction:column;justify-content:center;align-items:center;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 774:
  ```text
                  <div style="font-size:44px;font-weight:800;color:#165166;line-height:1;">${pct}%</div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 775:
  ```text
                  <div style="font-size:12px;color:#4F6E79;font-weight:700;margin-top:4px;">Renewal Score</div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 776:
  ```text
                </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 777:
  ```text
              </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 778:
  ```text
              <div style="margin-top:12px;text-align:center;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 779:
  ```text
                <div class="user-name">${customer_name ?? detailsState.customer?.name ?? "Customer"}</div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 780:
  ```text
                <div class="user-bookings">${total_bookings ?? 0} booking(s)</div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 781:
  ```text
                <span class="source-badge ${getRenewalBadgeClass(renewal_band)}">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 782:
  ```text
                  ${predicted_renewal ? "Likely Renew" : "Needs Follow-up"}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 783:
  ```text
                </span>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 784:
  ```text
              </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 785:
  ```text
              <div style="margin-top:8px;font-size:13px;color:#616161;text-align:center;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 786:
  ```text
                ${status_text || "Renewal status"}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 787:
  ```text
              </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 788:
  ```text
            </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 789:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 790:
  ```text
            <div style="margin-top:24px;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 791:
  ```text
              <h3 class="section-title">Renewal Insights</h3>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 792:
  ```text
              <p class="section-subtitle">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 793:
  ```text
                Dynamic prediction from current renewal profile and booking behavior.
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 794:
  ```text
              </p>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 795:
  ```text
            </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 796:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 797:
  ```text
            <div class="packages-grid" style="grid-template-columns:repeat(auto-fit,minmax(260px,1fr));">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 798:
  ```text
              <div class="package-card" style="min-height:unset;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 799:
  ```text
                <div class="package-card-header"><h3 class="package-name">Total Bookings</h3></div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 800:
  ```text
                <div class="package-card-body"><div class="price-value">${Number(total_bookings || 0)}</div></div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 801:
  ```text
              </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 802:
  ```text
              <div class="package-card" style="min-height:unset;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 803:
  ```text
                <div class="package-card-header"><h3 class="package-name">Booking Frequency</h3></div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 804:
  ```text
                <div class="package-card-body"><div class="price-value">${frequencyLabel}</div></div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 805:
  ```text
              </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 806:
  ```text
              <div class="package-card" style="min-height:unset;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 807:
  ```text
                <div class="package-card-header"><h3 class="package-name">Preferred Package Type</h3></div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 808:
  ```text
                <div class="package-card-body"><div class="price-value">${preferredPackage}</div></div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 809:
  ```text
              </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 810:
  ```text
              <div class="package-card" style="min-height:unset;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 811:
  ```text
                <div class="package-card-header"><h3 class="package-name">Average Booking Value</h3></div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 812:
  ```text
                <div class="package-card-body"><div class="price-value">${formatPrice(avg_booking_value || 0)}</div></div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 813:
  ```text
              </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 814:
  ```text
              <div class="package-card" style="min-height:unset;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 815:
  ```text
                <div class="package-card-header"><h3 class="package-name">Total Spent</h3></div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 816:
  ```text
                <div class="package-card-body"><div class="price-value">${formatPrice(total_spent || 0)}</div></div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 817:
  ```text
              </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 818:
  ```text
            </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 819:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 820:
  ```text
            <div class="key-factors" style="margin-top:24px;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 821:
  ```text
              <div class="factors-list">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 822:
  ```text
                <h3 class="factors-title">Key Factors Considered</h3>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 823:
  ```text
                <ul class="factors-items">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 824:
  ```text
                  ${factorsHTML || "<li class='factor-item'>Insufficient data</li>"}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 825:
  ```text
                </ul>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 826:
  ```text
              </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 827:
  ```text
              <div class="date-reference">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 828:
  ```text
                <span class="date-label">Generated</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 829:
  ```text
                <span class="date-value">${generated_at ? new Date(generated_at).toLocaleString() : "-"}</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 830:
  ```text
              </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 831:
  ```text
            </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 832:
  ```text
            <div id="renewalMetricsContainer">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 833:
  ```text
              ${renderRenewalMetricsCollapsible()}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 834:
  ```text
            </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 835:
  ```text
          </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 836:
  ```text
        </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 837:
  ```text
      </div>`;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 838:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 29. `renderPanels`

- Signature: `async function renderPanels()`
- Source range: lines 840-1024

#### Line-by-line

- Line 840:
  ```text
async function renderPanels() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 841:
  ```text
    const container = document.getElementById("panelsContainer");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 842:
  ```text
    debugModelMetrics("renderPanels:start", {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 843:
  ```text
        activeView: detailsState.activeView,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 844:
  ```text
        hasCache: Boolean(modelMetricsCache),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 845:
  ```text
        loadAttempted: modelMetricsLoadAttempted,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 846:
  ```text
        modelMetricsError: modelMetricsError || null,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 847:
  ```text
    });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 848:
  ```text
    ensureModelMetricsLoaded();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 849:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 850:
  ```text
    if (detailsState.activeView === "predict") {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 851:
  ```text
        const needsLoad =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 852:
  ```text
            !renewalCache ||
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 853:
  ```text
            renewalCache.customer_id !== detailsState.customer.id;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 854:
  ```text
        if (needsLoad) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 855:
  ```text
            predictionProgress = 0;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 856:
  ```text
            container.innerHTML = `
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 857:
  ```text
              <div class="flex gap-5" style="flex-direction:row;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 858:
  ```text
                <div class="recommendation-panel">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 859:
  ```text
                  <div class="recommendation-header">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 860:
  ```text
                    <div class="recommendation-header-title">RENEWAL PREDICTION</div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 861:
  ```text
                    <button class="panel-refresh-btn"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 862:
  ```text
                            onclick="refreshRenewalPanel()"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 863:
  ```text
                            title="Refresh renewal prediction"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 864:
  ```text
                            aria-label="Refresh renewal prediction">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 865:
  ```text
                      <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 866:
  ```text
                        <polyline points="23 4 23 10 17 10"></polyline>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 867:
  ```text
                        <path d="M20.49 15a9 9 0 1 1-2.12-9.36L23 10"></path>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 868:
  ```text
                      </svg>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 869:
  ```text
                    </button>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 870:
  ```text
                  </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 871:
  ```text
                  <div class="recommendation-content">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 872:
  ```text
                    <div class="loading-container">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 873:
  ```text
                      <div class="loading-progress-item">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 874:
  ```text
                        <div class="loading-progress-badge">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 875:
  ```text
                          <svg width="36" height="36" viewBox="0 0 36 36" fill="none">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 876:
  ```text
                            <circle cx="18" cy="18" r="16" stroke="white"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 877:
  ```text
                                    stroke-width="2" fill="none" opacity="0.3"/>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 878:
  ```text
                            <path d="M18 2 A16 16 0 0 1 34 18" stroke="white"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 879:
  ```text
                                  stroke-width="2" fill="none" stroke-linecap="round">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 880:
  ```text
                              <animateTransform attributeName="transform" type="rotate"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 881:
  ```text
                                from="0 18 18" to="360 18 18" dur="1s"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 882:
  ```text
                                repeatCount="indefinite"/>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 883:
  ```text
                            </path>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 884:
  ```text
                          </svg>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 885:
  ```text
                        </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 886:
  ```text
                        <div class="loading-progress-info">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 887:
  ```text
                          <div class="loading-progress-percentage">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 888:
  ```text
                            <span class="loading-progress-value">0%</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 889:
  ```text
                            <span class="loading-progress-status">Preparing renewal model...</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 890:
  ```text
                          </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 891:
  ```text
                          <div class="loading-progress-bar">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 892:
  ```text
                            <div class="loading-progress-bar-fill"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 893:
  ```text
                                 style="width:0%"></div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 894:
  ```text
                          </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 895:
  ```text
                        </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 896:
  ```text
                      </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 897:
  ```text
                    </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 898:
  ```text
                  </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 899:
  ```text
                </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 900:
  ```text
              </div>`;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 901:
  ```text
            const data = await loadRenewalPrediction(detailsState.customer.id);
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 902:
  ```text
            if (!data) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 903:
  ```text
                container.innerHTML = `
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 904:
  ```text
                  <div class="flex gap-5" style="flex-direction:row;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 905:
  ```text
                    <div class="recommendation-panel">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 906:
  ```text
                      <div class="recommendation-header">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 907:
  ```text
                        <div class="recommendation-header-title">RENEWAL PREDICTION</div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 908:
  ```text
                        <button class="panel-refresh-btn"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 909:
  ```text
                                onclick="refreshRenewalPanel()"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 910:
  ```text
                                title="Refresh renewal prediction"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 911:
  ```text
                                aria-label="Refresh renewal prediction">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 912:
  ```text
                          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 913:
  ```text
                            <polyline points="23 4 23 10 17 10"></polyline>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 914:
  ```text
                            <path d="M20.49 15a9 9 0 1 1-2.12-9.36L23 10"></path>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 915:
  ```text
                          </svg>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 916:
  ```text
                        </button>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 917:
  ```text
                      </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 918:
  ```text
                      <div class="recommendation-content">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 919:
  ```text
                        <div class="error-container">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 920:
  ```text
                          <div class="error-icon">⚠️</div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 921:
  ```text
                          <p class="error-message">Failed to load renewal prediction. Please try again.</p>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 922:
  ```text
                          <button class="retry-button" onclick="renderPanels()">Retry</button>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 923:
  ```text
                        </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 924:
  ```text
                      </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 925:
  ```text
                    </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 926:
  ```text
                  </div>`;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 927:
  ```text
                return;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 928:
  ```text
            }
  ```
  Explanation: Closes the current block/scope.
- Line 929:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 930:
  ```text
        container.innerHTML = renderRenewalPanel(renewalCache);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 931:
  ```text
        return;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 932:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 933:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 934:
  ```text
    if (detailsState.activeView === "recommendation") {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 935:
  ```text
        const needsLoad =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 936:
  ```text
            !recommendationCache ||
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 937:
  ```text
            recommendationCache.customer_id !== detailsState.customer.id;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 938:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 939:
  ```text
        if (needsLoad) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 940:
  ```text
            loadingProgress = 0;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 941:
  ```text
            container.innerHTML = `
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 942:
  ```text
              <div class="flex gap-5" style="flex-direction:row;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 943:
  ```text
                <div class="recommendation-panel">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 944:
  ```text
                  <div class="recommendation-header">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 945:
  ```text
                    <div class="recommendation-header-title">RECOMMENDATION RESULT</div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 946:
  ```text
                    <button class="panel-refresh-btn"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 947:
  ```text
                            onclick="refreshRecommendationPanel()"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 948:
  ```text
                            title="Refresh recommendations"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 949:
  ```text
                            aria-label="Refresh recommendations">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 950:
  ```text
                      <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 951:
  ```text
                        <polyline points="23 4 23 10 17 10"></polyline>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 952:
  ```text
                        <path d="M20.49 15a9 9 0 1 1-2.12-9.36L23 10"></path>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 953:
  ```text
                      </svg>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 954:
  ```text
                    </button>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 955:
  ```text
                  </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 956:
  ```text
                  <div class="recommendation-content">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 957:
  ```text
                    <div class="loading-container">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 958:
  ```text
                      <div class="loading-progress-item">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 959:
  ```text
                        <div class="loading-progress-badge">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 960:
  ```text
                          <svg width="36" height="36" viewBox="0 0 36 36" fill="none">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 961:
  ```text
                            <circle cx="18" cy="18" r="16" stroke="white"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 962:
  ```text
                                    stroke-width="2" fill="none" opacity="0.3"/>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 963:
  ```text
                            <path d="M18 2 A16 16 0 0 1 34 18" stroke="white"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 964:
  ```text
                                  stroke-width="2" fill="none" stroke-linecap="round">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 965:
  ```text
                              <animateTransform attributeName="transform" type="rotate"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 966:
  ```text
                                from="0 18 18" to="360 18 18" dur="1s"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 967:
  ```text
                                repeatCount="indefinite"/>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 968:
  ```text
                            </path>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 969:
  ```text
                          </svg>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 970:
  ```text
                        </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 971:
  ```text
                        <div class="loading-progress-info">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 972:
  ```text
                          <div class="loading-progress-percentage">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 973:
  ```text
                            <span class="loading-progress-value">0%</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 974:
  ```text
                            <span class="loading-progress-status">Starting…</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 975:
  ```text
                          </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 976:
  ```text
                          <div class="loading-progress-bar">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 977:
  ```text
                            <div class="loading-progress-bar-fill"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 978:
  ```text
                                 style="width:0%"></div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 979:
  ```text
                          </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 980:
  ```text
                        </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 981:
  ```text
                      </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 982:
  ```text
                    </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 983:
  ```text
                  </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 984:
  ```text
                </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 985:
  ```text
              </div>`;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 986:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 987:
  ```text
            const data = await loadRecommendations(detailsState.customer.id);
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 988:
  ```text
            if (!data) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 989:
  ```text
                container.innerHTML = `
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 990:
  ```text
                  <div class="flex gap-5" style="flex-direction:row;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 991:
  ```text
                    <div class="recommendation-panel">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 992:
  ```text
                      <div class="recommendation-header">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 993:
  ```text
                        <div class="recommendation-header-title">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 994:
  ```text
                          RECOMMENDATION RESULT
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 995:
  ```text
                        </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 996:
  ```text
                        <button class="panel-refresh-btn"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 997:
  ```text
                                onclick="refreshRecommendationPanel()"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 998:
  ```text
                                title="Refresh recommendations"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 999:
  ```text
                                aria-label="Refresh recommendations">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1000:
  ```text
                          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1001:
  ```text
                            <polyline points="23 4 23 10 17 10"></polyline>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1002:
  ```text
                            <path d="M20.49 15a9 9 0 1 1-2.12-9.36L23 10"></path>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1003:
  ```text
                          </svg>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1004:
  ```text
                        </button>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1005:
  ```text
                      </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1006:
  ```text
                      <div class="recommendation-content">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1007:
  ```text
                        <div class="error-container">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1008:
  ```text
                          <div class="error-icon">⚠️</div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1009:
  ```text
                          <p class="error-message">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1010:
  ```text
                            Failed to load recommendations. Please try again.
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1011:
  ```text
                          </p>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1012:
  ```text
                          <button class="retry-button"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1013:
  ```text
                                  onclick="renderPanels()">Retry</button>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1014:
  ```text
                        </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1015:
  ```text
                      </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1016:
  ```text
                    </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1017:
  ```text
                  </div>`;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1018:
  ```text
                return;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 1019:
  ```text
            }
  ```
  Explanation: Closes the current block/scope.
- Line 1020:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 1021:
  ```text
        console.log("Recommendation Data:", recommendationCache);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1022:
  ```text
        container.innerHTML = renderRecommendationPanel(recommendationCache);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1023:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 1024:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 30. `getSourceBadge`

- Signature: `function getSourceBadge(source)`
- Source range: lines 1026-1041

#### Line-by-line

- Line 1026:
  ```text
function getSourceBadge(source) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1027:
  ```text
    const map = {
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1028:
  ```text
        customer_booking_history:
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1029:
  ```text
            '<span class="source-badge ai-badge">Booking Loyalty</span>',
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1030:
  ```text
        loader: '<span class="source-badge ai-badge">AI Recommended</span>',
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1031:
  ```text
        collaborative_filtering:
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1032:
  ```text
            '<span class="source-badge ai-badge">AI Recommended</span>',
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1033:
  ```text
        popular_package_monthly:
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1034:
  ```text
            '<span class="source-badge trending-badge">Trending</span>',
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1035:
  ```text
        popular_package_overall:
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1036:
  ```text
            '<span class="source-badge popular-badge">Popular</span>',
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1037:
  ```text
        popular_fallback:
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1038:
  ```text
            '<span class="source-badge popular-badge">Popular Choice</span>',
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1039:
  ```text
    };
  ```
  Explanation: Closes the current block/scope.
- Line 1040:
  ```text
    return map[source] ?? '<span class="source-badge">Recommended</span>';
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 1041:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 31. `renderRecommendationPanel`

- Signature: `function renderRecommendationPanel(data)`
- Source range: lines 1043-1271

#### Line-by-line

- Line 1043:
  ```text
function renderRecommendationPanel(data) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1044:
  ```text
    const {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1045:
  ```text
        recommendations,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1046:
  ```text
        key_factors,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1047:
  ```text
        customer_name,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1048:
  ```text
        customer_email,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1049:
  ```text
        target_date,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1050:
  ```text
        total_bookings,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1051:
  ```text
    } = data;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1052:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 1053:
  ```text
    const cardsHTML = (recommendations ?? [])
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1054:
  ```text
        .map((rec) => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1055:
  ```text
            const {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1056:
  ```text
                package: pkg,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1057:
  ```text
                addons,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1058:
  ```text
                total_price,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1059:
  ```text
                base_price,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1060:
  ```text
                source,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1061:
  ```text
            } = rec;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1062:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 1063:
  ```text
            const inclusionsHTML = pkg.inclusions?.length
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1064:
  ```text
                ? `<div class="package-inclusions">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1065:
  ```text
                 <h4 class="inclusions-title">Package Inclusions:</h4>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1066:
  ```text
                 <ul class="inclusions-list">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1067:
  ```text
                   ${pkg.inclusions.map((i) => `<li>${i}</li>`).join("")}
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1068:
  ```text
                 </ul>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1069:
  ```text
               </div>`
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1070:
  ```text
                : "";
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1071:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 1072:
  ```text
            const portraitsHTML = pkg.included_portraits?.length
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1073:
  ```text
                ? `<div class="package-portraits">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1074:
  ```text
                 <h4 class="portraits-title">Included Portraits:</h4>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1075:
  ```text
                 <ul class="portraits-list">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1076:
  ```text
                   ${pkg.included_portraits.map((i) => `<li>${i}</li>`).join("")}
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1077:
  ```text
                 </ul>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1078:
  ```text
               </div>`
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1079:
  ```text
                : "";
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1080:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 1081:
  ```text
            const freebiesHTML = pkg.freebies?.length
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1082:
  ```text
                ? `<div class="package-freebies">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1083:
  ```text
                 <h4 class="freebies-title">Freebies:</h4>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1084:
  ```text
                 <ul class="freebies-list">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1085:
  ```text
                   ${pkg.freebies.map((i) => `<li>${i}</li>`).join("")}
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1086:
  ```text
                 </ul>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1087:
  ```text
               </div>`
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1088:
  ```text
                : "";
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1089:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 1090:
  ```text
            const addonsHTML = addons?.length
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1091:
  ```text
                ? `<div class="package-addons-section">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1092:
  ```text
                 <h4 class="addons-section-title">Recommended Add-ons:</h4>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1093:
  ```text
                 ${addons
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1094:
  ```text
                     .map(
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1095:
  ```text
                         (a) => `
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1096:
  ```text
                   <div class="addon-item">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1097:
  ```text
                     <div class="addon-header">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1098:
  ```text
                       <span class="addon-name">${a.name}</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1099:
  ```text
                       <span class="addon-price">${formatPrice(a.price)}</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1100:
  ```text
                     </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1101:
  ```text
                     ${
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1102:
  ```text
                         a.additional_info
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1103:
  ```text
                             ? `<p class="addon-info">${a.additional_info}</p>`
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1104:
  ```text
                             : ""
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1105:
  ```text
                     }
  ```
  Explanation: Closes the current block/scope.
- Line 1106:
  ```text
                     ${
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1107:
  ```text
                         a.applies_to
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1108:
  ```text
                             ? `<p class="addon-applies">Applies to: ${a.applies_to}</p>`
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1109:
  ```text
                             : ""
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1110:
  ```text
                     }
  ```
  Explanation: Closes the current block/scope.
- Line 1111:
  ```text
                   </div>`,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1112:
  ```text
                     )
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1113:
  ```text
                     .join("")}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1114:
  ```text
               </div>`
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1115:
  ```text
                : "";
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1116:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 1117:
  ```text
            const promoBadge =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1118:
  ```text
                pkg.promo_price && pkg.promo_price < pkg.price
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1119:
  ```text
                    ? `<div class="promo-badge">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1120:
  ```text
                 <span class="promo-text">PROMO</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1121:
  ```text
                 <span class="original-price">${formatPrice(pkg.price)}</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1122:
  ```text
               </div>`
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1123:
  ```text
                    : "";
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1124:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 1125:
  ```text
            // Safe serialisation for onclick attributes
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 1126:
  ```text
            const addonIdsJson = JSON.stringify(
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1127:
  ```text
                (addons ?? []).map((a) => a.id),
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1128:
  ```text
            ).replace(/"/g, "&quot;");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1129:
  ```text
            const safeName = (pkg.name ?? "").replace(/'/g, "\\'");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1130:
  ```text
            const numericBasePrice = Number(base_price ?? pkg.promo_price ?? pkg.price ?? 0);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1131:
  ```text
            const numericTotalPrice = Number(total_price ?? 0);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1132:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 1133:
  ```text
            return `
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 1134:
  ```text
          <div class="package-card" data-package-id="${pkg.id}">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1135:
  ```text
            <div class="package-card-header">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1136:
  ```text
              <div class="package-header-top">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1137:
  ```text
                <h3 class="package-name">${pkg.name}</h3>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1138:
  ```text
                ${getSourceBadge(source)}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1139:
  ```text
              </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1140:
  ```text
              <p class="package-category">${pkg.category}</p>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1141:
  ```text
              ${promoBadge}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1142:
  ```text
            </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1143:
  ```text
            <div class="package-card-body">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1144:
  ```text
              <div class="package-price-row">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1145:
  ```text
                <span class="price-label">Base Package</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1146:
  ```text
                <span class="price-value">${formatPrice(base_price)}</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1147:
  ```text
              </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1148:
  ```text
              ${
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1149:
  ```text
                  pkg.promo_price_condition
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1150:
  ```text
                      ? `<div class="promo-condition">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1151:
  ```text
                       <small>${pkg.promo_price_condition}</small>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1152:
  ```text
                     </div>`
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1153:
  ```text
                      : ""
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1154:
  ```text
              }
  ```
  Explanation: Closes the current block/scope.
- Line 1155:
  ```text
              ${
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1156:
  ```text
                  pkg.max_people
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1157:
  ```text
                      ? `<div class="package-capacity">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1158:
  ```text
                       <span class="capacity-icon">👥</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1159:
  ```text
                       <span>Up to ${pkg.max_people} people</span>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1160:
  ```text
                     </div>`
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1161:
  ```text
                      : ""
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1162:
  ```text
              }
  ```
  Explanation: Closes the current block/scope.
- Line 1163:
  ```text
              ${inclusionsHTML}${portraitsHTML}${freebiesHTML}${addonsHTML}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1164:
  ```text
              ${
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1165:
  ```text
                  pkg.notes
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1166:
  ```text
                      ? `<div class="package-notes">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1167:
  ```text
                       <p class="notes-text"><em>${pkg.notes}</em></p>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1168:
  ```text
                     </div>`
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1169:
  ```text
                      : ""
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1170:
  ```text
              }
  ```
  Explanation: Closes the current block/scope.
- Line 1171:
  ```text
            </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1172:
  ```text
            <div class="package-card-footer">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1173:
  ```text
              <div class="total-price-row">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1174:
  ```text
                <span class="total-label">Total Price</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1175:
  ```text
                <span class="total-value">${formatPrice(total_price)}</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1176:
  ```text
              </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1177:
  ```text
              <button class="book-button"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1178:
  ```text
                      onclick="handleBookPackage(${pkg.id},'${safeName}',${numericTotalPrice},${addonIdsJson},${numericBasePrice})">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1179:
  ```text
                <span class="book-icon">📅</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1180:
  ```text
                <span>Book Now</span>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1181:
  ```text
              </button>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1182:
  ```text
            </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1183:
  ```text
          </div>`;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1184:
  ```text
        })
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1185:
  ```text
        .join("");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1186:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 1187:
  ```text
    const factorsHTML = (key_factors ?? [])
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1188:
  ```text
        .map((f) => `<li class="factor-item">${f}</li>`)
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1189:
  ```text
        .join("");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1190:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 1191:
  ```text
    return `
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 1192:
  ```text
      <div class="flex gap-5" style="flex-direction:row;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1193:
  ```text
        <div class="recommendation-panel">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1194:
  ```text
          <div class="recommendation-header">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1195:
  ```text
            <div class="recommendation-header-title">RECOMMENDATION RESULT</div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1196:
  ```text
            <button class="panel-refresh-btn"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1197:
  ```text
                    onclick="refreshRecommendationPanel()"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1198:
  ```text
                    title="Refresh recommendations"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1199:
  ```text
                    aria-label="Refresh recommendations">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1200:
  ```text
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1201:
  ```text
                <polyline points="23 4 23 10 17 10"></polyline>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1202:
  ```text
                <path d="M20.49 15a9 9 0 1 1-2.12-9.36L23 10"></path>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1203:
  ```text
              </svg>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1204:
  ```text
            </button>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1205:
  ```text
          </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1206:
  ```text
          <div class="recommendation-content">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1207:
  ```text
            <div class="progress-item">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1208:
  ```text
              <div class="progress-badge">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1209:
  ```text
                <svg width="36" height="36" viewBox="0 0 36 36" fill="none">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1210:
  ```text
                  <path d="M15.87 23.37C15.57 23.37 15.285 23.25 15.075 23.04
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1211:
  ```text
                           L10.83 18.8C10.395 18.36 10.395 17.64 10.83 17.21
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1212:
  ```text
                           C11.265 16.77 11.985 16.77 12.42 17.21L15.87 20.66
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1213:
  ```text
                           L23.58 12.95C24.015 12.51 24.735 12.51 25.17 12.95
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1214:
  ```text
                           C25.605 13.38 25.605 14.1 25.17 14.54L16.665 23.04
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1215:
  ```text
                           C16.455 23.25 16.17 23.37 15.87 23.37Z"
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1216:
  ```text
                        fill="white"/>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1217:
  ```text
                </svg>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1218:
  ```text
              </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1219:
  ```text
              <div class="progress-info">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1220:
  ```text
                <div class="progress-percentage">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1221:
  ```text
                  <span class="progress-value">100%</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1222:
  ```text
                  <span class="progress-status">Completed</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1223:
  ```text
                </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1224:
  ```text
                <div class="progress-bar">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1225:
  ```text
                  <div class="progress-bar-fill" style="width:100%"></div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1226:
  ```text
                </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1227:
  ```text
              </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1228:
  ```text
              <div class="user-reference">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1229:
  ```text
                <div class="user-name">${customer_name ?? "Customer"}</div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1230:
  ```text
                <div class="user-email">${customer_email ?? ""}</div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1231:
  ```text
                ${
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1232:
  ```text
                    total_bookings > 0
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1233:
  ```text
                        ? `<div class="user-bookings">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1234:
  ```text
                         ${total_bookings} previous booking${total_bookings > 1 ? "s" : ""}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1235:
  ```text
                       </div>`
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1236:
  ```text
                        : `<div class="user-bookings">First-time customer</div>`
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1237:
  ```text
                }
  ```
  Explanation: Closes the current block/scope.
- Line 1238:
  ```text
              </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1239:
  ```text
            </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1240:
  ```text
            <div style="margin-top:24px;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1241:
  ```text
              <h3 class="section-title">Recommended Packages for You</h3>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1242:
  ```text
              <p class="section-subtitle">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1243:
  ```text
                ${
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1244:
  ```text
                    total_bookings > 0
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1245:
  ```text
                        ? "Personalised recommendations based on your booking history."
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1246:
  ```text
                        : "Popular packages chosen for first-time customers."
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1247:
  ```text
                }
  ```
  Explanation: Closes the current block/scope.
- Line 1248:
  ```text
              </p>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1249:
  ```text
            </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1250:
  ```text
            <div class="packages-grid">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1251:
  ```text
              ${cardsHTML || "<p class='no-recommendations'>No recommendations available.</p>"}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1252:
  ```text
            </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1253:
  ```text
            <div class="key-factors" style="margin-top:24px;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1254:
  ```text
              <div class="factors-list">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1255:
  ```text
                <h3 class="factors-title">Key Factors Considered</h3>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1256:
  ```text
                <ul class="factors-items">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1257:
  ```text
                  ${factorsHTML || "<li class='factor-item'>General popularity</li>"}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1258:
  ```text
                </ul>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1259:
  ```text
              </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1260:
  ```text
              <div class="date-reference">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1261:
  ```text
                <span class="date-label">Date</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1262:
  ```text
                <span class="date-value">${target_date}</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1263:
  ```text
              </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1264:
  ```text
            </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1265:
  ```text
            <div id="recommendationMetricsContainer">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1266:
  ```text
              ${renderRecommendationMetricsCollapsible()}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1267:
  ```text
            </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1268:
  ```text
          </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1269:
  ```text
        </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1270:
  ```text
      </div>`;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1271:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 32. `handleBookPackage`

- Signature: `function handleBookPackage(packageId, packageName, totalPrice, addonIds, recommendedBasePrice)`
- Source range: lines 1275-1334

#### Line-by-line

- Line 1275:
  ```text
function handleBookPackage(
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1276:
  ```text
    packageId,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1277:
  ```text
    packageName,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1278:
  ```text
    totalPrice,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1279:
  ```text
    addonIds,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1280:
  ```text
    recommendedBasePrice,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1281:
  ```text
) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1282:
  ```text
    const numericTotalPrice = Number(totalPrice);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1283:
  ```text
    if (
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 1284:
  ```text
        !confirm(
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1285:
  ```text
            `Book "${packageName}" for ${formatPrice(Number.isFinite(numericTotalPrice) ? numericTotalPrice : 0)}?\n\n` +
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1286:
  ```text
                `This will create a new booking for the customer.`,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1287:
  ```text
        )
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1288:
  ```text
    )
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1289:
  ```text
        return;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 1290:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 1291:
  ```text
    const packageIdNum = Number(packageId);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1292:
  ```text
    const pkg = detailsState.packages.find((p) => Number(p.id) === packageIdNum);
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1293:
  ```text
    if (!pkg) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 1294:
  ```text
        alert("Package data not available. Use the Add button instead.");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1295:
  ```text
        return;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 1296:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 1297:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 1298:
  ```text
    const ids = (Array.isArray(addonIds) ? addonIds : []).map((id) => Number(id));
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1299:
  ```text
    const recommendedAddons = detailsState.addons
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1300:
  ```text
        .filter((a) => ids.includes(a.id))
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1301:
  ```text
        .map((a) => ({
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1302:
  ```text
            addonId: a.id,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1303:
  ```text
            name: a.name,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1304:
  ```text
            price: Number(a.price ?? 0),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1305:
  ```text
            quantity: 1,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1306:
  ```text
        }));
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1307:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 1308:
  ```text
    const addonsTotal = recommendedAddons.reduce(
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1309:
  ```text
        (s, a) => s + Number(a.price || 0) * Number(a.quantity || 1),
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1310:
  ```text
        0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1311:
  ```text
    );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1312:
  ```text
    let basePrice = Number(recommendedBasePrice);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1313:
  ```text
    if (!Number.isFinite(basePrice) || basePrice < 0) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 1314:
  ```text
        basePrice = Number(pkg.promo_price ?? pkg.price ?? 0);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1315:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 1316:
  ```text
    if (!Number.isFinite(basePrice) || basePrice < 0) basePrice = 0;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 1317:
  ```text
    const computedTotal = basePrice + addonsTotal;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1318:
  ```text
    const safeTotal =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1319:
  ```text
        Number.isFinite(numericTotalPrice) && numericTotalPrice > 0
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1320:
  ```text
            ? numericTotalPrice
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1321:
  ```text
            : computedTotal;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1322:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 1323:
  ```text
    detailsState.currentBooking = {
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1324:
  ```text
        type: "Walk-In",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1325:
  ```text
        packageName: pkg.name,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1326:
  ```text
        packagePrice: basePrice,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1327:
  ```text
        addons: recommendedAddons,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1328:
  ```text
        discount: 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1329:
  ```text
        subtotal: computedTotal,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1330:
  ```text
        total: safeTotal,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1331:
  ```text
    };
  ```
  Explanation: Closes the current block/scope.
- Line 1332:
  ```text
    detailsState.isEditingBooking = false;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1333:
  ```text
    renderBookingSummaryModal();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1334:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 33. `toggleBookingHistory`

- Signature: `function toggleBookingHistory()`
- Source range: lines 1338-1341

#### Line-by-line

- Line 1338:
  ```text
function toggleBookingHistory() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1339:
  ```text
    detailsState.isHistoryExpanded = !detailsState.isHistoryExpanded;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1340:
  ```text
    renderCustomerDetails();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1341:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 34. `togglePredictView`

- Signature: `function togglePredictView()`
- Source range: lines 1343-1348

#### Line-by-line

- Line 1343:
  ```text
function togglePredictView() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1344:
  ```text
    detailsState.activeView =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1345:
  ```text
        detailsState.activeView === "predict" ? null : "predict";
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1346:
  ```text
    renderViewButtons();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1347:
  ```text
    renderPanels();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1348:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 35. `toggleRecommendationView`

- Signature: `async function toggleRecommendationView()`
- Source range: lines 1350-1355

#### Line-by-line

- Line 1350:
  ```text
async function toggleRecommendationView() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1351:
  ```text
    detailsState.activeView =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1352:
  ```text
        detailsState.activeView === "recommendation" ? null : "recommendation";
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1353:
  ```text
    renderViewButtons();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1354:
  ```text
    await renderPanels();
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 1355:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 36. `refreshRenewalPanel`

- Signature: `async function refreshRenewalPanel()`
- Source range: lines 1357-1363

#### Line-by-line

- Line 1357:
  ```text
async function refreshRenewalPanel() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1358:
  ```text
    renewalCache = null;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1359:
  ```text
    resetModelMetrics();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1360:
  ```text
    detailsState.activeView = "predict";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1361:
  ```text
    renderViewButtons();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1362:
  ```text
    await renderPanels();
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 1363:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 37. `refreshRecommendationPanel`

- Signature: `async function refreshRecommendationPanel()`
- Source range: lines 1365-1371

#### Line-by-line

- Line 1365:
  ```text
async function refreshRecommendationPanel() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1366:
  ```text
    recommendationCache = null;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1367:
  ```text
    resetModelMetrics();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1368:
  ```text
    detailsState.activeView = "recommendation";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1369:
  ```text
    renderViewButtons();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1370:
  ```text
    await renderPanels();
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 1371:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 38. `recomputeRecommendationEvaluation`

- Signature: `async function recomputeRecommendationEvaluation()`
- Source range: lines 1373-1403

#### Line-by-line

- Line 1373:
  ```text
async function recomputeRecommendationEvaluation() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1374:
  ```text
    if (modelMetricsRecomputeInFlight) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 1375:
  ```text
    modelMetricsRecomputeInFlight = true;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1376:
  ```text
    updateVisibleMetricsInPlace();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1377:
  ```text
    debugModelMetrics("recomputeRecommendationEvaluation:start");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1378:
  ```text
    try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 1379:
  ```text
        const result =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1380:
  ```text
            await window.apiClient.analytics.recomputeRecommendationMetrics();
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 1381:
  ```text
        debugModelMetrics("recomputeRecommendationEvaluation:success", result);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1382:
  ```text
        if (result && result.changed === false) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 1383:
  ```text
            console.warn(
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1384:
  ```text
                "[ModelMetrics] Recompute completed but evaluation_results.csv content is unchanged.",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1385:
  ```text
                result,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1386:
  ```text
            );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1387:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 1388:
  ```text
        resetModelMetrics();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1389:
  ```text
        await loadModelMetrics();
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 1390:
  ```text
        updateVisibleMetricsInPlace();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1391:
  ```text
    } catch (err) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1392:
  ```text
        console.error("recomputeRecommendationEvaluation:", err);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1393:
  ```text
        modelMetricsError = String(err?.message || err || "Unknown error");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1394:
  ```text
        debugModelMetrics("recomputeRecommendationEvaluation:error", {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1395:
  ```text
            error: modelMetricsError,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1396:
  ```text
        });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1397:
  ```text
        updateVisibleMetricsInPlace();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1398:
  ```text
    } finally {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1399:
  ```text
        modelMetricsRecomputeInFlight = false;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1400:
  ```text
        updateVisibleMetricsInPlace();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1401:
  ```text
        debugModelMetrics("recomputeRecommendationEvaluation:finally");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1402:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 1403:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 39. `handleAddBooking`

- Signature: `function handleAddBooking()`
- Source range: lines 1407-1411

#### Line-by-line

- Line 1407:
  ```text
function handleAddBooking() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1408:
  ```text
    detailsState.currentBooking = { addons: [] };
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1409:
  ```text
    detailsState.isEditingBooking = false;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1410:
  ```text
    renderPackageInfoModal();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1411:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 40. `handleEditBooking`

- Signature: `function handleEditBooking(bookingId)`
- Source range: lines 1413-1420

#### Line-by-line

- Line 1413:
  ```text
function handleEditBooking(bookingId) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1414:
  ```text
    const b = detailsState.customer.bookings.find((x) => x.id === bookingId);
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1415:
  ```text
    if (!b) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 1416:
  ```text
    detailsState.currentBooking = JSON.parse(JSON.stringify(b));
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1417:
  ```text
    detailsState.selectedBooking = b;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1418:
  ```text
    detailsState.isEditingBooking = true;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1419:
  ```text
    renderPackageInfoModal();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1420:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 41. `handleViewInvoice`

- Signature: `function handleViewInvoice(bookingId)`
- Source range: lines 1422-1432

#### Line-by-line

- Line 1422:
  ```text
function handleViewInvoice(bookingId) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1423:
  ```text
    const b = (detailsState.customer.bookings ?? []).find(
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1424:
  ```text
        (x) => x.id === bookingId,
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1425:
  ```text
    );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1426:
  ```text
    if (!b) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 1427:
  ```text
        console.error("Booking not found:", bookingId);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1428:
  ```text
        return;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 1429:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 1430:
  ```text
    detailsState.selectedBooking = b;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1431:
  ```text
    renderInvoiceModal();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1432:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 42. `handleDeleteBooking`

- Signature: `function handleDeleteBooking(bookingId)`
- Source range: lines 1434-1439

#### Line-by-line

- Line 1434:
  ```text
function handleDeleteBooking(bookingId) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1435:
  ```text
    const b = detailsState.customer.bookings.find((x) => x.id === bookingId);
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1436:
  ```text
    if (!b) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 1437:
  ```text
    detailsState.deleteConfirmation = { bookingId, date: b.date };
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1438:
  ```text
    renderDeleteConfirmation();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1439:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 43. `confirmDeleteBooking`

- Signature: `async function confirmDeleteBooking()`
- Source range: lines 1441-1467

#### Line-by-line

- Line 1441:
  ```text
async function confirmDeleteBooking() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1442:
  ```text
    const { bookingId } = detailsState.deleteConfirmation;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1443:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 1444:
  ```text
    // Clear confirmation UI immediately
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 1445:
  ```text
    detailsState.deleteConfirmation = null;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1446:
  ```text
    document.getElementById("bookingDeleteConfirm").innerHTML = "";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1447:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 1448:
  ```text
    try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 1449:
  ```text
        await window.apiClient.bookings.remove(
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 1450:
  ```text
            detailsState.customerId,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1451:
  ```text
            bookingId,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1452:
  ```text
        );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1453:
  ```text
    } catch (err) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1454:
  ```text
        // A 404 here means it was already deleted — treat as success
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 1455:
  ```text
        if (!err.message.includes("404")) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 1456:
  ```text
            alert("Failed to delete booking: " + err.message);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1457:
  ```text
            return;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 1458:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 1459:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 1460:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 1461:
  ```text
    // Update local state by removing the booking
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 1462:
  ```text
    detailsState.customer.bookings = (
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1463:
  ```text
        detailsState.customer.bookings ?? []
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1464:
  ```text
    ).filter((b) => b.id !== bookingId);
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1465:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 1466:
  ```text
    renderCustomerDetails();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1467:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 44. `cancelDeleteBooking`

- Signature: `function cancelDeleteBooking()`
- Source range: lines 1469-1472

#### Line-by-line

- Line 1469:
  ```text
function cancelDeleteBooking() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1470:
  ```text
    detailsState.deleteConfirmation = null;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1471:
  ```text
    document.getElementById("bookingDeleteConfirm").innerHTML = "";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1472:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 45. `renderPackageInfoModal`

- Signature: `function renderPackageInfoModal()`
- Source range: lines 1476-1571

#### Line-by-line

- Line 1476:
  ```text
function renderPackageInfoModal() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1477:
  ```text
    const booking = detailsState.currentBooking || {};
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1478:
  ```text
    const packages = detailsState.packages;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1479:
  ```text
    const addons = detailsState.addons;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1480:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 1481:
  ```text
    const pkgOptions = packages
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1482:
  ```text
        .map(
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1483:
  ```text
            (p) =>
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1484:
  ```text
                `<option value="${p.name}"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1485:
  ```text
                 ${booking.packageName === p.name ? "selected" : ""}>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1486:
  ```text
           ${p.name} – ₱${p.price}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1487:
  ```text
         </option>`,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1488:
  ```text
        )
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1489:
  ```text
        .join("");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1490:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 1491:
  ```text
    const addonsRows = (booking.addons || [])
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1492:
  ```text
        .map(
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1493:
  ```text
            (addon, idx) => `
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1494:
  ```text
      <div class="addon-item">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1495:
  ```text
        <select class="addon-select"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1496:
  ```text
                onchange="handleAddonChange(${idx}, this.value)">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1497:
  ```text
          <option value="">Select Addons</option>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1498:
  ```text
          ${addons
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1499:
  ```text
              .map(
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1500:
  ```text
                  (a) =>
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1501:
  ```text
                      `<option value="${a.id}"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1502:
  ```text
                       ${
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1503:
  ```text
                           addon.addonId === a.id || addon.name === a.name
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1504:
  ```text
                               ? "selected"
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1505:
  ```text
                               : ""
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1506:
  ```text
                       }>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1507:
  ```text
                 ${a.name} – ₱${a.price}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1508:
  ```text
               </option>`,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1509:
  ```text
              )
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1510:
  ```text
              .join("")}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1511:
  ```text
        </select>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1512:
  ```text
        <div class="quantity-input" id="quantity-${idx}">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1513:
  ```text
          ${addon.quantity ?? 1}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1514:
  ```text
        </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1515:
  ```text
        <div class="quantity-buttons">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1516:
  ```text
          <button type="button" class="quantity-button"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1517:
  ```text
                  onclick="handleAddonQuantity(${idx}, 1)">+</button>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1518:
  ```text
          <button type="button" class="quantity-button"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1519:
  ```text
                  onclick="handleAddonQuantity(${idx}, -1)">−</button>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1520:
  ```text
        </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1521:
  ```text
        <button type="button" class="addon-remove"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1522:
  ```text
                onclick="handleRemoveAddon(${idx})">🗑</button>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1523:
  ```text
      </div>`,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1524:
  ```text
        )
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1525:
  ```text
        .join("");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1526:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 1527:
  ```text
    document.getElementById("modalsContainer").innerHTML = `
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1528:
  ```text
      <div class="modal-overlay" id="packageModal"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1529:
  ```text
           onclick="if(event.target.id==='packageModal') closePackageModal()">
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1530:
  ```text
        <div class="modal-content">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1531:
  ```text
          <h2 class="modal-title">Package Info</h2>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1532:
  ```text
          <form class="modal-form" onsubmit="handlePackageInfoNext(event)">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1533:
  ```text
            <div class="form-group">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1534:
  ```text
              <label class="form-label">Booking Type</label>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1535:
  ```text
              <select class="form-select" id="bookingType">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1536:
  ```text
                <option value="Walk-In"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1537:
  ```text
                  ${(booking.type ?? "Walk-In") === "Walk-In" ? "selected" : ""}>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1538:
  ```text
                  Walk-In</option>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1539:
  ```text
                <option value="Reserved"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1540:
  ```text
                  ${booking.type === "Reserved" ? "selected" : ""}>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1541:
  ```text
                  Reserved</option>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1542:
  ```text
              </select>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1543:
  ```text
            </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1544:
  ```text
            <div class="form-group">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1545:
  ```text
              <label class="form-label">Package Name</label>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1546:
  ```text
              <select class="form-select" id="packageName"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1547:
  ```text
                      onchange="handlePackageSelect(this.value)" required>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1548:
  ```text
                <option value="">Select Package</option>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1549:
  ```text
                ${pkgOptions}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1550:
  ```text
              </select>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1551:
  ```text
            </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1552:
  ```text
            <div class="form-group">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1553:
  ```text
              <label class="form-label">Addons</label>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1554:
  ```text
              <div id="addonsList">${addonsRows}</div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1555:
  ```text
              <button type="button" class="addon-add"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1556:
  ```text
                      onclick="handleAddAddon()">+</button>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1557:
  ```text
            </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1558:
  ```text
            <div class="form-group">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1559:
  ```text
              <label class="form-label">Discount</label>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1560:
  ```text
              <input type="number" id="discount" min="0" step="1"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1561:
  ```text
                     value="${booking.discount ?? 0}" class="form-input">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1562:
  ```text
            </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1563:
  ```text
            <div class="modal-actions">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1564:
  ```text
              <button type="button" class="btn-back"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1565:
  ```text
                      onclick="closePackageModal()">Back</button>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1566:
  ```text
              <button type="submit" class="btn-next">Next</button>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1567:
  ```text
            </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1568:
  ```text
          </form>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1569:
  ```text
        </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1570:
  ```text
      </div>`;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1571:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 46. `handlePackageSelect`

- Signature: `function handlePackageSelect(value)`
- Source range: lines 1573-1580

#### Line-by-line

- Line 1573:
  ```text
function handlePackageSelect(value) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1574:
  ```text
    const pkg = detailsState.packages.find((p) => p.name === value);
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1575:
  ```text
    if (!pkg) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 1576:
  ```text
    detailsState.currentBooking = detailsState.currentBooking || {};
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1577:
  ```text
    detailsState.currentBooking.packageName = value;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1578:
  ```text
    detailsState.currentBooking.packagePrice =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1579:
  ```text
        pkg.promo_price ?? pkg.price ?? 0;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1580:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 47. `handleAddonChange`

- Signature: `function handleAddonChange(index, value)`
- Source range: lines 1582-1592

#### Line-by-line

- Line 1582:
  ```text
function handleAddonChange(index, value) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1583:
  ```text
    const found = detailsState.addons.find((a) => a.id === parseInt(value, 10));
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1584:
  ```text
    if (!found) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 1585:
  ```text
    const qty = detailsState.currentBooking.addons[index]?.quantity ?? 1;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1586:
  ```text
    detailsState.currentBooking.addons[index] = {
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1587:
  ```text
        addonId: found.id,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1588:
  ```text
        name: found.name,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1589:
  ```text
        price: found.price ?? 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1590:
  ```text
        quantity: qty,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1591:
  ```text
    };
  ```
  Explanation: Closes the current block/scope.
- Line 1592:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 48. `handleAddonQuantity`

- Signature: `function handleAddonQuantity(index, delta)`
- Source range: lines 1594-1600

#### Line-by-line

- Line 1594:
  ```text
function handleAddonQuantity(index, delta) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1595:
  ```text
    const addon = detailsState.currentBooking.addons[index];
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1596:
  ```text
    if (!addon) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 1597:
  ```text
    addon.quantity = Math.max(1, (addon.quantity ?? 1) + delta);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1598:
  ```text
    const el = document.getElementById(`quantity-${index}`);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1599:
  ```text
    if (el) el.textContent = addon.quantity;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 1600:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 49. `handleRemoveAddon`

- Signature: `function handleRemoveAddon(index)`
- Source range: lines 1602-1605

#### Line-by-line

- Line 1602:
  ```text
function handleRemoveAddon(index) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1603:
  ```text
    detailsState.currentBooking.addons.splice(index, 1);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1604:
  ```text
    renderPackageInfoModal();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1605:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 50. `handleAddAddon`

- Signature: `function handleAddAddon()`
- Source range: lines 1607-1615

#### Line-by-line

- Line 1607:
  ```text
function handleAddAddon() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1608:
  ```text
    (detailsState.currentBooking.addons =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1609:
  ```text
        detailsState.currentBooking.addons || []).push({
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1610:
  ```text
        name: "",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1611:
  ```text
        quantity: 1,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1612:
  ```text
        price: 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1613:
  ```text
    });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1614:
  ```text
    renderPackageInfoModal();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1615:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 51. `handlePackageInfoNext`

- Signature: `function handlePackageInfoNext(event)`
- Source range: lines 1617-1655

#### Line-by-line

- Line 1617:
  ```text
function handlePackageInfoNext(event) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1618:
  ```text
    event.preventDefault();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1619:
  ```text
    const packageName = document.getElementById("packageName").value;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1620:
  ```text
    if (!packageName) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 1621:
  ```text
        alert("Please select a package");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1622:
  ```text
        return;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 1623:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 1624:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 1625:
  ```text
    const bookingType = document.getElementById("bookingType").value;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1626:
  ```text
    const discount = parseFloat(document.getElementById("discount").value) || 0;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1627:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 1628:
  ```text
    // ── Always look up the price fresh — never trust currentBooking.packagePrice
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 1629:
  ```text
    // which may be stale if the modal was re-rendered after addon changes
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 1630:
  ```text
    const pkg = detailsState.packages.find((p) => p.name === packageName);
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1631:
  ```text
    console.log("Package: ", pkg);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1632:
  ```text
    console.log("package.price: ", pkg.price);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1633:
  ```text
    const packagePrice = pkg.price ? pkg.price : 0;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1634:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 1635:
  ```text
    const addonsTotal = (detailsState.currentBooking.addons || []).reduce(
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1636:
  ```text
        (s, a) => s + (a.price ?? 0) * (a.quantity ?? 1),
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1637:
  ```text
        0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1638:
  ```text
    );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1639:
  ```text
    console.log("Package price: ", detailsState.currentBooking.packagePrice);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1640:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 1641:
  ```text
    const subtotal = packagePrice + addonsTotal;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1642:
  ```text
    const total = subtotal - discount;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1643:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 1644:
  ```text
    detailsState.currentBooking = {
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1645:
  ```text
        ...detailsState.currentBooking,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1646:
  ```text
        type: bookingType,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1647:
  ```text
        packageName,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1648:
  ```text
        packagePrice, // ← now always correct
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1649:
  ```text
        discount,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1650:
  ```text
        subtotal,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1651:
  ```text
        total,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1652:
  ```text
    };
  ```
  Explanation: Closes the current block/scope.
- Line 1653:
  ```text
    document.getElementById("modalsContainer").innerHTML = "";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1654:
  ```text
    renderBookingSummaryModal();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1655:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 52. `goBackToPackageInfo`

- Signature: `function goBackToPackageInfo()`
- Source range: lines 1657-1659

#### Line-by-line

- Line 1657:
  ```text
function goBackToPackageInfo() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1658:
  ```text
    renderPackageInfoModal();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1659:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 53. `confirmBookingSummary`

- Signature: `async function confirmBookingSummary()`
- Source range: lines 1661-1731

#### Line-by-line

- Line 1661:
  ```text
async function confirmBookingSummary() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1662:
  ```text
    const c = detailsState.customer;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1663:
  ```text
    const cb = detailsState.currentBooking;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1664:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 1665:
  ```text
    // ── Translate frontend state → API field names ────────────────────────────
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 1666:
  ```text
    // Find the package by name to get its id
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 1667:
  ```text
    const pkg = detailsState.packages.find((p) => p.name === cb.packageName);
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1668:
  ```text
    if (!pkg) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 1669:
  ```text
        alert("Package not found. Please go back and reselect the package.");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1670:
  ```text
        return;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 1671:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 1672:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 1673:
  ```text
    const apiPayload = {
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1674:
  ```text
        package_id: pkg.id,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1675:
  ```text
        customer_id: detailsState.customerId,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1676:
  ```text
        session_status: "BOOKED", // Staff-added bookings are confirmed immediately
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1677:
  ```text
        total_price: cb.total ?? 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1678:
  ```text
        discounts: cb.discount ? String(cb.discount) : "",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1679:
  ```text
        // addons_input: array of { addonId, quantity } for the serializer
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 1680:
  ```text
        addons_input: (cb.addons ?? [])
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1681:
  ```text
            .filter((a) => a.addonId || a.id)
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1682:
  ```text
            .map((a) => ({
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1683:
  ```text
                addonId: a.addonId ?? a.id,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1684:
  ```text
                quantity: a.quantity ?? 1,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1685:
  ```text
            })),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1686:
  ```text
    };
  ```
  Explanation: Closes the current block/scope.
- Line 1687:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 1688:
  ```text
    const overlay = document.getElementById("pageLoadingOverlay");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1689:
  ```text
    if (overlay) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 1690:
  ```text
        overlay.style.display = "flex";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1691:
  ```text
        overlay.querySelector(".loading-label").textContent = "Saving booking…";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1692:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 1693:
  ```text
    try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 1694:
  ```text
        let saved;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1695:
  ```text
        if (detailsState.isEditingBooking && detailsState.selectedBooking) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 1696:
  ```text
            saved = await window.apiClient.bookings.update(
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 1697:
  ```text
                detailsState.customerId,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1698:
  ```text
                detailsState.selectedBooking.id,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1699:
  ```text
                apiPayload,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1700:
  ```text
            );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1701:
  ```text
            const idx = c.bookings.findIndex(
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1702:
  ```text
                (b) => b.id === detailsState.selectedBooking.id,
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1703:
  ```text
            );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1704:
  ```text
            if (idx !== -1) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 1705:
  ```text
                // Merge API response back — use returned data for display fields
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 1706:
  ```text
                c.bookings[idx] = _mergeBookingDisplay(saved, cb, pkg);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1707:
  ```text
            }
  ```
  Explanation: Closes the current block/scope.
- Line 1708:
  ```text
        } else {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1709:
  ```text
            saved = await window.apiClient.bookings.create(
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 1710:
  ```text
                detailsState.customerId,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1711:
  ```text
                apiPayload,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1712:
  ```text
            );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1713:
  ```text
            // Merge display fields onto the saved object for immediate rendering
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 1714:
  ```text
            c.bookings.push(_mergeBookingDisplay(saved, cb, pkg));
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1715:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 1716:
  ```text
    } catch (err) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1717:
  ```text
        console.error("confirmBookingSummary:", err);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1718:
  ```text
        alert("Failed to save booking: " + err.message);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1719:
  ```text
        return;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 1720:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 1721:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 1722:
  ```text
    Object.assign(detailsState, {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1723:
  ```text
        currentBooking: null,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1724:
  ```text
        isEditingBooking: false,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1725:
  ```text
        selectedBooking: null,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1726:
  ```text
        isHistoryExpanded: true,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1727:
  ```text
    });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1728:
  ```text
    if (overlay) overlay.style.display = "none";
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 1729:
  ```text
    document.getElementById("modalsContainer").innerHTML = "";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1730:
  ```text
    renderCustomerDetails();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1731:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 54. `_mergeBookingDisplay`

- Signature: `function _mergeBookingDisplay(apiResponse, localBooking, pkg)`
- Source range: lines 1738-1756

#### Line-by-line

- Line 1738:
  ```text
function _mergeBookingDisplay(apiResponse, localBooking, pkg) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1739:
  ```text
    return {
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 1740:
  ```text
        ...apiResponse,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1741:
  ```text
        // API response has these from BookingSerializer computed fields,
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 1742:
  ```text
        // but ensure they're populated even if the API returned nulls
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 1743:
  ```text
        packageName: apiResponse.packageName ?? localBooking.packageName,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1744:
  ```text
        packagePrice: apiResponse.packagePrice ?? localBooking.packagePrice,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1745:
  ```text
        total: apiResponse.total ?? localBooking.total ?? 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1746:
  ```text
        subtotal: apiResponse.subtotal ?? localBooking.subtotal ?? 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1747:
  ```text
        discount: apiResponse.discount ?? localBooking.discount ?? 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1748:
  ```text
        type: apiResponse.type ?? localBooking.type ?? "Walk-In",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1749:
  ```text
        date: apiResponse.date ?? _todayString(),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1750:
  ```text
        // Preserve full addon objects (with names) from local state for display
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 1751:
  ```text
        addons:
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1752:
  ```text
            (apiResponse.addons?.length ? apiResponse.addons : null) ??
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1753:
  ```text
            localBooking.addons ??
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1754:
  ```text
            [],
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1755:
  ```text
    };
  ```
  Explanation: Closes the current block/scope.
- Line 1756:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 55. `_todayString`

- Signature: `function _todayString()`
- Source range: lines 1758-1761

#### Line-by-line

- Line 1758:
  ```text
function _todayString() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1759:
  ```text
    const d = new Date();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1760:
  ```text
    return `${d.getMonth() + 1}/${d.getDate()}/${d.getFullYear()}`;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 1761:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 56. `closePackageModal`

- Signature: `function closePackageModal()`
- Source range: lines 1763-1769

#### Line-by-line

- Line 1763:
  ```text
function closePackageModal() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1764:
  ```text
    Object.assign(detailsState, {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1765:
  ```text
        currentBooking: null,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1766:
  ```text
        isEditingBooking: false,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1767:
  ```text
    });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1768:
  ```text
    document.getElementById("modalsContainer").innerHTML = "";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1769:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 57. `renderBookingSummaryModal`

- Signature: `function renderBookingSummaryModal()`
- Source range: lines 1773-1850

#### Line-by-line

- Line 1773:
  ```text
function renderBookingSummaryModal() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1774:
  ```text
    const b = detailsState.currentBooking;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1775:
  ```text
    if (!b) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 1776:
  ```text
    const addonsRows = (b.addons || [])
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1777:
  ```text
        .map(
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1778:
  ```text
            (a) =>
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1779:
  ```text
                `<div style="display:flex;justify-content:space-between;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1780:
  ```text
           <span style="color:#909090;font-size:14px;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1781:
  ```text
             ${a.name} x ${a.quantity}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1782:
  ```text
           </span>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1783:
  ```text
           <span style="color:#909090;font-size:14px;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1784:
  ```text
             ₱${((a.price ?? 0) * (a.quantity ?? 1)).toFixed(2)}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1785:
  ```text
           </span>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1786:
  ```text
         </div>`,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1787:
  ```text
        )
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1788:
  ```text
        .join("");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1789:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 1790:
  ```text
    document.getElementById("modalsContainer").innerHTML = `
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1791:
  ```text
      <div class="modal-overlay" id="summaryModal"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1792:
  ```text
           onclick="if(event.target.id==='summaryModal') closeBookingSummaryModal()">
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1793:
  ```text
        <div class="modal-content" style="justify-content:center;align-items:center;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1794:
  ```text
          <div style="display:flex;flex-direction:column;align-items:center;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1795:
  ```text
                      gap:20px;width:100%;">
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1796:
  ```text
            <h2 class="modal-title">Summary</h2>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1797:
  ```text
            <div style="width:310px;display:flex;flex-direction:column;gap:20px;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1798:
  ```text
              <div style="display:flex;flex-direction:column;gap:8px;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1799:
  ```text
                <span style="color:#ABB7C2;font-size:14px;">Package Name</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1800:
  ```text
                <div style="display:flex;justify-content:space-between;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1801:
  ```text
                  <span style="color:#909090;font-size:14px;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1802:
  ```text
                    ${b.packageName ?? "N/A"}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1803:
  ```text
                  </span>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1804:
  ```text
                  <span style="color:#909090;font-size:14px;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1805:
  ```text
                    ₱${(b.packagePrice ?? 0).toFixed(2)}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1806:
  ```text
                  </span>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1807:
  ```text
                </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1808:
  ```text
              </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1809:
  ```text
              ${
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1810:
  ```text
                  b.addons?.length
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1811:
  ```text
                      ? `<div style="display:flex;flex-direction:column;gap:12px;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1812:
  ```text
                       <span style="color:#616161;font-size:12px;">Addon</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1813:
  ```text
                       ${addonsRows}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1814:
  ```text
                     </div>`
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1815:
  ```text
                      : ""
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1816:
  ```text
              }
  ```
  Explanation: Closes the current block/scope.
- Line 1817:
  ```text
              <div style="height:1px;background:#A2A2A2;"></div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1818:
  ```text
              <div style="display:flex;flex-direction:column;gap:12px;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1819:
  ```text
                <div style="display:flex;justify-content:space-between;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1820:
  ```text
                  <span style="color:#616161;font-size:14px;">Subtotal:</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1821:
  ```text
                  <span style="color:#616161;font-size:14px;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1822:
  ```text
                    ₱${(b.subtotal ?? 0).toFixed(2)}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1823:
  ```text
                  </span>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1824:
  ```text
                </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1825:
  ```text
                <div style="display:flex;justify-content:space-between;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1826:
  ```text
                  <span style="color:#616161;font-size:14px;">Voucher Amount:</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1827:
  ```text
                  <span style="color:#616161;font-size:14px;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1828:
  ```text
                    ₱${(b.discount ?? 0).toFixed(2)}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1829:
  ```text
                  </span>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1830:
  ```text
                </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1831:
  ```text
                <div style="display:flex;justify-content:space-between;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1832:
  ```text
                  <span style="color:#616161;font-size:14px;font-weight:700;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1833:
  ```text
                    Total:
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1834:
  ```text
                  </span>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1835:
  ```text
                  <span style="color:#616161;font-size:14px;font-weight:700;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1836:
  ```text
                    ₱${(b.total ?? 0).toFixed(2)}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1837:
  ```text
                  </span>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1838:
  ```text
                </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1839:
  ```text
              </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1840:
  ```text
            </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1841:
  ```text
            <div style="display:flex;align-items:center;gap:15px;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1842:
  ```text
              <button type="button" class="btn-back"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1843:
  ```text
                      onclick="goBackToPackageInfo()">Back</button>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1844:
  ```text
              <button type="button" class="btn-next"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1845:
  ```text
                      onclick="confirmBookingSummary()">Confirm</button>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1846:
  ```text
            </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1847:
  ```text
          </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1848:
  ```text
        </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1849:
  ```text
      </div>`;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1850:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 58. `closeBookingSummaryModal`

- Signature: `function closeBookingSummaryModal()`
- Source range: lines 1852-1854

#### Line-by-line

- Line 1852:
  ```text
function closeBookingSummaryModal() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1853:
  ```text
    document.getElementById("modalsContainer").innerHTML = "";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1854:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 59. `renderInvoiceModal`

- Signature: `function renderInvoiceModal()`
- Source range: lines 1858-1976

#### Line-by-line

- Line 1858:
  ```text
function renderInvoiceModal() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1859:
  ```text
    const b = detailsState.selectedBooking;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1860:
  ```text
    if (!b) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 1861:
  ```text
    const c = detailsState.customer;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1862:
  ```text
    const addonsRows = (b.addons || [])
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1863:
  ```text
        .map(
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1864:
  ```text
            (a) =>
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1865:
  ```text
                `<div style="display:flex;justify-content:space-between;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1866:
  ```text
           <span style="color:#909090;font-size:14px;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1867:
  ```text
             ${a.name} x ${a.quantity}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1868:
  ```text
           </span>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1869:
  ```text
           <span style="color:#909090;font-size:14px;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1870:
  ```text
             ₱${((a.price ?? 0) * (a.quantity ?? 1)).toFixed(2)}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1871:
  ```text
           </span>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1872:
  ```text
         </div>`,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1873:
  ```text
        )
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1874:
  ```text
        .join("");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1875:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 1876:
  ```text
    document.getElementById("modalsContainer").innerHTML = `
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1877:
  ```text
      <div class="modal-overlay" id="invoiceModal"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1878:
  ```text
           onclick="if(event.target.id==='invoiceModal') closeInvoiceModal()">
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1879:
  ```text
        <div class="modal-content invoice-modal">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1880:
  ```text
          <div style="display:flex;flex-direction:column;align-items:center;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1881:
  ```text
                      gap:20px;width:100%;">
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1882:
  ```text
            <h2 class="modal-title">Invoice</h2>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1883:
  ```text
            <div class="invoice-items">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1884:
  ```text
              <div class="invoice-item">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1885:
  ```text
                <div style="display:flex;flex-direction:column;gap:8px;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1886:
  ```text
                  <span style="color:#ABB7C2;font-size:12px;">Full Name</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1887:
  ```text
                  <span style="color:#909090;font-size:14px;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1888:
  ```text
                    ${b.customer_name ?? c.name ?? "N/A"}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1889:
  ```text
                  </span>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1890:
  ```text
                </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1891:
  ```text
              </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1892:
  ```text
              <div class="invoice-item">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1893:
  ```text
                <div style="display:flex;flex-direction:column;gap:8px;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1894:
  ```text
                  <span style="color:#ABB7C2;font-size:12px;">Email</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1895:
  ```text
                  <span style="color:#909090;font-size:14px;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1896:
  ```text
                    ${c.email ?? "N/A"}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1897:
  ```text
                  </span>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1898:
  ```text
                </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1899:
  ```text
              </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1900:
  ```text
              <div class="invoice-item">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1901:
  ```text
                <div style="display:flex;flex-direction:column;gap:8px;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1902:
  ```text
                  <span style="color:#ABB7C2;font-size:12px;">Type</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1903:
  ```text
                  <span style="color:#909090;font-size:14px;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1904:
  ```text
                    ${b.type ?? "Walk-In"}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1905:
  ```text
                  </span>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1906:
  ```text
                </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1907:
  ```text
              </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1908:
  ```text
              <div class="invoice-item">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1909:
  ```text
                <div style="display:flex;flex-direction:column;gap:8px;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1910:
  ```text
                  <span style="color:#ABB7C2;font-size:12px;">Date</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1911:
  ```text
                  <span style="color:#909090;font-size:14px;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1912:
  ```text
                    ${b.date ?? "N/A"}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1913:
  ```text
                  </span>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1914:
  ```text
                </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1915:
  ```text
              </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1916:
  ```text
              <div class="invoice-item">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1917:
  ```text
                <div style="display:flex;flex-direction:column;gap:8px;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1918:
  ```text
                  <span style="color:#ABB7C2;font-size:14px;">Package Name</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1919:
  ```text
                  <div style="display:flex;justify-content:space-between;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1920:
  ```text
                    <span style="color:#909090;font-size:14px;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1921:
  ```text
                      ${b.packageName ?? "N/A"}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1922:
  ```text
                    </span>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1923:
  ```text
                    <span style="color:#909090;font-size:14px;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1924:
  ```text
                      ₱${(b.packagePrice ?? 0).toFixed(2)}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1925:
  ```text
                    </span>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1926:
  ```text
                  </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1927:
  ```text
                </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1928:
  ```text
              </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1929:
  ```text
              ${
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1930:
  ```text
                  b.addons?.length
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1931:
  ```text
                      ? `<div class="invoice-item">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1932:
  ```text
                       <div style="display:flex;flex-direction:column;gap:4px;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1933:
  ```text
                         <span style="color:#616161;font-size:12px;">Addon</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1934:
  ```text
                         ${addonsRows}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1935:
  ```text
                       </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1936:
  ```text
                     </div>`
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1937:
  ```text
                      : ""
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1938:
  ```text
              }
  ```
  Explanation: Closes the current block/scope.
- Line 1939:
  ```text
            </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1940:
  ```text
            <div class="invoice-divider"></div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1941:
  ```text
            <div class="summary-section">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1942:
  ```text
              <div class="summary-row">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1943:
  ```text
                <span class="summary-label">Subtotal:</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1944:
  ```text
                <span class="summary-value">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1945:
  ```text
                  ₱${(b.subtotal ?? 0).toFixed(2)}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1946:
  ```text
                </span>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1947:
  ```text
              </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1948:
  ```text
              <div class="summary-row">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1949:
  ```text
                <span class="summary-label">Voucher Amount:</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1950:
  ```text
                <span class="summary-value">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1951:
  ```text
                  ₱${(b.discount ?? 0).toFixed(2)}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1952:
  ```text
                </span>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1953:
  ```text
              </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1954:
  ```text
              <div class="summary-row">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1955:
  ```text
                <span class="summary-label bold">Total:</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1956:
  ```text
                <span class="summary-value bold">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1957:
  ```text
                  ₱${(b.total ?? 0).toFixed(2)}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1958:
  ```text
                </span>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1959:
  ```text
              </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1960:
  ```text
            </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1961:
  ```text
            <div class="invoice-actions">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1962:
  ```text
              <button type="button" class="btn-download"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1963:
  ```text
                      onclick="downloadInvoicePDF()">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1964:
  ```text
                <span class="btn-download-text">Download PDF</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1965:
  ```text
              </button>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1966:
  ```text
              <div style="display:flex;align-items:center;gap:15px;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1967:
  ```text
                <button type="button" class="btn-edit"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1968:
  ```text
                        onclick="handleEditBookingFromInvoice()">Edit</button>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1969:
  ```text
                <button type="button" class="btn-back"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1970:
  ```text
                        onclick="closeInvoiceModal()">Back</button>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1971:
  ```text
              </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1972:
  ```text
            </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1973:
  ```text
          </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1974:
  ```text
        </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1975:
  ```text
      </div>`;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1976:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 60. `closeInvoiceModal`

- Signature: `function closeInvoiceModal()`
- Source range: lines 1978-1981

#### Line-by-line

- Line 1978:
  ```text
function closeInvoiceModal() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1979:
  ```text
    detailsState.selectedBooking = null;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1980:
  ```text
    document.getElementById("modalsContainer").innerHTML = "";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1981:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 61. `handleEditBookingFromInvoice`

- Signature: `function handleEditBookingFromInvoice()`
- Source range: lines 1983-1987

#### Line-by-line

- Line 1983:
  ```text
function handleEditBookingFromInvoice() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1984:
  ```text
    const id = detailsState.selectedBooking?.id;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1985:
  ```text
    closeInvoiceModal();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1986:
  ```text
    if (id) handleEditBooking(id);
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 1987:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 62. `renderDeleteConfirmation`

- Signature: `function renderDeleteConfirmation()`
- Source range: lines 1991-2009

#### Line-by-line

- Line 1991:
  ```text
function renderDeleteConfirmation() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 1992:
  ```text
    document.getElementById("bookingDeleteConfirm").innerHTML = `
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1993:
  ```text
      <div class="confirmation-dialog">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1994:
  ```text
        <div class="confirmation-content">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1995:
  ```text
          <h3 class="confirmation-title">Delete Booking</h3>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1996:
  ```text
          <p class="confirmation-message">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 1997:
  ```text
            Delete the booking from
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1998:
  ```text
            <strong>${detailsState.deleteConfirmation.date}</strong>?
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 1999:
  ```text
            This cannot be undone.
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 2000:
  ```text
          </p>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 2001:
  ```text
          <div class="confirmation-actions">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 2002:
  ```text
            <button class="btn-confirm-cancel"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 2003:
  ```text
                    onclick="cancelDeleteBooking()">Cancel</button>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 2004:
  ```text
            <button class="btn-confirm-delete"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 2005:
  ```text
                    onclick="confirmDeleteBooking()">Delete</button>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 2006:
  ```text
          </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 2007:
  ```text
        </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 2008:
  ```text
      </div>`;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 2009:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 63. `downloadInvoicePDF`

- Signature: `function downloadInvoicePDF()`
- Source range: lines 2013-2060

#### Line-by-line

- Line 2013:
  ```text
function downloadInvoicePDF() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 2014:
  ```text
    const b = detailsState.selectedBooking;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 2015:
  ```text
    if (!b) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 2016:
  ```text
    const c = detailsState.customer;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 2017:
  ```text
    const win = window.open("", "", "height=600,width=800");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 2018:
  ```text
    if (!win) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 2019:
  ```text
    win.document.write(`<!DOCTYPE html><html><head>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 2020:
  ```text
      <title>Invoice</title>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 2021:
  ```text
      <style>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 2022:
  ```text
        body{font-family:'Segoe UI',Arial,sans-serif;padding:30px;max-width:600px;margin:0 auto}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 2023:
  ```text
        .title{font-size:28px;font-weight:bold;color:#165166;text-align:center}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 2024:
  ```text
        .row{display:flex;justify-content:space-between;padding:8px 0;font-size:13px}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 2025:
  ```text
        .bold{font-weight:bold;font-size:16px;color:#165166}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 2026:
  ```text
        .div{height:1px;background:#ddd;margin:20px 0}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 2027:
  ```text
      </style></head><body>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 2028:
  ```text
      <p class="title">INVOICE</p>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 2029:
  ```text
      <p style="text-align:center;font-size:12px;color:#666;margin-bottom:20px">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 2030:
  ```text
        Héigen Studio
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 2031:
  ```text
      </p>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 2032:
  ```text
      <div class="row"><b>Customer:</b><span>${b.customer_name ?? c.name}</span></div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 2033:
  ```text
      <div class="row"><b>Email:</b><span>${c.email ?? ""}</span></div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 2034:
  ```text
      <div class="row"><b>Date:</b><span>${b.date ?? "N/A"}</span></div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 2035:
  ```text
      <div class="div"></div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 2036:
  ```text
      <div class="row"><span>${b.packageName ?? "N/A"}</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 2037:
  ```text
        <span>₱${(b.packagePrice ?? 0).toFixed(2)}</span></div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 2038:
  ```text
      ${(b.addons ?? [])
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 2039:
  ```text
          .map(
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 2040:
  ```text
              (a) =>
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 2041:
  ```text
                  `<div class="row" style="padding-left:20px;font-size:12px;color:#666">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 2042:
  ```text
             <span>${a.name} x ${a.quantity}</span>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 2043:
  ```text
             <span>₱${((a.price ?? 0) * (a.quantity ?? 1)).toFixed(2)}</span>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 2044:
  ```text
           </div>`,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 2045:
  ```text
          )
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 2046:
  ```text
          .join("")}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 2047:
  ```text
      <div class="div"></div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 2048:
  ```text
      <div class="row"><span>Subtotal:</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 2049:
  ```text
        <span>₱${(b.subtotal ?? 0).toFixed(2)}</span></div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 2050:
  ```text
      <div class="row"><span>Discount:</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 2051:
  ```text
        <span>₱${(b.discount ?? 0).toFixed(2)}</span></div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 2052:
  ```text
      <div class="row bold"><span>TOTAL:</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 2053:
  ```text
        <span>₱${(b.total ?? 0).toFixed(2)}</span></div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 2054:
  ```text
      <p style="text-align:center;margin-top:40px;font-size:11px;color:#999">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 2055:
  ```text
        Thank you! Generated ${new Date().toLocaleString()}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 2056:
  ```text
      </p>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 2057:
  ```text
      <script>window.onload=function(){window.print();window.close()}<\/script>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 2058:
  ```text
    </body></html>`);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 2059:
  ```text
    win.document.close();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 2060:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

## File: `electron-app/scripts/heigen-api.js`

- Methods documented: 3

### 1. `getCustomers`

- Signature: `function getCustomers()`
- Source range: lines 153-155

#### Line-by-line

- Line 153:
  ```text
function getCustomers() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 154:
  ```text
    return CUSTOMERS;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 155:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 2. `getCustomerBookings`

- Signature: `function getCustomerBookings(customerId)`
- Source range: lines 157-159

#### Line-by-line

- Line 157:
  ```text
function getCustomerBookings(customerId) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 158:
  ```text
    return CUSTOMER_BOOKINGS[customerId] || [];
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 159:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 3. `mockDelay`

- Signature: `const mockDelay = (param) =>`
- Source range: lines 351-352

#### Line-by-line

- Line 351:
  ```text
const mockDelay = (ms = 500) =>
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 352:
  ```text
    new Promise((resolve) => setTimeout(resolve, ms));
  ```
  Explanation: Declares the callable signature and entry point for this method.

## File: `electron-app/scripts/heigen-app.js`

- Methods documented: 13

### 1. `getOnboardingFlag`

- Signature: `function getOnboardingFlag()`
- Source range: lines 16-18

#### Line-by-line

- Line 16:
  ```text
function getOnboardingFlag() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 17:
  ```text
    return sessionStorage.getItem("needsProfileSetup") === "true";
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 18:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 2. `setOnboardingFlag`

- Signature: `function setOnboardingFlag(value)`
- Source range: lines 20-22

#### Line-by-line

- Line 20:
  ```text
function setOnboardingFlag(value) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 21:
  ```text
    sessionStorage.setItem("needsProfileSetup", value ? "true" : "false");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 22:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 3. `isStaffAdminPage`

- Signature: `function isStaffAdminPage()`
- Source range: lines 24-27

#### Line-by-line

- Line 24:
  ```text
function isStaffAdminPage() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 25:
  ```text
    const path = window.location.pathname.toLowerCase();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 26:
  ```text
    return path.includes("/staff_admin/");
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 27:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 4. `isOnboardingPage`

- Signature: `function isOnboardingPage()`
- Source range: lines 29-32

#### Line-by-line

- Line 29:
  ```text
function isOnboardingPage() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 30:
  ```text
    const path = window.location.pathname.toLowerCase();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 31:
  ```text
    return path.endsWith("/onboarding.html") || path.includes("/onboarding.html");
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 32:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 5. `enforceOnboarding`

- Signature: `function enforceOnboarding()`
- Source range: lines 34-41

#### Line-by-line

- Line 34:
  ```text
function enforceOnboarding() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 35:
  ```text
    if (typeof MOCK_MODE !== "undefined" && MOCK_MODE) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 36:
  ```text
    if (!isStaffAdminPage()) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 37:
  ```text
    if (!getOnboardingFlag()) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 38:
  ```text
    if (isOnboardingPage()) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 39:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 40:
  ```text
    navigateTo("./onboarding.html");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 41:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 6. `navigateTo`

- Signature: `function navigateTo(page)`
- Source range: lines 47-49

#### Line-by-line

- Line 47:
  ```text
function navigateTo(page) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 48:
  ```text
    window.location.href = page;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 49:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 7. `getSignInPagePath`

- Signature: `function getSignInPagePath()`
- Source range: lines 51-56

#### Line-by-line

- Line 51:
  ```text
function getSignInPagePath() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 52:
  ```text
    const isStaffPage = window.location.pathname
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 53:
  ```text
        .toLowerCase()
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 54:
  ```text
        .includes("/pages/staff_admin/");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 55:
  ```text
    return isStaffPage ? "../../index.html" : "./index.html";
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 56:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 8. `logLoginDebugSnapshot`

- Signature: `function logLoginDebugSnapshot(stage)`
- Source range: lines 58-180

#### Line-by-line

- Line 58:
  ```text
function logLoginDebugSnapshot(stage) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 59:
  ```text
    const loginScreen = document.getElementById("login-screen");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 60:
  ```text
    const loginForm = document.getElementById("login-form");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 61:
  ```text
    const emailInput = document.getElementById("login-email");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 62:
  ```text
    const passwordInput = document.getElementById("login-password");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 63:
  ```text
    const successModal = document.getElementById("success-modal");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 64:
  ```text
    const logoutModal = document.getElementById("logoutModal");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 65:
  ```text
    const guestLoadingOverlay = document.getElementById("guestLoadingOverlay");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 66:
  ```text
    const mobileOverlay = document.getElementById("mobileOverlay");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 67:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 68:
  ```text
    const blockers = Array.from(
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 69:
  ```text
        document.querySelectorAll("body *"),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 70:
  ```text
    ).filter((el) => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 71:
  ```text
        const style = window.getComputedStyle(el);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 72:
  ```text
        if (style.display === "none" || style.visibility === "hidden") return false;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 73:
  ```text
        const z = Number(style.zIndex);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 74:
  ```text
        if (!Number.isFinite(z) || z < 20) return false;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 75:
  ```text
        const rect = el.getBoundingClientRect();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 76:
  ```text
        return rect.width > 0 && rect.height > 0;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 77:
  ```text
    });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 78:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 79:
  ```text
    const topBlockers = blockers
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 80:
  ```text
        .sort(
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 81:
  ```text
            (a, b) =>
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 82:
  ```text
                Number(window.getComputedStyle(b).zIndex || 0) -
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 83:
  ```text
                Number(window.getComputedStyle(a).zIndex || 0),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 84:
  ```text
        )
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 85:
  ```text
        .slice(0, 5)
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 86:
  ```text
        .map((el) => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 87:
  ```text
            const style = window.getComputedStyle(el);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 88:
  ```text
            const rect = el.getBoundingClientRect();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 89:
  ```text
            return {
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 90:
  ```text
                id: el.id || null,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 91:
  ```text
                className: el.className || null,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 92:
  ```text
                tagName: el.tagName,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 93:
  ```text
                zIndex: style.zIndex,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 94:
  ```text
                pointerEvents: style.pointerEvents,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 95:
  ```text
                display: style.display,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 96:
  ```text
                visibility: style.visibility,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 97:
  ```text
                opacity: style.opacity,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 98:
  ```text
                rect: {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 99:
  ```text
                    x: Math.round(rect.x),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 100:
  ```text
                    y: Math.round(rect.y),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 101:
  ```text
                    w: Math.round(rect.width),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 102:
  ```text
                    h: Math.round(rect.height),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 103:
  ```text
                },
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 104:
  ```text
            };
  ```
  Explanation: Closes the current block/scope.
- Line 105:
  ```text
        });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 106:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 107:
  ```text
    console.debug("[LOGIN_DEBUG] snapshot", {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 108:
  ```text
        stage,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 109:
  ```text
        url: window.location.href,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 110:
  ```text
        bodyPointerEvents: document.body.style.pointerEvents || "(unset)",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 111:
  ```text
        bodyComputedPointerEvents: window.getComputedStyle(document.body).pointerEvents,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 112:
  ```text
        loginScreen: loginScreen
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 113:
  ```text
            ? {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 114:
  ```text
                  className: loginScreen.className,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 115:
  ```text
                  inlinePointerEvents: loginScreen.style.pointerEvents || "(unset)",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 116:
  ```text
                  computedPointerEvents: window.getComputedStyle(loginScreen).pointerEvents,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 117:
  ```text
              }
  ```
  Explanation: Closes the current block/scope.
- Line 118:
  ```text
            : null,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 119:
  ```text
        loginFormControls: loginForm
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 120:
  ```text
            ? Array.from(
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 121:
  ```text
                  loginForm.querySelectorAll("input, button, select, textarea"),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 122:
  ```text
              ).map((el) => ({
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 123:
  ```text
                  id: el.id || null,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 124:
  ```text
                  tagName: el.tagName,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 125:
  ```text
                  type: el.type || null,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 126:
  ```text
                  disabled: !!el.disabled,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 127:
  ```text
                  readOnly: !!el.readOnly,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 128:
  ```text
                  pointerEvents: window.getComputedStyle(el).pointerEvents,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 129:
  ```text
              }))
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 130:
  ```text
            : null,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 131:
  ```text
        overlays: {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 132:
  ```text
            successModal: successModal
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 133:
  ```text
                ? {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 134:
  ```text
                      className: successModal.className,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 135:
  ```text
                      display: window.getComputedStyle(successModal).display,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 136:
  ```text
                  }
  ```
  Explanation: Closes the current block/scope.
- Line 137:
  ```text
                : null,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 138:
  ```text
            logoutModal: logoutModal
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 139:
  ```text
                ? {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 140:
  ```text
                      className: logoutModal.className,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 141:
  ```text
                      display: window.getComputedStyle(logoutModal).display,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 142:
  ```text
                  }
  ```
  Explanation: Closes the current block/scope.
- Line 143:
  ```text
                : null,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 144:
  ```text
            guestLoadingOverlay: guestLoadingOverlay
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 145:
  ```text
                ? {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 146:
  ```text
                      className: guestLoadingOverlay.className,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 147:
  ```text
                      display: window.getComputedStyle(guestLoadingOverlay).display,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 148:
  ```text
                      pointerEvents: window.getComputedStyle(guestLoadingOverlay)
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 149:
  ```text
                          .pointerEvents,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 150:
  ```text
                  }
  ```
  Explanation: Closes the current block/scope.
- Line 151:
  ```text
                : null,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 152:
  ```text
            mobileOverlay: mobileOverlay
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 153:
  ```text
                ? {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 154:
  ```text
                      className: mobileOverlay.className,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 155:
  ```text
                      display: window.getComputedStyle(mobileOverlay).display,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 156:
  ```text
                  }
  ```
  Explanation: Closes the current block/scope.
- Line 157:
  ```text
                : null,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 158:
  ```text
        },
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 159:
  ```text
        elementFromPointAtEmail:
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 160:
  ```text
            emailInput && emailInput.getBoundingClientRect().width > 0
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 161:
  ```text
                ? (() => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 162:
  ```text
                      const rect = emailInput.getBoundingClientRect();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 163:
  ```text
                      const x = Math.floor(rect.left + rect.width / 2);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 164:
  ```text
                      const y = Math.floor(rect.top + rect.height / 2);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 165:
  ```text
                      const topEl = document.elementFromPoint(x, y);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 166:
  ```text
                      return topEl
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 167:
  ```text
                          ? {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 168:
  ```text
                                x,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 169:
  ```text
                                y,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 170:
  ```text
                                id: topEl.id || null,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 171:
  ```text
                                className: topEl.className || null,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 172:
  ```text
                                tagName: topEl.tagName,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 173:
  ```text
                            }
  ```
  Explanation: Closes the current block/scope.
- Line 174:
  ```text
                          : null;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 175:
  ```text
                  })()
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 176:
  ```text
                : null,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 177:
  ```text
        passwordInputPresent: !!passwordInput,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 178:
  ```text
        topBlockers,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 179:
  ```text
    });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 180:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 9. `normalizeLoginPageState`

- Signature: `function normalizeLoginPageState()`
- Source range: lines 182-229

#### Line-by-line

- Line 182:
  ```text
function normalizeLoginPageState() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 183:
  ```text
    const loginScreen = document.getElementById("login-screen");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 184:
  ```text
    if (!loginScreen) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 185:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 186:
  ```text
    logLoginDebugSnapshot("before-normalize");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 187:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 188:
  ```text
    // Ensure no stale overlay/modal from previous navigation can block inputs.
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 189:
  ```text
    const successModal = document.getElementById("success-modal");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 190:
  ```text
    if (successModal) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 191:
  ```text
        successModal.classList.remove("active");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 192:
  ```text
        successModal.style.cssText = "display:none;";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 193:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 194:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 195:
  ```text
    ["logoutModal", "guestLoadingOverlay", "mobileOverlay"].forEach((id) => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 196:
  ```text
        const el = document.getElementById(id);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 197:
  ```text
        if (!el) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 198:
  ```text
        el.classList.remove("active", "show", "flex");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 199:
  ```text
        el.classList.add("hidden");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 200:
  ```text
        el.style.display = "none";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 201:
  ```text
    });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 202:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 203:
  ```text
    document.body.style.pointerEvents = "auto";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 204:
  ```text
    loginScreen.style.pointerEvents = "auto";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 205:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 206:
  ```text
    const loginForm = document.getElementById("login-form");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 207:
  ```text
    if (loginForm) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 208:
  ```text
        loginForm
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 209:
  ```text
            .querySelectorAll("input, button, select, textarea")
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 210:
  ```text
            .forEach((el) => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 211:
  ```text
                el.disabled = false;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 212:
  ```text
            });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 213:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 214:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 215:
  ```text
    // Clear logout hint query param (if present) after state reset.
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 216:
  ```text
    const url = new URL(window.location.href);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 217:
  ```text
    if (url.searchParams.has("from_logout")) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 218:
  ```text
        url.searchParams.delete("from_logout");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 219:
  ```text
        const next =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 220:
  ```text
            url.pathname +
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 221:
  ```text
            (url.searchParams.toString()
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 222:
  ```text
                ? `?${url.searchParams.toString()}`
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 223:
  ```text
                : "") +
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 224:
  ```text
            url.hash;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 225:
  ```text
        window.history.replaceState({}, "", next);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 226:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 227:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 228:
  ```text
    logLoginDebugSnapshot("after-normalize");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 229:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 10. `showToast`

- Signature: `function showToast(message, param)`
- Source range: lines 235-250

#### Line-by-line

- Line 235:
  ```text
function showToast(message, type = "info") {
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 236:
  ```text
    const toast = document.getElementById("toast");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 237:
  ```text
    const toastMessage = document.getElementById("toast-message");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 238:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 239:
  ```text
    if (!toast || !toastMessage) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 240:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 241:
  ```text
    toast.classList.remove("toast-success", "toast-error", "toast-info");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 242:
  ```text
    toast.classList.add(`toast-${type}`);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 243:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 244:
  ```text
    toastMessage.textContent = message;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 245:
  ```text
    toast.classList.add("show");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 246:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 247:
  ```text
    setTimeout(() => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 248:
  ```text
        toast.classList.remove("show");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 249:
  ```text
    }, 3000);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 250:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 11. `showSuccessModal`

- Signature: `function showSuccessModal(title, subtitle)`
- Source range: lines 256-264

#### Line-by-line

- Line 256:
  ```text
function showSuccessModal(title, subtitle) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 257:
  ```text
    const modal = document.getElementById("success-modal");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 258:
  ```text
    if (!modal) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 259:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 260:
  ```text
    document.getElementById("modal-title").textContent = title;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 261:
  ```text
    document.getElementById("modal-subtitle").textContent = subtitle;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 262:
  ```text
    modal.classList.add("active");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 263:
  ```text
    modal.style.cssText = "display:flex;position:fixed;inset:0;z-index:9000;align-items:center;justify-content:center;background:rgba(0,0,0,0.5);";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 264:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 12. `closeModal`

- Signature: `function closeModal()`
- Source range: lines 266-273

#### Line-by-line

- Line 266:
  ```text
function closeModal() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 267:
  ```text
    const modal = document.getElementById("success-modal");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 268:
  ```text
    if (!modal) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 269:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 270:
  ```text
    modal.classList.remove("active");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 271:
  ```text
    modal.style.cssText = "display:none;";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 272:
  ```text
    navigateTo(getSignInPagePath());
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 273:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 13. `logout`

- Signature: `function logout()`
- Source range: lines 369-382

#### Line-by-line

- Line 369:
  ```text
function logout() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 370:
  ```text
    if (!confirm("Are you sure you want to logout?")) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 371:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 372:
  ```text
    // TODO: API CALL (optional)
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 373:
  ```text
    // POST /auth/logout
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 374:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 375:
  ```text
    AppState.currentUser = null;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 376:
  ```text
    AppState.customers = [];
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 377:
  ```text
    AppState.packages = [];
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 378:
  ```text
    AppState.addons = [];
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 379:
  ```text
    setOnboardingFlag(false);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 380:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 381:
  ```text
    navigateTo(getSignInPagePath());
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 382:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

## File: `electron-app/scripts/notif.js`

- Methods documented: 14

### 1. `toggleBookingStatusPanel`

- Signature: `function toggleBookingStatusPanel()`
- Source range: lines 11-21

#### Line-by-line

- Line 11:
  ```text
function toggleBookingStatusPanel() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 12:
  ```text
    const panel   = document.getElementById("bookingStatusPanel");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 13:
  ```text
    const overlay = document.getElementById("bookingStatusOverlay");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 14:
  ```text
    if (!panel || !overlay) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 15:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 16:
  ```text
    _notifPanelOpen = !_notifPanelOpen;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 17:
  ```text
    panel.style.display   = _notifPanelOpen ? "flex" : "none";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 18:
  ```text
    overlay.classList.toggle("active", _notifPanelOpen);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 19:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 20:
  ```text
    if (_notifPanelOpen) loadPendingBookings();
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 21:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 2. `closeBookingStatusPanel`

- Signature: `function closeBookingStatusPanel()`
- Source range: lines 23-29

#### Line-by-line

- Line 23:
  ```text
function closeBookingStatusPanel() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 24:
  ```text
    _notifPanelOpen = false;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 25:
  ```text
    const panel   = document.getElementById("bookingStatusPanel");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 26:
  ```text
    const overlay = document.getElementById("bookingStatusOverlay");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 27:
  ```text
    if (panel)   panel.style.display = "none";
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 28:
  ```text
    if (overlay) overlay.classList.remove("active");
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 29:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 3. `loadPendingBookings`

- Signature: `async function loadPendingBookings()`
- Source range: lines 33-55

#### Line-by-line

- Line 33:
  ```text
async function loadPendingBookings() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 34:
  ```text
    const content = document.getElementById("bookingStatusContent");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 35:
  ```text
    if (content) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 36:
  ```text
        content.innerHTML = `
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 37:
  ```text
          <div class="notif-loading">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 38:
  ```text
            <div class="notif-spinner"></div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 39:
  ```text
            <span>Loading bookings…</span>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 40:
  ```text
          </div>`;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 41:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 42:
  ```text
    try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 43:
  ```text
        const all = await window.apiClient.bookings.listPendingOngoing();
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 44:
  ```text
        pendingBookings = Array.isArray(all) ? all : [];
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 45:
  ```text
        updateNotificationBadge();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 46:
  ```text
        renderBookingItems();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 47:
  ```text
    } catch (err) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 48:
  ```text
        console.error("loadPendingBookings:", err);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 49:
  ```text
        if (content) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 50:
  ```text
            content.innerHTML = `<div class="empty-bookings" style="color:#c00;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 51:
  ```text
              Could not load bookings.<br><small>${err.message}</small>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 52:
  ```text
            </div>`;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 53:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 54:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 55:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 4. `updateNotificationBadge`

- Signature: `function updateNotificationBadge()`
- Source range: lines 57-65

#### Line-by-line

- Line 57:
  ```text
function updateNotificationBadge() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 58:
  ```text
    const badge = document.getElementById("notificationBadge");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 59:
  ```text
    if (!badge) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 60:
  ```text
    // Only count true Pending (kiosk-originated, not yet accepted)
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 61:
  ```text
    const pending = pendingBookings.filter(b => b.session_status === "Pending");
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 62:
  ```text
    const count   = pending.length;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 63:
  ```text
    badge.textContent   = count;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 64:
  ```text
    badge.style.display = count > 0 ? "flex" : "none";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 65:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 5. `renderBookingItems`

- Signature: `function renderBookingItems()`
- Source range: lines 69-123

#### Line-by-line

- Line 69:
  ```text
function renderBookingItems() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 70:
  ```text
    const content = document.getElementById("bookingStatusContent");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 71:
  ```text
    if (!content) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 72:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 73:
  ```text
    if (!pendingBookings.length) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 74:
  ```text
        content.innerHTML = '<div class="empty-bookings">No pending bookings</div>';
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 75:
  ```text
        return;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 76:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 77:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 78:
  ```text
    content.innerHTML = pendingBookings.map((booking) => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 79:
  ```text
        const name    = booking.customer_name ?? "Unknown Customer";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 80:
  ```text
        const rawDate = booking.session_date ?? booking.created_at;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 81:
  ```text
        const dateStr = rawDate ? new Date(rawDate).toLocaleDateString("en-US") : "";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 82:
  ```text
        const status  = booking.session_status ?? "Pending";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 83:
  ```text
        const isPending  = status === "Pending";
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 84:
  ```text
        const isOngoing  = status === "Ongoing";
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 85:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 86:
  ```text
        const pendingIcon = `<svg width="16" height="16" viewBox="0 0 24 24" fill="none">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 87:
  ```text
            <circle cx="12" cy="12" r="10" stroke="white" stroke-width="2" fill="none"/>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 88:
  ```text
            <path d="M12 6V12L16 14" stroke="white" stroke-width="2" stroke-linecap="round"/>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 89:
  ```text
          </svg>`;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 90:
  ```text
        const ongoingIcon = `<svg width="16" height="16" viewBox="0 0 24 24" fill="none">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 91:
  ```text
            <circle cx="12" cy="12" r="10" stroke="white" stroke-width="2" fill="none"/>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 92:
  ```text
            <path d="M8 12H16M12 8V16" stroke="white" stroke-width="2" stroke-linecap="round"/>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 93:
  ```text
          </svg>`;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 94:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 95:
  ```text
        const actions = isPending ? `
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 96:
  ```text
            <button class="booking-action-btn start"  onclick="handleStartBooking(${booking.id})">Accept</button>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 97:
  ```text
            <button class="booking-action-btn deny"   onclick="handleDenyBooking(${booking.id})">Deny</button>` : "";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 98:
  ```text
        const ongoingActions = isOngoing ? `
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 99:
  ```text
            <button class="booking-action-btn done"   onclick="handleDoneBooking(${booking.id})">Done</button>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 100:
  ```text
            <button class="booking-action-btn cancel" onclick="handleCancelBooking(${booking.id})">Cancel</button>` : "";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 101:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 102:
  ```text
        return `
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 103:
  ```text
          <div class="booking-item">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 104:
  ```text
            <div class="booking-item-top">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 105:
  ```text
              <div class="booking-customer-info">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 106:
  ```text
                <div class="booking-customer-label">Customer</div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 107:
  ```text
                <div class="booking-customer-name">${name}</div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 108:
  ```text
              </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 109:
  ```text
              <div class="booking-meta">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 110:
  ```text
                ${dateStr ? `<div class="booking-date">${dateStr}</div>` : ""}
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 111:
  ```text
                <div class="booking-status-badge ${status.toLowerCase()}">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 112:
  ```text
                  ${isPending ? pendingIcon : ongoingIcon}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 113:
  ```text
                  ${status}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 114:
  ```text
                </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 115:
  ```text
              </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 116:
  ```text
            </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 117:
  ```text
            <div class="booking-actions">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 118:
  ```text
              ${actions}${ongoingActions}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 119:
  ```text
              <button class="booking-action-btn summary" onclick="handleViewBookingSummary(${booking.id})">Summary</button>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 120:
  ```text
            </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 121:
  ```text
          </div>`;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 122:
  ```text
    }).join("");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 123:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 6. `handleStartBooking`

- Signature: `async function handleStartBooking(bookingId)`
- Source range: lines 127-137

#### Line-by-line

- Line 127:
  ```text
async function handleStartBooking(bookingId) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 128:
  ```text
    try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 129:
  ```text
        _setItemLoading(bookingId, true);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 130:
  ```text
        await window.apiClient.bookings.updateStatus(bookingId, "Ongoing");
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 131:
  ```text
        await loadPendingBookings();
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 132:
  ```text
    } catch (err) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 133:
  ```text
        console.error("handleStartBooking:", err);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 134:
  ```text
        alert("Failed to accept booking: " + err.message);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 135:
  ```text
        await loadPendingBookings();
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 136:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 137:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 7. `handleDoneBooking`

- Signature: `async function handleDoneBooking(bookingId)`
- Source range: lines 139-149

#### Line-by-line

- Line 139:
  ```text
async function handleDoneBooking(bookingId) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 140:
  ```text
    try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 141:
  ```text
        _setItemLoading(bookingId, true);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 142:
  ```text
        await window.apiClient.bookings.updateStatus(bookingId, "BOOKED");
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 143:
  ```text
        await loadPendingBookings();
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 144:
  ```text
    } catch (err) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 145:
  ```text
        console.error("handleDoneBooking:", err);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 146:
  ```text
        alert("Failed to complete booking: " + err.message);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 147:
  ```text
        await loadPendingBookings();
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 148:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 149:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 8. `handleDenyBooking`

- Signature: `async function handleDenyBooking(bookingId)`
- Source range: lines 151-162

#### Line-by-line

- Line 151:
  ```text
async function handleDenyBooking(bookingId) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 152:
  ```text
    if (!confirm("Deny this booking? This cannot be undone.")) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 153:
  ```text
    try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 154:
  ```text
        _setItemLoading(bookingId, true);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 155:
  ```text
        await window.apiClient.bookings.removeById(bookingId);
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 156:
  ```text
        await loadPendingBookings();
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 157:
  ```text
    } catch (err) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 158:
  ```text
        console.error("handleDenyBooking:", err);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 159:
  ```text
        alert("Failed to deny booking: " + err.message);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 160:
  ```text
        await loadPendingBookings();
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 161:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 162:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 9. `handleCancelBooking`

- Signature: `async function handleCancelBooking(bookingId)`
- Source range: lines 164-175

#### Line-by-line

- Line 164:
  ```text
async function handleCancelBooking(bookingId) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 165:
  ```text
    if (!confirm("Move this booking back to Pending?")) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 166:
  ```text
    try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 167:
  ```text
        _setItemLoading(bookingId, true);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 168:
  ```text
        await window.apiClient.bookings.updateStatus(bookingId, "Pending");
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 169:
  ```text
        await loadPendingBookings();
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 170:
  ```text
    } catch (err) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 171:
  ```text
        console.error("handleCancelBooking:", err);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 172:
  ```text
        alert("Failed to cancel booking: " + err.message);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 173:
  ```text
        await loadPendingBookings();
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 174:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 175:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 10. `handleViewBookingSummary`

- Signature: `function handleViewBookingSummary(bookingId)`
- Source range: lines 177-318

#### Line-by-line

- Line 177:
  ```text
function handleViewBookingSummary(bookingId) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 178:
  ```text
    const booking = pendingBookings.find(b => b.id === bookingId);
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 179:
  ```text
    if (!booking) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 180:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 181:
  ```text
    const existing = document.getElementById("notifSummaryModal");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 182:
  ```text
    if (existing) existing.remove();
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 183:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 184:
  ```text
    // ── Field resolution: maps to the updated backend serializer fields ─────────
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 185:
  ```text
    const status      = booking.session_status ?? "Pending";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 186:
  ```text
    const statusColor = status === "Pending" ? "#f57c00" : "#1976d2";
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 187:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 188:
  ```text
    // preferred_date: now serialized from session_date by the backend
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 189:
  ```text
    const preferredDate = booking.preferred_date ?? null;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 190:
  ```text
    const bookingDate   = booking.date ?? null;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 191:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 192:
  ```text
    const customerName = booking.customer_name    ?? null;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 193:
  ```text
    const email        = booking.customer_email   ?? null;  // from customer.email
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 194:
  ```text
    const contact      = booking.customer_contact ?? null;  // from customer.contact_number
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 195:
  ```text
    const type         = booking.type             ?? null;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 196:
  ```text
    const category     = booking.category_name    ?? null;  // from package.category
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 197:
  ```text
    const pkgName      = booking.packageName      ?? null;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 198:
  ```text
    const pkgPrice     = Number(booking.packagePrice ?? 0);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 199:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 200:
  ```text
    // total: 0 is a known backend bug — fall back to subtotal
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 201:
  ```text
    const subtotal  = Number(booking.subtotal ?? pkgPrice);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 202:
  ```text
    const discount  = Number(booking.discount ?? 0);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 203:
  ```text
    const total     = Number(booking.total) > 0
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 204:
  ```text
        ? Number(booking.total)
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 205:
  ```text
        : subtotal - discount;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 206:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 207:
  ```text
    // Addons
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 208:
  ```text
    const addons = Array.isArray(booking.addons) ? booking.addons : [];
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 209:
  ```text
    const addonsHTML = addons.length
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 210:
  ```text
        ? addons.map(a => `
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 211:
  ```text
            <div class="ns-addon-row">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 212:
  ```text
              <span class="ns-addon-name">${a.name}${(a.quantity ?? 1) > 1 ? ` ×${a.quantity}` : ""}</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 213:
  ```text
              <span class="ns-addon-price">+₱${((a.price ?? 0) * (a.quantity ?? 1)).toLocaleString("en-PH", {minimumFractionDigits:2})}</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 214:
  ```text
            </div>`).join("")
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 215:
  ```text
        : `<div class="ns-no-addons">No add-ons</div>`;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 216:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 217:
  ```text
    // ── Helper: only render a field row if value is non-null/empty ─────────────
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 218:
  ```text
    const field = (label, value) => value
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 219:
  ```text
        ? `<div class="ns-field">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 220:
  ```text
             <div class="ns-field-label">${label}</div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 221:
  ```text
             <div class="ns-field-value">${value}</div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 222:
  ```text
           </div>`
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 223:
  ```text
        : "";
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 224:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 225:
  ```text
    // ── Payment method ────────────────────────────────────────────────────────
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 226:
  ```text
    const paymentMethod = booking.gcash_payment != null ? "GCash"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 227:
  ```text
        : booking.cash_payment  != null ? "Cash"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 228:
  ```text
        : null;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 229:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 230:
  ```text
    const modal = document.createElement("div");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 231:
  ```text
    modal.id = "notifSummaryModal";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 232:
  ```text
    modal.className = "ns-overlay";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 233:
  ```text
    modal.innerHTML = `
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 234:
  ```text
      <div class="ns-dialog" role="dialog" aria-modal="true">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 235:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 236:
  ```text
        <div class="ns-header">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 237:
  ```text
          <span class="ns-title">Booking Summary</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 238:
  ```text
          <button class="ns-close-btn" onclick="closeNotifSummary()" aria-label="Close">✕</button>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 239:
  ```text
        </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 240:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 241:
  ```text
        <div class="ns-ribbon" style="background:${statusColor};">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 242:
  ```text
          ${status.toUpperCase()}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 243:
  ```text
        </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 244:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 245:
  ```text
        <div class="ns-body">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 246:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 247:
  ```text
          <!-- Customer section: build fields first, only render section if any exist -->
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 248:
  ```text
          ${(() => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 249:
  ```text
            const rows = [
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 250:
  ```text
              field("Full Name",     customerName),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 251:
  ```text
              field("Email",         email),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 252:
  ```text
              field("Contact",       contact),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 253:
  ```text
              field("Preferred Date",preferredDate),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 254:
  ```text
              field("Booking Date",  bookingDate),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 255:
  ```text
            ].filter(Boolean).join("");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 256:
  ```text
            return rows ? `
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 257:
  ```text
              <div class="ns-section-label">Customer</div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 258:
  ```text
              <div class="ns-grid">${rows}</div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 259:
  ```text
              <div class="ns-divider"></div>` : "";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 260:
  ```text
          })()}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 261:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 262:
  ```text
          <!-- Booking section -->
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 263:
  ```text
          ${(() => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 264:
  ```text
            const rows = [
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 265:
  ```text
              field("Type",     type),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 266:
  ```text
              field("Category", category),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 267:
  ```text
              field("Payment",  paymentMethod),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 268:
  ```text
            ].filter(Boolean).join("");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 269:
  ```text
            return rows ? `
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 270:
  ```text
              <div class="ns-section-label">Booking</div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 271:
  ```text
              <div class="ns-grid">${rows}</div>` : "";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 272:
  ```text
          })()}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 273:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 274:
  ```text
          <!-- Package -->
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 275:
  ```text
          ${pkgName ? `
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 276:
  ```text
          <div class="ns-pkg-row">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 277:
  ```text
            <div style="flex:1;">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 278:
  ```text
              <div class="ns-field-label">Package</div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 279:
  ```text
              <div class="ns-field-value ns-pkg-name">${pkgName}</div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 280:
  ```text
            </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 281:
  ```text
            <div class="ns-pkg-price">₱${pkgPrice.toLocaleString("en-PH", {minimumFractionDigits:2})}</div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 282:
  ```text
          </div>` : ""}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 283:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 284:
  ```text
          <!-- Add-ons -->
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 285:
  ```text
          <div class="ns-field-label" style="margin-top:12px;">Add-ons</div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 286:
  ```text
          <div class="ns-addons-block">${addonsHTML}</div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 287:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 288:
  ```text
          <div class="ns-divider"></div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 289:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 290:
  ```text
          <!-- Totals -->
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 291:
  ```text
          <div class="ns-totals">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 292:
  ```text
            ${subtotal !== total ? `
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 293:
  ```text
            <div class="ns-total-row">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 294:
  ```text
              <span>Subtotal</span>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 295:
  ```text
              <span>₱${subtotal.toLocaleString("en-PH", {minimumFractionDigits:2})}</span>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 296:
  ```text
            </div>` : ""}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 297:
  ```text
            ${discount > 0 ? `
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 298:
  ```text
            <div class="ns-total-row">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 299:
  ```text
              <span>Voucher</span>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 300:
  ```text
              <span style="color:#2d8a6e;">−₱${discount.toLocaleString("en-PH", {minimumFractionDigits:2})}</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 301:
  ```text
            </div>` : ""}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 302:
  ```text
            <div class="ns-total-row ns-total-grand">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 303:
  ```text
              <span>Total</span>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 304:
  ```text
              <span>₱${total.toLocaleString("en-PH", {minimumFractionDigits:2})}</span>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 305:
  ```text
            </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 306:
  ```text
          </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 307:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 308:
  ```text
        </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 309:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 310:
  ```text
        <div class="ns-footer">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 311:
  ```text
          <button class="booking-action-btn summary" onclick="closeNotifSummary()">Close</button>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 312:
  ```text
        </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 313:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 314:
  ```text
      </div>`;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 315:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 316:
  ```text
    modal.addEventListener("click", e => { if (e.target === modal) closeNotifSummary(); });
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 317:
  ```text
    document.body.appendChild(modal);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 318:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 11. `field`

- Signature: `const field = (label, value) =>`
- Source range: lines 218-223

#### Line-by-line

- Line 218:
  ```text
    const field = (label, value) => value
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 219:
  ```text
        ? `<div class="ns-field">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 220:
  ```text
             <div class="ns-field-label">${label}</div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 221:
  ```text
             <div class="ns-field-value">${value}</div>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 222:
  ```text
           </div>`
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 223:
  ```text
        : "";
  ```
  Explanation: Executes a core operation in this method's control flow.

### 12. `closeNotifSummary`

- Signature: `function closeNotifSummary()`
- Source range: lines 320-323

#### Line-by-line

- Line 320:
  ```text
function closeNotifSummary() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 321:
  ```text
    const modal = document.getElementById("notifSummaryModal");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 322:
  ```text
    if (modal) modal.remove();
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 323:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 13. `_setItemLoading`

- Signature: `function _setItemLoading(bookingId, on)`
- Source range: lines 325-332

#### Line-by-line

- Line 325:
  ```text
function _setItemLoading(bookingId, on) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 326:
  ```text
    // Grey out the item while waiting
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 327:
  ```text
    const items = document.querySelectorAll(".booking-item");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 328:
  ```text
    items.forEach(item => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 329:
  ```text
        const btns = item.querySelectorAll("[onclick*='" + bookingId + "']");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 330:
  ```text
        if (btns.length) item.style.opacity = on ? "0.5" : "1";
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 331:
  ```text
    });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 332:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 14. `refreshPage`

- Signature: `async function refreshPage()`
- Source range: lines 336-356

#### Line-by-line

- Line 336:
  ```text
async function refreshPage() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 337:
  ```text
    const btn = document.getElementById("globalRefreshBtn");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 338:
  ```text
    if (btn) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 339:
  ```text
        btn.disabled = true;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 340:
  ```text
        btn.classList.add("refreshing");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 341:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 342:
  ```text
    try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 343:
  ```text
        await loadPendingBookings();
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 344:
  ```text
        // If the page has its own reload function, call it
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 345:
  ```text
        if (typeof loadCustomers === "function")               await loadCustomers();
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 346:
  ```text
        else if (typeof refreshDashboardRecs === "function")  { await refreshDashboardRecs(); if(typeof initCharts==="function") initCharts(); }
  ```
  Explanation: Defines the fallback path when previous conditions are not satisfied.
- Line 347:
  ```text
        else if (typeof loadDashboardData === "function")     { await loadDashboardData();    if(typeof initCharts==="function") initCharts(); }
  ```
  Explanation: Defines the fallback path when previous conditions are not satisfied.
- Line 348:
  ```text
        else if (typeof loadPackagesData === "function")       await loadPackagesData();
  ```
  Explanation: Defines the fallback path when previous conditions are not satisfied.
- Line 349:
  ```text
        else if (typeof initializeCustomerDetails === "function") await initializeCustomerDetails();
  ```
  Explanation: Defines the fallback path when previous conditions are not satisfied.
- Line 350:
  ```text
    } finally {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 351:
  ```text
        if (btn) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 352:
  ```text
            btn.disabled = false;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 353:
  ```text
            btn.classList.remove("refreshing");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 354:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 355:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 356:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

## File: `electron-app/scripts/onboarding.js`

- Methods documented: 8

### 1. `setError`

- Signature: `function setError(message)`
- Source range: lines 33-42

#### Line-by-line

- Line 33:
  ```text
    function setError(message) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 34:
  ```text
        if (!errorEl) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 35:
  ```text
        if (!message) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 36:
  ```text
            errorEl.classList.add("hidden");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 37:
  ```text
            errorEl.textContent = "";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 38:
  ```text
            return;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 39:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 40:
  ```text
        errorEl.textContent = message;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 41:
  ```text
        errorEl.classList.remove("hidden");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 42:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 2. `updateStepDots`

- Signature: `function updateStepDots()`
- Source range: lines 44-56

#### Line-by-line

- Line 44:
  ```text
    function updateStepDots() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 45:
  ```text
        for (let i = 1; i <= 3; i += 1) {
  ```
  Explanation: Iterates over a collection to apply repeated processing.
- Line 46:
  ```text
            const dot = document.getElementById(`step-dot-${i}`);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 47:
  ```text
            if (!dot) continue;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 48:
  ```text
            if (i - 1 === currentStep) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 49:
  ```text
                dot.classList.remove("bg-gray-300");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 50:
  ```text
                dot.classList.add("bg-[#165166]");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 51:
  ```text
            } else {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 52:
  ```text
                dot.classList.remove("bg-[#165166]");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 53:
  ```text
                dot.classList.add("bg-gray-300");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 54:
  ```text
            }
  ```
  Explanation: Closes the current block/scope.
- Line 55:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 56:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 3. `showStep`

- Signature: `function showStep(index)`
- Source range: lines 58-78

#### Line-by-line

- Line 58:
  ```text
    function showStep(index) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 59:
  ```text
        currentStep = index;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 60:
  ```text
        steps.forEach((panel, i) => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 61:
  ```text
            if (i === index) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 62:
  ```text
                panel.classList.remove("hidden");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 63:
  ```text
            } else {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 64:
  ```text
                panel.classList.add("hidden");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 65:
  ```text
            }
  ```
  Explanation: Closes the current block/scope.
- Line 66:
  ```text
        });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 67:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 68:
  ```text
        prevBtn.disabled = index === 0;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 69:
  ```text
        prevBtn.classList.toggle("opacity-50", index === 0);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 70:
  ```text
        prevBtn.classList.toggle("cursor-not-allowed", index === 0);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 71:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 72:
  ```text
        const isLastStep = passwordOnlyFlow
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 73:
  ```text
            ? index === 0
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 74:
  ```text
            : index === steps.length - 1;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 75:
  ```text
        nextBtn.textContent = isLastStep ? "Finish" : "Next";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 76:
  ```text
        setError("");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 77:
  ```text
        updateStepDots();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 78:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 4. `setPasswordOnlyUI`

- Signature: `function setPasswordOnlyUI()`
- Source range: lines 80-101

#### Line-by-line

- Line 80:
  ```text
    function setPasswordOnlyUI() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 81:
  ```text
        passwordOnlyFlow = true;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 82:
  ```text
        const subtitle = document.querySelector("h1 + p");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 83:
  ```text
        if (subtitle) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 84:
  ```text
            subtitle.textContent =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 85:
  ```text
                "Your profile is already complete. Update your password to continue.";
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 86:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 87:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 88:
  ```text
        const step2 = document.querySelector('[data-step="2"]');
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 89:
  ```text
        const step3 = document.querySelector('[data-step="3"]');
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 90:
  ```text
        if (step2) step2.classList.add("hidden");
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 91:
  ```text
        if (step3) step3.classList.add("hidden");
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 92:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 93:
  ```text
        const dot2 = document.getElementById("step-dot-2")?.parentElement;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 94:
  ```text
        const dot3 = document.getElementById("step-dot-3")?.parentElement;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 95:
  ```text
        const separators = Array.from(
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 96:
  ```text
            document.querySelectorAll(".h-\\[1px\\].flex-1.bg-gray-200"),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 97:
  ```text
        );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 98:
  ```text
        if (dot2) dot2.classList.add("hidden");
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 99:
  ```text
        if (dot3) dot3.classList.add("hidden");
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 100:
  ```text
        separators.forEach((el) => el.classList.add("hidden"));
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 101:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 5. `getInitials`

- Signature: `function getInitials()`
- Source range: lines 103-107

#### Line-by-line

- Line 103:
  ```text
    function getInitials() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 104:
  ```text
        const first = (firstNameEl?.value || "N").trim()[0] || "N";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 105:
  ```text
        const last = (lastNameEl?.value || "E").trim()[0] || "E";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 106:
  ```text
        return `${first}${last}`.toUpperCase();
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 107:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 6. `updatePhotoPreviewFromUrl`

- Signature: `function updatePhotoPreviewFromUrl(url)`
- Source range: lines 109-120

#### Line-by-line

- Line 109:
  ```text
    function updatePhotoPreviewFromUrl(url) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 110:
  ```text
        if (!photoPreview) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 111:
  ```text
        if (!url) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 112:
  ```text
            photoPreview.style.backgroundImage = "";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 113:
  ```text
            photoPreview.textContent = getInitials();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 114:
  ```text
            return;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 115:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 116:
  ```text
        photoPreview.textContent = "";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 117:
  ```text
        photoPreview.style.backgroundImage = `url('${url}')`;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 118:
  ```text
        photoPreview.style.backgroundSize = "cover";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 119:
  ```text
        photoPreview.style.backgroundPosition = "center";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 120:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 7. `updateInitials`

- Signature: `const updateInitials = () =>`
- Source range: lines 154-158

#### Line-by-line

- Line 154:
  ```text
        const updateInitials = () => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 155:
  ```text
            if (!photoUrl?.value && !photoDataUrl) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 156:
  ```text
                updatePhotoPreviewFromUrl("");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 157:
  ```text
            }
  ```
  Explanation: Closes the current block/scope.
- Line 158:
  ```text
        };
  ```
  Explanation: Closes the current block/scope.

### 8. `submitPasswordOnly`

- Signature: `async function submitPasswordOnly()`
- Source range: lines 171-201

#### Line-by-line

- Line 171:
  ```text
    async function submitPasswordOnly() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 172:
  ```text
        const pw = document.getElementById("new-password").value.trim();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 173:
  ```text
        const confirm = document.getElementById("confirm-password").value.trim();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 174:
  ```text
        if (!pw || !confirm) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 175:
  ```text
            setError("Please enter and confirm your new password.");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 176:
  ```text
            return;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 177:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 178:
  ```text
        if (pw.length < 6) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 179:
  ```text
            setError("Password must be at least 6 characters.");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 180:
  ```text
            return;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 181:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 182:
  ```text
        if (pw !== confirm) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 183:
  ```text
            setError("Passwords do not match.");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 184:
  ```text
            return;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 185:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 186:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 187:
  ```text
        nextBtn.disabled = true;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 188:
  ```text
        nextBtn.classList.add("opacity-70");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 189:
  ```text
        setError("");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 190:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 191:
  ```text
        const result = await API.updateProfile({ new_password: pw });
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 192:
  ```text
        if (!result || !result.success) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 193:
  ```text
            setError((result && result.error) || "Failed to update password.");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 194:
  ```text
            nextBtn.disabled = false;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 195:
  ```text
            nextBtn.classList.remove("opacity-70");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 196:
  ```text
            return;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 197:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 198:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 199:
  ```text
        sessionStorage.setItem("needsProfileSetup", "false");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 200:
  ```text
        window.location.href = "./dashboard.html";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 201:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

## File: `electron-app/scripts/package-api.js`

- Methods documented: 15

### 1. `request`

- Signature: `async function request(path, param)`
- Source range: lines 12-27

#### Line-by-line

- Line 12:
  ```text
async function request(path, options = {}) {
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 13:
  ```text
    const res = await fetch(`${API_BASE}${path}`, {
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 14:
  ```text
        headers: { "Content-Type": "application/json" },
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 15:
  ```text
        ...options,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 16:
  ```text
    });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 17:
  ```text
    if (!res.ok) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 18:
  ```text
        let detail = `HTTP ${res.status}`;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 19:
  ```text
        try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 20:
  ```text
            const err = await res.json();
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 21:
  ```text
            detail = err.detail || JSON.stringify(err);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 22:
  ```text
        } catch (_) {}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 23:
  ```text
        throw new Error(detail);
  ```
  Explanation: Raises an error/exception to signal failure to the caller.
- Line 24:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 25:
  ```text
    if (res.status === 204) return null;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 26:
  ```text
    return res.json();
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 27:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 2. `getFileTooLargeMessage`

- Signature: `function getFileTooLargeMessage()`
- Source range: lines 29-31

#### Line-by-line

- Line 29:
  ```text
function getFileTooLargeMessage() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 30:
  ```text
    return "Photo must be 2MB or smaller.";
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 31:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 3. `ensureUploadSize`

- Signature: `function ensureUploadSize(file)`
- Source range: lines 33-40

#### Line-by-line

- Line 33:
  ```text
function ensureUploadSize(file) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 34:
  ```text
    if (!file) return true;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 35:
  ```text
    if (file.size > MAX_UPLOAD_BYTES) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 36:
  ```text
        alert(getFileTooLargeMessage());
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 37:
  ```text
        return false;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 38:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 39:
  ```text
    return true;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 40:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 4. `fileToDataUrl`

- Signature: `function fileToDataUrl(file)`
- Source range: lines 42-49

#### Line-by-line

- Line 42:
  ```text
function fileToDataUrl(file) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 43:
  ```text
    return new Promise((resolve, reject) => {
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 44:
  ```text
        const reader = new FileReader();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 45:
  ```text
        reader.onload = () => resolve(reader.result || "");
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 46:
  ```text
        reader.onerror = () => reject(new Error("Failed to read image file."));
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 47:
  ```text
        reader.readAsDataURL(file);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 48:
  ```text
    });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 49:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 5. `loadCategories`

- Signature: `async function loadCategories()`
- Source range: lines 51-92

#### Line-by-line

- Line 51:
  ```text
async function loadCategories() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 52:
  ```text
    const [catRes, pkgRes] = await Promise.all([
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 53:
  ```text
        request("/categories/"),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 54:
  ```text
        request("/packages/"),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 55:
  ```text
    ]);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 56:
  ```text
    const categoryList = Array.isArray(catRes) ? catRes : catRes.results || [];
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 57:
  ```text
    const packages = Array.isArray(pkgRes) ? pkgRes : pkgRes.results || [];
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 58:
  ```text
    const packageCountMap = new Map();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 59:
  ```text
    for (const p of packages) {
  ```
  Explanation: Iterates over a collection to apply repeated processing.
- Line 60:
  ```text
        const cat = String(p.category || "").trim();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 61:
  ```text
        if (!cat) continue;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 62:
  ```text
        packageCountMap.set(cat.toLowerCase(), (packageCountMap.get(cat.toLowerCase()) || 0) + 1);
  ```
  Explanation: Triggers an external API/network request as part of this flow.
- Line 63:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 64:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 65:
  ```text
    const map = new Map();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 66:
  ```text
    for (const c of categoryList) {
  ```
  Explanation: Iterates over a collection to apply repeated processing.
- Line 67:
  ```text
        const name = String(c.name || "").trim();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 68:
  ```text
        if (!name) continue;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 69:
  ```text
        map.set(name.toLowerCase(), {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 70:
  ```text
            id: c.id,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 71:
  ```text
            name,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 72:
  ```text
            image: c.image_url || "",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 73:
  ```text
            packageCount: packageCountMap.get(name.toLowerCase()) || 0,
  ```
  Explanation: Triggers an external API/network request as part of this flow.
- Line 74:
  ```text
        });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 75:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 76:
  ```text
    // Keep backward compatibility: show package categories that do not have a category row yet.
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 77:
  ```text
    for (const p of packages) {
  ```
  Explanation: Iterates over a collection to apply repeated processing.
- Line 78:
  ```text
        const name = String(p.category || "").trim();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 79:
  ```text
        if (!name) continue;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 80:
  ```text
        const key = name.toLowerCase();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 81:
  ```text
        if (!map.has(key)) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 82:
  ```text
            map.set(key, {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 83:
  ```text
                id: null,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 84:
  ```text
                name,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 85:
  ```text
                image: "",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 86:
  ```text
                packageCount: packageCountMap.get(key) || 0,
  ```
  Explanation: Triggers an external API/network request as part of this flow.
- Line 87:
  ```text
            });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 88:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 89:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 90:
  ```text
    categories = Array.from(map.values()).sort((a, b) => a.name.localeCompare(b.name));
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 91:
  ```text
    renderCategories();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 92:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 6. `renderCategories`

- Signature: `function renderCategories()`
- Source range: lines 117-153

#### Line-by-line

- Line 117:
  ```text
function renderCategories() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 118:
  ```text
    const grid = document.getElementById("categoriesGrid");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 119:
  ```text
    if (!grid) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 120:
  ```text
    grid.innerHTML = "";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 121:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 122:
  ```text
    categories.forEach((category) => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 123:
  ```text
        const card = document.createElement("div");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 124:
  ```text
        card.className =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 125:
  ```text
            "flex flex-col rounded-2xl overflow-hidden bg-white shadow-sm hover:shadow-md transition-shadow";
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 126:
  ```text
        const safeName = category.name.replace(/"/g, "&quot;");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 127:
  ```text
        const safeImg = category.image || FALLBACK_IMG;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 128:
  ```text
        card.innerHTML = `
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 129:
  ```text
            <div onclick="navigateToPackagesList(this.dataset.cat)" data-cat="${safeName}" class="cursor-pointer">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 130:
  ```text
                <img src="${safeImg}" alt="${safeName}" class="w-full h-[156px] object-cover rounded-t-2xl"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 131:
  ```text
                     onerror="this.src='${FALLBACK_IMG}'">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 132:
  ```text
            </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 133:
  ```text
            <div class="flex items-center p-3 bg-white">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 134:
  ```text
                <div class="flex-1 text-center">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 135:
  ```text
                    <p class="text-[#4F6E79] font-segoe text-base font-bold">${safeName}</p>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 136:
  ```text
                    <p class="text-[#9AA8AF] font-segoe text-xs">${category.packageCount} package(s)</p>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 137:
  ```text
                </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 138:
  ```text
                <button onclick="openEditModal(${category.id == null ? "null" : category.id}, '${safeName.replace(/'/g, "\\'")}')" class="flex-shrink-0 p-2 hover:bg-gray-100 rounded">
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 139:
  ```text
                    ✎
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 140:
  ```text
                </button>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 141:
  ```text
            </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 142:
  ```text
        `;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 143:
  ```text
        grid.appendChild(card);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 144:
  ```text
    });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 145:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 146:
  ```text
    const createCard = document.createElement("button");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 147:
  ```text
    createCard.onclick = openCreateModal;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 148:
  ```text
    createCard.className =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 149:
  ```text
        "flex flex-col items-center justify-center rounded-2xl border-4 border-dashed border-[#BDDAE0] h-[224px] hover:border-[#9DBAC0] transition-colors";
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 150:
  ```text
    createCard.innerHTML =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 151:
  ```text
        '<p class="text-[#BDDAE0] font-segoe text-base font-bold">CREATE CATEGORY</p>';
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 152:
  ```text
    grid.appendChild(createCard);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 153:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 7. `openCreateModal`

- Signature: `function openCreateModal()`
- Source range: lines 155-161

#### Line-by-line

- Line 155:
  ```text
function openCreateModal() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 156:
  ```text
    pendingCategoryImage = null;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 157:
  ```text
    document.getElementById("createCategoryName").value = "";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 158:
  ```text
    document.getElementById("createCategoryPhoto").value = "";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 159:
  ```text
    document.getElementById("createPhotoName").textContent = "photo.png";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 160:
  ```text
    document.getElementById("createCategoryModal").classList.remove("hidden");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 161:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 8. `closeCreateModal`

- Signature: `function closeCreateModal()`
- Source range: lines 163-165

#### Line-by-line

- Line 163:
  ```text
function closeCreateModal() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 164:
  ```text
    document.getElementById("createCategoryModal").classList.add("hidden");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 165:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 9. `openEditModal`

- Signature: `function openEditModal(categoryId, categoryName)`
- Source range: lines 167-176

#### Line-by-line

- Line 167:
  ```text
function openEditModal(categoryId, categoryName) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 168:
  ```text
    editingCategoryId = categoryId;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 169:
  ```text
    editingCategoryName = categoryName;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 170:
  ```text
    pendingEditImage = null;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 171:
  ```text
    document.getElementById("editCategoryName").value = categoryName;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 172:
  ```text
    document.getElementById("editCategoryPhoto").value = "";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 173:
  ```text
    const label = document.getElementById("editPhotoName");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 174:
  ```text
    if (label) label.textContent = "photo.png";
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 175:
  ```text
    document.getElementById("editCategoryModal").classList.remove("hidden");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 176:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 10. `closeEditModal`

- Signature: `function closeEditModal()`
- Source range: lines 178-182

#### Line-by-line

- Line 178:
  ```text
function closeEditModal() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 179:
  ```text
    document.getElementById("editCategoryModal").classList.add("hidden");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 180:
  ```text
    editingCategoryId = null;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 181:
  ```text
    editingCategoryName = null;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 182:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 11. `saveCreateCategory`

- Signature: `async function saveCreateCategory()`
- Source range: lines 184-207

#### Line-by-line

- Line 184:
  ```text
async function saveCreateCategory() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 185:
  ```text
    const name = document.getElementById("createCategoryName").value.trim();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 186:
  ```text
    if (!name) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 187:
  ```text
        alert("Please enter a category name.");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 188:
  ```text
        return;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 189:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 190:
  ```text
    try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 191:
  ```text
        const imageUrl = pendingCategoryImage
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 192:
  ```text
            ? await fileToDataUrl(pendingCategoryImage)
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 193:
  ```text
            : null;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 194:
  ```text
        await request("/categories/", {
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 195:
  ```text
            method: "POST",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 196:
  ```text
            body: JSON.stringify({
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 197:
  ```text
                name,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 198:
  ```text
                image_url: imageUrl,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 199:
  ```text
            }),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 200:
  ```text
        });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 201:
  ```text
        closeCreateModal();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 202:
  ```text
        await loadCategories();
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 203:
  ```text
        navigateToPackagesList(name);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 204:
  ```text
    } catch (e) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 205:
  ```text
        alert(`Failed to create category: ${e.message}`);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 206:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 207:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 12. `saveEditCategory`

- Signature: `async function saveEditCategory()`
- Source range: lines 209-236

#### Line-by-line

- Line 209:
  ```text
async function saveEditCategory() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 210:
  ```text
    const newName = document.getElementById("editCategoryName").value.trim();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 211:
  ```text
    if (!editingCategoryName) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 212:
  ```text
    if (!editingCategoryId) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 213:
  ```text
        alert("This category needs to be recreated to support independent category photo.");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 214:
  ```text
        return;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 215:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 216:
  ```text
    if (!newName) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 217:
  ```text
        alert("Please enter a category name.");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 218:
  ```text
        return;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 219:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 220:
  ```text
    try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 221:
  ```text
        const imageUrl = pendingEditImage
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 222:
  ```text
            ? await fileToDataUrl(pendingEditImage)
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 223:
  ```text
            : null;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 224:
  ```text
        await request(`/categories/${editingCategoryId}/`, {
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 225:
  ```text
            method: "PATCH",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 226:
  ```text
            body: JSON.stringify({
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 227:
  ```text
                name: newName,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 228:
  ```text
                ...(imageUrl ? { image_url: imageUrl } : {}),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 229:
  ```text
            }),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 230:
  ```text
        });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 231:
  ```text
        closeEditModal();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 232:
  ```text
        await loadCategories();
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 233:
  ```text
    } catch (e) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 234:
  ```text
        alert(`Failed to update category: ${e.message}`);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 235:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 236:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 13. `deleteCategory`

- Signature: `async function deleteCategory()`
- Source range: lines 238-270

#### Line-by-line

- Line 238:
  ```text
async function deleteCategory() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 239:
  ```text
    if (!editingCategoryName) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 240:
  ```text
    if (!editingCategoryId) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 241:
  ```text
        alert("This category cannot be deleted until it is recreated in Categories.");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 242:
  ```text
        return;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 243:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 244:
  ```text
    if (
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 245:
  ```text
        !confirm(
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 246:
  ```text
            `Delete category "${editingCategoryName}" and all packages inside it?`,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 247:
  ```text
        )
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 248:
  ```text
    ) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 249:
  ```text
        return;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 250:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 251:
  ```text
    try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 252:
  ```text
        const pkgRes = await request("/packages/");
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 253:
  ```text
        const packages = Array.isArray(pkgRes) ? pkgRes : pkgRes.results || [];
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 254:
  ```text
        const matches = packages.filter(
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 255:
  ```text
            (p) =>
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 256:
  ```text
                String(p.category || "").trim().toLowerCase() ===
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 257:
  ```text
                editingCategoryName.toLowerCase(),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 258:
  ```text
        );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 259:
  ```text
        await Promise.all(
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 260:
  ```text
            matches.map((p) =>
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 261:
  ```text
                request(`/packages/${p.id}/`, { method: "DELETE" }),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 262:
  ```text
            ),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 263:
  ```text
        );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 264:
  ```text
        await request(`/categories/${editingCategoryId}/`, { method: "DELETE" });
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 265:
  ```text
        closeEditModal();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 266:
  ```text
        await loadCategories();
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 267:
  ```text
    } catch (e) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 268:
  ```text
        alert(`Failed to delete category: ${e.message}`);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 269:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 270:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 14. `navigateToPackagesList`

- Signature: `function navigateToPackagesList(categoryName)`
- Source range: lines 272-274

#### Line-by-line

- Line 272:
  ```text
function navigateToPackagesList(categoryName) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 273:
  ```text
    window.location.href = `packages-list.html?category=${encodeURIComponent(categoryName)}`;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 274:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 15. `startPackagePage`

- Signature: `async function startPackagePage()`
- Source range: lines 333-345

#### Line-by-line

- Line 333:
  ```text
async function startPackagePage() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 334:
  ```text
    const loading = document.getElementById("categoriesLoading");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 335:
  ```text
    const grid = document.getElementById("categoriesGrid");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 336:
  ```text
    try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 337:
  ```text
        await loadCategories();
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 338:
  ```text
    } catch (e) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 339:
  ```text
        console.error("Failed to load categories:", e);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 340:
  ```text
        alert(`Failed to load categories: ${e.message}`);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 341:
  ```text
    } finally {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 342:
  ```text
        if (loading) loading.style.display = "none";
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 343:
  ```text
        if (grid) grid.classList.remove("hidden");
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 344:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 345:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

## File: `electron-app/scripts/package-list-api.js`

- Methods documented: 32

### 1. `apiRequest`

- Signature: `async function apiRequest(path, param)`
- Source range: lines 29-44

#### Line-by-line

- Line 29:
  ```text
async function apiRequest(path, options = {}) {
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 30:
  ```text
    const res = await fetch(`${API_BASE}${path}`, {
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 31:
  ```text
        headers: { "Content-Type": "application/json" },
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 32:
  ```text
        ...options,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 33:
  ```text
    });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 34:
  ```text
    if (!res.ok) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 35:
  ```text
        let detail = `HTTP ${res.status}`;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 36:
  ```text
        try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 37:
  ```text
            const err = await res.json();
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 38:
  ```text
            detail = err.detail || JSON.stringify(err);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 39:
  ```text
        } catch (_) {}
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 40:
  ```text
        throw new Error(detail);
  ```
  Explanation: Raises an error/exception to signal failure to the caller.
- Line 41:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 42:
  ```text
    if (res.status === 204) return null;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 43:
  ```text
    return res.json();
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 44:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 2. `ensureUploadSize`

- Signature: `function ensureUploadSize(file)`
- Source range: lines 46-53

#### Line-by-line

- Line 46:
  ```text
function ensureUploadSize(file) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 47:
  ```text
    if (!file) return true;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 48:
  ```text
    if (file.size > MAX_UPLOAD_BYTES) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 49:
  ```text
        alert("Photo must be 2MB or smaller.");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 50:
  ```text
        return false;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 51:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 52:
  ```text
    return true;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 53:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 3. `fileToDataUrl`

- Signature: `function fileToDataUrl(file)`
- Source range: lines 55-62

#### Line-by-line

- Line 55:
  ```text
function fileToDataUrl(file) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 56:
  ```text
    return new Promise((resolve, reject) => {
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 57:
  ```text
        const reader = new FileReader();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 58:
  ```text
        reader.onload = () => resolve(reader.result || "");
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 59:
  ```text
        reader.onerror = () => reject(new Error("Failed to read image file."));
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 60:
  ```text
        reader.readAsDataURL(file);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 61:
  ```text
    });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 62:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 4. `formatPrice`

- Signature: `function formatPrice(price)`
- Source range: lines 64-66

#### Line-by-line

- Line 64:
  ```text
function formatPrice(price) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 65:
  ```text
    return `₱${toAmount(price).toFixed(2)}`;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 66:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 5. `toAmount`

- Signature: `function toAmount(value)`
- Source range: lines 68-76

#### Line-by-line

- Line 68:
  ```text
function toAmount(value) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 69:
  ```text
    if (typeof value === "number" && Number.isFinite(value)) return value;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 70:
  ```text
    if (typeof value === "string") {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 71:
  ```text
        const cleaned = value.replace(/[^\d.-]/g, "");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 72:
  ```text
        const parsed = Number(cleaned);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 73:
  ```text
        return Number.isFinite(parsed) ? parsed : 0;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 74:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 75:
  ```text
    return 0;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 76:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 6. `categoryImage`

- Signature: `function categoryImage(category)`
- Source range: lines 78-98

#### Line-by-line

- Line 78:
  ```text
function categoryImage(category) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 79:
  ```text
    const raw = String(category || "").trim();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 80:
  ```text
    if (!raw) return FALLBACK_IMG;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 81:
  ```text
    if (CATEGORY_IMG[raw]) return CATEGORY_IMG[raw];
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 82:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 83:
  ```text
    const normalized = raw.toLowerCase();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 84:
  ```text
    if (normalized.includes("christmas")) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 85:
  ```text
        return CATEGORY_IMG["Christmas Package 2024"];
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 86:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 87:
  ```text
    if (normalized.includes("graduation")) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 88:
  ```text
        return CATEGORY_IMG.Graduation;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 89:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 90:
  ```text
    if (normalized.includes("yearbook")) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 91:
  ```text
        return CATEGORY_IMG.Yearbook;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 92:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 93:
  ```text
    if (normalized.includes("regular")) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 94:
  ```text
        return CATEGORY_IMG.Regular;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 95:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 96:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 97:
  ```text
    return FALLBACK_IMG;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 98:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 7. `normalizePackage`

- Signature: `function normalizePackage(p)`
- Source range: lines 100-117

#### Line-by-line

- Line 100:
  ```text
function normalizePackage(p) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 101:
  ```text
    const basePrice = toAmount(p.price);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 102:
  ```text
    const promoPrice = p.promo_price == null ? null : toAmount(p.promo_price);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 103:
  ```text
    const displayPrice =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 104:
  ```text
        promoPrice != null && promoPrice > 0 ? promoPrice : basePrice;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 105:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 106:
  ```text
    return {
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 107:
  ```text
        id: p.id,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 108:
  ```text
        name: p.name || "",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 109:
  ```text
        category: p.category || "",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 110:
  ```text
        price: displayPrice,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 111:
  ```text
        originalPrice: basePrice,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 112:
  ```text
        promoPrice,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 113:
  ```text
        raw: p,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 114:
  ```text
        details: Array.isArray(p.inclusions) ? p.inclusions : [],
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 115:
  ```text
        image: p.image_url || categoryImage(p.category),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 116:
  ```text
    };
  ```
  Explanation: Closes the current block/scope.
- Line 117:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 8. `normalizeAddon`

- Signature: `function normalizeAddon(a)`
- Source range: lines 119-129

#### Line-by-line

- Line 119:
  ```text
function normalizeAddon(a) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 120:
  ```text
    return {
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 121:
  ```text
        id: a.id,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 122:
  ```text
        name: a.name || "",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 123:
  ```text
        price: Number(a.price || 0),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 124:
  ```text
        description: a.additional_info || "",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 125:
  ```text
        appliesTo: a.applies_to,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 126:
  ```text
        image: a.image_url || "",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 127:
  ```text
        raw: a,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 128:
  ```text
    };
  ```
  Explanation: Closes the current block/scope.
- Line 129:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 9. `addonAppliesToCategory`

- Signature: `function addonAppliesToCategory(addon, cat)`
- Source range: lines 131-139

#### Line-by-line

- Line 131:
  ```text
function addonAppliesToCategory(addon, cat) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 132:
  ```text
    if (!cat) return true;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 133:
  ```text
    const applies = addon.applies_to;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 134:
  ```text
    if (!applies) return true;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 135:
  ```text
    if (Array.isArray(applies)) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 136:
  ```text
        return applies.map(String).some((x) => x.toLowerCase() === cat.toLowerCase());
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 137:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 138:
  ```text
    return String(applies).toLowerCase().includes(cat.toLowerCase());
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 139:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 10. `initializeData`

- Signature: `async function initializeData()`
- Source range: lines 141-165

#### Line-by-line

- Line 141:
  ```text
async function initializeData() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 142:
  ```text
    const params = new URLSearchParams(window.location.search);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 143:
  ```text
    categoryName = decodeURIComponent(params.get("category") || "").trim();
  ```
  Explanation: Triggers an external API/network request as part of this flow.
- Line 144:
  ```text
    if (!categoryName) categoryName = "All";
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 145:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 146:
  ```text
    document.getElementById("pageTitle").textContent = `Package List - ${categoryName}`;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 147:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 148:
  ```text
    const [pkgRes, addonRes] = await Promise.all([
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 149:
  ```text
        apiRequest("/packages/"),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 150:
  ```text
        apiRequest("/addons/"),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 151:
  ```text
    ]);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 152:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 153:
  ```text
    const pkgList = Array.isArray(pkgRes) ? pkgRes : pkgRes.results || [];
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 154:
  ```text
    const addonList = Array.isArray(addonRes) ? addonRes : addonRes.results || [];
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 155:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 156:
  ```text
    packages = pkgList
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 157:
  ```text
        .filter((p) => categoryName === "All" || String(p.category || "").toLowerCase() === categoryName.toLowerCase())
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 158:
  ```text
        .map(normalizePackage);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 159:
  ```text
    addons = addonList
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 160:
  ```text
        .filter((a) => categoryName === "All" || addonAppliesToCategory(a, categoryName))
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 161:
  ```text
        .map(normalizeAddon);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 162:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 163:
  ```text
    filteredPackages = [...packages];
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 164:
  ```text
    filteredAddons = [...addons];
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 165:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 11. `renderItems`

- Signature: `function renderItems()`
- Source range: lines 167-232

#### Line-by-line

- Line 167:
  ```text
function renderItems() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 168:
  ```text
    const grid = document.getElementById("gridContainer");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 169:
  ```text
    if (!grid) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 170:
  ```text
    grid.innerHTML = "";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 171:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 172:
  ```text
    const items = currentTab === "package" ? filteredPackages : filteredAddons;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 173:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 174:
  ```text
    items.forEach((item) => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 175:
  ```text
        const card = document.createElement("div");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 176:
  ```text
        if (currentTab === "package") {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 177:
  ```text
            card.className = "flex flex-col rounded-2xl overflow-hidden bg-white shadow-sm";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 178:
  ```text
            card.innerHTML = `
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 179:
  ```text
                <img src="${item.image}" alt="${item.name}" class="w-full h-[156px] object-cover"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 180:
  ```text
                     onerror="this.src='${FALLBACK_IMG}'">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 181:
  ```text
                <div class="p-4 space-y-2">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 182:
  ```text
                    <div class="flex items-start justify-between gap-2">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 183:
  ```text
                        <div class="flex-1 min-w-0">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 184:
  ```text
                            <h3 class="text-[#4F6E79] font-segoe text-lg font-bold truncate">${item.name}</h3>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 185:
  ```text
                            ${
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 186:
  ```text
                                item.promoPrice != null &&
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 187:
  ```text
                                item.promoPrice > 0 &&
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 188:
  ```text
                                item.originalPrice > item.promoPrice
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 189:
  ```text
                                    ? `<p class="flex items-center gap-2">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 190:
  ```text
                                         <span class="text-[#4F6E79] font-segoe text-sm font-bold">${formatPrice(item.promoPrice)}</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 191:
  ```text
                                         <span class="text-[#9AA8AF] font-segoe text-xs line-through">${formatPrice(item.originalPrice)}</span>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 192:
  ```text
                                       </p>`
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 193:
  ```text
                                    : `<p class="text-[#4F6E79] font-segoe text-sm font-bold">${formatPrice(item.price)}</p>`
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 194:
  ```text
                            }
  ```
  Explanation: Closes the current block/scope.
- Line 195:
  ```text
                        </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 196:
  ```text
                        <button onclick="openEditModal(${item.id}, 'package')" class="p-1 hover:bg-gray-100 rounded">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 197:
  ```text
                            ✎
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 198:
  ```text
                        </button>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 199:
  ```text
                    </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 200:
  ```text
                    ${item.details.slice(0, 3).map((d) => `<p class="text-[#777] font-segoe text-sm">${d}</p>`).join("")}
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 201:
  ```text
                </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 202:
  ```text
            `;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 203:
  ```text
        } else {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 204:
  ```text
            card.className = "flex flex-col p-4 rounded-2xl bg-white shadow-sm space-y-2";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 205:
  ```text
            card.innerHTML = `
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 206:
  ```text
                ${
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 207:
  ```text
                    item.image
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 208:
  ```text
                        ? `<img src="${item.image}" alt="${item.name}" class="w-full h-[120px] object-cover rounded-xl" onerror="this.style.display='none'">`
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 209:
  ```text
                        : ""
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 210:
  ```text
                }
  ```
  Explanation: Closes the current block/scope.
- Line 211:
  ```text
                <div class="flex items-start justify-between gap-2">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 212:
  ```text
                    <div class="flex-1 min-w-0">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 213:
  ```text
                        <h3 class="text-[#4F6E79] font-segoe text-base font-bold truncate">${item.name}</h3>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 214:
  ```text
                        <p class="text-[#4F6E79] font-segoe text-xs font-bold">${formatPrice(item.price)}</p>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 215:
  ```text
                    </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 216:
  ```text
                    <button onclick="openEditModal(${item.id}, 'addon')" class="p-1 hover:bg-gray-100 rounded">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 217:
  ```text
                        ✎
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 218:
  ```text
                    </button>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 219:
  ```text
                </div>
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 220:
  ```text
                <p class="text-[#777] font-segoe text-xs">${item.description || ""}</p>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 221:
  ```text
            `;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 222:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 223:
  ```text
        grid.appendChild(card);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 224:
  ```text
    });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 225:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 226:
  ```text
    const createCard = document.createElement("button");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 227:
  ```text
    createCard.onclick = () => openCreateModal(currentTab);
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 228:
  ```text
    createCard.className =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 229:
  ```text
        `flex items-center justify-center rounded-2xl border-4 border-dashed border-[#BDDAE0] h-[${currentTab === "package" ? "291" : "110"}px] hover:border-[#9DBAC0] transition-colors`;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 230:
  ```text
    createCard.innerHTML = `<p class="text-[#BDDAE0] font-segoe text-base font-bold">${currentTab === "package" ? "CREATE PACKAGE" : "NEW ADDON"}</p>`;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 231:
  ```text
    grid.appendChild(createCard);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 232:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 12. `getInclusionValues`

- Signature: `function getInclusionValues(containerId)`
- Source range: lines 234-239

#### Line-by-line

- Line 234:
  ```text
function getInclusionValues(containerId) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 235:
  ```text
    const inputs = document.querySelectorAll(`#${containerId} input`);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 236:
  ```text
    return Array.from(inputs)
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 237:
  ```text
        .map((i) => i.value.trim())
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 238:
  ```text
        .filter(Boolean);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 239:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 13. `renderInclusionFields`

- Signature: `function renderInclusionFields(containerId, param)`
- Source range: lines 241-258

#### Line-by-line

- Line 241:
  ```text
function renderInclusionFields(containerId, values = [""]) {
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 242:
  ```text
    const container = document.getElementById(containerId);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 243:
  ```text
    if (!container) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 244:
  ```text
    container.innerHTML = "";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 245:
  ```text
    values.forEach((v) => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 246:
  ```text
        const wrapper = document.createElement("div");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 247:
  ```text
        wrapper.className = "flex items-center gap-2";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 248:
  ```text
        wrapper.innerHTML = `
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 249:
  ```text
            <input type="text" value="${v}" placeholder="Enter inclusion"
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 250:
  ```text
                   class="flex-1 border border-[#A2A2A2] rounded-md px-3 py-2 text-xs focus:outline-none focus:ring-2 focus:ring-[#165166]">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 251:
  ```text
            <button type="button" class="p-1 text-[#A2A2A2] hover:text-red-500" data-remove>🗑</button>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 252:
  ```text
        `;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 253:
  ```text
        wrapper.querySelector("[data-remove]").addEventListener("click", () => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 254:
  ```text
            wrapper.remove();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 255:
  ```text
        });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 256:
  ```text
        container.appendChild(wrapper);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 257:
  ```text
    });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 258:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 14. `openCreateModal`

- Signature: `function openCreateModal(type)`
- Source range: lines 260-280

#### Line-by-line

- Line 260:
  ```text
function openCreateModal(type) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 261:
  ```text
    editingItemId = null;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 262:
  ```text
    editingItemType = type;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 263:
  ```text
    document.getElementById("createModalTitle").textContent = type === "package" ? "Create Package" : "Create Addon";
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 264:
  ```text
    document.getElementById("createSaveBtn").textContent = type === "package" ? "Save" : "Confirm";
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 265:
  ```text
    document.getElementById("packageFormFields").style.display = type === "package" ? "block" : "none";
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 266:
  ```text
    document.getElementById("addonFormFields").style.display = type === "addon" ? "block" : "none";
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 267:
  ```text
    document.getElementById("createName").value = "";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 268:
  ```text
    document.getElementById("createAddonName").value = "";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 269:
  ```text
    document.getElementById("createAddonPrice").value = "";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 270:
  ```text
    document.getElementById("createAddonComment").value = "";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 271:
  ```text
    document.getElementById("createPhoto").value = "";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 272:
  ```text
    document.getElementById("createPhotoName").textContent = "photo.png";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 273:
  ```text
    document.getElementById("createAddonPhoto").value = "";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 274:
  ```text
    document.getElementById("createAddonPhotoName").textContent = "photo.png";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 275:
  ```text
    pendingCreatePackageImage = null;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 276:
  ```text
    pendingCreateAddonImage = null;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 277:
  ```text
    document.getElementById("commentCount").textContent = "0";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 278:
  ```text
    renderInclusionFields("inclusionsContainer", [""]);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 279:
  ```text
    document.getElementById("createModal").classList.remove("hidden");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 280:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 15. `closeCreateModal`

- Signature: `function closeCreateModal()`
- Source range: lines 282-284

#### Line-by-line

- Line 282:
  ```text
function closeCreateModal() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 283:
  ```text
    document.getElementById("createModal").classList.add("hidden");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 284:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 16. `openEditModal`

- Signature: `function openEditModal(itemId, type)`
- Source range: lines 286-316

#### Line-by-line

- Line 286:
  ```text
function openEditModal(itemId, type) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 287:
  ```text
    editingItemId = itemId;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 288:
  ```text
    editingItemType = type;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 289:
  ```text
    document.getElementById("editModalTitle").textContent = type === "package" ? "Edit Package" : "Edit Addon";
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 290:
  ```text
    document.getElementById("editSaveBtn").textContent = type === "package" ? "Save" : "Confirm";
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 291:
  ```text
    document.getElementById("editPackageFields").style.display = type === "package" ? "block" : "none";
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 292:
  ```text
    document.getElementById("editAddonFields").style.display = type === "addon" ? "block" : "none";
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 293:
  ```text
    document.getElementById("editPhoto").value = "";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 294:
  ```text
    const editPhotoLabel = document.getElementById("editPhotoName");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 295:
  ```text
    if (editPhotoLabel) editPhotoLabel.textContent = "photo.png";
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 296:
  ```text
    document.getElementById("editAddonPhoto").value = "";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 297:
  ```text
    const editAddonPhotoLabel = document.getElementById("editAddonPhotoName");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 298:
  ```text
    if (editAddonPhotoLabel) editAddonPhotoLabel.textContent = "photo.png";
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 299:
  ```text
    pendingEditPackageImage = null;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 300:
  ```text
    pendingEditAddonImage = null;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 301:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 302:
  ```text
    if (type === "package") {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 303:
  ```text
        const item = packages.find((p) => p.id === itemId);
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 304:
  ```text
        if (!item) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 305:
  ```text
        document.getElementById("editName").value = item.name;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 306:
  ```text
        renderInclusionFields("editInclusionsContainer", item.details.length ? item.details : [""]);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 307:
  ```text
    } else {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 308:
  ```text
        const item = addons.find((a) => a.id === itemId);
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 309:
  ```text
        if (!item) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 310:
  ```text
        document.getElementById("editAddonName").value = item.name;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 311:
  ```text
        document.getElementById("editAddonPrice").value = String(item.price || "");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 312:
  ```text
        document.getElementById("editAddonComment").value = item.description || "";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 313:
  ```text
        document.getElementById("editCommentCount").textContent = String((item.description || "").length);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 314:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 315:
  ```text
    document.getElementById("editModal").classList.remove("hidden");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 316:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 17. `closeEditModal`

- Signature: `function closeEditModal()`
- Source range: lines 318-321

#### Line-by-line

- Line 318:
  ```text
function closeEditModal() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 319:
  ```text
    document.getElementById("editModal").classList.add("hidden");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 320:
  ```text
    editingItemId = null;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 321:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 18. `saveCreateItem`

- Signature: `async function saveCreateItem()`
- Source range: lines 323-368

#### Line-by-line

- Line 323:
  ```text
async function saveCreateItem() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 324:
  ```text
    try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 325:
  ```text
        if (editingItemType === "package") {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 326:
  ```text
            const name = document.getElementById("createName").value.trim();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 327:
  ```text
            if (!name) throw new Error("Package name is required.");
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 328:
  ```text
            const inclusions = getInclusionValues("inclusionsContainer");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 329:
  ```text
            const imageUrl = pendingCreatePackageImage
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 330:
  ```text
                ? await fileToDataUrl(pendingCreatePackageImage)
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 331:
  ```text
                : null;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 332:
  ```text
            await apiRequest("/packages/", {
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 333:
  ```text
                method: "POST",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 334:
  ```text
                body: JSON.stringify({
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 335:
  ```text
                    name,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 336:
  ```text
                    category: categoryName === "All" ? "General" : categoryName,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 337:
  ```text
                    price: 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 338:
  ```text
                    inclusions,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 339:
  ```text
                    image_url: imageUrl,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 340:
  ```text
                }),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 341:
  ```text
            });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 342:
  ```text
        } else {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 343:
  ```text
            const name = document.getElementById("createAddonName").value.trim();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 344:
  ```text
            const price = Number(document.getElementById("createAddonPrice").value);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 345:
  ```text
            const additional_info = document.getElementById("createAddonComment").value.trim();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 346:
  ```text
            if (!name || Number.isNaN(price)) throw new Error("Addon name and numeric price are required.");
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 347:
  ```text
            const imageUrl = pendingCreateAddonImage
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 348:
  ```text
                ? await fileToDataUrl(pendingCreateAddonImage)
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 349:
  ```text
                : null;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 350:
  ```text
            await apiRequest("/addons/", {
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 351:
  ```text
                method: "POST",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 352:
  ```text
                body: JSON.stringify({
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 353:
  ```text
                    name,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 354:
  ```text
                    price,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 355:
  ```text
                    additional_info,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 356:
  ```text
                    applies_to: categoryName === "All" ? "" : categoryName,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 357:
  ```text
                    image_url: imageUrl,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 358:
  ```text
                }),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 359:
  ```text
            });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 360:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 361:
  ```text
        closeCreateModal();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 362:
  ```text
        await initializeData();
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 363:
  ```text
        applySearchFilter();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 364:
  ```text
        renderItems();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 365:
  ```text
    } catch (e) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 366:
  ```text
        alert(`Create failed: ${e.message}`);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 367:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 368:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 19. `saveEditItem`

- Signature: `async function saveEditItem()`
- Source range: lines 370-413

#### Line-by-line

- Line 370:
  ```text
async function saveEditItem() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 371:
  ```text
    if (!editingItemId) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 372:
  ```text
    try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 373:
  ```text
        if (editingItemType === "package") {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 374:
  ```text
            const name = document.getElementById("editName").value.trim();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 375:
  ```text
            if (!name) throw new Error("Package name is required.");
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 376:
  ```text
            const inclusions = getInclusionValues("editInclusionsContainer");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 377:
  ```text
            const imageUrl = pendingEditPackageImage
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 378:
  ```text
                ? await fileToDataUrl(pendingEditPackageImage)
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 379:
  ```text
                : null;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 380:
  ```text
            await apiRequest(`/packages/${editingItemId}/`, {
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 381:
  ```text
                method: "PATCH",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 382:
  ```text
                body: JSON.stringify({
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 383:
  ```text
                    name,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 384:
  ```text
                    inclusions,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 385:
  ```text
                    ...(imageUrl ? { image_url: imageUrl } : {}),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 386:
  ```text
                }),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 387:
  ```text
            });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 388:
  ```text
        } else {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 389:
  ```text
            const name = document.getElementById("editAddonName").value.trim();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 390:
  ```text
            const price = Number(document.getElementById("editAddonPrice").value);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 391:
  ```text
            const additional_info = document.getElementById("editAddonComment").value.trim();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 392:
  ```text
            if (!name || Number.isNaN(price)) throw new Error("Addon name and numeric price are required.");
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 393:
  ```text
            const imageUrl = pendingEditAddonImage
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 394:
  ```text
                ? await fileToDataUrl(pendingEditAddonImage)
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 395:
  ```text
                : null;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 396:
  ```text
            await apiRequest(`/addons/${editingItemId}/`, {
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 397:
  ```text
                method: "PATCH",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 398:
  ```text
                body: JSON.stringify({
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 399:
  ```text
                    name,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 400:
  ```text
                    price,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 401:
  ```text
                    additional_info,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 402:
  ```text
                    ...(imageUrl ? { image_url: imageUrl } : {}),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 403:
  ```text
                }),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 404:
  ```text
            });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 405:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 406:
  ```text
        closeEditModal();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 407:
  ```text
        await initializeData();
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 408:
  ```text
        applySearchFilter();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 409:
  ```text
        renderItems();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 410:
  ```text
    } catch (e) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 411:
  ```text
        alert(`Update failed: ${e.message}`);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 412:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 413:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 20. `deleteItem`

- Signature: `async function deleteItem()`
- Source range: lines 415-431

#### Line-by-line

- Line 415:
  ```text
async function deleteItem() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 416:
  ```text
    if (!editingItemId) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 417:
  ```text
    if (!confirm(`Delete this ${editingItemType}?`)) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 418:
  ```text
    try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 419:
  ```text
        if (editingItemType === "package") {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 420:
  ```text
            await apiRequest(`/packages/${editingItemId}/`, { method: "DELETE" });
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 421:
  ```text
        } else {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 422:
  ```text
            await apiRequest(`/addons/${editingItemId}/`, { method: "DELETE" });
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 423:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 424:
  ```text
        closeEditModal();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 425:
  ```text
        await initializeData();
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 426:
  ```text
        applySearchFilter();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 427:
  ```text
        renderItems();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 428:
  ```text
    } catch (e) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 429:
  ```text
        alert(`Delete failed: ${e.message}`);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 430:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 431:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 21. `applySearchFilter`

- Signature: `function applySearchFilter()`
- Source range: lines 433-437

#### Line-by-line

- Line 433:
  ```text
function applySearchFilter() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 434:
  ```text
    const q = (document.getElementById("searchInput")?.value || "").trim().toLowerCase();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 435:
  ```text
    filteredPackages = packages.filter((p) => p.name.toLowerCase().includes(q));
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 436:
  ```text
    filteredAddons = addons.filter((a) => a.name.toLowerCase().includes(q));
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 437:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 22. `switchTab`

- Signature: `function switchTab(tab)`
- Source range: lines 439-450

#### Line-by-line

- Line 439:
  ```text
function switchTab(tab) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 440:
  ```text
    currentTab = tab;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 441:
  ```text
    document.getElementById("packageTab").className =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 442:
  ```text
        tab === "package"
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 443:
  ```text
            ? "px-6 h-[30px] rounded-l-[17px] bg-[#FFE8AD] text-[#4F6E79] font-segoe text-sm font-semibold transition-colors"
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 444:
  ```text
            : "px-6 h-[30px] rounded-l-[17px]  text-[#FFE8AD] border-2 border-[#FFE8AD] font-segoe text-sm font-semibold transition-colors";
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 445:
  ```text
    document.getElementById("addonTab").className =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 446:
  ```text
        tab === "addon"
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 447:
  ```text
            ? "px-6 h-[30px] rounded-r-[17px] bg-[#FFE8AD] text-[#4F6E79] font-segoe text-sm font-semibold transition-colors"
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 448:
  ```text
            : "px-6 h-[30px] rounded-r-[17px] text-[#FFE8AD] border-2 border-[#FFE8AD] font-segoe text-sm font-semibold transition-colors";
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 449:
  ```text
    renderItems();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 450:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 23. `addInclusionField`

- Signature: `function addInclusionField()`
- Source range: lines 452-456

#### Line-by-line

- Line 452:
  ```text
function addInclusionField() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 453:
  ```text
    const vals = getInclusionValues("inclusionsContainer");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 454:
  ```text
    vals.push("");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 455:
  ```text
    renderInclusionFields("inclusionsContainer", vals);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 456:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 24. `addEditInclusionField`

- Signature: `function addEditInclusionField()`
- Source range: lines 458-462

#### Line-by-line

- Line 458:
  ```text
function addEditInclusionField() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 459:
  ```text
    const vals = getInclusionValues("editInclusionsContainer");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 460:
  ```text
    vals.push("");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 461:
  ```text
    renderInclusionFields("editInclusionsContainer", vals);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 462:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 25. `removeInclusionField`

- Signature: `function removeInclusionField(idx)`
- Source range: lines 464-467

#### Line-by-line

- Line 464:
  ```text
function removeInclusionField(idx) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 465:
  ```text
    const inputs = document.querySelectorAll("#inclusionsContainer input");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 466:
  ```text
    if (inputs[idx]) inputs[idx].parentElement.remove();
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 467:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 26. `removeEditInclusionField`

- Signature: `function removeEditInclusionField(idx)`
- Source range: lines 469-472

#### Line-by-line

- Line 469:
  ```text
function removeEditInclusionField(idx) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 470:
  ```text
    const inputs = document.querySelectorAll("#editInclusionsContainer input");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 471:
  ```text
    if (inputs[idx]) inputs[idx].parentElement.remove();
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 472:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 27. `toggleMobileSidebar`

- Signature: `function toggleMobileSidebar(show)`
- Source range: lines 474-484

#### Line-by-line

- Line 474:
  ```text
function toggleMobileSidebar(show) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 475:
  ```text
    const sidebar = document.getElementById("sidebar");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 476:
  ```text
    const overlay = document.getElementById("mobileOverlay");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 477:
  ```text
    if (show) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 478:
  ```text
        sidebar.classList.add("mobile-open");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 479:
  ```text
        overlay.classList.add("show");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 480:
  ```text
    } else {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 481:
  ```text
        sidebar.classList.remove("mobile-open");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 482:
  ```text
        overlay.classList.remove("show");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 483:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 484:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 28. `goBackToPackages`

- Signature: `function goBackToPackages()`
- Source range: lines 486-488

#### Line-by-line

- Line 486:
  ```text
function goBackToPackages() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 487:
  ```text
    window.location.href = "packages.html";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 488:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 29. `openLogoutModal`

- Signature: `function openLogoutModal(e)`
- Source range: lines 490-494

#### Line-by-line

- Line 490:
  ```text
function openLogoutModal(e) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 491:
  ```text
    e.preventDefault();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 492:
  ```text
    document.getElementById("logoutModal").classList.remove("hidden");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 493:
  ```text
    document.getElementById("logoutModal").classList.add("flex");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 494:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 30. `closeLogoutModal`

- Signature: `function closeLogoutModal()`
- Source range: lines 496-499

#### Line-by-line

- Line 496:
  ```text
function closeLogoutModal() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 497:
  ```text
    document.getElementById("logoutModal").classList.add("hidden");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 498:
  ```text
    document.getElementById("logoutModal").classList.remove("flex");
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 499:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 31. `confirmLogout`

- Signature: `function confirmLogout()`
- Source range: lines 501-503

#### Line-by-line

- Line 501:
  ```text
function confirmLogout() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 502:
  ```text
    window.location.href = "../../index.html";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 503:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 32. `startListPage`

- Signature: `async function startListPage()`
- Source range: lines 592-606

#### Line-by-line

- Line 592:
  ```text
async function startListPage() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 593:
  ```text
    const loading = document.getElementById("listLoading");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 594:
  ```text
    const grid = document.getElementById("gridContainer");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 595:
  ```text
    try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 596:
  ```text
        await initializeData();
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 597:
  ```text
        applySearchFilter();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 598:
  ```text
        renderItems();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 599:
  ```text
    } catch (e) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 600:
  ```text
        console.error("Failed to initialize package list:", e);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 601:
  ```text
        alert(`Failed to load package list: ${e.message}`);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 602:
  ```text
    } finally {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 603:
  ```text
        if (loading) loading.style.display = "none";
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 604:
  ```text
        if (grid) grid.classList.remove("hidden");
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 605:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 606:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

## File: `electron-app/scripts/profile-page.js`

- Methods documented: 4

### 1. `setPhotoStatus`

- Signature: `function setPhotoStatus(message)`
- Source range: lines 32-35

#### Line-by-line

- Line 32:
  ```text
    function setPhotoStatus(message) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 33:
  ```text
        if (!photoStatus) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 34:
  ```text
        photoStatus.textContent = message || "";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 35:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 2. `buildFallbackAvatar`

- Signature: `function buildFallbackAvatar(fullName)`
- Source range: lines 37-51

#### Line-by-line

- Line 37:
  ```text
    function buildFallbackAvatar(fullName) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 38:
  ```text
        const initials = (fullName || "NE")
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 39:
  ```text
            .split(" ")
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 40:
  ```text
            .filter(Boolean)
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 41:
  ```text
            .map((p) => p[0])
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 42:
  ```text
            .slice(0, 2)
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 43:
  ```text
            .join("")
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 44:
  ```text
            .toUpperCase();
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 45:
  ```text
        const svg = `
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 46:
  ```text
<svg xmlns="http://www.w3.org/2000/svg" width="200" height="200">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 47:
  ```text
  <rect width="100%" height="100%" fill="#E8F0F2"/>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 48:
  ```text
  <text x="50%" y="54%" font-size="72" text-anchor="middle" fill="#165166" font-family="Segoe UI, sans-serif" dy=".1em">${initials}</text>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 49:
  ```text
</svg>`;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 50:
  ```text
        return `data:image/svg+xml;utf8,${encodeURIComponent(svg)}`;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 51:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 3. `applyProfile`

- Signature: `function applyProfile(user, profile)`
- Source range: lines 53-90

#### Line-by-line

- Line 53:
  ```text
    function applyProfile(user, profile) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 54:
  ```text
        const fullName =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 55:
  ```text
            user?.name ||
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 56:
  ```text
            `${user?.first_name || ""} ${user?.last_name || ""}`.trim() ||
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 57:
  ```text
            user?.email ||
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 58:
  ```text
            "New User";
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 59:
  ```text
        const role = user?.role || "STAFF";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 60:
  ```text
        const email = user?.email || "";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 61:
  ```text
        const username = user?.email || "";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 62:
  ```text
        const photo = profile?.profile_photo_url || "";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 63:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 64:
  ```text
        if (nameEl) nameEl.textContent = fullName;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 65:
  ```text
        if (roleEl) roleEl.textContent = role;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 66:
  ```text
        if (roleTextEl) roleTextEl.textContent = role;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 67:
  ```text
        if (emailEl) emailEl.textContent = email;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 68:
  ```text
        if (usernameEl) usernameEl.textContent = username;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 69:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 70:
  ```text
        const nameParts = fullName.split(" ").filter(Boolean);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 71:
  ```text
        if (firstNameEl && !firstNameEl.value) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 72:
  ```text
            firstNameEl.value = nameParts[0] || "";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 73:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 74:
  ```text
        if (lastNameEl && !lastNameEl.value) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 75:
  ```text
            lastNameEl.value = nameParts.slice(1).join(" ");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 76:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 77:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 78:
  ```text
        if (photoImg) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 79:
  ```text
            photoImg.src = photo || buildFallbackAvatar(fullName);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 80:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 81:
  ```text
        if (phoneEl && profile?.phone_number) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 82:
  ```text
            phoneEl.value = profile.phone_number;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 83:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 84:
  ```text
        if (dobEl && profile?.date_of_birth) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 85:
  ```text
            dobEl.value = profile.date_of_birth;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 86:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 87:
  ```text
        if (nicknameEl && profile?.nickname) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 88:
  ```text
            nicknameEl.value = profile.nickname;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 89:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 90:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 4. `refreshProfile`

- Signature: `async function refreshProfile()`
- Source range: lines 92-103

#### Line-by-line

- Line 92:
  ```text
    async function refreshProfile() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 93:
  ```text
        if (typeof MOCK_MODE !== "undefined" && MOCK_MODE) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 94:
  ```text
            const user = JSON.parse(sessionStorage.getItem("user") || "{}");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 95:
  ```text
            applyProfile(user, { profile_photo_url: null });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 96:
  ```text
            return;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 97:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 98:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 99:
  ```text
        const result = await API.getProfile();
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 100:
  ```text
        if (!result || !result.success) return;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 101:
  ```text
        sessionStorage.setItem("user", JSON.stringify(result.user || {}));
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 102:
  ```text
        applyProfile(result.user || {}, result.profile || {});
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 103:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

## File: `electron-app/scripts/recommendations.js`

- Methods documented: 2

### 1. `getCustomerRecommendations`

- Signature: `async function getCustomerRecommendations(customerId, param, param)`
- Source range: lines 12-49

#### Line-by-line

- Line 12:
  ```text
export async function getCustomerRecommendations(
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 13:
  ```text
    customerId,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 14:
  ```text
    targetDate = null,
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 15:
  ```text
    k = 3,
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 16:
  ```text
) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 17:
  ```text
    try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 18:
  ```text
        let url = `${API_BASE_URL}/recommendations/${customerId}/`;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 19:
  ```text
        const params = new URLSearchParams();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 20:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 21:
  ```text
        if (targetDate) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 22:
  ```text
            params.append("date", targetDate);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 23:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 24:
  ```text
        params.append("k", k);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 25:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 26:
  ```text
        if (params.toString()) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 27:
  ```text
            url += `?${params.toString()}`;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 28:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 29:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 30:
  ```text
        const response = await fetch(url, {
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 31:
  ```text
            method: "GET",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 32:
  ```text
            headers: {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 33:
  ```text
                "Content-Type": "application/json",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 34:
  ```text
            },
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 35:
  ```text
        });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 36:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 37:
  ```text
        if (!response.ok) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 38:
  ```text
            const errorData = await response.json();
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 39:
  ```text
            throw new Error(
  ```
  Explanation: Raises an error/exception to signal failure to the caller.
- Line 40:
  ```text
                errorData.detail || "Failed to fetch recommendations",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 41:
  ```text
            );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 42:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 43:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 44:
  ```text
        return await response.json();
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 45:
  ```text
    } catch (error) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 46:
  ```text
        console.error("Error fetching recommendations:", error);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 47:
  ```text
        throw error;
  ```
  Explanation: Raises an error/exception to signal failure to the caller.
- Line 48:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 49:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 2. `formatPrice`

- Signature: `function formatPrice(price)`
- Source range: lines 56-58

#### Line-by-line

- Line 56:
  ```text
export function formatPrice(price) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 57:
  ```text
    return `₱${price.toFixed(2)}`;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 58:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

## File: `electron-app/scripts/shared_package_data.js`

- Methods documented: 5

### 1. `fetchPackages`

- Signature: `async function fetchPackages()`
- Source range: lines 13-33

#### Line-by-line

- Line 13:
  ```text
async function fetchPackages() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 14:
  ```text
    if (cache) return cache;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 15:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 16:
  ```text
    let all = [];
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 17:
  ```text
    let url = "http://localhost:8000/api/packages/";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 18:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 19:
  ```text
    while (url) {
  ```
  Explanation: Repeats execution while the condition remains true.
- Line 20:
  ```text
        const res = await fetch(url);
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 21:
  ```text
        const data = await res.json();
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 22:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 23:
  ```text
        const results = Array.isArray(data)
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 24:
  ```text
            ? data
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 25:
  ```text
            : data.results || [];
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 26:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 27:
  ```text
        all = all.concat(results);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 28:
  ```text
        url = data.next;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 29:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 30:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 31:
  ```text
    cache = all;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 32:
  ```text
    return cache;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 33:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 2. `fetchAddons`

- Signature: `async function fetchAddons()`
- Source range: lines 35-55

#### Line-by-line

- Line 35:
  ```text
async function fetchAddons() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 36:
  ```text
    if (addonCache) return addonCache;
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 37:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 38:
  ```text
    let all = [];
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 39:
  ```text
    let url = "http://localhost:8000/api/addons/";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 40:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 41:
  ```text
    while (url) {
  ```
  Explanation: Repeats execution while the condition remains true.
- Line 42:
  ```text
        const res = await fetch(url);
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 43:
  ```text
        const data = await res.json();
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 44:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 45:
  ```text
        const results = Array.isArray(data)
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 46:
  ```text
            ? data
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 47:
  ```text
            : data.results || [];
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 48:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 49:
  ```text
        all = all.concat(results);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 50:
  ```text
        url = data.next; // DRF pagination link
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 51:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
- Line 52:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 53:
  ```text
    addonCache = all;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 54:
  ```text
    return addonCache;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 55:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 3. `getCategories`

- Signature: `async function getCategories()`
- Source range: lines 59-69

#### Line-by-line

- Line 59:
  ```text
export async function getCategories() {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 60:
  ```text
    const packages = await fetchPackages();
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 61:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 62:
  ```text
    const uniqueNames = [...new Set(packages.map(p => p.category))];
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 63:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 64:
  ```text
    return uniqueNames.map((name, idx) => ({
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 65:
  ```text
        id: idx + 1,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 66:
  ```text
        name,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 67:
  ```text
        image: CATEGORY_IMAGES[name] || CATEGORY_IMAGES["Regular"]
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 68:
  ```text
    }));
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 69:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 4. `getPackagesByCategory`

- Signature: `async function getPackagesByCategory(categoryName)`
- Source range: lines 72-75

#### Line-by-line

- Line 72:
  ```text
export async function getPackagesByCategory(categoryName) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 73:
  ```text
    const packages = await fetchPackages();
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 74:
  ```text
    return packages.filter(p => p.category === categoryName);
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 75:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

### 5. `getAddonsByCategory`

- Signature: `async function getAddonsByCategory(categoryName)`
- Source range: lines 78-102

#### Line-by-line

- Line 78:
  ```text
export async function getAddonsByCategory(categoryName) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 79:
  ```text
    const addons = await fetchAddons();
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 80:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 81:
  ```text
    const list = Array.isArray(addons)
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 82:
  ```text
        ? addons
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 83:
  ```text
        : addons.results || addons.addons || [];
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 84:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 85:
  ```text
    const normalizedCategory = categoryName.trim().toLowerCase();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 86:
  ```text
    console.log("CATEGORY:", categoryName);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 87:
  ```text
    list.forEach(a => console.log("APPLIES_TO:", a.applies_to));
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 88:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 89:
  ```text
    return list.filter(a => {
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 90:
  ```text
        if (Array.isArray(a.applies_to)) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 91:
  ```text
            return a.applies_to
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 92:
  ```text
                .map(v => v.trim().toLowerCase())
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 93:
  ```text
                .includes(normalizedCategory);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 94:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 95:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 96:
  ```text
        if (typeof a.applies_to === 'string') {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 97:
  ```text
            return a.applies_to.trim().toLowerCase() === normalizedCategory;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 98:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 99:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 100:
  ```text
        return false;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 101:
  ```text
    });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 102:
  ```text
}
  ```
  Explanation: Closes the current block/scope.

## File: `electron-app/scripts/staff-profile.js`

- Methods documented: 3

### 1. `getInitials`

- Signature: `function getInitials(fullName)`
- Source range: lines 13-19

#### Line-by-line

- Line 13:
  ```text
    function getInitials(fullName) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 14:
  ```text
        if (!fullName) return "NE";
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 15:
  ```text
        const parts = fullName.trim().split(" ").filter(Boolean);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 16:
  ```text
        const first = parts[0]?.[0] || "N";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 17:
  ```text
        const last = parts.length > 1 ? parts[parts.length - 1][0] : "E";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 18:
  ```text
        return `${first}${last}`.toUpperCase();
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 19:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 2. `buildFallbackAvatar`

- Signature: `function buildFallbackAvatar(fullName)`
- Source range: lines 21-29

#### Line-by-line

- Line 21:
  ```text
    function buildFallbackAvatar(fullName) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 22:
  ```text
        const initials = getInitials(fullName);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 23:
  ```text
        const svg = `
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 24:
  ```text
<svg xmlns="http://www.w3.org/2000/svg" width="200" height="200">
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 25:
  ```text
  <rect width="100%" height="100%" fill="#E8F0F2"/>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 26:
  ```text
  <text x="50%" y="54%" font-size="72" text-anchor="middle" fill="#165166" font-family="Segoe UI, sans-serif" dy=".1em">${initials}</text>
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 27:
  ```text
</svg>`;
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 28:
  ```text
        return `data:image/svg+xml;utf8,${encodeURIComponent(svg)}`;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 29:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 3. `applyProfile`

- Signature: `function applyProfile(user, profile)`
- Source range: lines 31-53

#### Line-by-line

- Line 31:
  ```text
    function applyProfile(user, profile) {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 32:
  ```text
        const fullName =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 33:
  ```text
            user?.name ||
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 34:
  ```text
            `${user?.first_name || ""} ${user?.last_name || ""}`.trim() ||
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 35:
  ```text
            user?.email ||
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 36:
  ```text
            "New User";
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 37:
  ```text
        const role = user?.role || "STAFF";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 38:
  ```text
        const email = user?.email || "";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 39:
  ```text
        const photoUrl =
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 40:
  ```text
            profile?.profile_photo_url ||
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 41:
  ```text
            sessionStorage.getItem("profilePhotoUrl") ||
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 42:
  ```text
            "";
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 43:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 44:
  ```text
        nameEls.forEach((el) => (el.textContent = fullName));
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 45:
  ```text
        roleEls.forEach((el) => (el.textContent = role));
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 46:
  ```text
        emailEls.forEach((el) => (el.textContent = email));
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 47:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 48:
  ```text
        avatarEls.forEach((el) => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 49:
  ```text
            if (el.tagName.toLowerCase() === "img") {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 50:
  ```text
                el.src = photoUrl || buildFallbackAvatar(fullName);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 51:
  ```text
            }
  ```
  Explanation: Closes the current block/scope.
- Line 52:
  ```text
        });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 53:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

## File: `electron-app/scripts/supabase-client.js`

- Methods documented: 12

### 1. `constructor`

- Signature: `constructor(url, key)`
- Source range: lines 11-14

#### Line-by-line

- Line 11:
  ```text
    constructor(url, key) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 12:
  ```text
        this.url = url;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 13:
  ```text
        this.key = key;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 14:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 2. `request`

- Signature: `async request(method, endpoint, param, param)`
- Source range: lines 17-65

#### Line-by-line

- Line 17:
  ```text
    async request(method, endpoint, body = null, filters = "") {
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 18:
  ```text
        const options = {
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 19:
  ```text
            method,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 20:
  ```text
            headers: {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 21:
  ```text
                "Content-Type": "application/json",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 22:
  ```text
                Authorization: `Bearer ${this.key}`,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 23:
  ```text
                apikey: this.key,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 24:
  ```text
                Prefer: "return=representation",
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 25:
  ```text
            },
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 26:
  ```text
        };
  ```
  Explanation: Closes the current block/scope.
- Line 27:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 28:
  ```text
        if (body) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 29:
  ```text
            options.body = JSON.stringify(body);
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 30:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 31:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 32:
  ```text
        const filterStr = filters ? `?${filters}` : "";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 33:
  ```text
        const fullUrl = `${this.url}/rest/v1${endpoint}${filterStr}`;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 34:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 35:
  ```text
        console.log(
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 36:
  ```text
            `[Supabase] ${method} ${endpoint}${filterStr ? " with filters" : ""}`,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 37:
  ```text
        );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 38:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 39:
  ```text
        try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 40:
  ```text
            const response = await fetch(fullUrl, options);
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 41:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 42:
  ```text
            console.log(`[Supabase] Response status: ${response.status}`);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 43:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 44:
  ```text
            if (!response.ok) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 45:
  ```text
                const errorData = await response.text();
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 46:
  ```text
                console.error(`[Supabase] Error response:`, errorData);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 47:
  ```text
                throw new Error(
  ```
  Explanation: Raises an error/exception to signal failure to the caller.
- Line 48:
  ```text
                    `Supabase error: ${response.status} ${response.statusText} - ${errorData}`,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 49:
  ```text
                );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 50:
  ```text
            }
  ```
  Explanation: Closes the current block/scope.
- Line 51:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 52:
  ```text
            if (response.status === 204) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 53:
  ```text
                return null;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 54:
  ```text
            }
  ```
  Explanation: Closes the current block/scope.
- Line 55:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 56:
  ```text
            const data = await response.json();
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 57:
  ```text
            console.log(
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 58:
  ```text
                `[Supabase] Got ${Array.isArray(data) ? data.length : "1"} records`,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 59:
  ```text
            );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 60:
  ```text
            return data;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 61:
  ```text
        } catch (error) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 62:
  ```text
            console.error(`[Supabase] Request failed:`, error.message);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 63:
  ```text
            throw error;
  ```
  Explanation: Raises an error/exception to signal failure to the caller.
- Line 64:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 65:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 3. `customers`

- Signature: `customers()`
- Source range: lines 71-105

#### Line-by-line

- Line 71:
  ```text
    customers() {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 72:
  ```text
        return {
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 73:
  ```text
            select: () =>
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 74:
  ```text
                this.request(
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 75:
  ```text
                    "GET",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 76:
  ```text
                    "/backend_customer",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 77:
  ```text
                    null,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 78:
  ```text
                    "order=customer_id.asc",
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 79:
  ```text
                ),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 80:
  ```text
            selectWithPagination: (offset = 0, limit = 1000) =>
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 81:
  ```text
                this.request(
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 82:
  ```text
                    "GET",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 83:
  ```text
                    "/backend_customer",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 84:
  ```text
                    null,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 85:
  ```text
                    `order=customer_id.asc&offset=${offset}&limit=${limit}`,
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 86:
  ```text
                ),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 87:
  ```text
            insert: (data) => this.request("POST", "/backend_customer", data),
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 88:
  ```text
            update: (customerId, data) =>
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 89:
  ```text
                this.request(
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 90:
  ```text
                    "PATCH",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 91:
  ```text
                    `/backend_customer?customer_id=eq.${customerId}`,
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 92:
  ```text
                    data,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 93:
  ```text
                ),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 94:
  ```text
            delete: (customerId) =>
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 95:
  ```text
                this.request(
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 96:
  ```text
                    "DELETE",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 97:
  ```text
                    `/backend_customer?customer_id=eq.${customerId}`,
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 98:
  ```text
                ),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 99:
  ```text
            get: (customerId) =>
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 100:
  ```text
                this.request(
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 101:
  ```text
                    "GET",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 102:
  ```text
                    `/backend_customer?customer_id=eq.${customerId}`,
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 103:
  ```text
                ),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 104:
  ```text
        };
  ```
  Explanation: Closes the current block/scope.
- Line 105:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 4. `bookings`

- Signature: `bookings()`
- Source range: lines 111-184

#### Line-by-line

- Line 111:
  ```text
    bookings() {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 112:
  ```text
        const self = this; // Capture 'this' context
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 113:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 114:
  ```text
        return {
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 115:
  ```text
            select: () =>
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 116:
  ```text
                self.request("GET", "/backend_booking", null, "order=id.asc"),
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 117:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 118:
  ```text
            selectWithPagination: (offset = 0, limit = 1000) =>
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 119:
  ```text
                self.request(
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 120:
  ```text
                    "GET",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 121:
  ```text
                    "/backend_booking",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 122:
  ```text
                    null,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 123:
  ```text
                    `order=id.asc&offset=${offset}&limit=${limit}`,
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 124:
  ```text
                ),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 125:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 126:
  ```text
            insert: (data) => self.request("POST", "/backend_booking", data),
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 127:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 128:
  ```text
            update: (bookingId, data) =>
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 129:
  ```text
                self.request(
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 130:
  ```text
                    "PATCH",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 131:
  ```text
                    `/backend_booking?id=eq.${bookingId}`,
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 132:
  ```text
                    data,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 133:
  ```text
                ),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 134:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 135:
  ```text
            delete: (bookingId) =>
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 136:
  ```text
                self.request("DELETE", `/backend_booking?id=eq.${bookingId}`),
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 137:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 138:
  ```text
            getByCustomer: (customerId) =>
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 139:
  ```text
                self.request(
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 140:
  ```text
                    "GET",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 141:
  ```text
                    `/backend_booking?customer_id=eq.${customerId}&order=id.asc`,
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 142:
  ```text
                ),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 143:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 144:
  ```text
            // ✅ NEW: Get booking counts per customer (optimized for table display)
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 145:
  ```text
            selectCounts: async () => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 146:
  ```text
                try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 147:
  ```text
                    const data = await self.request(
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 148:
  ```text
                        "GET",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 149:
  ```text
                        "/backend_booking",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 150:
  ```text
                        null,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 151:
  ```text
                        "select=customer_id",
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 152:
  ```text
                    );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 153:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 154:
  ```text
                    // Count bookings per customer
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 155:
  ```text
                    const counts = {};
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 156:
  ```text
                    if (data && Array.isArray(data)) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 157:
  ```text
                        data.forEach((booking) => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 158:
  ```text
                            const customerId = booking.customer_id;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 159:
  ```text
                            counts[customerId] = (counts[customerId] || 0) + 1;
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 160:
  ```text
                        });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 161:
  ```text
                    }
  ```
  Explanation: Closes the current block/scope.
- Line 162:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 163:
  ```text
                    return counts;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 164:
  ```text
                } catch (error) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 165:
  ```text
                    console.error("Error fetching booking counts:", error);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 166:
  ```text
                    return {};
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 167:
  ```text
                }
  ```
  Explanation: Closes the current block/scope.
- Line 168:
  ```text
            },
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 169:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 170:
  ```text
            // ✅ NEW: Get bookings for a specific customer only (for lazy loading)
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 171:
  ```text
            selectByCustomer: async (customerId) => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 172:
  ```text
                try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 173:
  ```text
                    const data = await self.request(
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 174:
  ```text
                        "GET",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 175:
  ```text
                        `/backend_booking?customer_id=eq.${customerId}&order=id.asc`,
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 176:
  ```text
                    );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 177:
  ```text
                    return data || [];
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 178:
  ```text
                } catch (error) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 179:
  ```text
                    console.error("Error fetching customer bookings:", error);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 180:
  ```text
                    return [];
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 181:
  ```text
                }
  ```
  Explanation: Closes the current block/scope.
- Line 182:
  ```text
            },
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 183:
  ```text
        };
  ```
  Explanation: Closes the current block/scope.
- Line 184:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 5. `bookingAddons`

- Signature: `bookingAddons()`
- Source range: lines 190-253

#### Line-by-line

- Line 190:
  ```text
    bookingAddons() {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 191:
  ```text
        const self = this; // Capture 'this' context
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 192:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 193:
  ```text
        return {
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 194:
  ```text
            select: () =>
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 195:
  ```text
                self.request(
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 196:
  ```text
                    "GET",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 197:
  ```text
                    "/backend_bookingaddon",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 198:
  ```text
                    null,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 199:
  ```text
                    "order=id.asc",
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 200:
  ```text
                ),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 201:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 202:
  ```text
            selectWithPagination: (offset = 0, limit = 1000) =>
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 203:
  ```text
                self.request(
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 204:
  ```text
                    "GET",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 205:
  ```text
                    "/backend_bookingaddon",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 206:
  ```text
                    null,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 207:
  ```text
                    `order=id.asc&offset=${offset}&limit=${limit}`,
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 208:
  ```text
                ),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 209:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 210:
  ```text
            getByBooking: (bookingId) =>
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 211:
  ```text
                self.request(
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 212:
  ```text
                    "GET",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 213:
  ```text
                    `/backend_bookingaddon?booking_id=eq.${bookingId}&order=id.asc`,
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 214:
  ```text
                ),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 215:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 216:
  ```text
            insert: (data) =>
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 217:
  ```text
                self.request("POST", "/backend_bookingaddon", data),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 218:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 219:
  ```text
            update: (addonId, data) =>
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 220:
  ```text
                self.request(
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 221:
  ```text
                    "PATCH",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 222:
  ```text
                    `/backend_bookingaddon?id=eq.${addonId}`,
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 223:
  ```text
                    data,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 224:
  ```text
                ),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 225:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 226:
  ```text
            delete: (addonId) =>
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 227:
  ```text
                self.request(
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 228:
  ```text
                    "DELETE",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 229:
  ```text
                    `/backend_bookingaddon?id=eq.${addonId}`,
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 230:
  ```text
                ),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 231:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 232:
  ```text
            // ✅ NEW: Get all addons for multiple booking IDs at once (batch query)
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 233:
  ```text
            selectByBookingIds: async (bookingIds) => {
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 234:
  ```text
                try {
  ```
  Explanation: Begins protected execution to catch runtime failures cleanly.
- Line 235:
  ```text
                    if (!bookingIds || bookingIds.length === 0) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 236:
  ```text
                        return [];
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 237:
  ```text
                    }
  ```
  Explanation: Closes the current block/scope.
- Line 238:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 239:
  ```text
                    // Build filter string: booking_id.in.(id1,id2,id3)
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 240:
  ```text
                    const idList = bookingIds.join(",");
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 241:
  ```text
                    const data = await self.request(
  ```
  Explanation: Waits for an asynchronous operation before continuing.
- Line 242:
  ```text
                        "GET",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 243:
  ```text
                        `/backend_bookingaddon?booking_id=in.(${idList})&order=id.asc`,
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 244:
  ```text
                    );
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 245:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 246:
  ```text
                    return data || [];
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 247:
  ```text
                } catch (error) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 248:
  ```text
                    console.error("Error fetching booking addons:", error);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 249:
  ```text
                    return [];
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 250:
  ```text
                }
  ```
  Explanation: Closes the current block/scope.
- Line 251:
  ```text
            },
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 252:
  ```text
        };
  ```
  Explanation: Closes the current block/scope.
- Line 253:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 6. `packages`

- Signature: `packages()`
- Source range: lines 259-266

#### Line-by-line

- Line 259:
  ```text
    packages() {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 260:
  ```text
        return {
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 261:
  ```text
            select: () =>
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 262:
  ```text
                this.request("GET", "/backend_package", null, "order=id.asc"),
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 263:
  ```text
            getById: (packageId) =>
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 264:
  ```text
                this.request("GET", `/backend_package?id=eq.${packageId}`),
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 265:
  ```text
        };
  ```
  Explanation: Closes the current block/scope.
- Line 266:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 7. `addons`

- Signature: `addons()`
- Source range: lines 272-279

#### Line-by-line

- Line 272:
  ```text
    addons() {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 273:
  ```text
        return {
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 274:
  ```text
            select: () =>
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 275:
  ```text
                this.request("GET", "/backend_addon", null, "order=id.asc"),
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 276:
  ```text
            getById: (addonId) =>
  ```
  Explanation: Declares the callable signature and entry point for this method.
- Line 277:
  ```text
                this.request("GET", `/backend_addon?id=eq.${addonId}`),
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 278:
  ```text
        };
  ```
  Explanation: Closes the current block/scope.
- Line 279:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 8. `transformCustomer`

- Signature: `transformCustomer(dbCustomer)`
- Source range: lines 286-303

#### Line-by-line

- Line 286:
  ```text
    transformCustomer(dbCustomer) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 287:
  ```text
        return {
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 288:
  ```text
            id: dbCustomer.customer_id,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 289:
  ```text
            name: dbCustomer.full_name || "",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 290:
  ```text
            email: dbCustomer.email || "",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 291:
  ```text
            contactNo: dbCustomer.contact_number || "",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 292:
  ```text
            instagramHandle: dbCustomer.instagram_handle || "",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 293:
  ```text
            acquisitionSource: dbCustomer.acquisition_source || "",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 294:
  ```text
            isFirstTime: dbCustomer.is_first_time,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 295:
  ```text
            previousSessionCounts: dbCustomer.previous_session_counts || 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 296:
  ```text
            registrationDate: dbCustomer.registration_date,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 297:
  ```text
            consent: dbCustomer.consent || "Disagree",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 298:
  ```text
            packageId: dbCustomer.package_id,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 299:
  ```text
            bookings: [],
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 300:
  ```text
            createdAt: dbCustomer.created_at,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 301:
  ```text
            updatedAt: dbCustomer.last_updated,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 302:
  ```text
        };
  ```
  Explanation: Closes the current block/scope.
- Line 303:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 9. `transformBooking`

- Signature: `transformBooking(dbBooking, param, param, param)`
- Source range: lines 306-352

#### Line-by-line

- Line 306:
  ```text
    transformBooking(
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 307:
  ```text
        dbBooking,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 308:
  ```text
        addons = [],
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 309:
  ```text
        customer = null,
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 310:
  ```text
        packageDetails = null,
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 311:
  ```text
    ) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 312:
  ```text
        // Map acquisition_source to booking type
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 313:
  ```text
        let type = "Walk-In"; // default
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 314:
  ```text
        if (customer && customer.acquisitionSource) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 315:
  ```text
            const source = customer.acquisitionSource.toLowerCase();
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 316:
  ```text
            if (
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 317:
  ```text
                source === "reserved" ||
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 318:
  ```text
                source.includes("booked") ||
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 319:
  ```text
                source.includes("schedule")
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 320:
  ```text
            ) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 321:
  ```text
                type = "Reserved";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 322:
  ```text
            } else if (source === "walk-in" || source.includes("walk")) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 323:
  ```text
                type = "Walk-In";
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 324:
  ```text
            }
  ```
  Explanation: Closes the current block/scope.
- Line 325:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 326:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 327:
  ```text
        return {
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 328:
  ```text
            id: dbBooking.id,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 329:
  ```text
            date: dbBooking.session_date
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 330:
  ```text
                ? new Date(dbBooking.session_date).toLocaleDateString("en-US")
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 331:
  ```text
                : "",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 332:
  ```text
            sessionDate: dbBooking.session_date,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 333:
  ```text
            totalPrice: dbBooking.total_price || 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 334:
  ```text
            gcashPayment: dbBooking.gcash_payment || 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 335:
  ```text
            cashPayment: dbBooking.cash_payment || 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 336:
  ```text
            discounts: dbBooking.discounts || "",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 337:
  ```text
            discount: dbBooking.discounts || 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 338:
  ```text
            sessionStatus: dbBooking.session_status || "Pending",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 339:
  ```text
            customerId: dbBooking.customer_id,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 340:
  ```text
            packageId: dbBooking.package_id,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 341:
  ```text
            addons: addons,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 342:
  ```text
            total: dbBooking.total_price || 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 343:
  ```text
            subtotal: dbBooking.total_price || 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 344:
  ```text
            fullName: customer ? customer.name : "",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 345:
  ```text
            email: customer ? customer.email : "",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 346:
  ```text
            type: type,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 347:
  ```text
            packageName: packageDetails ? packageDetails.name : "",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 348:
  ```text
            packagePrice: packageDetails ? packageDetails.price : 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 349:
  ```text
            createdAt: dbBooking.created_at,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 350:
  ```text
            updatedAt: dbBooking.last_updated,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 351:
  ```text
        };
  ```
  Explanation: Closes the current block/scope.
- Line 352:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 10. `transformAddon`

- Signature: `transformAddon(dbAddon, param)`
- Source range: lines 355-366

#### Line-by-line

- Line 355:
  ```text
    transformAddon(dbAddon, addonDetails = null) {
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 356:
  ```text
        return {
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 357:
  ```text
            id: dbAddon.id,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 358:
  ```text
            addonId: dbAddon.addon_id,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 359:
  ```text
            name: addonDetails ? addonDetails.name : "",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 360:
  ```text
            quantity: dbAddon.addon_quantity,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 361:
  ```text
            price:
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 362:
  ```text
                dbAddon.addon_price || (addonDetails ? addonDetails.price : 0),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 363:
  ```text
            totalCost: dbAddon.total_addon_cost,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 364:
  ```text
            bookingId: dbAddon.booking_id,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 365:
  ```text
        };
  ```
  Explanation: Closes the current block/scope.
- Line 366:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 11. `customerToDatabase`

- Signature: `customerToDatabase(customer)`
- Source range: lines 369-384

#### Line-by-line

- Line 369:
  ```text
    customerToDatabase(customer) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 370:
  ```text
        return {
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 371:
  ```text
            customer_id: customer.id,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 372:
  ```text
            full_name: customer.name,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 373:
  ```text
            email: customer.email,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 374:
  ```text
            contact_number: customer.contactNo,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 375:
  ```text
            instagram_handle: customer.instagramHandle || null,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 376:
  ```text
            acquisition_source: customer.acquisitionSource || null,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 377:
  ```text
            is_first_time: customer.isFirstTime || false,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 378:
  ```text
            previous_session_counts: customer.previousSessionCounts || 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 379:
  ```text
            registration_date: customer.registrationDate || null,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 380:
  ```text
            consent: customer.consent,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 381:
  ```text
            package_id: customer.packageId || null,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 382:
  ```text
            last_updated: new Date().toISOString(),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 383:
  ```text
        };
  ```
  Explanation: Closes the current block/scope.
- Line 384:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.

### 12. `bookingToDatabase`

- Signature: `bookingToDatabase(booking)`
- Source range: lines 387-414

#### Line-by-line

- Line 387:
  ```text
    bookingToDatabase(booking) {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 388:
  ```text
        console.log("🔄 bookingToDatabase input:", booking);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 389:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 390:
  ```text
        const dbBooking = {
  ```
  Explanation: Assigns or updates a variable used by subsequent logic.
- Line 391:
  ```text
            session_date: booking.sessionDate || new Date().toISOString(),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 392:
  ```text
            total_price: booking.totalPrice || booking.total || 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 393:
  ```text
            gcash_payment: booking.gcashPayment || 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 394:
  ```text
            cash_payment: booking.cashPayment || 0,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 395:
  ```text
            discounts: booking.discounts || booking.discount || "", // Handle both field names
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 396:
  ```text
            session_status: booking.sessionStatus || "Pending",
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 397:
  ```text
            customer_id: booking.customerId,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 398:
  ```text
            package_id: booking.packageId || null,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 399:
  ```text
            last_updated: new Date().toISOString(),
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 400:
  ```text
        };
  ```
  Explanation: Closes the current block/scope.
- Line 401:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 402:
  ```text
        console.log("🔄 bookingToDatabase output:", dbBooking);
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 403:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 404:
  ```text
        // ✅ VALIDATION - Catch the error before it hits the database
  ```
  Explanation: Comment line describing behavior or rationale.
- Line 405:
  ```text
        if (!dbBooking.customer_id) {
  ```
  Explanation: Checks a condition to decide whether this execution path should run.
- Line 406:
  ```text
            console.error("❌ CRITICAL: customer_id is missing!", {
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 407:
  ```text
                booking,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 408:
  ```text
                dbBooking,
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 409:
  ```text
            });
  ```
  Explanation: Executes a core operation in this method's control flow.
- Line 410:
  ```text
            throw new Error("Cannot save booking without customer_id");
  ```
  Explanation: Raises an error/exception to signal failure to the caller.
- Line 411:
  ```text
        }
  ```
  Explanation: Closes the current block/scope.
- Line 412:
  ```text

  ```
  Explanation: Blank line used for readability and logical separation.
- Line 413:
  ```text
        return dbBooking;
  ```
  Explanation: Returns a value to the caller and ends method execution for this path.
- Line 414:
  ```text
    }
  ```
  Explanation: Closes the current block/scope.
